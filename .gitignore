# ============================================================================
# Binaries / 编译产物
# ============================================================================
*.exe
*.exe~
*.dll
*.so
*.dylib
# bin/ 目录用于存放 ffmpeg 和 ffprobe 等运行时文件
# 忽略 bin/ 目录中的所有文件，但保留 .gitkeep 占位
bin/*
!bin/.gitkeep
dist/

# Test binary
*.test

# Output of go coverage tool
*.out

# Go workspace file
go.work
go.work.sum

# ============================================================================
# 数据库 / 运行时数据
# ============================================================================
data/*.db
data/*.db-shm
data/*.db-wal
data/

# ============================================================================
# 日志
# ============================================================================
logs/
*.log

# ============================================================================
# IDE / 编辑器
# ============================================================================
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# ============================================================================
# ⚠️ 敏感信息保护（关键）
# ============================================================================
# 部署时通过环境变量注入，避免硬编码到仓库
.env
.env.local
.env.*.local
config.yaml
config.local.yaml
config.*.local.yaml

# TLS 证书 / 私钥（部署时重新生成）
certs/*.crt
certs/*.pem
certs/*.key

# 前端 .env（含 VITE_SM4_SECRET、内网 IP 等敏感信息）
frontend/.env.development
frontend/.env.production

# Claude agent 临时工作树（不应进入版本控制）
.claude/worktrees/

# Claude / GSD 流程记录（项目级 skill 流程、个人笔记，不入仓库）
/.claude/
/.planning/

# ============================================================================
# 前端构建产物
# ============================================================================
frontend/node_modules/
frontend/dist/
frontend/.env.local
frontend/.env.*.local
*.tsbuildinfo
frontend/tsconfig.tsbuildinfo
internal/frontend/dist/

# ============================================================================
# 构建临时目录
# ============================================================================
scripts/bin/
scripts/internal/

# ============================================================================
# 临时文件
# ============================================================================
tmp/
temp/
*.tmp

# ============================================================================
# OS / 杂项
# ============================================================================
Thumbs.db
*.bak
*.bak*
nul

# ============================================================================
# GitHub API 临时响应（含 token，不入仓库）
# ============================================================================
remote_ref.json
sec_remote.json
