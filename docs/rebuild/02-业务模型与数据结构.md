# 业务模型与数据结构

## 一、数据模型总览

### 1.1 核心实体关系图

```
┌─────────────┐     ┌──────────────┐     ┌─────────────────┐
│    User     │────>│   APIKey     │     │ VideoRecording  │
│  (用户)      │1   N│  (API密钥)    │N   1│     Task        │
├─────────────┤     └──────────────┘     │  (录制任务)       │
│ ID          │                           ├─────────────────┤
│ Username    │     ┌──────────────┐     │ ID              │
│ PasswordHash │     │ HuaweiConfig │     │ Name            │
│ RoleID      │     │ (华为配置)     │     │ StartTime       │
└─────────────┘     ├──────────────┤     │ EndTime         │
       │            │ ID           │     │ Status          │
       │            │ Server       │     │ HuaweiConfigID  │
       │            │ Username     │     └─────────────────┘
       v            │ Password     │              │
┌─────────────┐     │ USBCamera... │              │
│    Role     │     └──────────────┘              v
│   (角色)     │              │             ┌─────────────────┐
├─────────────┤              v             │ ConferenceRecord│
│ ID          │     ┌──────────────┐      │  (会议记录)       │
│ Name        │     │Conference    │      ├─────────────────┤
└─────────────┘     │   Record     │      │ ID              │
       │            ├──────────────┤      │ ConferenceNumber│
       v            │ ID           │      │ Status          │
┌─────────────┐     │ Conference...│      │ VideoFiles      │
│ Permission  │     │ Recordings   │      └─────────────────┘
│  (权限)      │     └──────────────┘              │
├─────────────┤              │                      │
│ ID          │              v                      v
│ Resource    │     ┌──────────────┐     ┌─────────────────┐
│ Action      │     │  VideoFile   │     │   PPTFile       │
└─────────────┘     │  (视频文件)    │     │  (PPT文件)       │
                    ├──────────────┤     ├─────────────────┤
                    │ ID           │     │ ID              │
                    │ FilePath     │     │ FilePath        │
                    │ Duration     │     │ PageCount       │
                    │ FileSize     │     └─────────────────┘
                    └──────────────┘
```

### 1.2 模型统计

| 模型 | 文件 | 代码行数 | 说明 |
|------|------|---------|------|
| User | user.go | 85 | 用户和认证 |
| Role | role.go | 45 | 角色定义 |
| Permission | permission.go | 30 | 权限定义 |
| APIKey | api_key.go | 60 | API密钥管理 |
| VideoRecordingTask | video_recording_task.go | 190 | 视频录制任务 |
| ConferenceRecord | conference_record.go | 120 | 会议记录 |
| VideoFile | video_file.go | 75 | 视频文件 |
| PPTFile | ppt_file.go | 50 | PPT文件 |
| HuaweiConfig | huawei_config.go | 74 | 华为配置 |
| Session | session.go | 40 | 会话管理 |

## 二、核心模型详解

### 2.1 User (用户模型)

```go
// internal/models/user.go
type User struct {
    Base                                  // 基础模型(ID, CreatedAt, UpdatedAt)
    Username        string    `gorm:"type:varchar(50);uniqueIndex;not null" json:"username"`
    PasswordHash    string    `gorm:"type:varchar(255);not null" json:"-"`
    Email           string    `gorm:"type:varchar(100)" json:"email"`
    FullName        string    `gorm:"type:varchar(100)" json:"full_name"`
    RoleID          uint      `gorm:"not null" json:"role_id"`
    Role            *Role     `gorm:"foreignKey:RoleID" json:"role,omitempty"`
    IsActive        bool      `gorm:"default:true" json:"is_active"`
    LastLoginAt     *time.Time `json:"last_login_at"`
    APIKeys         []APIKey  `gorm:"foreignKey:UserID" json:"api_keys,omitempty"`
}

// 关联方法
func (u *User) SetPassword(password string) error
func (u *User) CheckPassword(password string) bool
func (u *User) HasPermission(resource, action string) bool
func (u *User) GenerateToken() (string, error)
```

**字段说明**：

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| ID | uint | 主键 | AUTO_INCREMENT |
| Username | string | 用户名 | unique, not null |
| PasswordHash | string | 密码哈希 | bcrypt |
| Email | string | 邮箱 | optional |
| FullName | string | 全名 | optional |
| RoleID | uint | 角色ID | foreign key |
| IsActive | bool | 是否激活 | default: true |
| LastLoginAt | time.Time | 最后登录时间 | nullable |

### 2.2 Role (角色模型)

```go
// internal/models/role.go
type Role struct {
    Base
    Name        string       `gorm:"type:varchar(50);uniqueIndex;not null" json:"name"`
    Description string       `gorm:"type:text" json:"description"`
    Users       []User       `gorm:"foreignKey:RoleID" json:"users,omitempty"`
    Permissions []Permission `gorm:"many2many:role_permissions;" json:"permissions,omitempty"`
}

// 预定义角色
const (
    RoleAdmin      = "admin"       // 系统管理员
    RoleOperator   = "operator"    // 操作员
    RoleViewer     = "viewer"      // 查看者
    RoleAPIClient  = "api_client"  // API客户端
)

// 角色权限映射
var RolePermissions = map[string][]string{
    RoleAdmin:      {"*"},                              // 全部权限
    RoleOperator:   {"conferences:*", "tasks:*"},      // 会议和任务管理
    RoleViewer:     {"conferences:view", "tasks:view"}, // 只读权限
    RoleAPIClient:  {"api:access"},                     // API访问
}
```

### 2.3 Permission (权限模型)

```go
// internal/models/permission.go
type Permission struct {
    Base
    Resource  string `gorm:"type:varchar(50);not null;index" json:"resource"`
    Action    string `gorm:"type:varchar(50);not null;index" json:"action"`
    Roles     []Role `gorm:"many2many:role_permissions;" json:"roles,omitempty"`
}

// 唯一约束
func (p *Permission) BeforeCreate(tx *gorm.DB) error {
    // resource + action 组合唯一
}

// 权限格式: resource:action
// 示例:
// - conferences:create  - 创建会议
// - conferences:view    - 查看会议
// - conferences:delete  - 删除会议
// - tasks:execute       - 执行任务
// - files:download      - 下载文件
```

**权限资源列表**：

| 资源 | 可用操作 | 说明 |
|------|---------|------|
| conferences | create, view, update, delete, control | 会议管理 |
| tasks | create, view, update, delete, execute, cancel | 任务管理 |
| recordings | create, view, update, delete, start, stop | 录制管理 |
| files | view, download, delete | 文件管理 |
| users | create, view, update, delete | 用户管理 |
| system | view, configure | 系统配置 |
| api | access | API访问 |

### 2.4 APIKey (API密钥模型)

```go
// internal/models/api_key.go
type APIKey struct {
    Base
    Name        string     `gorm:"type:varchar(100);not null" json:"name"`
    Key         string     `gorm:"type:varchar(64);uniqueIndex;not null" json:"key"`
    UserID      uint       `gorm:"not null;index" json:"user_id"`
    User        *User      `gorm:"foreignKey:UserID" json:"user,omitempty"`
    ExpiresAt   *time.Time `json:"expires_at"`
    LastUsedAt  *time.Time `json:"last_used_at"`
    IsActive    bool       `gorm:"default:true" json:"is_active"`
    Scopes      string     `gorm:"type:text" json:"scopes"` // JSON数组字符串
}

// 作用域定义
const (
    ScopeRead       = "read"
    ScopeWrite      = "write"
    ScopeExecute    = "execute"
    ScopeAdmin      = "admin"
)

// 方法
func (a *APIKey) IsExpired() bool
func (a *APIKey) HasScope(scope string) bool
func (a *APIKey) GenerateKey() string
func (a *APIKey) Validate() error
```

### 2.5 VideoRecordingTask (视频录制任务模型)

```go
// internal/models/video_recording_task.go
type VideoRecordingTask struct {
    Base
    Name                string    `gorm:"type:varchar(200);not null" json:"name"`
    Description         string    `gorm:"type:text" json:"description"`
    StartTime           time.Time `gorm:"not null;index" json:"start_time"`
    EndTime             time.Time `gorm:"not null;index" json:"end_time"`
    PreJoinMinutes      int       `gorm:"default:5" json:"pre_join_minutes"`
    RecordDelayMinutes  int       `gorm:"default:0" json:"record_delay_minutes"`
    ConferenceNumber    string    `gorm:"type:varchar(50);not null;index" json:"conference_number"`
    HuaweiConfigID      uint      `gorm:"not null;index" json:"huawei_config_id"`
    HuaweiConfig        *HuaweiConfig `gorm:"foreignKey:HuaweiConfigID" json:"huawei_config,omitempty"`
    Status              VideoRecordingTaskStatus `gorm:"type:varchar(20);index" json:"status"`
    RecordingFile       string    `gorm:"type:varchar(500)" json:"recording_file"`
    RecordingDuration   int       `json:"recording_duration"` // 秒
    ErrorMsg            string    `gorm:"type:text" json:"error_msg,omitempty"`
    CreatedBy           uint      `gorm:"not null" json:"created_by"`
    Creator             *User     `gorm:"foreignKey:CreatedBy" json:"creator,omitempty"`
    ConferenceRecord    *ConferenceRecord `gorm:"foreignKey:ConferenceRecordID" json:"conference_record,omitempty"`
    ConferenceRecordID  *uint     `json:"conference_record_id,omitempty"`
}

// 任务状态枚举
type VideoRecordingTaskStatus string

const (
    VideoStatusPending    VideoRecordingTaskStatus = "pending"     // 待执行
    VideoStatusConnecting VideoRecordingTaskStatus = "connecting"  // 连接会议中
    VideoStatusRecording  VideoRecordingTaskStatus = "recording"   // 录制中
    VideoStatusCompleted  VideoRecordingTaskStatus = "completed"   // 已完成
    VideoStatusFailed     VideoRecordingTaskStatus = "failed"      // 执行失败
    VideoStatusCancelled  VideoRecordingTaskStatus = "cancelled"   // 已取消
)

// 状态流转图
/*
    pending ──connecting──> recording ──> completed
        │                       │
        │                       └───────> failed
        │
        └──────────────────────────────> cancelled
*/

// 关键方法
func (t *VideoRecordingTask) GetTriggerTime() time.Time {
    // 返回实际触发时间 = StartTime - PreJoinMinutes
    return t.StartTime.Add(-time.Duration(t.PreJoinMinutes) * time.Minute)
}

func (t *VideoRecordingTask) GetRecordDelayMinutes() int {
    // 返回连接后延迟录制时间
    return t.RecordDelayMinutes
}

func (t *VideoRecordingTask) IsExpired() bool {
    // 检查任务是否已过期(超过EndTime 24小时)
    expiryTime := t.EndTime.Add(24 * time.Hour)
    return time.Now().After(expiryTime)
}

func (t *VideoRecordingTask) IsValid() error {
    // 验证任务数据
    if t.StartTime.After(t.EndTime) {
        return errors.New("开始时间不能晚于结束时间")
    }
    if t.PreJoinMinutes < 0 || t.PreJoinMinutes > 60 {
        return errors.New("提前进入时间必须在0-60分钟之间")
    }
    if t.ConferenceNumber == "" {
        return errors.New("会议号不能为空")
    }
    if t.HuaweiConfigID == 0 {
        return errors.New("必须指定华为配置")
    }
    return nil
}

func (t *VideoRecordingTask) CanTransitionTo(newStatus VideoRecordingTaskStatus) bool {
    // 检查状态转换是否合法
    validTransitions := map[VideoRecordingTaskStatus][]VideoRecordingTaskStatus{
        VideoStatusPending:    {VideoStatusConnecting, VideoStatusCancelled},
        VideoStatusConnecting: {VideoStatusRecording, VideoStatusFailed, VideoStatusCancelled},
        VideoStatusRecording:  {VideoStatusCompleted, VideoStatusFailed, VideoStatusCancelled},
        VideoStatusCompleted:  {}, // 终态
        VideoStatusFailed:     {VideoStatusPending}, // 可重试
        VideoStatusCancelled:  {}, // 终态
    }

    allowed, ok := validTransitions[t.Status]
    if !ok {
        return false
    }

    for _, status := range allowed {
        if status == newStatus {
            return true
        }
    }
    return false
}
```

**字段说明**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| Name | string | 任务名称 | - |
| StartTime | time.Time | 会议开始时间 | - |
| EndTime | time.Time | 会议结束时间 | - |
| PreJoinMinutes | int | 提前进入时间(分钟) | 5 |
| RecordDelayMinutes | int | 连接后延迟录制(分钟) | 0 |
| ConferenceNumber | string | 华为会议号 | - |
| HuaweiConfigID | uint | 华为配置ID | - |
| Status | string | 任务状态 | pending |
| RecordingFile | string | 录制文件路径 | - |
| RecordingDuration | int | 录制时长(秒) | 0 |

### 2.6 HuaweiConfig (华为配置模型)

```go
// internal/models/huawei_config.go
type HuaweiConfig struct {
    Base
    Name                string `gorm:"type:varchar(100);not null" json:"name"`
    Description         string `gorm:"type:text" json:"description"`
    Server              string `gorm:"type:varchar(100);not null" json:"server"`
    Port                int    `gorm:"default:80" json:"port"`
    Username            string `gorm:"type:varchar(50);not null" json:"username"`
    Password            string `gorm:"type:varchar(100);not null" json:"-"`
    TerminalNumber      string `gorm:"type:varchar(50);not null" json:"terminal_number"`
    ConferenceNumber    string `gorm:"type:varchar(50)" json:"conference_number"`

    // USB摄像头配置
    USBCameraName       string `gorm:"type:varchar(100)" json:"usb_camera_name"`
    USBCameraDevice     string `gorm:"type:varchar(100)" json:"usb_camera_device"`
    USBCameraPath       string `gorm:"type:varchar(200)" json:"usb_camera_path"`
    CameraBindingStatus string `gorm:"type:varchar(20);default:unbound" json:"camera_binding_status"`
    // unbound | binding | bound | error

    // USB音频设备配置
    USBAudioDevice      string `gorm:"type:varchar(100)" json:"usb_audio_device"`
    USBAudioPath        string `gorm:"type:varchar(200)" json:"usb_audio_path"`
    AudioBindingStatus  string `gorm:"type:varchar(20);default:unbound" json:"audio_binding_status"`

    // 录制配置
    RecordDirectory     string `gorm:"type:varchar(200)" json:"record_directory"`
    OutputFormat        string `gorm:"type:varchar(20);default:mp4" json:"output_format"` // mp4, mkv, avi

    IsActive            bool   `gorm:"default:true" json:"is_active"`
    IsLocked            bool   `gorm:"default:false" json:"is_locked"` // 终端锁定标志
    LockedBy            *uint  `json:"locked_by,omitempty"`            // 锁定者任务ID
    LockedAt            *time.Time `json:"locked_at,omitempty"`

    VideoRecordingTasks  []VideoRecordingTask `gorm:"foreignKey:HuaweiConfigID" json:"video_recording_tasks,omitempty"`
}

// 设备绑定状态常量
const (
    DeviceStatusUnbound = "unbound"
    DeviceStatusBinding = "binding"
    DeviceStatusBound   = "bound"
    DeviceStatusError   = "error"
)

// 方法
func (h *HuaweiConfig) IsCameraBound() bool
func (h *HuaweiConfig) IsAudioBound() bool
func (h *HuaweiConfig) Lock(taskID uint) error
func (h *HuaweiConfig) Unlock() error
func (h *HuaweiConfig) IsLockedByTask(taskID uint) bool
func (h *HuaweiConfig) GetRTSPURL() string
func (h *HuaweiConfig) Validate() error
```

### 2.7 ConferenceRecord (会议记录模型)

```go
// internal/models/conference_record.go
type ConferenceRecord struct {
    Base
    ConferenceNumber   string    `gorm:"type:varchar(50);uniqueIndex;not null" json:"conference_number"`
    Title              string    `gorm:"type:varchar(200)" json:"title"`
    StartTime          time.Time `gorm:"index" json:"start_time"`
    EndTime            *time.Time `json:"end_time,omitempty"`
    Status             ConferenceStatus `gorm:"type:varchar(20);index" json:"status"`
    Attendees          int       `gorm:"default:0" json:"attendees"`
    Description        string    `gorm:"type:text" json:"description"`
    HuaweiConfigID     *uint     `json:"huawei_config_id,omitempty"`
    HuaweiConfig       *HuaweiConfig `gorm:"foreignKey:HuaweiConfigID" json:"huawei_config,omitempty"`

    VideoFiles         []VideoFile         `gorm:"foreignKey:ConferenceRecordID" json:"video_files,omitempty"`
    VideoRecordingTask *VideoRecordingTask `gorm:"foreignKey:ConferenceRecordID" json:"video_recording_task,omitempty"`
}

// 会议状态
type ConferenceStatus string

const (
    ConferenceStatusNotStarted = "not_started"
    ConferenceStatusInProgress = "in_progress"
    ConferenceStatusCompleted  = "completed"
    ConferenceStatusFailed     = "failed"
)

// 方法
func (c *ConferenceRecord) GetDuration() time.Duration
func (c *ConferenceRecord) IsInProgress() bool
func (c *ConferenceRecord) HasRecordings() bool
func (c *ConferenceRecord) GetTotalRecordingSize() int64
```

### 2.8 VideoFile (视频文件模型)

```go
// internal/models/video_file.go
type VideoFile struct {
    Base
    FileName           string    `gorm:"type:varchar(200);not null" json:"file_name"`
    FilePath           string    `gorm:"type:varchar(500);not null" json:"file_path"`
    FileSize           int64     `gorm:"default:0" json:"file_size"` // 字节
    Duration           int       `gorm:"default:0" json:"duration"`  // 秒
    Format             string    `gorm:"type:varchar(20)" json:"format"` // mp4, mkv
    Resolution         string    `gorm:"type:varchar(20)" json:"resolution"` // 1920x1080
    Bitrate            int       `json:"bitrate"` // kbps
    Codec              string    `gorm:"type:varchar(50)" json:"codec"`
    ConferenceRecordID *uint     `gorm:"index" json:"conference_record_id,omitempty"`
    ConferenceRecord   *ConferenceRecord `gorm:"foreignKey:ConferenceRecordID" json:"conference_record,omitempty"`
    Status             string    `gorm:"type:varchar(20);default:ready" json:"status"` // ready, processing, error
    ThumbnailPath      *string   `json:"thumbnail_path,omitempty"`
    RecordedAt         *time.Time `json:"recorded_at,omitempty"`
}

// 文件状态
const (
    FileStatusReady      = "ready"
    FileStatusProcessing = "processing"
    FileStatusError      = "error"
    FileStatusDeleting   = "deleting"
)

// 方法
func (v *VideoFile) GetSizeMB() float64
func (v *VideoFile) GetDurationString() string
func (v *VideoFile) Exists() bool
func (v *VideoFile) Delete() error
func (v *VideoFile) GetMetadata() (*VideoMetadata, error)
```

### 2.9 PPTFile (PPT文件模型)

```go
// internal/models/ppt_file.go
type PPTFile struct {
    Base
    FileName           string    `gorm:"type:varchar(200);not null" json:"file_name"`
    FilePath           string    `gorm:"type:varchar(500);not null" json:"file_path"`
    FileSize           int64     `gorm:"default:0" json:"file_size"`
    PageCount          int       `gorm:"default:0" json:"page_count"`
    Format             string    `gorm:"type:varchar(20)" json:"format"` // pptx
    ConferenceRecordID *uint     `json:"conference_record_id,omitempty"`
    ConferenceRecord   *ConferenceRecord `gorm:"foreignKey:ConferenceRecordID" json:"conference_record,omitempty"`
    SourceVideoFileID  *uint     `json:"source_video_file_id,omitempty"`
    SourceVideoFile    *VideoFile `gorm:"foreignKey:SourceVideoFileID" json:"source_video_file,omitempty"`
}

// 方法
func (p *PPTFile) GenerateFromVideo(videoFile *VideoFile) error
```

### 2.10 Session (会话模型)

```go
// internal/models/session.go
type Session struct {
    Base
    Token      string    `gorm:"type:varchar(64);uniqueIndex;not null" json:"token"`
    UserID     uint      `gorm:"not null;index" json:"user_id"`
    User       *User     `gorm:"foreignKey:UserID" json:"user,omitempty"`
    ExpiresAt  time.Time `gorm:"index" json:"expires_at"`
    IPAddress  string    `gorm:"type:varchar(50)" json:"ip_address"`
    UserAgent  string    `gorm:"type:varchar(500)" json:"user_agent"`
    IsActive   bool      `gorm:"default:true" json:"is_active"`
}

// 方法
func (s *Session) IsExpired() bool
func (s *Session) Refresh(duration time.Duration) error
func (s *Session) Revoke() error
```

## 三、数据库关系

### 3.1 关系类型

| 关系 | 类型 | 说明 |
|------|------|------|
| User ↔ Role | Many-to-One | 多个用户属于一个角色 |
| Role ↔ Permission | Many-to-Many | 角色拥有多个权限 |
| User ↔ APIKey | One-to-Many | 用户拥有多个API密钥 |
| User ↔ VideoRecordingTask | One-to-Many | 用户创建多个任务 |
| VideoRecordingTask ↔ HuaweiConfig | Many-to-One | 多个任务使用一个配置 |
| HuaweiConfig ↔ VideoRecordingTask | One-to-Many | 配置被多个任务使用 |
| ConferenceRecord ↔ VideoFile | One-to-Many | 会议产生多个视频 |
| ConferenceRecord ↔ VideoRecordingTask | One-to-One | 会议关联一个录制任务 |
| VideoFile ↔ PPTFile | One-to-One | 视频可生成PPT |

### 3.2 级联规则

```go
// 删除用户时
// - 不删除关联的任务(保留创建者ID)
// - 撤销所有活动会话
// - 禁用所有API密钥

// 删除华为配置时
// - 不删除关联的任务(保留配置ID)
// - 但标记任务为无效状态

// 删除会议记录时
// - 级联删除关联的视频文件
// - 级联删除关联的PPT文件

// 删除视频文件时
// - 级联删除关联的PPT文件
// - 从文件系统中删除实际文件
```

## 四、数据验证规则

### 4.1 模型验证

```go
// 统一验证接口
type Validator interface {
    Validate() error
}

// VideoRecordingTask 验证规则
func (t *VideoRecordingTask) Validate() error {
    var errs []string

    // 必填字段
    if t.Name == "" {
        errs = append(errs, "任务名称不能为空")
    }
    if t.ConferenceNumber == "" {
        errs = append(errs, "会议号不能为空")
    }
    if t.HuaweiConfigID == 0 {
        errs = append(errs, "必须指定华为配置")
    }

    // 时间验证
    if t.StartTime.IsZero() {
        errs = append(errs, "开始时间不能为空")
    }
    if t.EndTime.IsZero() {
        errs = append(errs, "结束时间不能为空")
    }
    if !t.StartTime.Before(t.EndTime) {
        errs = append(errs, "开始时间必须早于结束时间")
    }

    // 数值范围
    if t.PreJoinMinutes < 0 || t.PreJoinMinutes > 60 {
        errs = append(errs, "提前进入时间必须在0-60分钟之间")
    }
    if t.RecordDelayMinutes < 0 || t.RecordDelayMinutes > 30 {
        errs = append(errs, "录制延迟必须在0-30分钟之间")
    }

    if len(errs) > 0 {
        return fmt.Errorf("验证失败: %s", strings.Join(errs, "; "))
    }
    return nil
}

// HuaweiConfig 验证规则
func (h *HuaweiConfig) Validate() error {
    var errs []string

    if h.Name == "" {
        errs = append(errs, "配置名称不能为空")
    }
    if h.Server == "" {
        errs = append(errs, "服务器地址不能为空")
    }
    if h.Username == "" {
        errs = append(errs, "用户名不能为空")
    }
    if h.Password == "" {
        errs = append(errs, "密码不能为空")
    }
    if h.TerminalNumber == "" {
        errs = append(errs, "终端号码不能为空")
    }

    if len(errs) > 0 {
        return fmt.Errorf("验证失败: %s", strings.Join(errs, "; "))
    }
    return nil
}
```

### 4.2 数据库约束

```sql
-- 索引定义
CREATE UNIQUE INDEX idx_users_username ON users(username);
CREATE INDEX idx_tasks_status ON video_recording_tasks(status);
CREATE INDEX idx_tasks_start_time ON video_recording_tasks(start_time);
CREATE INDEX idx_conferences_number ON conference_records(conference_number);
CREATE INDEX idx_api_keys_key ON api_keys(key);
CREATE INDEX idx_sessions_token ON sessions(token);

-- 外键约束
ALTER TABLE video_recording_tasks
ADD CONSTRAINT fk_tasks_huawei_config
FOREIGN KEY (huawei_config_id) REFERENCES huawei_configs(id);

ALTER TABLE video_recording_tasks
ADD CONSTRAINT fk_tasks_creator
FOREIGN KEY (created_by) REFERENCES users(id);

-- 检查约束
ALTER TABLE video_recording_tasks
ADD CONSTRAINT chk_tasks_time_range
CHECK (end_time > start_time);

ALTER TABLE video_recording_tasks
ADD CONSTRAINT chk_tasks_pre_join
CHECK (pre_join_minutes >= 0 AND pre_join_minutes <= 60);
```

## 五、数据迁移

### 5.1 自动迁移

```go
// cmd/server/app.go
func (a *MinimalApp) migrateDatabase() error {
    return a.db.AutoMigrate(
        &models.User{},
        &models.Role{},
        &models.Permission{},
        &models.APIKey{},
        &models.VideoRecordingTask{},
        &models.ConferenceRecord{},
        &models.VideoFile{},
        &models.PPTFile{},
        &models.HuaweiConfig{},
        &models.Session{},
    )
}
```

### 5.2 种子数据

```go
// scripts/seed.go
func SeedDatabase(db *gorm.DB) error {
    // 创建默认角色
    roles := []*models.Role{
        {Name: models.RoleAdmin, Description: "系统管理员"},
        {Name: models.RoleOperator, Description: "操作员"},
        {Name: models.RoleViewer, Description: "查看者"},
        {Name: models.RoleAPIClient, Description: "API客户端"},
    }

    for _, role := range roles {
        db.FirstOrCreate(role, models.Role{Name: role.Name})
    }

    // 创建默认管理员用户
    admin := &models.User{
        Username:     "admin",
        Email:        "admin@example.com",
        FullName:     "系统管理员",
        RoleID:       adminRole.ID,
        IsActive:     true,
    }
    admin.SetPassword("admin123")
    db.FirstOrCreate(admin, models.User{Username: "admin"})

    return nil
}
```

## 六、查询优化

### 6.1 常用查询

```go
// 使用索引的查询
db.Where("status = ?", VideoStatusPending).
   Where("start_time > ?", time.Now()).
   Order("start_time ASC").
   Find(&tasks)

// 预加载关联
db.Preload("HuaweiConfig").
   Preload("Creator").
   Preload("ConferenceRecord").
   First(&task, taskID)

// 分页查询
db.Offset((page - 1) * pageSize).
   Limit(pageSize).
   Find(&tasks)
```

### 6.2 N+1 问题解决

```go
// 错误示例: N+1 查询
var tasks []VideoRecordingTask
db.Find(&tasks)
for _, task := range tasks {
    db.Model(&task).Association("HuaweiConfig").Find() // N次查询
}

// 正确示例: 使用 Preload
var tasks []VideoRecordingTask
db.Preload("HuaweiConfig").Find(&tasks) // 1次查询

// 或使用 Joins
db.Joins("HuaweiConfig").Find(&tasks)
```

## 七、相关文档

- [01-系统架构总览.md](./01-系统架构总览.md)
- [03-视频录制任务生命周期.md](./03-视频录制任务生命周期.md)
