---
phase: 20-handleerror-classify-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/errors/errors.go
  - internal/errors/mapping.go
  - internal/errors/mapping_test.go
  - pkg/response/sentinel.go
  - pkg/response/sentinel_field_test.go
  - internal/auth/ad_auth.go
  - internal/handlers/auth_handler_test.go
autonomous: true
requirements: [REQ-20b-sentinel-field, REQ-20b-priority, REQ-20a-ad-user-not-registered]
user_setup: []

must_haves:
  truths:
    - "Calling response.SentinelField(err) on a sentinel-wrapped error returns zap.Field with key 'sentinel_type' and value matching the sentinel's identifier (e.g., 'ErrTaskNotFound')"
    - "Calling response.SentinelField on a BusinessError returns 'BusinessError(code=NOT_FOUND)' style value"
    - "Calling response.SentinelField on an unknown error returns 'ad-hoc'"
    - "Calling response.SentinelField(nil) returns zap.Skip() (no field emitted)"
    - "internal/errors.FirstKnownSentinelName mirrors IsKnownError slice order — the first errors.Is hit wins"
    - "auth.ErrADUserNotRegistered local var is deleted from internal/auth/ad_auth.go; callers route through internal/errors.ErrADUserNotRegistered"
    - "403 Forbidden semantics for unregistered AD users preserved (no regression to 500)"
    - "typed error kind enum (Sentinel vs BusinessError vs ad-hoc) is explicitly out of scope per D-01.1; SentinelField returns zap.Field (string-valued) to preserve future extensibility per R-6"
  artifacts:
    - path: "internal/errors/mapping.go"
      provides: "FirstKnownSentinelName(err) (string, bool) + ErrADUserNotRegistered case in Forbidden branch + slice entry"
      contains: "FirstKnownSentinelName"
    - path: "pkg/response/sentinel.go"
      provides: "SentinelField(err error) zap.Field public helper"
      exports: ["SentinelField"]
    - path: "internal/errors/errors.go"
      provides: "ErrADUserNotRegistered sentinel in 认证 group"
      contains: "ErrADUserNotRegistered"
  key_links:
    - from: "pkg/response/sentinel.go"
      to: "internal/errors.FirstKnownSentinelName"
      via: "direct call"
      pattern: "FirstKnownSentinelName"
    - from: "internal/auth/ad_auth.go"
      to: "internal/errors.ErrADUserNotRegistered"
      via: "apperrors alias import"
      pattern: "apperrors\\.ErrADUserNotRegistered"
---

<objective>
Lay the Phase 20 foundation: (1) export `FirstKnownSentinelName` from `internal/errors` so downstream helpers mirror `IsKnownError` slice order without duplicating the slice; (2) add `response.SentinelField(err) zap.Field` in `pkg/response` with the 4-state contract (sentinel / BusinessError / unknown / nil) for REQ-20b; (3) migrate `auth.ErrADUserNotRegistered` into `internal/errors` per user-locked R-3 decision (D-02.2 补漏 exception) so the auth login path can route through `HandleError` without falling to 500 ad-hoc.

Purpose: Every subsequent plan (20-02 handler classify replacement, 20-03 light handlers, 20-04 service zap upgrade) consumes `response.SentinelField` and depends on the migrated sentinel. This plan MUST land first.

Output: Three test-backed additions to `internal/errors` + `pkg/response` + migration of one cross-package sentinel, preserving all existing 403 login-rejection semantics. Zero behavior regression — `go build ./...` + `go test -race ./internal/... ./pkg/response/...` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-handleerror-classify-convergence/20-CONTEXT.md
@.planning/phases/20-handleerror-classify-convergence/20-RESEARCH.md
@.planning/phases/20-handleerror-classify-convergence/20-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs — extracted from current codebase (HEAD 570a2bc). -->
<!-- Executor SHOULD read these files fresh at task time to confirm signatures, but the -->
<!-- contracts below are the source of truth for what this plan must produce. -->

From internal/errors/mapping.go (lines 125-158) — IsKnownError slice (must be mirrored by FirstKnownSentinelName):
```go
func IsKnownError(err error) bool {
    if err == nil { return false }
    var be *BusinessError
    if errors.As(err, &be) { return true }
    for _, sentinel := range []error{
        ErrNotFound, ErrTaskNotFound, ErrVideoFileNotFound,
        ErrUserNotFound, ErrRoleNotFound, ErrADAccountNotFound, ErrPermissionNotFound,
        ErrAPIKeyNotFound, ErrPPTFileNotFound,
        ErrUnauthorized, ErrForbidden, ErrInvalidInput, ErrInvalidFileType,
        ErrAlreadyExists, ErrTaskInProgress, ErrUsernameExists, ErrEmailExists,
        ErrRoleNameExists, ErrRoleInUse,
        ErrSystemAdminProtected, ErrSystemRoleProtected, ErrUserDisabled,
        ErrAPIKeyDisabled, ErrAPIKeyIPNotAllowed,
        ErrTokenInvalid, ErrTokenExpired, ErrTokenNotYetValid, ErrTokenReplayed,
        ErrAPIKeyInvalid, ErrAPIKeyExpired,
        ErrInsufficientQuota,
        ErrServiceUnavailable, ErrADConfigError, ErrADUnreachable,
        ErrTranscriptionUnavailable,
        ErrFFmpegFailed, ErrTranscriptionFailed,
        ErrSplitFailed, ErrInternal,
        ErrDuplicateRecord, ErrForeignKeyConstraint,
    } {
        if errors.Is(err, sentinel) { return true }
    }
    return false
}
```

From internal/errors/mapping.go (lines 63-69) — Forbidden branch where ErrADUserNotRegistered must be added:
```go
case errors.Is(err, ErrForbidden),
    errors.Is(err, ErrSystemAdminProtected),
    errors.Is(err, ErrSystemRoleProtected),
    errors.Is(err, ErrUserDisabled),
    errors.Is(err, ErrAPIKeyDisabled),
    errors.Is(err, ErrAPIKeyIPNotAllowed):
    return http.StatusForbidden, respCodeForbidden, "禁止访问"
```

From internal/errors/errors.go (around line 50-60) — 认证 domain sentinel group where ErrADUserNotRegistered will be added:
```go
// 认证
ErrADAccountNotFound = errors.New("...")
ErrUserDisabled      = errors.New("...")
ErrADConfigError     = errors.New("...")
ErrADUnreachable     = errors.New("...")
```

From internal/auth/ad_auth.go (lines 10-28) — current state before migration:
```go
apperrors "github.com/NDCCCCCC/video-meeting-recorder/internal/errors"  // already imported as apperrors

var ErrADUserNotRegistered = errors.New("账号未在系统中注册，请联系管理员添加")

func IsADUserNotRegistered(err error) bool {
    return errors.Is(err, ErrADUserNotRegistered)
}
```

From internal/errors/errors.go (lines 117-150) — BusinessError typed structure (SentinelField must errors.As this):
```go
type BusinessError struct {
    Code    string `json:"code"`
    Message string `json:"message"`
    Err     error  `json:"-"`
}
func (e *BusinessError) Error() string { /* ... */ }
func (e *BusinessError) Unwrap() error { return e.Err }
func NewBusinessError(code, message string, err error) *BusinessError { ... }
```

From pkg/response/response.go (lines 1-9) — current imports (SentinelField will add go.uber.org/zap):
```go
package response
import (
    "encoding/json"
    "net/http"
    "github.com/NDCCCCCC/video-meeting-recorder/internal/errors"
    "github.com/gin-gonic/gin"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export FirstKnownSentinelName from internal/errors</name>
  <files>internal/errors/mapping.go, internal/errors/mapping_test.go</files>
  <read_first>
    - .planning/phases/20-handleerror-classify-convergence/20-RESEARCH.md (§2.4 — full IsKnownError slice + commentary on slice order = D-03.3 priority)
    - internal/errors/mapping.go (full file — current IsKnownError + MapToHTTPStatus + FromGORM + NotFound)
    - internal/errors/mapping_test.go (existing — append, do not rewrite)
    - internal/errors/errors.go (sentinel declaration block — names must match exactly)
  </read_first>
  <behavior>
    - Test 1 (each sentinel hit by name): for every one of the 42 sentinels in the IsKnownError slice (41 existing + new ErrADUserNotRegistered from Task 3, but Task 1 test uses the 41 existing only — Task 3 test extends), FirstKnownSentinelName(sentinel) returns ("ErrNotFound", true) etc. — name matches the Go identifier exactly.
    - Test 2 (wrapped sentinel): `FirstKnownSentinelName(fmt.Errorf("ctx: %w", ErrTaskNotFound))` returns ("ErrTaskNotFound", true).
    - Test 3 (priority — first hit wins): if an error wraps both ErrNotFound and ErrTaskNotFound (rare but constructable via nested fmt.Errorf), the result matches whichever appears FIRST in the slice (ErrNotFound per current slice order).
    - Test 4 (BusinessError → false): `FirstKnownSentinelName(NewBusinessError(CodeInvalidInput, "x", nil))` returns ("", false) — callers must errors.As BusinessError separately.
    - Test 5 (nil): `FirstKnownSentinelName(nil)` returns ("", false).
    - Test 6 (unknown): `FirstKnownSentinelName(errors.New("random"))` returns ("", false).
  </behavior>
  <action>
Add exported function `FirstKnownSentinelName(err error) (name string, ok bool)` to internal/errors/mapping.go immediately after IsKnownError. It must iterate the SAME slice (copy the literal — do not refactor IsKnownError to share a package-level var unless both functions use it; if refactoring to shared slice, do it in same commit and retest IsKnownError). Use a `switch { case errors.Is(err, ErrXxx): return "ErrXxx", true; ... }` style OR iterate a `[]error` slice and derive name via `reflect.TypeOf(sentinel).String()` — NO, do NOT use reflect; instead iterate a parallel `[]struct{Name string; Err error}` literal so the name is a stable string literal (not derived via reflection — reflection on errors.New sentinels yields "*errors.errorString" which is useless). The parallel struct slice is the only correct approach. The Name field MUST be the exact Go identifier (e.g., "ErrTaskNotFound", "ErrADUserNotRegistered").

Mirror the slice order EXACTLY per D-03.3 (do not reorder to "more specific first" — CONTEXT locked the order). Return on first `errors.Is` hit. For nil and BusinessError and unknown, return ("", false).

Update IsKnownError to delegate to FirstKnownSentinelName if cleaner: `_, ok := FirstKnownSentinelName(err); return ok || errors.As(err, &be)`. If doing this refactor, run existing TestIsKnownError (if present) to confirm no regression.

Append to internal/errors/mapping_test.go a new TestFirstKnownSentinelName table-driven subtests covering all 6 behaviors above.
  </action>
  <verify>
    <automated>go test ./internal/errors/ -run TestFirstKnownSentinelName -count=1 -v</automated>
    <automated>go test ./internal/errors/ -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func FirstKnownSentinelName' internal/errors/mapping.go` returns 1
    - `go test ./internal/errors/ -run TestFirstKnownSentinelName -count=1` exits 0 with all 6 subtests passing
    - `go build ./...` exits 0
    - Existing `go test ./internal/errors/ -count=1` still passes (no regression to IsKnownError or MapToHTTPStatus)
    - Slice order in FirstKnownSentinelName matches IsKnownError byte-for-byte (visual diff confirms same ordering)
  </acceptance_criteria>
  <done>
FirstKnownSentinelName exported, tested, mirrors slice order. Ready for SentinelField (Task 2) to consume.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SentinelField helper to pkg/response + 4-state table-driven test</name>
  <files>pkg/response/sentinel.go, pkg/response/sentinel_field_test.go</files>
  <read_first>
    - .planning/phases/20-handleerror-classify-convergence/20-CONTEXT.md (§specifics — SentinelField 4-state contract per D-03.4/5)
    - .planning/phases/20-handleerror-classify-convergence/20-RESEARCH.md (§3 — pkg/response already imports internal/errors; §9 R-6 — return zap.Field not zap.String for future extensibility)
    - pkg/response/response.go (current imports + Response struct + HandleError pattern)
    - pkg/response/response_test.go (existing — to mirror test style and confirm testify/assert usage)
    - internal/errors/errors.go (BusinessError shape for errors.As)
    - internal/errors/mapping.go (Task 1 output — FirstKnownSentinelName signature)
  </read_first>
  <behavior>
    - Test 1 (sentinel hit): `SentinelField(apperrors.ErrTaskNotFound)` yields zap.Field with Key="sentinel_type" and String value "ErrTaskNotFound".
    - Test 2 (sentinel wrapped): `SentinelField(fmt.Errorf("ctx: %w", apperrors.ErrUserDisabled))` yields String value "ErrUserDisabled".
    - Test 3 (BusinessError): `SentinelField(apperrors.NewBusinessError(apperrors.CodeNotFound, "missing", nil))` yields String value "BusinessError(code=NOT_FOUND)".
    - Test 4 (unknown error): `SentinelField(errors.New("random"))` yields String value "ad-hoc".
    - Test 5 (nil): `SentinelField(nil)` returns zap.Skip() — callers can detect via reflect.TypeOf(field) == skip field or by checking field.Key == "" (zap.Skip returns a field with Key=""; verify in test by asserting the field's String serialization yields "" or no key — consult zap docs for the canonical assertion).
    - Test 6 (priority — mirrors IsKnownError slice): if an error wraps multiple sentinels, SentinelField returns the first-hit name (delegated to FirstKnownSentinelName).
  </behavior>
  <action>
Create new file pkg/response/sentinel.go with:
- package response
- imports: "errors" (std), "fmt", "github.com/NDCCCCCC/video-meeting-recorder/internal/errors" (alias as `apperrors` to match project convention OR keep as `errors` if no std "errors" conflict — RESEARCH §3 confirms pkg/response already imports internal/errors as `errors`; use a DIFFERENT alias to avoid shadowing std errors; recommended alias: `apperrors`), "go.uber.org/zap".
- Function `SentinelField(err error) zap.Field` implementing the 4-state contract:
  (a) err == nil → return zap.Skip()
  (b) errors.As(err, &be) where be is *apperrors.BusinessError → return zap.String("sentinel_type", fmt.Sprintf("BusinessError(code=%s)", be.Code))
  (c) name, ok := apperrors.FirstKnownSentinelName(err); ok → return zap.String("sentinel_type", name)
  (d) fallback → return zap.String("sentinel_type", "ad-hoc")
- Return type MUST be `zap.Field` (not a pre-built zap.String) per R-6 — the function returns a concrete zap.Field value each call; this leaves room for a future refactor to zap.Object or multi-field without changing call sites.

Add code comment at top citing D-03.1 (placed in pkg/response, not pkg/logging, per user decision) and R-6 (zap.Field return type preserves future typed-kind extensibility).

Create new file pkg/response/sentinel_field_test.go with table-driven TestSentinelField covering all 6 behaviors. For nil case, assert via `assert.Equal(t, zap.Field{Type: zap.SkipType}, field)` OR call field.Equals(zap.Skip()) if available — consult zap docs; fallback assertion is `assert.Equal(t, "", field.Key)` since zap.Skip() produces a field with empty Key. Use testify/assert (already in go.mod per RESEARCH §8).
  </action>
  <verify>
    <automated>go test ./pkg/response/ -run TestSentinelField -count=1 -v</automated>
    <automated>go build ./... && go vet ./pkg/response/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func SentinelField' pkg/response/sentinel.go` returns 1
    - `grep -c 'FirstKnownSentinelName' pkg/response/sentinel.go` returns >= 1 (delegation confirmed)
    - `go test ./pkg/response/ -run TestSentinelField -count=1` exits 0 with 6 subtests passing
    - `go build ./...` exits 0
    - No new package imported beyond go.uber.org/zap (already in go.mod)
  </acceptance_criteria>
  <done>
SentinelField public helper live, 4-state contract verified, ready for handler/service call-site upgrades in 20-02/20-03/20-04.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate auth.ErrADUserNotRegistered → internal/errors (R-3 user-locked decision)</name>
  <files>internal/errors/errors.go, internal/errors/mapping.go, internal/auth/ad_auth.go, internal/handlers/auth_handler_test.go</files>
  <read_first>
    - .planning/phases/20-handleerror-classify-convergence/20-CONTEXT.md (§decisions D-02.2 "补漏" principle)
    - .planning/phases/20-handleerror-classify-convergence/20-RESEARCH.md (§7 cross-package survey — ad_auth.go:22 + §9 R-3 risk full analysis)
    - planning_context user_decisions_locked item 1 (R-3 migration is user-locked, NOT optional)
    - internal/auth/ad_auth.go (full file — locate every usage of ErrADUserNotRegistered: declaration, IsADUserNotRegistered helper, every `return ErrADUserNotRegistered` site)
    - internal/errors/errors.go (find 认证 group to place new sentinel)
    - internal/errors/mapping.go (Forbidden branch + IsKnownError slice to extend)
    - internal/handlers/auth_handler_test.go (TestClassifyAuthLoginError — will be fully rewritten in 20-02, but THIS task only does the compilation fix: auth.ErrADUserNotRegistered → apperrors.ErrADUserNotRegistered)
    - internal/handlers/auth_handler.go (classifyAuthLoginError uses auth.IsADUserNotRegistered — keep helper as wrapper so classifyAuthLoginError still compiles; 20-02 deletes both)
  </read_first>
  <action>
This is the R-3 user-locked decision: migrate `auth.ErrADUserNotRegistered` into `internal/errors` as a D-02.2 "补漏" exception (NOT "主动加新 sentinel"). Commit message body MUST contain: "R-3 决策：迁移 ad_auth.ErrADUserNotRegistered 至 internal/errors 属 D-02.2 补漏例外（user-locked in plan-phase）".

(a) In `internal/errors/errors.go`: add `ErrADUserNotRegistered = errors.New("账号未在系统中注册，请联系管理员添加")` to the 认证 domain group (alongside ErrADAccountNotFound, ErrUserDisabled, ErrADConfigError, ErrADUnreachable). Use the EXACT same message string as the current ad_auth.go:22 declaration (preserves log output / user feedback).

(b) In `internal/errors/mapping.go`:
- Add `errors.Is(err, ErrADUserNotRegistered),` to the Forbidden switch case (lines 63-68) so MapToHTTPStatus returns 403/respCodeForbidden/"禁止访问".
- Add `ErrADUserNotRegistered,` to the IsKnownError slice (lines 135-151) — place it adjacent to the 认证 Forbidden sentinels (e.g., right after ErrUserDisabled for grouping clarity). Also add it to the FirstKnownSentinelName parallel struct slice added in Task 1.

(c) In `internal/auth/ad_auth.go`:
- DELETE line 22: `var ErrADUserNotRegistered = errors.New(...)`.
- KEEP `func IsADUserNotRegistered(err error) bool` but reroute body to `return errors.Is(err, apperrors.ErrADUserNotRegistered)`. This preserves the helper so `classifyAuthLoginError` (still alive until 20-02) compiles unchanged.
- Update every `return ErrADUserNotRegistered` site inside ad_auth.go to `return apperrors.ErrADUserNotRegistered` (the `apperrors` alias is already imported at line 10).
- Remove the `"errors"` std import if ad_auth.go no longer uses it directly (it does — for `errors.Is` in the new IsADUserNotRegistered body and likely elsewhere; keep import).

(d) In `internal/handlers/auth_handler_test.go`: change every `auth.ErrADUserNotRegistered` reference to `apperrors.ErrADUserNotRegistered` (the test file already imports `apperrors` per line 11). The 2 affected sub-tests are "sentinel maps to forbidden" and "wrapped sentinel maps to forbidden". DO NOT rewrite the test function or rename it — 20-02 Task 3 does the full rewrite. This task only fixes the compilation break caused by deleting the auth package var. The wantCode/wantStatus for these subtests stays at CodeForbidden/StatusForbidden (now via mapping.go instead of classifyAuthLoginError — but classifyAuthLoginError still returns CodeForbidden via auth.IsADUserNotRegistered wrapper, so behavior unchanged).

Note: ad_auth.go is touched again in 20-04 Task 2 for zap.Error upgrades. No conflict — sequential waves.
  </action>
  <verify>
    <automated>grep -c 'var ErrADUserNotRegistered' internal/auth/ad_auth.go  # must return 0</automated>
    <automated>grep -c 'ErrADUserNotRegistered = errors.New' internal/errors/errors.go  # must return 1</automated>
    <automated>grep -c 'ErrADUserNotRegistered' internal/errors/mapping.go  # must return >= 2 (Forbidden branch + IsKnownError slice + FirstKnownSentinelName slice = 3)</automated>
    <automated>go build ./... && go test ./internal/errors/ ./internal/auth/ ./internal/handlers/ -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'var ErrADUserNotRegistered' internal/auth/ad_auth.go` returns 0 (local var deleted)
    - `grep -c 'ErrADUserNotRegistered = errors.New' internal/errors/errors.go` returns 1 (sentinel centralized)
    - `grep -c 'errors.Is(err, ErrADUserNotRegistered)' internal/errors/mapping.go` returns 1 (Forbidden branch)
    - `grep -c 'ErrADUserNotRegistered' internal/errors/mapping.go` returns >= 3 (Forbidden + IsKnownError slice + FirstKnownSentinelName slice)
    - `grep -c 'auth.ErrADUserNotRegistered' internal/` returns 0 (all references migrated)
    - `go build ./...` exits 0
    - `go test ./internal/auth/ ./internal/errors/ ./internal/handlers/ -count=1 -race` exits 0
    - Existing TestClassifyAuthLoginError sub-test "sentinel maps to forbidden" still passes with wantStatus=StatusForbidden (403 preserved)
  </acceptance_criteria>
  <done>
auth.ErrADUserNotRegistered local var eliminated; sentinel centralized in internal/errors with 403 mapping; auth_handler_test.go compiles; IsADUserNotRegistered helper preserved as wrapper for backward compat until 20-02 deletes classifyAuthLoginError.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → handler (login) | Untrusted input crosses here; login errors (401/403/404/503) reflect auth state |
| internal/auth → internal/errors | Cross-package sentinel migration; identity must remain stable via `errors.Is` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-info-disclosure | Information Disclosure | SentinelField output / FirstKnownSentinelName | accept | SentinelField returns ONLY the sentinel identifier string (e.g., "ErrTaskNotFound") or "ad-hoc" — never err.Error() body. Logging-only field; not in HTTP response. No PII leak surface. |
| T-20-02-auth-sidechannel | Information Disclosure | Login 403 for ErrADUserNotRegistered | accept | Behavior UNCHANGED — classifyAuthLoginError already returned 403 for this case; R-3 migration preserves 403 via mapping.go. Not a new side channel. Existing user-enumeration risk (if any) is pre-existing and accepted. |
| T-20-03-nil-deref | Denial of Service | SentinelField / FirstKnownSentinelName on nil err | mitigate | Both functions explicitly guard `if err == nil` before any errors.Is/As call. Test 5 (nil input) in both Task 1 and Task 2 verifies. |
| T-20-SC-supply-chain | Tampering | go.uber.org/zap import (re-used) | accept | zap already in go.mod (verified RESEARCH §3). No new package install in this plan. Legitimacy pre-validated. |
</threat_model>

<verification>
Plan-level checks (run after all 3 tasks complete):
- `go build ./...` exits 0
- `go vet ./internal/errors/... ./pkg/response/... ./internal/auth/... ./internal/handlers/...` exits 0
- `go test -race ./internal/errors/ ./pkg/response/ ./internal/auth/ ./internal/handlers/ -count=1` exits 0
- `grep -rn 'auth.ErrADUserNotRegistered' internal/` returns empty (migration complete)
- `grep -c 'func SentinelField' pkg/response/sentinel.go` returns 1
- `grep -c 'func FirstKnownSentinelName' internal/errors/mapping.go` returns 1
</verification>

<success_criteria>
- FirstKnownSentinelName exported with full table-driven coverage mirroring IsKnownError slice order
- SentinelField helper public in pkg/response, 4 input classes (sentinel/BusinessError/unknown/nil) verified
- R-3 migration complete: auth.ErrADUserNotRegistered eliminated from internal/auth, centralized in internal/errors with 403 Forbidden mapping preserved
- All existing tests pass; no behavior regression
- Foundation ready for 20-02/20-03/20-04 to consume SentinelField + ErrADUserNotRegistered
</success_criteria>

<output>
Create `.planning/phases/20-handleerror-classify-convergence/20-01-SUMMARY.md` when done
</output>
