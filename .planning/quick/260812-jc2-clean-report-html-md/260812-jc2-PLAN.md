---
phase: quick-260812-jc2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - clean/internal/report/html.go
  - clean/internal/report/report.go
  - clean/cmd/clean/main.go
autonomous: true
requirements: [QUICK-260812-JC2]
must_haves:
  truths:
    - "执行 clean / clean scan / clean clean / clean report 后会在 reports 目录生成 <ts>-report.html"
    - "生成的 HTML 文件双击（file:// 协议）在 Edge/Chrome 打开能渲染出与同名 .md 完全一致的五节结构"
    - "HTML 内每一个文件路径（敏感文件表 + 命中详情标题）都是 <a> 超链接，点击调起 explorer 打开所在目录"
    - "路径、命中行文本里的 < > & \" ' 等字符被 HTML 转义，不破坏结构也不注入"
    - "HTML 自包含：内联 <style>，不依赖任何外部 CSS/JS/字体 CDN"
    - "go build ./... 和 go vet ./... 在 clean 模块根目录均 exit 0"
  artifacts:
    - path: clean/internal/report/html.go
      provides: "writeHTML + escapeHTML + fileDirLink helper"
      contains: "func writeHTML"
    - path: clean/internal/report/report.go
      provides: "Write 返回 (jsonPath, mdPath, htmlPath, err)"
      contains: "htmlPath"
    - path: clean/cmd/clean/main.go
      provides: "runAll/runScan/runClean/runReport 适配 3 路径返回值"
  key_links:
    - from: clean/internal/report/report.go
      to: clean/internal/report/html.go
      via: "writeHTML 调用"
      pattern: "writeHTML\\("
    - from: clean/cmd/clean/main.go
      to: clean/internal/report/report.go
      via: "report.Write 返回值解构"
      pattern: "report\\.Write\\("
---

<objective>
为 clean 工具的 report 包增加 HTML 报告输出（`<ts>-report.html`），结构完全对齐现有 MD 报告的五节布局，并把所有文件路径渲染成 `file:///` 目录超链接——本地双击打开 .html 后点击路径即可在资源管理器中打开所在目录。

Purpose: MD 报告不可点击，每条命中路径用户都得手动复制到 explorer；HTML + file:// 链接让审计流程从 "复制路径 → win+R → 粘贴" 变成 "点一下"，零额外依赖（不需要自定义协议 / 管理员权限 / 本地 http 服务）。

Output:
- 新文件 `clean/internal/report/html.go`（writeHTML + 转义 + file:// 目录链接 helper）
- 改 `clean/internal/report/report.go`：Write 返回值加 htmlPath
- 改 `clean/cmd/clean/main.go`：runAll / runScan / runClean / runReport 4 处调用点适配新签名
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@clean/internal/report/report.go
@clean/internal/browser/browser.go
@clean/internal/scanner/walk.go
@clean/internal/sensitive/sensitive.go
@clean/reports/20260812-001021-report.md

<interfaces>
<!-- 执行器直接用以下契约，无需再 grep/explore 代码库 -->

### report.FullReport 结构（来自 clean/internal/report/report.go）
```go
type FullReport struct {
    Timestamp string
    Hostname  string
    UserHome  string

    BrowserCleaned []browser.Target          // 已清理浏览器目标（Enumerate 后含 SizeBytes）
    IMCleaned      []im.Target               // 已清理 IM 目标
    SystemCleaned  []system.Target           // 已清理系统临时目标
    SensitiveFiles []sensitive.SensitiveFile // 敏感文件清单（仅报告）
    PasswordHits   []scanner.FileHit         // 敏感数据扫描命中（按 Reason 分组）

    BrowserBytes int64
    IMBytes      int64
    SystemBytes  int64
}
```

### browser.Target / browser.Summary（来自 clean/internal/browser/browser.go）
```go
type Target struct {
    Browser   string
    Category  Category
    Path      string
    SizeBytes int64
}
type Summary struct {
    Browser string
    Count   int
    Bytes   int64
}
func Summarize(targets []Target) []Summary
func HumanBytes(b int64) string  // 友好显示 "1.23 MB"
```

### im.Summary（report.go 中已用，签名同 browser.Summary 三字段）
```go
func Summarize(targets []Target) []Summary  // im.Summarize，字段 App / Count / Bytes
```
注意：MD 里 IM 行用的是 `s.App` 而不是 `s.Browser`，HTML 也要保持这个差异。

### system.Target（report.go 中已用）
```go
type Target struct {
    Name      string
    SizeBytes int64
    // ... 其它字段
}
```

### sensitive.SensitiveFile（来自 clean/internal/sensitive/sensitive.go）
```go
type SensitiveFile struct {
    Path     string
    Size     int64
    Category string    // credential_file / ssh_key / secret_file / tool_credential
    Risk     RiskLevel // high / medium / low
    Reason   string
}
type RiskLevel string  // RiskHigh="high" / RiskMedium="medium" / RiskLow="low"
```

### scanner.FileHit / LineEvidence（来自 clean/internal/scanner/walk.go）
```go
type FileHit struct {
    Path     string
    Reason   string         // "credential" 或 "pii"
    Evidence []LineEvidence
}
type LineEvidence struct {
    LineNo   int
    LineText string  // 已经过 maskLine 脱敏（含 [REDACTED]）
}
```

### report 包内已有的 Risk 别名（report.go 末尾）
```go
type Risk = sensitive.RiskLevel
const (
    RiskHigh   = sensitive.RiskHigh
    RiskMedium = sensitive.RiskMedium
    RiskLow    = sensitive.RiskLow
)
```
html.go 可直接用 `RiskHigh / RiskMedium / RiskLow` 做排序 rank 映射，与 writeMarkdown 保持一致。

### 现有 writeMarkdown 的排序 + 分组规约（HTML 必须对齐）
- 敏感文件表（第四节）：按 Risk rank 排序，rank 映射 `{RiskHigh:0, RiskMedium:1, RiskLow:2}`
- 第五节命中分组：按固定顺序 `credential` → `pii`，每组标题分别为「凭据类（G4 系统用户鉴权）」「结构化个人信息（G4）」
- 第五节每个文件一个子标题 + 代码块，证据按行号 `L%d: %s` 列出
- 空命中显示「_未发现敏感数据_」（HTML 用 `<p><em>未发现敏感数据</em></p>`）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 clean/internal/report/html.go — writeHTML + 转义 + file:// 目录链接 helper</name>
  <files>clean/internal/report/html.go</files>
  <behavior>
    - 调用 writeHTML(f, rep) 后，f 内的 HTML 字符串满足：
      1. 顶层结构：`<!DOCTYPE html><html lang="zh-CN">...<head>...<style>...</style></head><body>...</body></html>`
      2. <title> 与 <h1> 均为 "清理报告  {Timestamp}"（与 MD 第一行 "# 清理报告  {ts}" 对齐，注意 MD 里是两个空格）
      3. 元信息两行：「主机: {Hostname}」「用户目录: {UserHome}」
      4. 五节标题文字与 MD 完全一致（含「（已移到回收站）」「（**仅报告，不删除**）」；HTML 里 ** 自然不出现，但文字"仅报告，不删除"必须保留）
      5. 一/二/三节为 <table>：列头分别 = [浏览器|条目|估算大小] / [应用|条目|估算大小] / [名称|估算大小]，末尾「总计: {HumanBytes}」行
      6. 第四节 <table> 列头 [风险|类别|路径|大小]，按 Risk rank (high=0, medium=1, low=2) 升序排序，与 writeMarkdown 同一 rank map
      7. 第五节：命中数 = len(rep.PasswordHits)；按 credential → pii 顺序分组；每组一个 <h3>「{标题} — {N} 个文件」+ 说明 <p>；每个文件一个 <h4> 内含路径超链接 + <pre><code> 代码块按 "L{LineNo}: {LineText}" 列证据
      8. 空命中时输出 <p><em>未发现敏感数据</em></p>
    - fileDirLink(path) 返回的 <a> 满足：
      1. href 形如 `file:///{encoded-dir}/`（三斜杠 + 末尾斜杠），dir = filepath.Dir(path)
      2. 路径分隔符已统一为正斜杠（filepath.ToSlash）
      3. 中文 / 空格 / 特殊字符已百分号编码（用 net/url.PathEscape 按段编码，或手写等价实现）
      4. <a> 文本 = html.EscapeString(path)（完整文件路径，未编码以便人读）
      5. title 属性 = "点击在资源管理器中打开所在目录"
    - 安全不变量：writeHTML 输出的所有动态内容（路径、Reason、Category、LineText、Hostname、UserHome、Timestamp）均经过 html.EscapeString；任一含 `<script>alert(1)</script>`、`</style>`、`"`、`'` 的输入在输出中都不会破坏 HTML 结构或注入标签
    - 自包含：output 不 import 任何第三方包，仅用标准库（html, path/filepath, net/url, strings, fmt, sort, os）
  </behavior>
  <action>
    新建 clean/internal/report/html.go，package report。

    实现 3 个导出/非导出符号：

    1. `func writeHTML(f *os.File, rep *FullReport) error`
       - 用 bufio.Writer 包 f，结尾 Flush。
       - 写 `<!DOCTYPE html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>` + escape(Timestamp) + `</title>` + 内联 `<style>` 块 + `</head><body>`。
       - 内联 style 至少覆盖：body 字体栈 `"Microsoft YaHei", "PingFang SC", "Hiragino Sans GB", "WenQuanYi Micro Hei", sans-serif`；最大宽度 1100px 居中；表格 border-collapse、th/td border 1px #ccc、padding 6px 8px、tr:nth-child(even) 浅灰底；pre/code 用等宽字体 `"Cascadia Code", Consolas, "Courier New", monospace` + 浅灰底 + 横向滚动 (overflow-x:auto)；a 颜色 #1565c0 + hover 下划线；h1/h2/h3/h4 颜色层级；.meta 行弱化色。
       - 按 writeMarkdown 的同样顺序写 body：
         a. `<h1>清理报告  ` + escape(rep.Timestamp) + `</h1>`（注意两个空格对齐 MD）
         b. `<p class="meta">主机: <code>` + escape(rep.Hostname) + `</code></p>` 同样写用户目录
         c. 一节：`<h2>一、浏览器缓存（已移到回收站）</h2>` + `<table>` 表头 + 遍历 `browser.Summarize(rep.BrowserCleaned)` 每行 `<tr><td>{Browser}</td><td>{Count}</td><td>{HumanBytes(Bytes)}</td></tr>` + 表末 `<tr><td colspan="2"><strong>总计</strong></td><td>{HumanBytes(rep.BrowserBytes)}</td></tr>`
         d. 二节同上，列头 [应用|条目|估算大小]，遍历 `im.Summarize(rep.IMCleaned)`，字段 s.App；总计 rep.IMBytes
         e. 三节：列头 [名称|估算大小]，遍历 rep.SystemCleaned，每行 t.Name + HumanBytes(t.SizeBytes)；总计 rep.SystemBytes
         f. 四节：列头 [风险|类别|路径|大小]；**先 sort.Slice(rep.SensitiveFiles, ...)** 用与 writeMarkdown 完全相同的 rank map `{RiskHigh:0, RiskMedium:1, RiskLow:2}`；每行 `<td>` + s.Risk + s.Category + `<td>` + `fileDirLink(s.Path)` + `<td>` + HumanBytes(s.Size)。**不要**修改 rep.SensitiveFiles 元素本身——如需排序副本请 copy 一份，避免副作用（writeMarkdown 当前是 in-place 排序，但 HTML 写入在 MD 之后，再排一次是幂等的，安全）。
         g. 五节：`<h2>五、敏感数据扫描命中（<strong>仅报告，不删除</strong>）</h2>` + 两个 <blockquote> 写"依据《敏感数据清理与加密操作指引》"和"只报告含实际敏感数据..."；如 len(rep.PasswordHits)==0 → `<p><em>未发现敏感数据</em></p>`；否则 `<p>共 <strong>{len}</strong> 个文件命中：</p>` + 按 `categories := []struct{reason, title, desc string}{{"credential","凭据类（G4 系统用户鉴权）","配置文件里的 password / token / api_key = 真实值"},{"pii","结构化个人信息（G4）","通过校验位的真实身份证号 / 银行卡号 / 手机号"}}` 顺序遍历，过滤同 Reason 的 FileHit；每组 `<h3>{title} — {len(inCat)} 个文件</h3><p>{desc}</p>` + 遍历 inCat：`<h4>` + fileDirLink(h.Path) + `</h4><pre><code>` + 每条 Evidence 写 `L{LineNo}: {LineText}` 换行 + `</code></pre>`。所有 LineText 必须 escapeHTML。
       - 收尾 `</body></html>`。
       - **不要内联实现代码块（``` fenced block）—— HTML 里用 <pre><code> 替代，不要在 <pre> 里再插入字面 ``` 字符。**

    2. `func fileDirLink(path string) string`
       - dir := filepath.Dir(path)；slash := filepath.ToSlash(dir)
       - 按 "/" split 成段，每段用 url.PathEscape 编码，再 join("/")；注意 url.PathEscape 会把 ":"、"空格"、"中文"等编码为 %XX，正是我们要的
       - href := "file:///" + encoded + "/" （三斜杠开头 + 末尾斜杠）
       - 返回 `<a href="` + href + `" title="点击在资源管理器中打开所在目录">` + html.EscapeString(path) + `</a>`
       - **注意 href 里不要二次 escape**：href 是 URL，url.PathEscape 已经做了 URL 编码；只对显示文本（<a> 之间）用 html.EscapeString。
       - 边界：path 为空字符串时（理论上不会出现，但 defensive），返回空字符串而不是崩。

    3. `func escapeHTML(s string) string`
       - 直接 `return html.EscapeString(s)`（薄封装，便于全文统一调用风格；也可不封装直接调 html.EscapeString，二者皆可——选择一种并在文件内一致使用）

    4. （可选小工具）`func writeEscaped(w *bufio.Writer, s string)`：等价于 w.WriteString(html.EscapeString(s))，减少重复；非必须。

    导入清单（建议）：`bufio`、`fmt`、`html`、`net/url`、`os`、`path/filepath`、`sort`、`strings`。**禁止引入任何第三方包。**

    全文使用与 writeMarkdown 一致的中文文案、章节序号、表格列顺序、总计行 colspan 与位置。MD 是 single source of truth，HTML 是其渲染版本。

    测试文件 `clean/internal/report/html_test.go`：写 `TestWriteHTML_StructureAndEscape`：
      - 构造一个最小 FullReport（Hostname=`<b>bold</b>`、UserHome=`C:\Users\A B`、PasswordHits 一条 Reason="credential" Path=`C:\Users\A B\.env` Evidence=[{1, `password = "x<y>z"`}]、SensitiveFiles 一条 Path=`C:\Users\A B\.ssh\id_rsa`）
      - 调 writeHTML(&bytes.Buffer{}, rep)（签名若用 *os.File，测试里用 os.Pipe() 或改写接受 io.Writer；**推荐 writeHTML 直接接受 io.Writer** 以便测试，report.go 调用处传 *os.File 即可——这样测试更简单；如果选择 *os.File 签名则测试用 os.CreateTemp）
      - 用 strings.Contains 断言：输出含 `&lt;b&gt;bold&lt;/b&gt;`（Hostname 已转义）、含 `file:///C%3A/Users/A%20B/.ssh/` （路径段已 URL 编码）、含 `点击在资源管理器中打开所在目录`（title 文本）、含 `&lt;` 而非裸 `<script`（LineText 已转义）、含 `<pre><code>` 块且里面有 `L1:`、含 `<h2>五、`、空 PasswordHits 分支输出 `未发现敏感数据`。
    此测试在 Task 2 改签名后仍需保持绿（writeHTML 是 *os.File 还是 io.Writer 取决于实现，但测试本身不依赖 report.Write）。
  </action>
  <verify>
    在 D:\code\ClaudeCode\record_V2\clean 目录运行：
    `go test ./internal/report/... -run TestWriteHTML -v`
    以及 `go vet ./internal/report/...`
    两者必须 exit 0 且测试通过。
  </verify>
  <done>
    - clean/internal/report/html.go 存在，含 writeHTML / fileDirLink / escapeHTML
    - 单元测试 TestWriteHTML_StructureAndEscape 通过
    - 仅依赖 Go 标准库，go.sum 无新增条目
    - 所有动态内容经过 HTML 转义（断言 &lt;b&gt; 出现、裸 <script> 不出现）
    - file:// 链接含 %3A（冒号编码）+ %20（空格编码）+ 三斜杠 + 末尾斜杠
  </done>
</task>

<task type="auto">
  <name>Task 2: 改 report.Write 返回 (jsonPath, mdPath, htmlPath, err) + 适配 main.go 4 个调用点 + go build/vet 验证</name>
  <files>clean/internal/report/report.go, clean/cmd/clean/main.go</files>
  <action>
    **A. 改 clean/internal/report/report.go**：
    1. 更新文件顶部 doc comment：把"同时输出：.json / .md"扩成".json / .md / .html"。
    2. 更新 Write 函数 doc comment，加一行 `.html` 说明。
    3. 把 Write 签名从：
       `func Write(rep *FullReport, outDir string) (jsonPath, mdPath string, err error)`
       改为：
       `func Write(rep *FullReport, outDir string) (jsonPath, mdPath, htmlPath string, err error)`
    4. 在 mdPath 旁边加 `htmlPath = filepath.Join(outDir, ts+"-report.html")`。
    5. 在 writeMarkdown 调用块之后新增 html 文件创建 + 写入：
       - `hf, err := os.Create(htmlPath)` → 失败 `return "", "", "", err`
       - `defer hf.Close()`
       - `if err := writeHTML(hf, rep); err != nil { return "", "", "", err }`
    6. 所有早退 return（MkdirAll 失败、JSON Encode 失败、Create 失败、writeMarkdown 失败）的返回值改为 3 个空串 + err。
    7. 最终 `return jsonPath, mdPath, htmlPath, nil`。
    8. writeHTML 的参数类型必须与 Task 1 选定的签名一致（推荐 `func writeHTML(w io.Writer, rep *FullReport) error`，这样此处直接传 hf；若 Task 1 用 *os.File，此处也兼容）。

    **B. 改 clean/cmd/clean/main.go** 4 个调用点，把 `jsonPath, mdPath, err := report.Write(...)` 全部改为 `jsonPath, mdPath, htmlPath, err := report.Write(...)`：

    1. **runAll**（约 177 行）：当前 `jsonPath, mdPath, err := report.Write(rep, *out)`，改为 3 返回值；下方 `fmt.Printf("    %s\n", ...)` 块再追加一行 `fmt.Printf("    %s\n", htmlPath)`（紧跟 mdPath 那行之后）。
    2. **runScan**（约 391 行）：当前 `jsonPath, mdPath, err := report.Write(rep, *out)`，改为 3 返回值；下方 `fmt.Printf("[scan] 报告已生成:\n  %s\n  %s\n", jsonPath, mdPath)` 改为 `\n  %s\n  %s\n  %s\n` 三路径。
    3. **runClean**（约 527 行）：当前 `jsonPath, mdPath, err := report.Write(rep, *out)`，改为 3 返回值；下方 `fmt.Printf("[clean] 完成。报告:\n  %s\n  %s\n", jsonPath, mdPath)` 改为 3 路径。
    4. **runReport**（约 811 行）：当前 `_, md, err := report.Write(&rep, out)`，改为 `_, md, htmlPath, err := report.Write(&rep, out)`；下方 `fmt.Printf("已重新生成: %s\n", md)` 追加 `fmt.Printf("已重新生成: %s\n", htmlPath)`（保留最小改动的同时让 report 子命令也能再生成 html——本来 report 子命令的设计就是"基于 JSON 重生成所有人类可读报告"，扩展到 html 符合语义）。

    **C. 不要动**：
    - main.go 的 usage 字符串不必为新增 html 改动（usage 里 report 子命令说明是"重新生成 Markdown"，扩展到 html 也算"重新生成可读报告"——若想完美可加一行"clean report <json>  重新生成 Markdown 和 HTML"，非必需，保持最小改动即可）。
    - reports/ 目录下的历史 .md / .json 文件不动。

    **D. 注意**：所有调用点改成 3 返回值后，若有任何一处仍写 2 返回值会编译失败——这就是天然的编译期 gate，无需额外断言。
  </action>
  <verify>
    在 D:\code\ClaudeCode\record_V2\clean 目录运行以下命令，均必须 exit 0：
    1. `go build ./...`（验证签名变更已全量适配）
    2. `go vet ./...`（无新 warning）
    3. `go test ./internal/report/...`（Task 1 测试仍绿）
    4. 端到端验证 HTML 实际能从现成 JSON 生成：
       `go build -o clean.exe ./cmd/clean && ./clean.exe report clean/reports/20260812-001021-report.json`
       执行后应在 clean/reports/ 目录下看到 `20260812-001021-report.html`，文件大小 > 5KB（样本含 145 个命中文件，HTML 应至少几十 KB）。
       用 grep（或 Read 工具）抽查该 html：
       - 含 `<!DOCTYPE html>` 和 `<title>清理报告  20260812-001021</title>`
       - 含 `<h2>一、浏览器缓存` / `<h2>五、敏感数据扫描命中`
       - 含 `file:///` 至少一处（证明 fileDirLink 被调用）
       - 含 `点击在资源管理器中打开所在目录`
       - **不含** 裸 `[REDACTED]` 之外的 `<script` 字符串（防注入）
       （注意：源 JSON 里的命中行文本本身已被 scanner.maskLine 脱敏为 `[REDACTED]`，不会再含真实密码，本步骤只验证 HTML 结构/转义正确。）
  </verify>
  <done>
    - report.Write 签名为 `(jsonPath, mdPath, htmlPath string, err error)`
    - main.go 4 个调用点（runAll/runScan/runClean/runReport）全部适配，打印输出含 htmlPath
    - go build ./... exit 0
    - go vet ./... exit 0
    - go test ./internal/report/... 全绿
    - clean.exe report clean/reports/20260812-001021-report.json 能生成结构正确、含 file:// 链接、内容已转义的 .html
  </done>
</task>

</tasks>

<verification>
Phase 整体验证（Task 2 完成后）：

1. 编译 + 静态检查（在 D:\code\ClaudeCode\record_V2\clean）：
   - `go build ./...` exit 0
   - `go vet ./...` exit 0
   - `go test ./...` 全绿（含 Task 1 的 TestWriteHTML）

2. 端到端报告重生成：
   - `go build -o clean.exe ./cmd/clean`
   - `./clean.exe report clean/reports/20260812-001021-report.json`
   - 产物 `clean/reports/20260812-001021-report.html` 存在且 > 5KB

3. 人工抽查 HTML 结构（Read 工具读取生成的 html 文件头部 + 某命中段）：
   - DOCTYPE / title / 五节 h2 齐全
   - 至少一处 `<a href="file:///...">` 含 % 编码
   - 至少一处 `<pre><code>` 命中代码块
   - 无未转义的 `<script` 字面字符串
</verification>

<success_criteria>
- 新文件 clean/internal/report/html.go 存在且仅依赖 Go 标准库
- report.Write 多返回一个 htmlPath；4 个调用点全部适配
- 生成的 HTML 五节结构与同名 MD 文件完全对齐
- 所有文件路径渲染为 file:/// 目录超链接（三斜杠 + 末尾斜杠 + 段级百分号编码）
- 路径 / 命中行文本 / 元信息均经 html.EscapeString
- 报告自包含：内联 <style>，无外部依赖
- go build ./... / go vet ./... / go test ./internal/report/... 均 exit 0
- clean.exe report <现成 JSON> 能正常生成 .html
</success_criteria>

<output>
完成后创建 D:\code\ClaudeCode\record_V2\.planning\quick\260812-jc2-clean-report-html-md\260812-jc2-SUMMARY.md
</output>
