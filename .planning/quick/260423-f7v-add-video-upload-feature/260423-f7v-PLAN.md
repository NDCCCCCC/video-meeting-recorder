---
phase: quick
plan: 260423-f7v
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/files/index.tsx
  - frontend/src/api/video-file.ts
  - frontend/src/components/VideoUploadModal.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User can upload video files directly from the file management page"
    - "Upload progress is displayed to the user"
    - "Uploaded videos appear in the file list after successful upload"
  artifacts:
    - path: "frontend/src/components/VideoUploadModal.tsx"
      provides: "Video upload modal with progress tracking"
      min_lines: 50
    - path: "frontend/src/api/video-file.ts"
      provides: "Upload API function"
      exports: ["uploadVideoFile"]
    - path: "frontend/src/pages/files/index.tsx"
      provides: "Upload button and modal integration"
      min_lines: 10
  key_links:
    - from: "frontend/src/pages/files/index.tsx"
      to: "frontend/src/api/video-file.ts"
      via: "import uploadVideoFile"
      pattern: "uploadVideoFile.*from"
    - from: "frontend/src/components/VideoUploadModal.tsx"
      to: "/api/v1/storage/upload"
      via: "apiRequest"
      pattern: "/api/v1/storage/upload"
---

<objective>
Add video upload functionality to the file management page

Purpose: Allow users to upload video files directly through the web interface instead of relying solely on server-side file scanning
Output: A new upload button in the file management page that opens a modal with file selection and upload progress
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/pages/files/index.tsx
@frontend/src/api/video-file.ts
@internal/handlers/file_handler.go
@internal/services/storage/file_service.go

<interfaces>
<!-- Backend upload endpoint already exists at /api/v1/storage/upload -->
<!-- From internal/handlers/file_handler.go: -->
<!-- Upload accepts multipart/form-data with fields: -->
<!-- - file: the file to upload -->
<!-- - folder: destination folder (default: "uploads") -->
<!-- - is_public: whether file is public (default: false) -->
<!-- - expires_in: optional expiration in seconds -->

<!-- From internal/services/storage/file_service.go: -->
<!-- Returns FileUploadResult with: -->
<!-- - file_id: uint -->
<!-- - file_name: string -->
<!-- - file_path: string -->
<!-- - file_size: int64 -->
<!-- - mime_type: string -->
<!-- - access_url: string -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VideoUploadModal component</name>
  <files>frontend/src/components/VideoUploadModal.tsx</files>
  <action>Create a new modal component for video upload with:
1. File selection using Ant Design Upload.Dragger component
2. File size validation (max 5GB per backend defaultMaxFileSize)
3. File type validation (accept: .mp4, .mkv, .avi, .mov video formats)
4. Upload progress display using Upload component's progress prop
5. Success/error message handling
6. Auto-close on successful upload
7. Use uploadVideoFile API function (to be created in Task 2)

Component props:
- visible: boolean
- onCancel: () => void
- onUploadSuccess: (file: VideoFile) => void

Use Ant Design Modal + Upload.Dragger components. Show progress bar during upload.
</action>
  <verify>Component file exists with export, accepts props, uses Upload.Dragger</verify>
  <done>VideoUploadModal component created with file selection, validation, and progress display</done>
</task>

<task type="auto">
  <name>Task 2: Add uploadVideoFile API function</name>
  <files>frontend/src/api/video-file.ts</files>
  <action>Add uploadVideoFile function to video-file.ts API client:
1. Accept file parameter (File type)
2. Create FormData with the file
3. Set folder to "videos" to organize uploaded videos separately
4. Call POST /api/v1/storage/upload endpoint (already exists in backend)
5. Return Promise with FileUploadResult type

Function signature:
```typescript
export async function uploadVideoFile(file: File): Promise<ApiResponse<FileUploadResult>>
```

Add FileUploadResult interface matching backend response.
Use apiRequest helper with FormData body.
</action>
  <verify>Function exported in video-file.ts, accepts File parameter, calls /api/v1/storage/upload</verify>
  <done>uploadVideoFile API function added to video-file.ts</done>
</task>

<task type="auto">
  <name>Task 3: Integrate upload button into file management page</name>
  <files>frontend/src/pages/files/index.tsx</files>
  <action>Integrate the upload functionality:
1. Import VideoUploadModal component
2. Import uploadVideoFile API function
3. Add state: uploadModalVisible (boolean)
4. Add "上传视频" button next to "扫描导入" button (around line 718)
5. Add onUploadSuccess handler that calls loadFiles() and loadStats()
6. Render VideoUploadModal with appropriate props

Button styling: Use Ant Design Button with UploadOutlined icon, place it in the same Space as "扫描导入" button.

Ensure the button is visible to all users who can see the file management page (no special permission required beyond basic access).
</action>
  <verify>Upload button present in file management page, clicking opens modal, successful upload refreshes file list</verify>
  <done>Upload button integrated into file management page with modal and list refresh</done>
</task>

</tasks>

<verification>
1. Click "上传视频" button in file management page
2. Select a video file (.mp4, .mkv, etc.)
3. Verify upload progress is displayed
4. After upload completes, verify the file appears in the file list
5. Verify file size and metadata are correctly displayed
6. Test with non-video file to verify validation works
7. Test with large file (>5GB) to verify error handling
</verification>

<success_criteria>
1. Upload button visible in file management page toolbar
2. Upload modal opens on button click
3. Video files can be selected and uploaded
4. Upload progress is shown in real-time
5. Uploaded videos appear in file list after successful upload
6. Non-video files are rejected with clear error message
7. Upload errors are properly displayed to user
</success_criteria>

<output>
After completion, create `.planning/quick/260423-f7v-add-video-upload-feature/260423-f7v-SUMMARY.md`
</output>
