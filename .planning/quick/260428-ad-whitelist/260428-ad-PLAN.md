---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/auth/ad_auth.go
  - internal/config/config.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "AD 认证只允许系统中已存在的用户登录"
    - "未在系统中的 AD 账号会被拒绝登录"
    - "错误消息明确指出需要联系管理员添加账号"
  artifacts:
    - path: "internal/auth/ad_auth.go"
      provides: "修改后的 AD 认证器，只允许已存在的用户登录"
      contains: "findOrCreateLocalUser 改为 findExistingLocalUser"
    - path: "internal/config/config.go"
      provides: "新增配置选项控制是否启用 AD 白名单模式"
      contains: "ADAllowAutoCreate bool 字段"
  key_links:
    - from: "internal/auth/ad_auth.go"
      to: "internal/config/config.go"
      via: "读取 ADAllowAutoCreate 配置决定是否允许自动创建用户"
      pattern: "cfg.Auth.AD.AllowAutoCreate"
---

<objective>
实现 AD 用户白名单功能：只允许系统中已存在的 AD 用户登录，拒绝未注册的 AD 账号。

当前行为：AD 用户首次登录时，系统会自动创建本地用户记录并分配默认角色。
新行为：AD 用户必须在系统中预先存在才能登录，否则返回明确的错误消息。

目的：提高安全性，允许管理员精确控制哪些 AD 用户可以访问系统。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@internal/auth/ad_auth.go
@internal/config/config.go
@internal/handlers/admin_handler.go

<interfaces>
<!-- From internal/auth/ad_auth.go (lines 200-262): -->

Current `findOrCreateLocalUser` implementation:
```go
func (a *ADAuthenticator) findOrCreateLocalUser(adUser *ADUser) (*models.User, error) {
	// First, try to find existing user by ad_guid or username
	var user models.User
	err := a.db.Where("ad_guid = ? OR username = ?", adUser.ObjectGUID, adUser.Username).First(&user).Error

	if err == nil {
		// Found existing user, update AD information
		a.updateADInfo(&user, adUser)
		// Reload with Roles.Permissions for toUserDTO
		a.db.Preload("Roles.Permissions").First(&user, user.ID)
		return &user, nil
	}

	if !errors.Is(err, gorm.ErrRecordNotFound) {
		return nil, err
	}

	// Not found, create new user with random password (per D-07)
	// ... auto-creation logic ...
}
```

This is called from Login() at line 123:
```go
// Step 6: Find or create local user (transparent mapping per D-06, D-08)
localUser, err := a.findOrCreateLocalUser(adUser)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 AD 白名单配置选项</name>
  <files>internal/config/config.go</files>
  <action>
在 `internal/config/config.go` 的 `ADAuthConfig` 结构体中添加新字段：

```go
type ADAuthConfig struct {
	Server   string `mapstructure:"server" json:"server" yaml:"server"`
	BindDN   string `mapstructure:"bind_dn" json:"bind_dn" yaml:"bind_dn"`
	Password string `mapstructure:"password" json:"-" yaml:"password"`
	BaseDN   string `mapstructure:"base_dn" json:"base_dn" yaml:"base_dn"`
	UseTLS   bool   `mapstructure:"use_tls" json:"use_tls" yaml:"use_tls"`
	PoolSize int    `mapstructure:"pool_size" json:"pool_size" yaml:"pool_size"`
	DialTimeout    int `mapstructure:"dial_timeout" json:"dial_timeout" yaml:"dial_timeout"`
	RequestTimeout int `mapstructure:"request_timeout" json:"request_timeout" yaml:"request_timeout"`
	InsecureSkipVerify bool `mapstructure:"insecure_skip_verify" json:"insecure_skip_verify" yaml:"insecure_skip_verify"`
	AllowAutoCreate bool `mapstructure:"allow_auto_create" json:"allow_auto_create" yaml:"allow_auto_create"` // NEW: 是否允许自动创建AD用户
}
```

同时在 `internal/auth/ad_config.go` 中也添加此字段以保持一致性。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/record_V2 && go build ./internal/config/ && go build ./internal/auth/</automated>
  </verify>
  <done>
    - ADAuthConfig 结构体添加 AllowAutoCreate 字段
    - 包含完整的 mapstructure/json/yaml 标签
    - internal/auth/ad_config.go 也添加了相应字段
    - 代码编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 AD 认证器实现白名单逻辑</name>
  <files>internal/auth/ad_auth.go</files>
  <action>
修改 `findOrCreateLocalUser` 函数，改为根据配置决定是否允许自动创建用户：

1. 重命名函数为 `findOrCreateLocalUser` 保持兼容性
2. 检查 `a.adConfig.AllowAutoCreate` 配置
3. 如果不允许自动创建且用户不存在，返回明确的错误

```go
// findOrCreateLocalUser finds an existing user or creates a new one (if allowed)
func (a *ADAuthenticator) findOrCreateLocalUser(adUser *ADUser) (*models.User, error) {
	// First, try to find existing user by ad_guid or username
	var user models.User
	err := a.db.Where("ad_guid = ? OR username = ?", adUser.ObjectGUID, adUser.Username).First(&user).Error

	if err == nil {
		// Found existing user, update AD information
		a.updateADInfo(&user, adUser)
		// Reload with Roles.Permissions for toUserDTO
		a.db.Preload("Roles.Permissions").First(&user, user.ID)
		return &user, nil
	}

	if !errors.Is(err, gorm.ErrRecordNotFound) {
		return nil, err
	}

	// User not found - check if auto-create is allowed
	if !a.adConfig.AllowAutoCreate {
		a.logger.Warn("AD user not found in system and auto-create is disabled",
			zap.String("username", adUser.Username))
		return nil, errors.New("账号未在系统中注册，请联系管理员添加")
	}

	// Auto-create is allowed, proceed with user creation
	// ... existing auto-creation logic ...
}
```

关键改动：
- 添加 `if !a.adConfig.AllowAutoCreate` 检查
- 返回明确的中文错误消息："账号未在系统中注册，请联系管理员添加"
- 保留原有的自动创建逻辑作为可选项
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/record_V2 && go build ./internal/auth/</automated>
  </verify>
  <done>
    - findOrCreateLocalUser 检查 AllowAutoCreate 配置
    - 白名单模式下拒绝未注册用户
    - 错误消息清晰明确
    - 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 管理员 API 支持配置 AllowAutoCreate</name>
  <files>internal/handlers/admin_handler.go</files>
  <action>
在 `UpdateAuthConfig` 处理器中支持 `allow_auto_create` 参数：

1. 更新请求结构体绑定
2. 验证并保存配置

查看当前 handler 代码，确保：
- `allow_auto_create` 可以通过 API 设置
- 默认值为 true（保持向后兼容，即默认允许自动创建）
- 配置变更会持久化到数据库
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/record_V2 && go build ./cmd/server/</automated>
  </verify>
  <done>
    - UpdateAuthConfig 支持 allow_auto_create 参数
    - 默认值为 true
    - 配置持久化到数据库
    - 代码编译通过
  </done>
</task>

</tasks>

<verification>
1. `go build ./cmd/server/` 编译通过
2. 测试白名单模式：设置 allow_auto_create=false，未注册 AD 用户登录被拒绝
3. 测试非白名单模式：设置 allow_auto_create=true，AD 用户自动创建成功
4. 错误消息清晰："账号未在系统中注册，请联系管理员添加"
</verification>

<success_criteria>
- AllowAutoCreate 配置选项已添加
- 白名单模式下拒绝未注册 AD 用户
- 错误消息明确指出需要联系管理员
- 向后兼容：默认允许自动创建
- 配置可通过管理员 API 修改
</success_criteria>

<output>
After completion, create `.planning/quick/260428-ad-whitelist/260428-ad-SUMMARY.md`
</output>
