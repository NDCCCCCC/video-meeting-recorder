---
phase: quick
plan: 260730-bc3
quick_id: 260730-bc3
slug: 38-recording-5-storage-3-ppts-4-apikey-3
subsystem: audit
type: execute
wave: 1
depends_on: ["260729-m8l", "260729-mwt"]
files_modified:
  - internal/services/video_recording_task_service.go
  - internal/handlers/video_recording_task_handler.go
  - internal/services/storage/file_service.go
  - internal/services/storage/file_service_test.go
  - internal/handlers/file_handler.go
  - internal/services/apikey_service.go
  - internal/handlers/apikey_handler.go
  - internal/services/ppt_file_service.go
  - internal/services/ppt_editor_service.go
  - internal/services/ppt_editor_service_test.go
  - internal/handlers/ppt_handler.go
  - internal/services/notification/notification_service.go
  - internal/handlers/notification_handler.go
  - cmd/server/app.go
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-03
  - REC-04
  - REC-05
  - STG-01
  - STG-02
  - STG-03
  - PPT-01
  - PPT-02
  - PPT-03
  - PPT-04
  - KEY-01
  - KEY-02
  - KEY-03
  - NOT-01
tags: [audit, old-data, record-change, recording, storage, ppt, apikey, notification, backend]
requires:
  - "audit.AuditLogService.RecordChange(ctx, opts) exists from quick task 260729-m8l"
  - "LogOperation sanitizes OldData and NewData before persistence"
  - "m8l/mwt establish the service snapshot -> return old/new -> handler RecordChange pattern"
provides:
  - "OldData/NewData capture for the 16 explicitly listed recording, storage, PPT, API-key, and notification mutation sites"
  - "AuditLogService constructor wiring for five additional handlers"
  - "Preserved business responses and non-blocking audit failure behavior"
affects:
  - "recording task update/delete/batch-delete/start/stop"
  - "storage upload/delete/share"
  - "PPT delete/delete-slides/rollback/reorder-slides"
  - "API-key update/delete/toggle"
  - "notification setting update"
decisions:
  - "C-01: Follow the m8l/mwt contract exactly: snapshot in the service before mutation, return detached old/new values, then call RecordChange from the handler with c.Request.Context()."
  - "C-02: Do not add audit helpers or change RecordChange/RecordChangeOpts; only adjust service return values, handler auditService injection/calls, affected call sites/tests, and cmd/server/app.go wiring."
  - "C-03: RecordChange failures only call h.logger.Warn and never change the successful business response."
  - "C-04: RecordChange module/action pairs are the prompt-locked values, even where existing middleware labels differ: ModuleTask for recording; ModuleStorage for file_handler; ModulePPT; ModuleAPIKey; ModuleNotification."
  - "C-05: Resource naming is task:<id>, storage:<fileID>, ppt:<id>, apikey:<id>, and notification_setting:<userID>; batch recording uses task:<comma-joined-request-ids> with ResourceID zero."
  - "C-06: Scope is exactly the 16 listed sites; do not alter or add RecordChange calls to other endpoints."
must_haves:
  truths:
    - "Each of the 16 listed successful mutations emits one service-layer RecordChange row containing the requested before/after data in addition to the existing middleware audit row."
    - "Update/start/stop/toggle/edit operations preserve a detached pre-mutation snapshot and return the actual post-operation snapshot; delete operations return the deleted snapshot with NewData nil."
    - "BatchDeleteTasks emits one RecordChange row with OldData []models.VideoRecordingTask and NewData nil while preserving partial-delete response semantics."
    - "PPT slide edits capture the complete pre/post PPTFile slide state represented by PageCount, DeletedSlides, EditHistory, BackupPath, and the service's validated reorder result."
    - "API-key and file-share secrets are passed through the existing Sanitizer pipeline and are not written in clear text to audit_logs."
    - "An audit enqueue/write error is warned through the handler logger and never turns a successful mutation into an error response."
    - "Existing authorization, validation, physical-file handling, scheduler behavior, response bodies, and route middleware remain unchanged."
  artifacts:
    - path: "internal/handlers/video_recording_task_handler.go"
      provides: "Five recording RecordChange calls and AuditLogService injection"
      contains: "auditService"
    - path: "internal/services/video_recording_task_service.go"
      provides: "Detached old/new recording snapshots returned by five service methods"
      contains: "oldTask"
    - path: "internal/handlers/file_handler.go"
      provides: "Storage create/delete/share RecordChange calls and AuditLogService injection"
      contains: "models.ModuleStorage"
    - path: "internal/services/storage/file_service.go"
      provides: "Pre-delete UploadedFile and pre/post FileShare snapshots"
      contains: "oldShare"
    - path: "internal/handlers/apikey_handler.go"
      provides: "API-key update/delete/toggle RecordChange calls and AuditLogService injection"
      contains: "models.ModuleAPIKey"
    - path: "internal/services/apikey_service.go"
      provides: "Detached pre/post APIKey snapshots"
      contains: "oldAPIKey"
    - path: "internal/handlers/ppt_handler.go"
      provides: "PPT delete/delete-slides/rollback/reorder-slides RecordChange calls and AuditLogService injection"
      contains: "models.ModulePPT"
    - path: "internal/services/ppt_editor_service.go"
      provides: "Pre/post PPTFile snapshots for three slide-edit operations"
      contains: "oldPPT"
    - path: "internal/handlers/notification_handler.go"
      provides: "Notification-setting RecordChange call and AuditLogService injection"
      contains: "models.ModuleNotification"
    - path: "cmd/server/app.go"
      provides: "The existing auditService passed to all five modified handler constructors"
      contains: "auditService"
  key_links:
    - from: "internal/handlers/video_recording_task_handler.go"
      to: "internal/services/audit/audit_log_service.go"
      via: "auditService.RecordChange(c.Request.Context(), audit.RecordChangeOpts{...})"
      pattern: 'RecordChange\(c\.Request\.Context\(\)'
    - from: "internal/handlers/file_handler.go"
      to: "internal/services/storage/file_service.go"
      via: "Upload result and Delete/ShareFile old/new return values"
      pattern: 'OldData:.*old'
    - from: "internal/handlers/ppt_handler.go"
      to: "internal/services/ppt_editor_service.go"
      via: "DeleteSlides/Rollback/ReorderSlides old/new return values"
      pattern: 'NewData:.*newPPT'
    - from: "internal/handlers/apikey_handler.go"
      to: "internal/services/apikey_service.go"
      via: "UpdateAPIKey/DeleteAPIKey/ToggleAPIKeyStatus detached snapshots"
      pattern: 'OldData:.*oldAPIKey'
    - from: "internal/handlers/notification_handler.go"
      to: "internal/services/notification/notification_service.go"
      via: "UpdateUserSetting old/new return values"
      pattern: 'RecordChange\(c\.Request\.Context\(\)'
    - from: "cmd/server/app.go"
      to: "internal/handlers"
      via: "auditService constructor arguments for VideoRecordingTaskHandler, FileHandler, PPThandler, APIKeyHandler, and NotificationHandler"
      pattern: 'New.*Handler\(.*auditService'
---

<objective>
在用户明确列出的 16 个 P1 变更站点接入 OldData/NewData 捕获，完整延续 m8l + mwt 已验证的 `service-layer snapshot -> return (old, new, err) -> handler.RecordChange(c.Request.Context(), opts)` 模式。

Purpose: 让 recording、storage、PPT、API key、notification 的更新、删除和状态变更具备可追溯的前后差异，同时不改变现有业务成功/失败语义。

Output: 5 个 handler 注入现有 `AuditLogService`，相关 service 返回独立的 before/after snapshot，16 个 handler 成功路径调用 `RecordChange`，现有受影响测试完成返回值适配。
</objective>

<execution_context>
@C:/Users/CPIC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/CPIC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260729-mwt-input-config-system-file-olddata-6/260729-mwt-PLAN.md
@.planning/quick/260729-mwt-input-config-system-file-olddata-6/260729-mwt-SUMMARY.md
@.planning/quick/260729-m8l-olddata-update-delete/260729-m8l-SUMMARY.md
@internal/services/audit/audit_log_service.go
@internal/models/audit_log.go
@internal/services/video_recording_task_service.go
@internal/handlers/video_recording_task_handler.go
@internal/services/storage/file_service.go
@internal/handlers/file_handler.go
@internal/services/apikey_service.go
@internal/handlers/apikey_handler.go
@internal/services/ppt_file_service.go
@internal/services/ppt_editor_service.go
@internal/handlers/ppt_handler.go
@internal/services/notification/notification_service.go
@internal/handlers/notification_handler.go
@cmd/server/app.go

<interfaces>
Existing audit contract — do not modify:
- `type audit.RecordChangeOpts struct { Action string; Module string; Resource string; ResourceID *uint; OldData interface{}; NewData interface{} }`
- `func (s *audit.AuditLogService) RecordChange(ctx context.Context, opts audit.RecordChangeOpts) error`
- `RecordChange -> LogOperation -> Sanitizer(old/new) -> async audit queue`

Required module/action mapping:
- Recording: `models.ModuleTask`; update/delete use `models.ActionUpdate` / `models.ActionDelete`; start/stop/batch use `"start"`, `"stop"`, `"batch_delete"`.
- Storage: `models.ModuleStorage`; upload/delete use `models.ActionCreate` / `models.ActionDelete`; share uses `"share"`.
- PPT: `models.ModulePPT`; delete uses `models.ActionDelete`; edits use `"delete_slides"`, `"rollback"`, `"reorder_slides"`.
- API key: `models.ModuleAPIKey`; update/delete use `models.ActionUpdate` / `models.ActionDelete`; toggle uses `"toggle"`.
- Notification: `models.ModuleNotification`; setting update uses `models.ActionUpdate`.

Source-alignment mappings:
- `storage.FileService.Delete` currently mutates `models.UploadedFile`; use `*models.UploadedFile` as the concrete old storage-file snapshot. The prompt table's `*models.VideoFile` label is semantic, not the model loaded by this service.
- `models.PPTFile` has no `Slides` field. Its persisted slide state is represented by `PageCount`, `DeletedSlides`, `EditHistory`, and `BackupPath`, while reorder also mutates slide-cache ordering. Return complete detached `*models.PPTFile` values before and after each operation; do not add a model field or helper.
- `APIKey` currently stores the key in `Key`; the existing Sanitizer covers key/hash/token/password names. Pass the model to RecordChange without manually rewriting the snapshot.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

This quick task has no dedicated ROADMAP phase, REQUIREMENTS.md IDs, CONTEXT.md, or RESEARCH.md. The prompt-level site table is therefore assigned stable requirement IDs below; prompt-level locked constraints are assigned C-01 through C-06 in frontmatter.

| SOURCE | ID | Feature / constraint | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | QUICK-GOAL | Add before/after audit capture to the 16 explicitly listed P1 mutation sites | 1-3 | COVERED | Quick-task description is the goal source |
| REQ | REC-01 | recording UpdateTask old/new | 1 | COVERED | update / ModuleTask |
| REQ | REC-02 | recording DeleteTask old/nil | 1 | COVERED | delete / ModuleTask |
| REQ | REC-03 | recording BatchDeleteTasks old slice/nil | 1 | COVERED | one batch row |
| REQ | REC-04 | recording StartTask pre/post state | 1 | COVERED | start / ModuleTask |
| REQ | REC-05 | recording StopTask pre/post state | 1 | COVERED | stop / ModuleTask |
| REQ | STG-01 | storage Upload nil/result | 2 | COVERED | create / ModuleStorage |
| REQ | STG-02 | storage Delete old file/nil | 2 | COVERED | concrete model is UploadedFile |
| REQ | STG-03 | storage Share prior share/new share | 2 | COVERED | prior share may be nil; new contains generated token before sanitization |
| REQ | PPT-01 | PPT DeletePPT old/nil | 3 | COVERED | delete / ModulePPT |
| REQ | PPT-02 | PPT DeleteSlides old/new slide state | 3 | COVERED | delete_slides |
| REQ | PPT-03 | PPT Rollback old/new slide state | 3 | COVERED | rollback |
| REQ | PPT-04 | PPT ReorderSlides old/new slide state | 3 | COVERED | reorder_slides |
| REQ | KEY-01 | API key UpdateAPIKey old/new | 2 | COVERED | update / sanitizer retained |
| REQ | KEY-02 | API key DeleteAPIKey old/nil | 2 | COVERED | delete / sanitizer retained |
| REQ | KEY-03 | API key ToggleAPIKeyStatus old/new | 2 | COVERED | toggle / sanitizer retained |
| REQ | NOT-01 | notification UpdateUserSetting old/new | 3 | COVERED | update / ModuleNotification |
| RESEARCH | R-01 | m8l/mwt service snapshot and handler RecordChange pattern | 1-3 | COVERED | Level 0 discovery: established codebase pattern, no new dependency |
| RESEARCH | R-02 | Existing Sanitizer applies to OldData/NewData | 2 | COVERED | No sanitizer/helper change |
| CONTEXT | C-01 | Service snapshot -> detached old/new return -> handler RecordChange | 1-3 | COVERED | Referenced in every task action |
| CONTEXT | C-02 | No helper or RecordChange signature changes | 1-3 | COVERED | Production changes limited to listed services, handlers, and wiring |
| CONTEXT | C-03 | RecordChange error warns and does not block response | 1-3 | COVERED | Every handler call follows same guard |
| CONTEXT | C-04 | Exact module/action mapping | 1-3 | COVERED | Existing middleware labels do not override prompt-locked values |
| CONTEXT | C-05 | Resource naming and batch resource policy | 1-3 | COVERED | Stable entity IDs used |
| CONTEXT | C-06 | Only the explicitly listed 16 sites | 1-3 | COVERED | Unlisted endpoints are not requirements for this quick task |
</source_audit>

<dependency_graph>
- Task 1 needs the existing RecordChange contract and creates recording service/handler signatures plus VideoTask constructor wiring.
- Task 2 needs the same contract and creates storage/API-key signatures, handler calls, test adaptations, and constructor wiring. It follows Task 1 because both edit `cmd/server/app.go`.
- Task 3 creates PPT/notification signatures, handler calls, test adaptations, and final constructor wiring. It follows Task 2 because it also edits `cmd/server/app.go` and runs the final repository-wide checks.
- Checkpoints: none. All work is local Go code with no external setup or package installation.
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Capture recording task before/after state at five mutation sites</name>
  <files>internal/services/video_recording_task_service.go, internal/handlers/video_recording_task_handler.go, cmd/server/app.go</files>
  <needs>Existing `audit.RecordChange` contract and the m8l/mwt detached snapshot pattern.</needs>
  <creates>Old/new return contracts for UpdateTask, DeleteTask, BatchDeleteTasks, StartTask, and StopTask; five handler RecordChange calls; VideoRecordingTaskHandler audit injection.</creates>
  <action>Implement REC-01 through REC-05 per C-01 through C-06. In `VideoRecordingTaskService`, take value-copy snapshots before the first mutation and return pointers/slices that cannot be changed when the live model is subsequently updated: `UpdateTask` returns `(oldTask, newTask, error)` after its existing preload/validation; `DeleteTask` returns `(oldTask, error)` after successful deletion; `BatchDeleteTasks` returns `(oldTasks []models.VideoRecordingTask, result *BatchDeleteTasksResult, error)` with the rows from the initial `id IN ?` query captured before eligibility filtering and deletion; `StartTask` returns `(oldTask, newTask, error)` with OldData preserving the pre-start status and NewData obtained from the post-dispatch state actually observable from the existing asynchronous scheduler path; `StopTask` snapshots before `CancelTaskExecution` and returns the final saved converting/cancelled task as NewData. Preserve every existing permission, validation, scheduler, partial-delete, log, and error branch; update all returns to the new tuple shapes without fabricating a status write or blocking on scheduler completion. In `VideoRecordingTaskHandler`, add `auditService *audit.AuditLogService`, add it to `NewVideoRecordingTaskHandler`, and after each successful service call invoke `RecordChange(c.Request.Context(), ...)`: UpdateTask uses `models.ActionUpdate`, DeleteTask uses `models.ActionDelete`, and batch/start/stop use the exact literals `"batch_delete"`, `"start"`, `"stop"`; all five use `models.ModuleTask`, not the route middleware's `models.ModuleRecording`. Use `task:<id>` with the task ID for single operations; use one batch row with `task:<comma-joined req.IDs>`, `ResourceID=&zero`, OldData equal to the pre-delete slice, and NewData nil. Every audit failure must only call `h.logger.Warn` with the task ID/action and error, then continue the unchanged success response per C-03. Pass the already-created `auditService` into the VideoTask constructor in `cmd/server/app.go`. Do not touch CancelTask or other recording endpoints.</action>
  <verify>
    <automated>gofmt -w internal/services/video_recording_task_service.go internal/handlers/video_recording_task_handler.go cmd/server/app.go &amp;&amp; go test -count=1 ./internal/services/... ./internal/handlers/... &amp;&amp; go build ./...</automated>
  </verify>
  <done>All five recording handlers compile with the new service return tuples, each successful path emits exactly one correctly labeled service-layer RecordChange call, batch deletion remains one logical audit event, and existing recording behavior and responses are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Capture storage and API-key changes with sanitizer-safe snapshots</name>
  <files>internal/services/storage/file_service.go, internal/services/storage/file_service_test.go, internal/handlers/file_handler.go, internal/services/apikey_service.go, internal/handlers/apikey_handler.go, cmd/server/app.go</files>
  <needs>Task 1's app.go edit is complete; RecordChange and Sanitizer remain unchanged.</needs>
  <creates>Three storage and three API-key RecordChange sites, detached old/new service values, affected storage test adaptations, and constructor wiring.</creates>
  <action>Implement STG-01 through STG-03 and KEY-01 through KEY-03 per C-01 through C-06. For storage, keep `Upload`'s existing return signature and record OldData nil/NewData `*storage.FileUploadResult` after success. Change `FileService.Delete` to return the concrete pre-mutation `*models.UploadedFile` loaded by this service (the prompt's semantic old file snapshot), captured before the soft-delete status update and asynchronous physical deletion. Change `ShareFile` to return exactly `(oldShare, newShare, error)`: before `Create`, query the most recent existing `models.FileShare` for the same `file_id` and `shared_by`, map `gorm.ErrRecordNotFound` to nil OldData, propagate other query errors, then return the newly created share containing its generated token. Preserve the handler's `share_url` response key by deriving the same download URL from `newShare.ShareToken` and the incoming request's externally visible scheme/host; do not add a helper or model field. Add `auditService` to `FileHandler` and its constructor, then call RecordChange after each successful operation with `models.ModuleStorage`; actions are `models.ActionCreate`, `models.ActionDelete`, and `"share"`; resources are `storage:<fileID>` and ResourceID is the file ID. For API keys, value-copy the result of `findAPIKeyForUser` before `SetScopes`, `SetIPWhitelist`, DB updates, deletion, or `IsActive` toggle. Return `(oldAPIKey, newAPIKey, error)` from UpdateAPIKey and ToggleAPIKeyStatus and `(oldAPIKey, error)` from DeleteAPIKey; adjust every error return accordingly. Add `auditService` to `APIKeyHandler` and its constructor, then record update/delete/toggle with `models.ModuleAPIKey`, `apikey:<id>`, and the exact actions `models.ActionUpdate`, `models.ActionDelete`, and `"toggle"`. Pass full APIKey/FileShare snapshots to RecordChange and rely on the existing Sanitizer for `Key`/`KeyHash`, share token, and password fields; do not manually redact or alter snapshots. Audit failures only warn and successful response bodies remain unchanged. In `cmd/server/app.go`, create/use `auditService` before constructing `fileHandler`, pass it to NewFileHandler and NewAPIKeyHandler, and retain APIKeyService.SetAuditService. Update `internal/services/storage/file_service_test.go` only where changed Delete/ShareFile return tuples must be unpacked; retain all assertions and add assertions that old/new share and deleted-file snapshots correspond to the persisted rows.</action>
  <verify>
    <automated>gofmt -w internal/services/storage/file_service.go internal/services/storage/file_service_test.go internal/handlers/file_handler.go internal/services/apikey_service.go internal/handlers/apikey_handler.go cmd/server/app.go &amp;&amp; go test -count=1 ./internal/services/storage ./internal/services/audit/... ./internal/handlers/... &amp;&amp; go build ./...</automated>
  </verify>
  <done>Upload, storage delete/share, and API-key update/delete/toggle return and record the required snapshots; stored audit data remains sanitizer-protected; share URL and API-key response contracts are preserved; all six audit failures are non-blocking warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Capture PPT edit state and notification-setting changes, then verify all 16 sites</name>
  <files>internal/services/ppt_file_service.go, internal/services/ppt_editor_service.go, internal/services/ppt_editor_service_test.go, internal/handlers/ppt_handler.go, internal/services/notification/notification_service.go, internal/handlers/notification_handler.go, cmd/server/app.go</files>
  <needs>Tasks 1 and 2 have established all prior constructor edits in app.go.</needs>
  <creates>Four PPT and one notification RecordChange sites, pre/post PPTFile and notification-setting snapshots, affected PPT test adaptations, final five-handler wiring, and repository-wide verification.</creates>
  <action>Implement PPT-01 through PPT-04 and NOT-01 per C-01 through C-06. Change `PPTFileService.DeletePPTFile` to return the detached pre-delete `*models.PPTFile` after the existing physical/cache/database deletion succeeds. Change `PPTEditorService.DeleteSlides` and `Rollback` to return `(oldPPT, newPPT, error)`: capture OldData before backup creation, file replacement, cache invalidation, or DB mutation, then reload and return the committed PPTFile as NewData. Change `ReorderSlides` to the same `(oldPPT, newPPT, error)` contract, capturing before backup/cache reordering and returning a reloaded post-save model; on the existing no-change success path return two detached snapshots without adding a write. Preserve the handler's `new_order` response by using the service-validated request order after success rather than adding a fourth service return. Complete PPT snapshots must carry all current slide-state fields (`PageCount`, `DeletedSlides`, `EditHistory`, `BackupPath`) before and after; do not invent a `Slides` model field or an audit helper. Add `auditService` to PPThandler and its constructor. After successful DeletePPT/DeleteSlides/Rollback/ReorderSlides calls, record `models.ModulePPT`, resource `ppt:<id>`, ResourceID equal to the PPT ID, and actions `models.ActionDelete`, `"delete_slides"`, `"rollback"`, and exactly `"reorder_slides"` (not the middleware's current `"reorder"`). Use old/nil for delete and old/new for edits; use returned NewData for existing response fields wherever available. Update `ppt_editor_service_test.go` for the new tuples and assert that old snapshots retain pre-edit PageCount/deletion/history/backup values while new snapshots reflect committed values. For notifications, change `UpdateUserSetting` to return `(oldSetting, newSetting, error)`: load the current setting before assigning request UserID or saving, use the existing default setting as OldData when no row exists, take a detached copy, save, reload, and return NewData. Add `auditService` to NotificationHandler and its constructor; after success call RecordChange with `models.ActionUpdate`, `models.ModuleNotification`, resource `notification_setting:<userID>`, ResourceID equal to userID, and the returned old/new settings. Preserve the current message response and warn-only audit failure behavior. Pass auditService to NewPPThandler and NewNotificationHandler in app.go while retaining NotificationHandler.SetLogger. Finally confirm no production file outside the listed services, handlers, and app wiring changed and that `audit_log_service.go`, `RecordChangeOpts`, go.mod, and go.sum are untouched.</action>
  <verify>
    <automated>gofmt -w internal/services/ppt_file_service.go internal/services/ppt_editor_service.go internal/services/ppt_editor_service_test.go internal/handlers/ppt_handler.go internal/services/notification/notification_service.go internal/handlers/notification_handler.go cmd/server/app.go &amp;&amp; go test -count=1 ./internal/services/audit/... ./internal/services/... ./internal/handlers/... &amp;&amp; go build ./...</automated>
  </verify>
  <done>All four PPT operations and notification setting update capture the requested pre/post state, all five modified handlers receive the existing audit service, all 16 sites use the exact module/action policy, affected tests/build pass, and no helper/dependency/scope expansion occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP client -> Gin handler | Untrusted IDs, payloads, file metadata, share passwords, API-key updates, slide orders, and notification settings enter existing validation/authorization paths. |
| Service mutation -> audit snapshot | Full models cross into RecordChange and can contain key/token/password data before Sanitizer processing. |
| Handler -> asynchronous audit queue | Audit persistence may fail after the business mutation has already succeeded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260730-bc3-01 | Tampering / Elevation of Privilege | All 16 service methods | mitigate | Preserve every existing ownership, role, status-transition, and input-validation check; snapshot only after loading through the authorized service path and do not move mutation into handlers. |
| T-260730-bc3-02 | Information Disclosure | APIKey and FileShare OldData/NewData | mitigate | Route full snapshots through the unchanged `RecordChange -> LogOperation -> Sanitizer` pipeline; run audit sanitizer tests and verify key/token/password fields are redacted in persisted audit JSON. |
| T-260730-bc3-03 | Repudiation | BatchDeleteTasks and PPT state changes | mitigate | Emit one deterministic resource string per operation, retain detached pre-state, and return committed post-state so audit diffs cannot be overwritten through pointer aliasing. |
| T-260730-bc3-04 | Denial of Service | RecordChange / async queue | accept | Per C-03, audit failure logs `h.logger.Warn` and cannot block a successful business response; this is the prompt-locked availability policy. |
| T-260730-bc3-SC | Tampering | npm/pip/cargo/go package installs | mitigate | No install task exists; require go.mod/go.sum to remain unchanged and run existing Go tests/build. |
</threat_model>

<verification>
1. Run `go build ./...`.
2. Run `go test -count=1 ./internal/services/audit/... ./internal/services/... ./internal/handlers/...`.
3. Run `rg -n "RecordChange\\(c\\.Request\\.Context\\(\\)" internal/handlers/video_recording_task_handler.go internal/handlers/file_handler.go internal/handlers/ppt_handler.go internal/handlers/apikey_handler.go internal/handlers/notification_handler.go` and inspect that the new calls total 5 + 3 + 4 + 3 + 1 = 16.
4. Run `rg -n "models\\.Module(Task|Storage|PPT|APIKey|Notification)|models\\.Action(Create|Update|Delete)|\"(start|stop|batch_delete|share|delete_slides|rollback|reorder_slides|toggle)\""` over the five handlers and match every call to the source-audit table.
5. Run `rg -n "func \(s \*AuditLogService\) RecordChange\(ctx context.Context, opts RecordChangeOpts\) error" internal/services/audit/audit_log_service.go` to confirm the helper signature is unchanged.
6. Run `git diff -- go.mod go.sum internal/services/audit/audit_log_service.go internal/models/audit_log.go`; it must show no changes.
7. Run `git diff --name-only -- frontend` as an informational check; the output may contain the user's pre-existing frontend edits, but this quick task must add no frontend path and must not stage or modify those files.
</verification>

<success_criteria>
- The 16 source-audit requirements REC-01..NOT-01 are all implemented and no unlisted endpoint is modified.
- Each service returns detached pre/post data without changing existing authorization, validation, transaction, file, scheduler, or response behavior.
- Each successful handler path calls RecordChange with the exact requested module/action, resource ID, OldData, and NewData.
- API-key and share secrets remain protected by the existing Sanitizer.
- Every RecordChange failure is warning-only and non-blocking.
- All targeted tests and `go build ./...` pass; RecordChange helper/model constants/dependency manifests and no frontend path is added or staged by this quick task.
</success_criteria>

<output>
Create `.planning/quick/260730-bc3-38-recording-5-storage-3-ppts-4-apikey-3/260730-bc3-SUMMARY.md` when execution completes.
</output>
