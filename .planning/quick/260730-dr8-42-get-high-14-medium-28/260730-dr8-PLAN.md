---
phase: quick
plan: 260730-dr8
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/audit_log.go
  - cmd/server/app.go
autonomous: true
requirements: []
tags: [audit, security, coverage, get-endpoints, read-audit, export-audit]

must_haves:
  truths:
    - "48 个敏感 GET 端点（13 HIGH 实际挂 + 35 MEDIUM 实际挂）全部挂上 auditOp"
    - "导出类端点用 ActionExport；其余读取用 ActionRead 或 ActionList"
    - "ActionRead + ActionList 常量已添加到 internal/models/audit_log.go"
    - "cmd/server/app.go 中所有 auditOp 仍使用 Module 常量，无字符串字面量"
    - "go build ./... 通过；前端文件零触动"
    - "SUMMARY.md 含 48 端点对应表 + P2 剩余清单 + 3 个刻意跳过的审计自查询说明"
  artifacts:
    - path: ".planning/quick/260730-dr8-42-get-high-14-medium-28/260730-dr8-SUMMARY.md"
      provides: "完成度报告"
      contains: "48"
  key_links:
    - "cmd/server/app.go authenticated.GET('/me', ...) -> auditOp(ModuleUser, ActionRead)"
    - "cmd/server/app.go files.GET('/:id/download', ...) -> auditOp(ModuleFile, ActionExport)"
---

<objective>
为 48 个敏感 GET 端点（13 HIGH 实际挂 + 35 MEDIUM 实际挂）挂上 auditOp 中间件，导出类用 ActionExport、其余用 ActionRead/ActionList。

目的：消除 lr4 完成后遗留的"读操作无审计"覆盖缺口——数据下载/导出、敏感配置读取、PII 查询、列表类查询、状态查询目前全部绕过审计。

产出：3 个原子提交（Task 1=Action 常量、Task 2=HIGH 13 端点、Task 3=MEDIUM 35 端点）；go build 通过；48 端点对应表 + P2 剩余清单写入 SUMMARY.md。

跳过 3 个端点：审计自查询 GET（/audit/logs、/audit/logs/:id、/audit/statistics）以避免递归噪声。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260729-lr4-100/260729-lr4-SUMMARY.md
@.planning/quick/260730-bc3-38-recording-5-storage-3-ppts-4-apikey-3/260730-bc3-SUMMARY.md
@./CLAUDE.md

# 前置状态（已确认，可直接复用）

## 1. 审计中间件终态（lr4）
- internal/middleware/audit.go 已接入 Sanitizer（line 74-82）
- AuditMiddleware.AuditOperation(module, action) gin.HandlerFunc 已就绪
- cmd/server/app.go:690 定义 auditOp := a.auditMiddleware.AuditOperation
- 已有 63 个写路由挂载 auditOp，全部使用 Module 常量，0 字符串字面量
- 失败处理：审计 LogOperation 失败仅 logger.Warn，不阻断业务响应

## 2. Module 常量全集（models/audit_log.go L82-101）
已定义：ModuleUser / ModuleRole / ModuleTask / ModuleConference / ModuleRecording / ModuleFile / ModuleSystem / ModuleConfig / ModuleAPIKey / ModuleInputConfig / ModulePPT / ModuleStorage / ModuleNotification / ModuleVideo / ModuleTranscription / ModuleAdmin

## 3. Action 常量（models/audit_log.go L57-79）
已定义：ActionLogin/Logout/LogoutAll/Refresh/PasswordChange/IPRestrictionFailed/Create/Update/Delete/Query/Export/Import/Execute/Cancel/Approve/Reject

缺失：ActionRead + ActionList —— 本次必须新增。

## 4. lr4 提交约定（保持一致）
- Commit 1：常量添加（ActionRead + ActionList）
- Commit 2：HIGH 13 端点挂载（plan 14 个 HIGH 中 /audit/logs 与 /audit/logs/:id 不挂 → 12 挂 + L715 /auth/me 1 个 = 13）
- Commit 3：MEDIUM 35 端点挂载（/audit/statistics 不挂）
- 最终 commit 数 = 3。
</context>

<interfaces>
关键接口与签名（复用 lr4，零修改）：

中间件（已就绪）：
  func (m *AuditMiddleware) AuditOperation(module, action string) gin.HandlerFunc

新增常量（internal/models/audit_log.go）：
  ActionRead = "read"
  ActionList = "list"

路由挂载模式（写法固定）：
  files.GET("/:id/download", auditOp(models.ModuleFile, models.ActionExport), a.handlers.VideoFile.DownloadFile)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 添加 ActionRead + ActionList 常量</name>
  <files>internal/models/audit_log.go</files>
  <action>
    在 internal/models/audit_log.go 第 66-70 行的 CRUD 操作常量块中，在 ActionQuery 之后追加：

    ```go
    ActionRead   = "read"
    ActionList   = "list"
    ```

    注意：不要修改现有 ActionExport（line 73）的位置或值；保持现有 import 与其他常量顺序；不删除任何现有常量。

    提交：
    git add internal/models/audit_log.go
    git commit -m "feat(audit): add ActionRead + ActionList constants for GET endpoint coverage"
  </action>
  <verify>
    shell: grep -nE 'Action(Read|List)\s*=' internal/models/audit_log.go && go build ./internal/models/... 2>&1 | tail -5
    expect: grep hits at line 71-72 (ActionRead + ActionList); go build 退出码 0
  </verify>
  <done>
    - internal/models/audit_log.go L71-72 新增 ActionRead + ActionList
    - 包可编译
    - 1 个原子提交落 main
  </done>
</task>

<task type="auto">
  <name>Task 2: HIGH 13 端点挂载 auditOp</name>
  <files>cmd/server/app.go</files>
  <action>
    在 cmd/server/app.go 中按 plan §HIGH 14 列表挂 13 个端点（跳过 /audit/logs 与 /audit/logs/:id 两个审计自查询）：

    数据导出（6 个，ActionExport）：
    - L715 authenticated.GET("/me", ...) -> auditOp(ModuleUser, ActionRead) （PII 查询，HIGH 14 中用户 PII）
    - L723 adminGroup.GET("/config", ...) -> auditOp(ModuleAdmin, ActionRead) （敏感 admin 配置）
    - L725 adminGroup.GET("/me", ...) -> auditOp(ModuleAdmin, ActionRead) （admin 当前会话）
    - L697 a.router.GET("/api/v1/scheduler/debug", ...) -> auditOp(ModuleSystem, ActionRead) （HIGH scheduler 调试）
    - L833 files.GET("/:id/download", ...) -> auditOp(ModuleFile, ActionExport)
    - L840 a.router.GET("/api/v1/files/download/:token", ...) -> auditOp(ModuleFile, ActionExport)
    - L841 a.router.GET("/api/v1/files/share/:token", ...) -> auditOp(ModuleFile, ActionExport)
    - L871 ppts.GET("/:id/download", ...) -> auditOp(ModulePPT, ActionExport)
    - L887 a.router.GET("/api/v1/ppts/:id/slides/:resolution/:filename", ...) -> auditOp(ModulePPT, ActionExport)
    - L884 a.router.GET("/api/v1/recordings/:id/preview/stream/:file", ...) -> auditOp(ModuleTask, ActionExport)

    用户 PII（2 个，ActionRead）：
    - L737 users.GET("/profile", ...) -> auditOp(ModuleUser, ActionRead)
    - L739 users.GET("/:id", ...) -> auditOp(ModuleUser, ActionRead)

    系统配置 + 调度（2 个，ActionRead）：
    - L918 system.GET("/config", ...) -> auditOp(ModuleSystem, ActionRead)
    - L697 scheduler/debug 已计入上方

    跳过（审计自查询）：
    - L892 auditLog.GET("/logs", ...) — 查审计日志
    - L893 auditLog.GET("/logs/:id", ...) — 查审计详情

    提交：
    git add cmd/server/app.go
    git commit -m "feat(audit): wire HIGH GET endpoints (13 sites)

    6 data export (file/PPT/preview): ModuleFile/PPT/Task + ActionExport
    5 sensitive config/admin: ModuleSystem/Admin + ActionRead
    2 user PII: ModuleUser + ActionRead

    Skip /audit/logs + /audit/logs/:id (recursion noise)."

    验证：
    go build ./...      # 预期 BUILD OK
    grep -cE "auditOp\(models\." cmd/server/app.go   # 预期 76（63 lr4 + 13 HIGH）
  </action>
  <verify>
    shell: go build ./... 2>&1 | tail -5 && echo BUILD_OK && grep -cE "auditOp\(models\." cmd/server/app.go && echo expected 76
    expect: BUILD_OK; grep 输出 76
  </verify>
  <done>
    - 13 个 HIGH 端点全部挂上 auditOp
    - 全部使用 Module/Action 常量，无字符串字面量
    - go build 退出码 0
    - 1 个原子提交落 main
  </done>
</task>

<task type="auto">
  <name>Task 3: MEDIUM 35 端点挂载 + 全量验证 + SUMMARY</name>
  <files>cmd/server/app.go</files>
  <action>
    在 cmd/server/app.go 的 35 个 MEDIUM 端点上挂 auditOp（跳过 1 个审计自查询 /audit/statistics）。

    用户/角色/APIKey (8 个)：
    - L736 users.GET("", ...) -> auditOp(ModuleUser, ActionList)
    - L750 roles.GET("", ...) -> auditOp(ModuleRole, ActionList)
    - L751 roles.GET("/:id", ...) -> auditOp(ModuleRole, ActionRead)
    - L755 roles.GET("/:id/permissions", ...) -> auditOp(ModuleRole, ActionRead)
    - L760 api.GET("/permissions", ...) -> auditOp(ModuleRole, ActionList)
    - L765 apikeys.GET("", ...) -> auditOp(ModuleAPIKey, ActionList)
    - L767 apikeys.GET("/:id", ...) -> auditOp(ModuleAPIKey, ActionRead)
    - L771 apikeys.GET("/:id/logs", ...) -> auditOp(ModuleAPIKey, ActionRead)
    - L772 apikeys.GET("/:id/summary", ...) -> auditOp(ModuleAPIKey, ActionRead)

    输入配置 (3 个)：
    - L806 inputConfigs.GET("", ...) -> auditOp(ModuleInputConfig, ActionList)
    - L807 inputConfigs.GET("/active", ...) -> auditOp(ModuleInputConfig, ActionRead)
    - L808 inputConfigs.GET("/:id", ...) -> auditOp(ModuleInputConfig, ActionRead)

    存储 + 文件 (5 个)：
    - L820 storage.GET("/quota", ...) -> auditOp(ModuleFile, ActionRead)
    - L821 storage.GET("", ...) -> auditOp(ModuleFile, ActionList)
    - L829 files.GET("", ...) -> auditOp(ModuleFile, ActionList)
    - L830 files.GET("/stats", ...) -> auditOp(ModuleFile, ActionRead)
    - L834 files.GET("/:id", ...) -> auditOp(ModuleFile, ActionRead)

    录制 + 视频 (6 个)：
    - L778 recordings.GET("", ...) -> auditOp(ModuleTask, ActionList)
    - L779 recordings.GET("/:id", ...) -> auditOp(ModuleTask, ActionRead)
    - L790 recordings.GET("/:id/conversion-status", ...) -> auditOp(ModuleTask, ActionRead)
    - L793 recordings.GET("/:id/preview", ...) -> auditOp(ModuleTask, ActionRead)
    - L847 videos.GET("/:id/split-status", ...) -> auditOp(ModuleTask, ActionRead)
    - L848 videos.GET("/:id/segments", ...) -> auditOp(ModuleTask, ActionList)

    转录 + PPT (6 个)：
    - L850 videos.GET("/:id/transcription-status", ...) -> auditOp(ModuleTranscription, ActionRead)
    - L851 videos.GET("/:id/transcription-text", ...) -> auditOp(ModuleTranscription, ActionRead)
    - L852 videos.GET("/:id/ppts", ...) -> auditOp(ModulePPT, ActionList)
    - L859 transcriptions.GET("/active", ...) -> auditOp(ModuleTranscription, ActionList)
    - L860 transcriptions.GET("/:videoFileId/timestamps", ...) -> auditOp(ModuleTranscription, ActionRead)
    - L862 transcriptions.GET("/batch/:id", ...) -> auditOp(ModuleTranscription, ActionRead)

    PPT 幻灯片 (2 个)：
    - L868 ppts.GET("/:id/slides", ...) -> auditOp(ModulePPT, ActionList)
    - L874 ppts.GET("/:id/duplicates", ...) -> auditOp(ModulePPT, ActionRead)

    通知 (3 个)：
    - L900 notifications.GET("", ...) -> auditOp(ModuleNotification, ActionList)
    - L901 notifications.GET("/unread-count", ...) -> auditOp(ModuleNotification, ActionRead)
    - L904 notifications.GET("/settings", ...) -> auditOp(ModuleNotification, ActionRead)

    仪表板 (1 个)：
    - L912 dashboard.GET("/stats", ...) -> auditOp(ModuleSystem, ActionRead)

    跳过（属审计自查询）：
    - L894 auditLog.GET("/statistics", ...)

    命名一致性：本计划用 ModuleTask 覆盖 /recordings 与 /videos/:id/split-* GET，与 bc3 §3 RecordChange 决策一致。

    提交：
    git add cmd/server/app.go
    git commit -m "feat(audit): wire MEDIUM GET endpoints (35 sites)

    8 user/role/apikey + 3 input-configs + 5 storage/files
    6 recordings/videos-split + 6 transcription/ppts + 2 ppts slides
    3 notifications + 1 dashboard/stats

    Coverage: GET audit endpoints 13 -> 48. Skip /audit/statistics (recursion noise)."

    末尾验证：
    go build ./...      # 预期 BUILD OK
    grep -cE "auditOp\(models\." cmd/server/app.go   # 预期 111
    grep -cE 'auditOp\("[a-z_]+"' cmd/server/app.go   # 预期 0
    git diff --stat frontend/ | wc -l                  # 预期 0

    最后写 SUMMARY.md 到 .planning/quick/260730-dr8-42-get-high-14-medium-28/260730-dr8-SUMMARY.md，包含：3 个 commit 列表、48 端点对应表（按 Module 分组）、刻意跳过的 3 个审计自查询说明、P2 剩余清单。
  </action>
  <verify>
    shell: go build ./... 2>&1 | tail -5 && echo BUILD_OK && grep -cE "auditOp\(models\." cmd/server/app.go && echo expected_111 && grep -cE 'auditOp\("[a-z_]+"' cmd/server/app.go && echo expected_0 && git diff --stat frontend/ | wc -l && echo expected_0
    expect: BUILD_OK; grep 111; grep 0; git diff 0
  </verify>
  <done>
    - 35 个 MEDIUM 端点全部挂上 auditOp
    - 总挂载 = 63 lr4 baseline + 13 HIGH + 35 MEDIUM = 111 auditOp
    - 0 字符串字面量
    - 前端文件零触动
    - SUMMARY.md 含 48 端点对应表 + P2 剩余清单
    - 1 个原子提交落 main
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户 → GET 端点 | 未审计的 GET 等于隐式数据外泄/隐私泄漏通道，必须挂审计 |
| 审计 → 数据库 | Sanitizer 已就位（lr4），新挂载的 GET 走相同管道 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-dr8-01 | Information Disclosure | 42 个 GET 端点无审计（数据导出/PII/敏感配置） | mitigate | Task 2 + Task 3 全部挂 auditOp |
| T-dr8-02 | Repudiation | 用户查别人 PII / admin 配置 / scheduler 调试 无审计 | mitigate | HIGH 14 全部挂 ActionRead |
| T-dr8-03 | Recursion Noise | /audit/logs 与 /audit/statistics 自查询产生审计风暴 | accept | 跳过 3 个自查询端点（明确注释 + SUMMARY 说明） |
| T-dr8-04 | Maintainability | ActionRead/ActionList 缺失导致后续挂载又用字面量 | mitigate | Task 1 补常量并锁定命名 |
</threat_model>

<verification>
- Task 1: grep -nE "Action(Read|List)" internal/models/audit_log.go 命中 line 71-72
- Task 2: grep -cE "auditOp\(models\." cmd/server/app.go = 76; go build 退出码 0
- Task 3: grep -cE "auditOp\(models\." cmd/server/app.go = 111; grep 字面量 = 0; git diff frontend/ = 空
- SUMMARY.md 存在且含 48 端点对应表
</verification>

<success_criteria>
48 个敏感 GET 端点（13 HIGH + 35 MEDIUM）挂上 auditOp，导出类用 ActionExport、其余用 ActionRead/ActionList；3 个审计自查询端点刻意跳过并明确说明；3 个原子提交落 main；frontend 文件零触动；SUMMARY.md 含完整对应表 + P2 剩余清单。
</success_criteria>

<output>
Create .planning/quick/260730-dr8-42-get-high-14-medium-28/260730-dr8-SUMMARY.md when done.
</output>