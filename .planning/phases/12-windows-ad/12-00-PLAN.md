---
phase: 12
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/auth/ad_authenticator_test.go
  - internal/auth/local_authenticator_test.go
  - internal/auth/ad_validator_test.go
  - internal/models/user_ad_test.go
  - internal/config/ad_config_test.go
  - internal/handlers/admin_ad_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "All test stub files exist with failing tests"
    - "Test files import required dependencies"
    - "Tests compile successfully (fail at runtime)"
  artifacts:
    - path: "internal/auth/ad_authenticator_test.go"
      provides: "Test stubs for AD authentication logic"
      min_lines: 50
    - path: "internal/auth/local_authenticator_test.go"
      provides: "Test stubs for local authenticator extraction"
      min_lines: 30
    - path: "internal/auth/ad_validator_test.go"
      provides: "Test stubs for AD config validation"
      min_lines: 40
    - path: "internal/models/user_ad_test.go"
      provides: "Test stubs for User AD fields"
      min_lines: 30
    - path: "internal/config/ad_config_test.go"
      provides: "Test stubs for AD configuration"
      min_lines: 30
    - path: "internal/handlers/admin_ad_test.go"
      provides: "Test stubs for admin AD endpoints"
      min_lines: 40
  key_links:
    - from: "All test files"
      to: "go-ldap/v3 library"
      via: "import github.com/go-ldap/ldap/v3"
      pattern: "import.*github.com/go-ldap/ldap/v3"
---

<objective>
Create test infrastructure stubs for all AD authentication functionality, establishing the test-driven development foundation for Phase 12 implementation.

Purpose: Enable test-driven development by defining test expectations before implementation, ensuring all functionality has corresponding test coverage from the start.

Output: Six test files with failing test stubs that compile and establish the testing contract for subsequent implementation plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-windows-ad/12-CONTEXT.md
@.planning/phases/12-windows-ad/12-RESEARCH.md
@.planning/phases/12-windows-ad/12-PATTERNS.md
@.planning/phases/12-windows-ad/12-VALIDATION.md
@.claude/skills/spike-findings-record-v2/SKILL.md
@internal/auth/service.go
@internal/models/user.go
@internal/config/config.go
@internal/migrations/011_add_ip_restrictions.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install go-ldap/v3 dependency and create AD authentication test stubs</name>
  <files>
    go.mod
    internal/auth/ad_authenticator_test.go
    internal/auth/ad_validator_test.go
    internal/auth/ad_config.go
  </files>
  <action>
    **Step 1: Add go-ldap/v3 dependency**
    Run: `go get github.com/go-ldap/ldap/v3@latest`

    **Step 2: Create internal/auth/ad_config.go** (AD types and interfaces)
    Create file with:
    - ADUser struct (Username, DN, ObjectGUID, Email, DisplayName, Department, UserPrincipalName, UserAccountControl)
    - IsDisabled() method checking ACCOUNTDISABLE bit (0x0002)
    - ADAuthConfig struct (Server, BindDN, Password, BaseDN, UseTLS, PoolSize, DialTimeout, RequestTimeout, InsecureSkipVerify)
    - ADConfigValidationResult struct (Valid, Level, Errors, Warnings, ServerInfo, ResponseTime)
    - Authenticator interface (Login, Logout, ValidateToken, Name methods)

    **Step 3: Create internal/auth/ad_authenticator_test.go**
    Create test stubs:
    - TestADAuthenticator_Login_Success (expect user found, token returned)
    - TestADAuthenticator_Login_UserNotFound (expect "域控账号不存在" error)
    - TestADAuthenticator_Login_InvalidPassword (expect "域控密码错误" error)
    - TestADAuthenticator_Login_AccountDisabled (expect "域控账号已禁用" error)
    - TestADAuthenticator_Login_ConnectionFailed (expect connection error)
    - TestADAuthenticator_CreateLocalUser (expect local user created with AD fields)
    - TestADAuthenticator_ExistingUserUpdated (expect AD fields updated on login)
    - All tests should use mock LDAP connection or testify/mock
    - Import: github.com/go-ldap/ldap/v3, github.com/stretchr/testify/assert

    **Step 4: Create internal/auth/ad_validator_test.go**
    Create test stubs:
    - TestADConfigValidator_FormatValidation (expect level 1 failure on empty fields)
    - TestADConfigValidator_NetworkValidation (expect level 2 failure on connection refused)
    - TestADConfigValidator_AuthValidation (expect level 3 failure on invalid credentials)
    - TestADConfigValidator_FunctionalityValidation (expect level 4 success with user search)
    - TestADConfigValidator_LDAPInjectionPrevention (expect escaped username in filter)
    - TestADConfigValidator_Port389Warning (expect warning when UseTLS=false)
  </action>
  <verify>
    <automated>go test ./internal/auth/... -run TestAD -v 2>&1 | grep -E "(FAIL|PASS|no such file)"</automated>
  </verify>
  <done>
    - go.mod contains github.com/go-ldap/ldap/v3 dependency
    - internal/auth/ad_config.go exists with ADUser, ADAuthConfig, ADConfigValidationResult, Authenticator interface
    - internal/auth/ad_authenticator_test.go exists with 7 test stubs
    - internal/auth/ad_validator_test.go exists with 5 test stubs
    - Tests compile (may fail at runtime with "no such file" for missing implementations)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for local authenticator, User model, and config</name>
  <files>
    internal/auth/local_authenticator_test.go
    internal/models/user_ad_test.go
    internal/config/ad_config_test.go
  </files>
  <action>
    **Step 1: Create internal/auth/local_authenticator_test.go**
    Create test stubs for extracted local authentication:
    - TestLocalAuthenticator_Name (expect "local")
    - TestLocalAuthenticator_Login_Success (expect token returned for valid credentials)
    - TestLocalAuthenticator_Login_UserNotFound (expect user not found error)
    - TestLocalAuthenticator_Login_InvalidPassword (expect invalid credential error)
    - TestLocalAuthenticator_Login_InactiveUser (expect error for inactive user)
    - TestLocalAuthenticator_Login_IPRestriction (expect IP restriction error)
    - TestLocalAuthenticator_Logout (expect token revoked)
    - TestLocalAuthenticator_ValidateToken (expect user DTO returned for valid token)
    - All tests should use mock database (sqlmock or testify/suite)
    - Tests verify existing service.go behavior is preserved

    **Step 2: Create internal/models/user_ad_test.go**
    Create test stubs for User model AD fields:
    - TestUser_ADFieldsExist (expect ADUsername, ADDN, ADGUID, ADDepartment, ADUPN, LastADLogin fields exist)
    - TestUser_ADFieldsNullable (expect AD fields can be NULL for local users)
    - TestUser_ADGUIDIndexed (expect idx_users_ad_guid index exists)
    - TestUser_ADFieldValidation (expect field constraints: VARCHAR lengths, CHAR(36) for GUID)
    - Tests verify database schema matches migration

    **Step 3: Create internal/config/ad_config_test.go**
    Create test stubs for AD configuration:
    - TestADConfig_DefaultValues (expect Mode="local", PoolSize=10, DialTimeout=10, RequestTimeout=30)
    - TestADConfig_EnvironmentVariablePassword (expect AD_PASSWORD env var loaded)
    - TestADConfig_TLSDefaults (expect InsecureSkipVerify=false in production)
    - TestADConfig_Validation (expect required fields validated)
    - Tests verify viper config loading and defaults
  </action>
  <verify>
    <automated>go test ./internal/auth/... ./internal/models/... ./internal/config/... -run "TestLocal|TestUser.*AD|TestADConfig" -v 2>&1 | grep -E "(FAIL|PASS|no such file)"</automated>
  </verify>
  <done>
    - internal/auth/local_authenticator_test.go exists with 8 test stubs
    - internal/models/user_ad_test.go exists with 4 test stubs
    - internal/config/ad_config_test.go exists with 4 test stubs
    - Tests compile (may fail at runtime with missing implementations)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create admin handler test stubs and verify Wave 0 completion</name>
  <files>
    internal/handlers/admin_ad_test.go
  </files>
  <action>
    **Create internal/handlers/admin_ad_test.go**
    Create test stubs for admin AD configuration endpoints:
    - TestAdminHandler_GetAuthConfig (expect config returned with password hidden)
    - TestAdminHandler_GetAuthConfig_Sanitized (expect password field excluded from response)
    - TestAdminHandler_UpdateAuthConfig_LocalMode (expect local mode update succeeds)
    - TestAdminHandler_UpdateAuthConfig_ADMode_ValidConfig (expect AD mode update after validation)
    - TestAdminHandler_UpdateAuthConfig_ADMode_InvalidConfig (expect validation failure blocks update)
    - TestAdminHandler_TestADConnection (expect 4-layer validation result)
    - TestAdminHandler_TestADConnection_Port389Warning (expect warning in response for port 389)
    - Tests should use gin test mode and mock services

    **Verify Wave 0 completion:**
    Run: `go test ./internal/auth/... ./internal/models/... ./internal/config/... ./internal/handlers/... -run "TestAD|TestLocal|TestUser.*AD|TestADConfig|TestAdmin" -v`
    Expect: All tests compile (some fail with missing implementations, none fail with "no such file")
  </action>
  <verify>
    <automated>go test ./internal/handlers/... -run TestAdmin -v 2>&1 | grep -E "(FAIL|PASS|no such file|compile)"</automated>
  </verify>
  <done>
    - internal/handlers/admin_ad_test.go exists with 7 test stubs
    - All Wave 0 test files exist and compile
    - Test count: 7 (ad_auth) + 5 (ad_validator) + 8 (local_auth) + 4 (user_ad) + 4 (ad_config) + 7 (admin) = 35 test stubs
    - No "no such file" errors when running tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → Implementation | Test stubs define contracts, implementation plans fulfill them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-00-01 | Tampering | Test dependencies | mitigate | Verify go-ldap/v3 signature after go get, use go.sum |
| T-12-00-02 | Information Disclosure | Test AD credentials | mitigate | Never use real AD credentials in tests, use mocks |
| T-12-00-03 | Spoofing | Mock LDAP servers | mitigate | Clearly label mock connections in test code |
</threat_model>

<verification>
- [ ] All 6 test files created
- [ ] go-ldap/v3 dependency added to go.mod
- [ ] 35 test stubs total across all files
- [ ] Tests compile without "no such file" errors
- [ ] Test stubs use testify/assert and testify/mock patterns
- [ ] AD types and interfaces defined in ad_config.go
- [ ] No real AD credentials in test code
</verification>

<success_criteria>
**Wave 0 Complete When:**
- go-ldap/v3 library installed and verified in go.mod
- 6 test files exist with 35 total test stubs
- All tests compile successfully (runtime failures acceptable)
- Test stubs cover all D-01 to D-23 requirements from CONTEXT.md
- Test files follow existing project test patterns (testify, table-driven tests)
- No sensitive credentials or real AD server addresses in test code
</success_criteria>

<output>
After completion, create `.planning/phases/12-windows-ad/12-00-SUMMARY.md`
</output>
