---
phase: 11
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: ["WAVE-0"]
user_setup: []
must_haves:
  truths:
    - Test stubs exist for IP validation logic
    - Test stubs exist for IP restriction checking
    - Test stubs exist for frontend IP input component
    - All test files compile without errors
  artifacts:
    - path: "internal/auth/ip_validator_test.go"
      provides: "IP validation test stubs"
      min_lines: 30
    - path: "internal/auth/ip_restriction_test.go"
      provides: "IP restriction integration test stubs"
      min_lines: 40
    - path: "internal/models/ip_restriction_test.go"
      provides: "GORM JSON field test stubs"
      min_lines: 30
    - path: "frontend/src/components/__tests__/IPInput.test.tsx"
      provides: "Frontend IP input component test stubs"
      min_lines: 25
  key_links: []
---

<objective>
Create test infrastructure stubs for IP address restriction feature following Wave 0 requirements from 11-VALIDATION.md.

Purpose: Establish test framework before implementation to enable TDD workflow and ensure validation coverage.
Output: Test stub files for all IP-related functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ip-ip/11-VALIDATION.md
@.planning/phases/11-ip-ip/11-RESEARCH.md
@.planning/phases/11-ip-ip/11-PATTERNS.md

# Key patterns from existing codebase
@internal/auth/service.go
@internal/models/user.go
@internal/models/role.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IP validator test stubs</name>
  <files>internal/auth/ip_validator_test.go</files>
  <read_first>
  - internal/auth/service.go (to understand existing auth patterns)
  - internal/models/api_key.go (lines 126-150 for IP validation reference)
  </read_first>
  <action>
Create test file `internal/auth/ip_validator_test.go` with stub test functions for:
- `TestValidateIP_ValidIP` - tests valid IPv4 addresses
- `TestValidateIP_InvalidIP` - tests invalid IP formats
- `TestValidateIP_IPv6Rejected` - tests IPv6 rejection per D-09
- `TestValidateCIDR_ValidCIDR` - tests valid CIDR ranges
- `TestValidateCIDR_InvalidCIDR` - tests invalid CIDR formats
- `TestValidateIPRange_ValidRange` - tests valid IP ranges
- `TestValidateIPRange_InvalidRange` - tests invalid IP range formats
- `TestIsIPAllowed_SingleIP` - tests single IP matching
- `TestIsIPAllowed_CIDRRange` - tests CIDR range matching
- `TestIsIPAllowed_IPRange` - tests IP range matching
- `TestIsIPAllowed_NoMatch` - tests IP not in allowed list
- `TestIsIPAllowed_EmptyList` - tests empty allowed list (should allow all)

Each test should have `t.Run()` for now with `t.Skip("TODO: implement")` to establish structure.
Add package-level documentation explaining these tests validate the IPValidator from ip_validator.go.
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/record_V2 && go test ./internal/auth/... -run TestValidate -v 2>&1 | head -30</automated>
  </verify>
  <done>Test file exists with 12 test function stubs, all marked with t.Skip(), file compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create IP restriction service test stubs</name>
  <files>internal/auth/ip_restriction_test.go</files>
  <read_first>
  - internal/auth/service.go (Login method pattern, lines 88-177)
  - internal/models/user.go (User model and HasPermission method)
  - internal/models/role.go (Role model)
  </read_first>
  <action>
Create test file `internal/auth/ip_restriction_test.go` with stub test functions for:
- `TestCheckIPRestriction_UserOnly` - tests user-level IP restrictions only
- `TestCheckIPRestriction_RoleOnly` - tests role-level IP restrictions only
- `TestCheckIPRestriction_UserAndRole_OR` - tests OR logic merging user + role IPs per D-02
- `TestCheckIPRestriction_NoRestrictions` - tests empty IP lists (allow all per D-03)
- `TestCheckIPRestriction_IPNotInList` - tests IP not allowed
- `TestCheckIPRestriction_MultiRoleMerge` - tests multiple roles' IPs merged correctly
- `TestCheckIPRestriction_InvalidClientIP` - tests invalid client IP handling
- `TestCheckIPRestriction_AuditLogOnFailure` - tests audit logging on IP check failure

Add setup/teardown helpers for test database, test fixtures for users/roles with IP restrictions.
Use testify/assert for assertions (existing pattern in codebase).
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/record_V2 && go test ./internal/auth/... -run TestCheckIPRestriction -v 2>&1 | head -30</automated>
  </verify>
  <done>Test file exists with 8 test function stubs, helper fixtures defined, file compiles without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create GORM JSON field test stubs</name>
  <files>internal/models/ip_restriction_test.go</files>
  <read_first>
  - internal/models/user.go (User model structure)
  - internal/models/role.go (Role model structure)
  - internal/models/api_key.go (lines 25-26, 86-124 for JSON field pattern)
  </read_first>
  <action>
Create test file `internal/models/ip_restriction_test.go` with stub test functions for:
- `TestUser_GetAllowedIPs_Empty` - tests GetAllowedIPs() with empty field
- `TestUser_GetAllowedIPs_JSONArray` - tests GetAllowedIPs() deserializes JSON array
- `TestUser_SetAllowedIPs_Serializes` - tests SetAllowedIPs() serializes to JSON
- `TestRole_GetAllowedIPs_Empty` - tests Role GetAllowedIPs() with empty field
- `TestRole_GetAllowedIPs_JSONArray` - tests Role GetAllowedIPs() deserializes JSON array
- `TestRole_SetAllowedIPs_Serializes` - tests Role SetAllowedIPs() serializes to JSON
- `TestAllowedIPs_GORMScan` - tests GORM Scan() interface implementation
- `TestAllowedIPs_GORMValue` - tests GORM Value() interface implementation

Follow the existing pattern from api_key.go (Scopes, IPWhitelist JSON fields).
Add test helpers for creating test User/Role with IP restrictions.
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/record_V2 && go test ./internal/models/... -run TestAllowedIPs -v 2>&1 | head -30</automated>
  </verify>
  <done>Test file exists with 8 test function stubs, GORM interface tests defined, file compiles without errors</done>
</task>

<task type="auto">
  <name>Task 4: Create frontend IP input test stubs</name>
  <files>frontend/src/components/__tests__/IPInput.test.tsx</files>
  <read_first>
  - frontend/src/pages/system/users/index.tsx (form pattern, lines 90-116)
  - frontend/package.json (verify vitest is available)
  </read_first>
  <action>
Create test file `frontend/src/components/__tests__/IPInput.test.tsx` with stub test cases for:
- `renders textarea for IP input` - validates TextArea component renders
- `converts textarea lines to array on change` - tests line-by-line conversion
- `trims whitespace from IP entries` - tests whitespace handling
- `filters empty lines` - tests empty line filtering
- `supports single IP format` - tests "192.168.1.100" format
- `supports CIDR format` - tests "192.168.1.0/24" format
- `supports IP range format` - tests "192.168.1.100-192.168.1.200" format
- `displays placeholder with examples` - validates placeholder text

Use Vitest test framework (existing pattern in frontend).
Add describe blocks grouping related tests.
Mark with `test.skip()` for now to establish structure.
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/record_V2/frontend && npm test -- IPInput.test.ts 2>&1 | head -20</automated>
  </verify>
  <done>Test file exists with 8 test case stubs, all marked with test.skip(), file compiles without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Production Code | Test code validates production implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-00-01 | Tampering | Test fixtures | accept | Test environment only, no production impact |
| T-11-00-02 | Spoofing | Mock IP addresses | accept | Test data only, validates spoofing protection logic |

All Wave 0 tests are infrastructure-only with no security requirements.
</threat_model>

<verification>
- All test files compile without errors
- Test stubs cover all requirements from 11-VALIDATION.md
- Each test has clear name and documentation
- Test structure supports TDD workflow (write test → implement → pass)
</verification>

<success_criteria>
- 4 test files created with 36 total test stubs
- `go test ./internal/auth/... -run TestIP` runs without compilation errors
- `npm test -- IPInput` runs without compilation errors
- All tests marked as skipped with clear TODO comments
- Test structure follows existing codebase patterns (testify, vitest)
</success_criteria>

<output>
After completion, create `.planning/phases/11-ip-ip/11-00-SUMMARY.md`
</output>
