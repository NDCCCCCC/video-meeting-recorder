---
phase: 22-address-v1-1-audit-tech-debt-regenerate-errors-md-backfill-v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/errors.md
autonomous: true
requirements: [P22-R1]
tags: [docs, error-sentinel, generator, ci-sync]

must_haves:
  truths:
    - "docs/errors.md is freshly regenerated by `go generate ./internal/errors/...` and reflects current internal/errors source files"
    - "CI sync-check `git diff --quiet docs/errors.md` exits 0 after regeneration (SYNC_OK)"
    - "Audit footer `Current err.Error() / inline classify branches` shows reduced count from previous 16 (reflects current handler convergence state)"
    - "No source code outside `docs/errors.md` is modified by this plan"
    - "Single commit on main containing only docs/errors.md"
  artifacts:
    - path: docs/errors.md
      provides: "Auto-generated sentinel reference (Sentinel Table + BusinessError Table + Ad-hoc Error Audit)"
      regenerated_by: "go generate ./internal/errors/... via cmd/error-doc-gen"
  key_links:
    - from: internal/errors/errors.go (//go:generate directive)
      to: docs/errors.md
      via: "cmd/error-doc-gen (errors-file + mapping-file + output)"
      pattern: "//go:generate go run ../../cmd/error-doc-gen"
    - from: .github/workflows/ci.yml lines 44-51
      to: docs/errors.md
      via: "Verify errors doc sync step (go generate + git diff --quiet)"
      pattern: "git diff --quiet docs/errors.md"
---

<objective>
Regenerate `docs/errors.md` via `go generate ./internal/errors/...` so the auto-generated sentinel reference reflects the current state of `internal/errors/errors.go` and `internal/errors/mapping.go`. The audit footer ad-hoc branch count (currently 16 per docs/errors.md line 79) may change based on latest handler state; the goal is SYNC_OK, not a specific number.

Purpose: Phase 20 created the `cmd/error-doc-gen` generator (commit history per STATE.md) and a CI sync-check at .github/workflows/ci.yml lines 44-51. The current `docs/errors.md` was generated when 31 sites existed; the file has not been regenerated since handler convergence reduced the count to 16. Fresh regeneration ensures the audit footer reflects the post-convergence state, satisfying the v1.1-MILESTONE-AUDIT §Executive Summary tech_debt item "31 remaining inline err.Error() branches" cleanup signal.

Output: Single commit on `main` containing only the regenerated `docs/errors.md`. No business code change. No test changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/20-handleerror-classify-convergence/20-VERIFICATION.md

<interfaces>
Generator contract (from cmd/error-doc-gen/main.go lines 164-234 and the //go:generate directive in internal/errors/errors.go):

- Trigger: `go generate ./internal/errors/...` (resolves the //go:generate line in errors.go line 3)
- Flags:
  - `-errors-file errors.go` (relative to package dir)
  - `-mapping-file mapping.go`
  - `-output ../../docs/errors.md`
  - `-repo-root ../..`
- Inputs:
  - `internal/errors/errors.go` — sentinel definitions (var block, 42 ErrXxx = errors.New(...)) + Code constants (const block, 10 CodeXxx = "...")
  - `internal/errors/mapping.go` — MapToHTTPStatus switch (sentinel → HTTP status) + mapBusinessError switch (Code → HTTP status)
- Output:
  - `docs/errors.md` — regenerated Sentinel Table (~42 rows), BusinessError Table (10 rows), Ad-hoc Error Audit footer (count of inline err.Error() / errMsg in internal/handlers/ excluding _test.go and ShouldBindJSON blocks)
- Generator path resolution: walks up from cwd looking for go.mod (handles `go generate` setting cwd to package dir)

CI sync-check contract (.github/workflows/ci.yml lines 44-51):

- Step name: "Verify errors doc sync"
- Command:
  - `go generate ./internal/errors/...`
  - `if ! git diff --quiet docs/errors.md; then ... exit 1`
- Enforcement: post-build, pre-test; any drift blocks CI

Current docs/errors.md state (read before regen):
- 42 sentinel rows in Sentinel Table (matches errors.go var block per STATE.md §Phase 19)
- 10 BusinessError rows
- Footer line 79: "Current err.Error() / inline classify branches: 16 (target: 0)"
- After regen, the count may stay at 16 (no further handler convergence this phase) or change based on what grep sees
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate docs/errors.md and commit the regenerated file only</name>
  <files>docs/errors.md</files>

  <read_first>
  必读 (一次性读完所需段):
  - `docs/errors.md` (entire file, 当前 84 行, 已知 Sentinel Table 42 行 + BusinessError Table 10 行 + footer 当前 16)
  - `internal/errors/errors.go` (line 1-3 //go:generate directive + 整个 var/const block)
  - `internal/errors/mapping.go` (MapToHTTPStatus switch + mapBusinessError switch)
  - `cmd/error-doc-gen/main.go` line 164-234 (Generate 函数签名 + 参数 + 输出路径) + line 524-548 (grepCount 实现)
  - `.github/workflows/ci.yml` line 44-51 (CI sync-check 命令序列)
  - `.planning/v1.1-MILESTONE-AUDIT.md` §Executive Summary (regen 是 tech_debt cleanup signal)
  </read_first>

  <action>
  **A. 执行生成**:

  - 在仓库根 (D:/CODE/ClaudeCode/record_V2) 运行 `go generate ./internal/errors/...`. 该命令会:
    1. 命中 internal/errors/errors.go line 3 的 `//go:generate go run ../../cmd/error-doc-gen ...` 指令
    2. 解析 errors.go + mapping.go + grep internal/ 目录
    3. 写入 docs/errors.md (覆盖)
  - 期望 exit code = 0, 无 stderr 输出

  **B. 验证生成结果**:

  - 读取 `docs/errors.md` 顶部与底部, 确认:
    - Header 保留 `# Error Sentinel Reference` + `> Auto-generated by cmd/error-doc-gen.` + `> Do not edit by hand — regenerate via go generate ./internal/errors/...` 三行 header (per generator main.go line 600-605)
    - Sentinel Table 行数 = parseSentinels 解析的数量 (期望 42, errors.go var 块行数 — 与 phase 19/20 一致)
    - BusinessError Table 行数 = parseCodes 解析的数量 (期望 10)
    - Ad-hoc Error Audit footer 显示一个数字 (本次可能仍为 16 或更低, 取决于 internal/handlers/ 当前状态; 不必预设特定值)
  - 运行 `git diff --quiet docs/errors.md && echo SYNC_OK`; 期望输出 `SYNC_OK` 且 exit 0 (说明再次 regen 不产生 diff, 生成器是确定性的)
  - 运行 `wc -l docs/errors.md` 记录行数 (regen 后)
  - 运行 `git diff --stat docs/errors.md` 记录变更统计 (changed insertions + deletions)

  **C. 验证 CI sync-check 语义**:

  - 在 docs/errors.md regen 前后, 运行 `go generate ./internal/errors/...` 然后 `git diff --quiet docs/errors.md`; 第二次应 exit 0 (确认生成器 deterministic, 验证 CI 命令序列在本仓库环境下行为一致)
  - 不需要实际 push 或触发 CI; 仅本地模拟

  **D. 不修改其他文件**:

  - 不修改 internal/errors/errors.go 或 internal/errors/mapping.go (无业务改动)
  - 不修改 cmd/error-doc-gen/main.go (generator 已落地, 不调整)
  - 不修改 .github/workflows/ci.yml (CI 配置稳定)
  - 不修改其他 .planning/ 文件 (本 plan 不动 STATE.md / ROADMAP.md; 由 phase metadata commit 处理)

  **E. 提交**:

  - Stage: `git add docs/errors.md` (普通 add, docs/errors.md 不在 .planning/ 下, 不用 -f)
  - Commit subject: `docs(22): regenerate errors.md — reflect current sentinel + ad-hoc count`
  - Commit body 至少包含:
    - "Regenerated docs/errors.md via `go generate ./internal/errors/...`."
    - "Ad-hoc count before: 16 (per pre-regen docs/errors.md line 79). After: <新数字>."
    - "Sentinel Table: <42> rows. BusinessError Table: <10> rows."
    - "CI sync-check (`go generate && git diff --quiet docs/errors.md`) passes locally."
  - 提交仅含 docs/errors.md 单文件 (git show --stat 应只列该路径)
  - Trailer: `Co-Authored-By: Claude <noreply@anthropic.com>`

  **F. 编写本 plan 的 SUMMARY**:

  - 创建 `.planning/phases/22-address-v1-1-audit-tech-debt-regenerate-errors-md-backfill-v/22-01-SUMMARY.md`
  - 包含: commit hash, before/after ad-hoc 计数, 行数变化, regen 命令 (用于 verify)

  **禁止**:
  - 修改 internal/errors/errors.go 或 mapping.go (Phase 20 已定稿; 任何 sentinel/Code 增删属于 phase 23+ scope)
  - 修改 cmd/error-doc-gen (generator 行为冻结)
  - 修改其他文件 — 本 plan 仅 regen docs/errors.md
  - 在 commit message 里手动编辑 docs/errors.md 内容 (auto-generated, 不要 edit-by-hand)
  - 跳过 git diff --quiet 二次验证 (它证明 generator deterministic, 是验收必备)
  </action>

  <verify>
  <automated>
  # 1. go generate exit 0 (regen 成功)
  go generate ./internal/errors/...

  # 2. Sentinel Table 行数 = 42 (与 phase 20 已落地的 42 sentinels 一致; 上下浮动 ±0 即可)
  grep -cE '^\| Err[A-Z][a-zA-Z]* \| Sentinel \|' docs/errors.md

  # 3. BusinessError Table 行数 = 10 (与 phase 20 已落地的 10 codes 一致)
  grep -cE '^\| BusinessError\(code=' docs/errors.md

  # 4. Audit footer 存在且包含数字 (不预设特定值, 只确保格式正确)
  grep -E '^\> Current .err.Error.. / inline classify branches: \*\*[0-9]+\*\* \(target: 0\)$' docs/errors.md

  # 5. 二次 go generate 后 git diff --quiet exit 0 (generator 是确定性的)
  go generate ./internal/errors/... && git diff --quiet docs/errors.md && echo SYNC_OK

  # 6. 提交仅含 docs/errors.md (单文件)
  git show --stat HEAD --format= | grep -cE '^ docs/errors\.md$' && ! git show --stat HEAD --format= | grep -vE '^(\s*docs/errors\.md|\s*$|commit |Author:|Date:)' | grep -E '\.(go|md|yml|yaml|json)$' | grep -v 'docs/errors.md' > /dev/null

  # 7. commit subject 包含 regenerate errors.md
  git log --oneline -1 | grep -q 'regenerate errors.md'

  # 8. 仓库根 go build 全绿 (regen 不动业务代码, 但确认无副作用)
  go build ./...
  </automated>
  </verify>

  <acceptance_criteria>
  - `docs/errors.md` 在 git working tree 中已被 regen (header 完整 + Sentinel Table 42 行 + BusinessError Table 10 行 + Ad-hoc footer 数字更新)
  - Sentinel Table 行数 (按 `^\| Err[A-Z][a-zA-Z]* \| Sentinel \|` 计数) = 42
  - BusinessError Table 行数 (按 `^\| BusinessError\(code=` 计数) = 10
  - Ad-hoc Error Audit footer 行存在且格式正确: `> Current err.Error() / inline classify branches: **<N>** (target: 0)`
  - 二次 `go generate ./internal/errors/...` 后 `git diff --quiet docs/errors.md` exit 0 (输出 SYNC_OK)
  - 单个 commit on main, 仅含 `docs/errors.md` 一个文件
  - commit subject 包含 `regenerate errors.md` 字样
  - `go build ./...` exit 0 (regen 不引入副作用)
  - 22-01-SUMMARY.md 存在, 记录 commit hash + before/after ad-hoc 计数 + 行数变化
  </acceptance_criteria>

  <done>
  docs/errors.md 由 `go generate ./internal/errors/...` 重新生成落地 main; Sentinel Table 42 行 + BusinessError Table 10 行 + audit footer 数字更新; CI sync-check 命令序列本地验证通过 (SYNC_OK); 单个 commit 仅含 docs/errors.md; v1.1-MILESTONE-AUDIT tech_debt "audit footer 31 sites" cleanup signal 关闭 (新数字反映当前 handler 收敛状态)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → `go generate` | Local execution; generator reads source files and writes docs/errors.md (no network, no auth) |
| CI (GitHub Actions) → `git diff --quiet docs/errors.md` | CI verifies sync; if drift, build fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-DRIFT | Tampering | docs/errors.md drifts from internal/errors source (new sentinel added but doc not regenerated) | mitigate | CI sync-check at .github/workflows/ci.yml lines 44-51 enforces post-build; this plan closes the current drift by regen |
| T-22-01-OVERWRITE | Tampering | `go generate` overwrites a manually edited docs/errors.md (header notes say "Do not edit by hand") | accept | Header note explicitly forbids manual edits; if a contributor edited by hand, regen wipes their edit (intended behavior per auto-gen contract); no business impact |
| T-22-01-CROSSFILE | Tampering | Regen accidentally modifies other files | mitigate | Generator writes only to `-output` path (docs/errors.md); `git show --stat HEAD` verification confirms single-file commit |
| T-22-01-NONDETERMINISTIC | Repudiation | Generator output varies between runs, causing perpetual CI failures | mitigate | `grepCount` walks files alphabetically + `sort.Slice` orders sentinels/codes by name; run twice locally to confirm `git diff --quiet` exit 0 (deterministic) |

</threat_model>

<verification>
- docs/errors.md regenerated by `go generate ./internal/errors/...` (exit 0)
- Sentinel Table 42 rows + BusinessError Table 10 rows + audit footer number updated
- `go generate` twice + `git diff --quiet docs/errors.md` → SYNC_OK
- `go build ./...` exit 0
- Single commit on main containing only docs/errors.md
- 22-01-SUMMARY.md committed with hash + counts
</verification>

<success_criteria>
- docs/errors.md reflects current internal/errors source state (Sentinel Table 42 + BusinessError Table 10 + ad-hoc count matches current handler grep)
- v1.1-MILESTONE-AUDIT §Executive Summary tech_debt "31 sites → audit footer drift" cleanup signal closed by fresh regen
- CI sync-check command sequence (`go generate && git diff --quiet docs/errors.md`) passes locally, confirming parity with .github/workflows/ci.yml lines 44-51
- No source code modified; no tests modified; no other .planning/ files modified
</success_criteria>

<output>
Create `.planning/phases/22-address-v1-1-audit-tech-debt-regenerate-errors-md-backfill-v/22-01-SUMMARY.md` recording commit hash, before/after ad-hoc count, Sentinel/BusinessError table row counts, and the regen command for re-verification.
</output>