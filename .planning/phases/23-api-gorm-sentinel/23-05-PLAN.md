---
phase: 23-api-gorm-sentinel
plan: 05
type: execute
wave: 2
depends_on:
  - "23-04"
files_modified:
  - config.yaml
  - bin/config.yaml
  - internal/config/smart_end_yaml_test.go
autonomous: true
requirements:
  - CFG-02

must_haves:
  truths:
    - "Root config.yaml contains a smart_end section with exactly 14 keys matching SmartEndConfig field tags"
    - "bin/config.yaml mirrors the same smart_end section exactly (key set, indentation, values identical to root)"
    - "Loading either YAML populates cfg.SmartEnd with the 14 mapped fields in addition to defaults"
  artifacts:
    - path: config.yaml
      provides: "Operator-facing smart_end section with 14 keys"
      contains: "smart_end:"
    - path: bin/config.yaml
      provides: "Release-template smart_end section with 14 keys (mirrors root)"
      contains: "smart_end:"
    - path: internal/config/smart_end_yaml_test.go
      provides: "Exact-14-key sync test between root and release YAML"
      contains: "TestSmartEndYAML"
  key_links:
    - from: config.yaml
      to: internal/config/smart_end.go
      via: "Viper unmarshal resolves smart_end section into SmartEndConfig struct"
      pattern: "smart_end"
    - from: bin/config.yaml
      to: config.yaml
      via: "Both files must contain the same 14 keys under smart_end: to prevent deployment drift"
      pattern: "smart_end:"
---

<objective>
Add the smart_end: operator configuration section to both the root config.yaml and the bin/config.yaml (release template) so production deployment works without manual sync, and add an automated sync test to prevent future drift.

Purpose: CFG-02 - surface the 14 smart-end knobs to operators with the same values across both deployment templates, and prevent the documented drift problem (root-only updates creating divergent release config).
Output: smart_end: section in config.yaml with 14 keys + same section in bin/config.yaml + sync test enforcing key-set parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-api-gorm-sentinel/23-RESEARCH.md
@.planning/phases/23-api-gorm-sentinel/23-VALIDATION.md

Interfaces the executor must understand (from codebase read):
- config.yaml at D:/CODE/ClaudeCode/record_V2/config.yaml uses standard YAML, with a top-level huawei: section at line 65. The smart_end: section is NEW and is NOT a sub-section under huawei; it is a top-level sibling of huawei, ffmpeg, etc.
- bin/config.yaml at D:/CODE/ClaudeCode/record_V2/bin/config.yaml is the packaged runtime template; it mirrors config.yaml top-level structure. Both must have identical smart_end: sections.
- RESEARCH.md Common Pitfall section Anti-Pattern: "Updating only root config.yaml: packaged bin/config.yaml would drift." The sync test in this plan guards against this.
- The 14 keys and their default values are defined in REQUIREMENTS.md:58 and reproduced in Plan 23-04 (smart_end.go). Keys must be EXACTLY: enabled / silence_db / silence_duration_s / file_stall_s / file_min_growth_bps / huawei_enabled / huawei_poll_interval_s / huawei_persist_s / huawei_failure_threshold / check_interval_s / extend_step_min / max_extend_count / stat_failure_threshold / degrade_on_silence_loss.
- Default values to write: enabled=true, silence_db=-30, silence_duration_s=30, file_stall_s=120, file_min_growth_bps=1024, huawei_enabled=true, huawei_poll_interval_s=30, huawei_persist_s=30, huawei_failure_threshold=3, check_interval_s=5, extend_step_min=30, max_extend_count=4, stat_failure_threshold=3, degrade_on_silence_loss=true.
- Plan 23-04 already creates internal/config/smart_end_test.go with go-side tests. This plan creates a SEPARATE file internal/config/smart_end_yaml_test.go for YAML-specific assertions so there is no test-file conflict.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add smart_end: section to config.yaml + bin/config.yaml + sync test</name>
  <files>config.yaml, bin/config.yaml, internal/config/smart_end_yaml_test.go (new)</files>
  <read_first>
    - D:/CODE/ClaudeCode/record_V2/config.yaml (full file; location of new smart_end: section is between the storage: section ending at line 63 and the huawei: section starting at line 65 - add as new top-level section AFTER huawei, around line 76)
    - D:/CODE/ClaudeCode/record_V2/bin/config.yaml (full file; mirror root file's smart_end: section at the same top-level position)
    - internal/config/smart_end.go (already created by Plan 23-04; reference the 14 field tags exactly)
    - internal/config/smart_end_test.go (already created by Plan 23-04; do NOT modify - this plan creates a separate file)
    - .planning/phases/23-api-gorm-sentinel/23-RESEARCH.md (CFG-02 section, Anti-Pattern "Updating only root config.yaml", and Files to Modify table)
    - .planning/phases/23-api-gorm-sentinel/23-VALIDATION.md (Task 23-05-01 row)
  </read_first>
  <behavior>
    - Test 1 (TestSmartEndYAML_Exactly14Keys): parse both root and release YAML files; extract the smart_end section; assert both contain exactly 14 keys; assert the key set matches the expected list (enabled, silence_db, silence_duration_s, file_stall_s, file_min_growth_bps, huawei_enabled, huawei_poll_interval_s, huawei_persist_s, huawei_failure_threshold, check_interval_s, extend_step_min, max_extend_count, stat_failure_threshold, degrade_on_silence_loss)
    - Test 2 (TestSmartEndYAML_RootBinSync): parse both root and release YAML files; the key set under smart_end: must be identical (sorted-set equality); values for every shared key must be identical too
    - Test 3 (TestSmartEndYAML_ExpectedDefaults): parse root config.yaml smart_end section; assert the 14 values match the documented defaults (enabled=true, silence_db=-30, silence_duration_s=30, file_stall_s=120, file_min_growth_bps=1024, huawei_enabled=true, huawei_poll_interval_s=30, huawei_persist_s=30, huawei_failure_threshold=3, check_interval_s=5, extend_step_min=30, max_extend_count=4, stat_failure_threshold=3, degrade_on_silence_loss=true)
    - Test 4 (TestSmartEndYAML_ViperLoadsCleanly): build a viper instance with a temp copy of root config.yaml, unmarshal into Config struct; cfg.SmartEnd.* reflects the YAML values (which equal defaults)
  </behavior>
  <action>
    Edit config.yaml (D:/CODE/ClaudeCode/record_V2/config.yaml):

    1. Add a new top-level section smart_end: AFTER the existing huawei: section. Use 2-space indentation matching the rest of the file. Include all 14 keys with default values. Place it BEFORE ffmpeg: (line 77) so it groups logically with recording/recorder-adjacent config:
       smart_end:
         enabled: true
         silence_db: -30
         silence_duration_s: 30
         file_stall_s: 120
         file_min_growth_bps: 1024
         huawei_enabled: true
         huawei_poll_interval_s: 30
         huawei_persist_s: 30
         huawei_failure_threshold: 3
         check_interval_s: 5
         extend_step_min: 30
         max_extend_count: 4
         stat_failure_threshold: 3
         degrade_on_silence_loss: true

    2. Add a one-line comment above the smart_end: section explaining its purpose and pointing to SmartEndConfig (so future maintainers know where the typed struct lives):
       # Smart-end recording (Phase 23 CFG-02). Bounded by SmartEndConfig in internal/config/smart_end.go.

    Edit bin/config.yaml (D:/CODE/ClaudeCode/record_V2/bin/config.yaml):

    3. Add the IDENTICAL smart_end: section at the same top-level position (search for the analogous huawei: section in the release file, then insert smart_end: AFTER it, BEFORE ffmpeg:). The 14 keys and their values must be byte-for-byte identical to root config.yaml (same key order, same indentation, same values). Use the same comment line.

    Create internal/config/smart_end_yaml_test.go (NEW file, package config):

    4. Test setup: add a small helper that reads config.yaml and bin/config.yaml from the repo root (use paths relative to working directory via filepath.Join + runtime.Caller(0) OR a fixed path "../../config.yaml" since this test runs from internal/config). Parse both via gopkg.in/yaml.v3 (already a transitive Viper dependency; import only if not already in the go.mod direct set). For tests 1-3, parse into a generic map[string]interface{} and drill into the smart_end section. For test 4, use the Viper pattern from config_test.go.

    5. TestSmartEndYAML_Exactly14Keys: parse both files, extract smart_end section map, assert len(m) == 14, assert sorted keys equal the expected 14-name slice. Run as two subtests (root and release) via t.Run.

    6. TestSmartEndYAML_RootBinSync: parse both files, assert smart_end map keys are equal (sorted slice comparison) and values are equal (sorted-key iteration). This is the regression guard for the drift anti-pattern.

    7. TestSmartEndYAML_ExpectedDefaults: parse root config.yaml smart_end; build map[name]expectedValue; for each key assert the actual value matches the expected default. Use float64 comparison for ints because YAML numbers are float64 in generic decoding (cast to int via int(v.(float64)) OR use yaml.v3 with KnownFields true and decode into a typed map).

    8. TestSmartEndYAML_ViperLoadsCleanly: v := viper.New(); v.SetConfigType(yaml); v.ReadConfig(strings.NewReader(root YAML contents)); v.Unmarshal(&cfg); cfg.SmartEnd enabled equals true; cfg.SmartEnd.SilenceDurationS equals 30; cfg.SmartEnd.MaxExtendCount equals 4. This proves Viper + mapstructure triple tags resolve keys correctly.

    Do NOT touch internal/config/smart_end_test.go (created by Plan 23-04). Do NOT touch cmd/server/app.go. Do NOT modify SmartEndConfig definition (Plan 23-04 owns it).
  </action>
  <verify>
    <automated>go test ./internal/config -run TestSmartEndYAML -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - File config.yaml contains a top-level smart_end: section with exactly 14 keys
    - File bin/config.yaml contains a top-level smart_end: section with exactly 14 keys
    - The 14 keys in both files match the exact list: enabled, silence_db, silence_duration_s, file_stall_s, file_min_growth_bps, huawei_enabled, huawei_poll_interval_s, huawei_persist_s, huawei_failure_threshold, check_interval_s, extend_step_min, max_extend_count, stat_failure_threshold, degrade_on_silence_loss
    - The 14 default values in both files match the documented values (true / -30 / 30 / 120 / 1024 / true / 30 / 30 / 3 / 5 / 30 / 4 / 3 / true)
    - Root and release YAML smart_end sections are byte-for-byte identical (same key order, same values)
    - New file internal/config/smart_end_yaml_test.go exists and is valid Go (no syntax errors)
    - The test file contains 4 test functions (TestSmartEndYAML_Exactly14Keys, TestSmartEndYAML_RootBinSync, TestSmartEndYAML_ExpectedDefaults, TestSmartEndYAML_ViperLoadsCleanly)
    - All 4 new tests pass
    - Existing smart_end_test.go (from Plan 23-04) tests still pass (no regression)
    - Existing config_test.go tests still pass (no regression)
  </acceptance_criteria>
  <done>Both deployment templates expose the 14 smart_end keys with their defaults; sync test prevents future drift; viper unmarshal resolves to SmartEndConfig fields.</done>
</task>

</tasks>

<verification>
After task commit:
- go test ./internal/config -run TestSmartEndYAML -count=1 -v - 4 tests pass
- go test ./internal/config -count=1 -race - full package pass (Plan 23-04 tests still green)
- go vet ./internal/config exits 0
- diff -u config.yaml bin/config.yaml restricted to the smart_end section must be empty (verified by reading both side-by-side)

Validation chain:
1. Definition synced: smart_end: section present in BOTH config.yaml AND bin/config.yaml
2. Key-set parity: 14 keys identical between root and release
3. Default values match REQUIREMENTS.md
4. Viper resolves: typed unmarshal into SmartEndConfig works
5. No test file conflict: smart_end_yaml_test.go is separate from smart_end_test.go (Plan 23-04)
</verification>

<success_criteria>
[Measurable completion of Plan 23-05]
1. config.yaml has a top-level smart_end: section with 14 keys and the documented default values
2. bin/config.yaml mirrors config.yaml smart_end: section byte-for-byte (key set, order, values)
3. internal/config/smart_end_yaml_test.go has 4 passing test functions: Exactly14Keys, RootBinSync, ExpectedDefaults, ViperLoadsCleanly
4. Sync test catches drift: any future change to one file without the other fails the test
5. Viper unmarshal resolves all 14 keys into SmartEndConfig fields without errors
6. No regression on Plan 23-04 tests (smart_end_test.go still passes)
7. No modification to SmartEndConfig definition (owned by Plan 23-04)
</success_criteria>

<output>
Create .planning/phases/23-api-gorm-sentinel/23-05-SUMMARY.md when done
</output>
