---
phase: 23-api-gorm-sentinel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/video_recording_task.go
  - internal/models/video_recording_task_test.go
autonomous: true
requirements:
  - AUDIT-01

must_haves:
  truths:
    - "VideoRecordingTask model has 5 new GORM-tagged fields: ExtensionCount, LastExtensionReason, EndedEarly, EndedEarlyReason, EndedByHuaWeAPI"
    - "SQLite AutoMigrate creates columns extension_count, last_extension_reason, ended_early, ended_early_reason, ended_by_huawei_api (or stable equivalent) with correct defaults and NOT NULL constraints"
    - "GORM can read a freshly-migrated row, write a row with all five fields populated, then re-read the same row and recover the values exactly"
  artifacts:
    - path: internal/models/video_recording_task.go
      provides: "Five smart-end persistence fields with GORM tags"
      contains: "ExtensionCount"
    - path: internal/models/video_recording_task_test.go
      provides: "Schema + default + read/write round-trip integration test using SQLite :memory:"
      contains: "TestVideoRecordingTaskSmartEndFields"
  key_links:
    - from: internal/models/video_recording_task.go
      to: video_recording_tasks table
      via: "GORM naming strategy maps PascalCase Go fields to snake_case columns"
      pattern: "ended_by_huawei_api"
    - from: internal/models/video_recording_task.go
      to: cmd/server/app.go AutoMigrate list
      via: "VideoRecordingTask{} already registered; adding model fields automatically registers new columns"
      pattern: "VideoRecordingTask{}"
---

<objective>
Extend VideoRecordingTask GORM model with the 5 smart-end audit fields (Phase 23 AUDIT-01) so the schema is ready for Phase 25 service-layer writes.

Purpose: AUDIT-01 — migrate video_recording_tasks table to support smart-end audit fields via the existing AutoMigrate pipeline (no custom migration registry entry).
Output: Five new Pascal-case fields with GORM tags, an explicit column tag for the acronym HuaWeAPI to lock the SQL column name, and an SQLite :memory: integration test that exercises schema creation, defaults, and read/write round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-api-gorm-sentinel/23-RESEARCH.md
@.planning/phases/23-api-gorm-sentinel/23-VALIDATION.md

Interfaces the executor must understand (from codebase read):
- VideoRecordingTask model file uses PascalCase Go field names with explicit GORM tags: numeric defaults `gorm:"default:0"`, long text reasons use `gorm:"type:text"`, and snake_case JSON tags follow field name.
- `&models.VideoRecordingTask{}` is already registered in `cmd/server/app.go` startup AutoMigrate list (lines 317-342 per RESEARCH.md). Adding new struct fields causes AutoMigrate to ALTER TABLE on next boot — no app.go edits required for Phase 23.
- The model file currently has 33 fields ending at `ConversionRetryCount int gorm:\"default:0\"` and `Creator *User gorm:\"foreignKey:CreatedBy\"`. Insert the 5 new fields AFTER `ConversionRetryCount` and BEFORE `CreatedBy` so related conversion/audit fields stay grouped.
- Go field `EndedByHuaWeAPI` has unusual casing. GORM naming strategy may produce different snake_case depending on version (`ended_by_hua_we_api` vs `ended_by_huawei_api`). RESEARCH.md recommendation [ASSUMED]: use explicit `gorm:\"column:ended_by_huawei_api\"` tag to LOCK the column name. The integration test MUST verify the actual column name via `db.Migrator().HasColumn(&models.VideoRecordingTask{}, \"ended_by_huawei_api\")`.
- Test convention: existing `internal/services/video_recording_task_service_test.go:18-29` shows the SQLite :memory: + AutoMigrate pattern to reuse.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 5 GORM fields + SQLite migration round-trip test
</name>
  <files>internal/models/video_recording_task.go, internal/models/video_recording_task_test.go</files>
  <read_first>
    - internal/models/video_recording_task.go (lines 28-42 for the existing Conversion fields block; lines 134-136 for TableName)
    - internal/services/video_recording_task_service_test.go (lines 18-29 for newTestDB SQLite :memory: pattern)
    - cmd/server/app.go (lines 317-342 for AutoMigrate list — confirm VideoRecordingTask is already there)
    - .planning/phases/23-api-gorm-sentinel/23-RESEARCH.md (AUDIT-01 section for column name recommendation)
  </read_first>
  <behavior>
    - Test 1 (TestVideoRecordingTaskSmartEndFields_SchemaMigration): Fresh SQLite :memory: db with db.AutoMigrate(&models.VideoRecordingTask{}); db.Migrator().HasColumn(&models.VideoRecordingTask{}, "extension_count"), "last_extension_reason", "ended_early", "ended_early_reason", "ended_by_huawei_api" all return true (locks column names explicitly)
    - Test 2 (TestVideoRecordingTaskSmartEndFields_Defaults): Create a row with only required fields (Name, StartTime, EndTime, ConferenceNumber, Status, CreatedBy); read it back; all 5 new fields hold their column defaults (ExtensionCount=0, LastExtensionReason="", EndedEarly=false, EndedEarlyReason="", EndedByHuaWeAPI=false) — NOT NULL does not block insert with column defaults
    - Test 3 (TestVideoRecordingTaskSmartEndFields_RoundTrip): Create a row with the 5 fields set to ExtensionCount=3, LastExtensionReason="huawei_persist", EndedEarly=true, EndedEarlyReason="both_silence_and_stall", EndedByHuaWeAPI=true; then db.First(&got, task.ID); all five match exactly
  </behavior>
  <action>
    In internal/models/video_recording_task.go:

    1. Insert 5 new fields BETWEEN ConversionRetryCount (line 37) AND CreatedBy (line 38) in the VideoRecordingTask struct. Use these exact GORM tags and JSON tags (preserve the existing convention):
       - ExtensionCount       int    `gorm:"not null;default:0" json:"extension_count"`
       - LastExtensionReason string `gorm:"type:text;not null;default:''" json:"last_extension_reason,omitempty"` — NOTE: the SQLite default for text needs an empty string literal; if GORM complains use `type:text"` without not null constraint (existing LongDescription-like patterns work this way). Inspect existing similar field `Description string \`gorm:"type:text"\`` at line 14 to confirm.
       - EndedEarly           bool   `gorm:"not null;default:false" json:"ended_early"`
       - EndedEarlyReason     string `gorm:"type:text" json:"ended_early_reason,omitempty"` (reason is optional, follows the omitempty pattern)
       - EndedByHuaWeAPI      bool   `gorm:"not null;default:false;column:ended_by_huawei_api" json:"ended_by_huawei_api"` — explicit column tag locks the SQL name to `ended_by_huawei_api` (locks against GORM naming strategy ambiguity around `HuaWe` acronym)

    2. Above the struct (line 9 area where ConversionStatus is declared), add a phase-tagged comment block:
       `// Smart-end audit fields (Phase 23 AUDIT-01). Written by service layer in Phase 25. Columns are added via AutoMigrate on next boot.`

    Create internal/models/video_recording_task_test.go (NEW file, `package models`):

    3. Import testing + testify/assert + gorm.io/driver/sqlite + gorm.io/gorm + gormlogger + time.

    4. Add `newTestDB(t *testing.T) *gorm.DB` helper that opens :memory: sqlite and runs AutoMigrate(&models.VideoRecordingTask{}) only. Pattern matches internal/services/video_recording_task_service_test.go:20-30 but scoped to just the one model.

    5. Add three test functions (subtests under one top-level TestVideoRecordingTaskSmartEndFields via t.Run, OR three flat functions — pick whichever matches the existing models/ test style; if no existing style precedent in models package, default to flat functions named TestVideoRecordingTaskSmartEndFields_SchemaMigration / _Defaults / _RoundTrip):
       - _SchemaMigration: db.AutoMigrate, then assert db.Migrator().HasColumn for each of the 5 expected column names
       - _Defaults: create minimal task (only required fields; CreatedBy=1, ConferenceNumber="TEST", StartTime/EndTime now+/-1h, Status=VideoStatusPending, Name="x"), Save, re-read, assert five new fields all default values
       - _RoundTrip: create task with all five fields populated to non-default values, Save, re-First into a different struct, assert exact equality on each of the five fields

    6. DO NOT modify cmd/server/app.go AutoMigrate list — VideoRecordingTask is already there. DO NOT add index — Phase 23 access path is by task.ID.
  </action>
  <verify>
    <automated>go test ./internal/models -run "TestVideoRecordingTaskSmartEndFields" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `internal/models/video_recording_task.go` contains 5 new struct fields named exactly `ExtensionCount`, `LastExtensionReason`, `EndedEarly`, `EndedEarlyReason`, `EndedByHuaWeAPI` (case matters for the acronym field)
    - The `EndedByHuaWeAPI` field has a GORM tag containing literal text `column:ended_by_huawei_api`
    - LastExtensionReason OR EndedEarlyReason has a GORM tag containing literal text `type:text` (verify one of them)
    - New file `internal/models/video_recording_task_test.go` exists and is valid Go (no syntax errors)
    - The new test file contains function names matching `*SchemaMigration*`, `*Defaults*`, `*RoundTrip*` for the three required behaviors
    - The 3 tests pass; the SQLite integration test MUST NOT use PostgreSQL or require an external DB
    - Existing test `internal/services/video_recording_task_service_test.go` tests still pass (no regression from new file or model changes)
  </acceptance_criteria>
  <done>video_recording_tasks schema gains 5 smart-end columns via AutoMigrate (no app.go edits), locked column names verified via HasColumn, defaults and round-trip work.</done>
</task>

</tasks>

<verification>
Run quick package test after task commit:
- `go test ./internal/models -count=1 -race -v` (includes new tests + existing tests)

Run vet:
- `go vet ./internal/models` exits 0

Three behaviors validated:
1. Schema migration produces the 5 expected columns (lints true under db.Migrator().HasColumn for snake_case names)
2. Insert with only required fields produces default values on all 5 new columns
3. Insert with explicit values, then re-read, returns the exact same values
</verification>

<success_criteria>
[Measurable completion of Plan 23-02]
1. VideoRecordingTask struct has 5 new GORM-tagged fields added between ConversionRetryCount and CreatedBy
2. The new TestVideoRecordingTaskSmartEndFields_* tests pass in SQLite :memory:
3. db.Migrator().HasColumn returns true for column names: extension_count, last_extension_reason, ended_early, ended_early_reason, ended_by_huawei_api (locked via explicit column tag on the acronym)
4. A round-trip insert+read preserves all 5 new field values bit-for-bit
5. NO modifications to cmd/server/app.go (VideoRecordingTask is already in AutoMigrate list per RESEARCH.md verification)
6. NO new index added (Phase 23 access is by task.ID per RESEARCH.md recommendation)
</success_criteria>

<output>
Create `.planning/phases/23-api-gorm-sentinel/23-02-SUMMARY.md` when done
</output>
