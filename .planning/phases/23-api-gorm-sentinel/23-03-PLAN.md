---
phase: 23-api-gorm-sentinel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/errors/errors.go
  - internal/errors/mapping.go
  - internal/errors/mapping_test.go
  - docs/errors.md
autonomous: true
requirements:
  - AUDIT-05

must_haves:
  truths:
    - "Three new sentinel errors exist: ErrRecordingSmartExtend, ErrRecordingSmartEarlyEnd, ErrRecordingHuaWeiStateFetchFailed"
    - "All three are recognized by IsKnownError/FirstKnownSentinelName (added to knownSentinels list)"
    - "All three map to HTTP 500 with respCodeInternalError via MapToHTTPStatus"
    - "docs/errors.md is regenerated by `go generate ./internal/errors/...` and `git diff --exit-code -- docs/errors.md` exits 0"
  artifacts:
    - path: internal/errors/errors.go
      provides: "Three sentinel definitions in a phase-tagged comment block"
      contains: "ErrRecordingHuaWeiStateFetchFailed"
    - path: internal/errors/mapping.go
      provides: "knownSentinels entries + MapToHTTPStatus cases for the three new sentinels"
      contains: "ErrRecordingHuaWeiStateFetchFailed"
    - path: internal/errors/mapping_test.go
      provides: "Extended TestMapToHTTPStatus_Sentinels and TestFirstKnownSentinelName cases"
      contains: "ErrRecordingHuaWeiStateFetchFailed"
    - path: docs/errors.md
      provides: "Auto-regenerated sentinel table with 3 new rows after go generate"
      contains: "ErrRecordingHuaWeiStateFetchFailed"
  key_links:
    - from: internal/errors/errors.go
      to: docs/errors.md
      via: "go:generate directive at top of file invokes cmd/error-doc-gen"
      pattern: "go:generate go run"
    - from: internal/errors/mapping.go knownSentinels
      to: internal/errors/errors.go sentinel vars
      via: "FirstKnownSentinelName iterates the list to find errors.Is matches"
      pattern: "knownSentinels"
---

<objective>
Add the three Phase 23 sentinels to internal/errors and regenerate docs/errors.md so the AUDIT-05 vocabulary is recognized by IsKnownError, classified as HTTP 500, and exposed in generated documentation — keeping the CI sync-check gate green.

Purpose: AUDIT-05 — wire the three recording-sentinel errors through source-of-truth definitions, HTTP classifier, recognition list, and the generator pipeline.
Output: 3 sentinel definitions + MapToHTTPStatus switch case entries + knownSentinels list entries + mapping_test.go coverage + auto-regenerated docs/errors.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-api-gorm-sentinel/23-RESEARCH.md
@.planning/phases/23-api-gorm-sentinel/23-VALIDATION.md

Interfaces the executor must understand (from codebase read):
- `internal/errors/errors.go:1-9` contains the package doc comment with `//go:generate go run ../../cmd/error-doc-gen ...` directive. Adding new sentinels requires the generator to be re-run to refresh docs/errors.md.
- The sentinel var block (lines 11-98) groups sentinels by phase prefix comment. Pattern: phase-tagged comment block `// Phase XX <name> sentinels` then 1+ `ErrXxx = errors.New(\"...\")` lines. Add a new `// Smart-end recording sentinels (Phase 23 AUDIT-05)` group at the end of the var block before the closing `)`.
- `internal/errors/mapping.go:44-95` switch statement groups cases by HTTP status. A free default (lines 94-95) returns 500 for any unmapped error. Strategy per Pitfall 5 in RESEARCH.md: add EXPLICIT cases to the existing 500 group `case errors.Is(err, ErrFFmpegFailed), errors.Is(err, ErrTranscriptionFailed), errors.Is(err, ErrSplitFailed):` so intent is documented; do NOT rely solely on the default.
- `internal/errors/mapping.go:157-203` knownSentinels list: append the 3 new entries to the END of the list, preserving insertion order. Last-wins-style precedence is acceptable here since the three new ones don't overlap existing sentinels.
- `internal/errors/mapping_test.go:13-54` TestMapToHTTPStatus_Sentinels is table-driven; the new three sentinels add 3 rows (`name/err/wantStatus/wantResp`). A TestFirstKnownSentinelName function may exist further down — search for it; if present extend its cases too.
- `cmd/error-doc-gen/main.go` is the generator. Do NOT edit it or docs/errors.md manually — always via `go generate ./internal/errors/...`.
- After changes, run `go generate ./internal/errors/...` and then `git diff --exit-code -- docs/errors.md` must exit 0 (CI sync-check gate at .github/workflows/ci.yml:53-60).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 sentinels + mapping cases + recognition + docs regeneration</name>
  <files>internal/errors/errors.go, internal/errors/mapping.go, internal/errors/mapping_test.go, docs/errors.md</files>
  <read_first>
    - internal/errors/errors.go (full file: 168 lines, especially lines 11-98 var block, lines 1-3 go:generate directive)
    - internal/errors/mapping.go (full file: 228 lines, especially lines 44-95 switch, lines 128-153 IsKnownError/FirstKnownSentinelName, lines 157-203 knownSentinels list)
    - internal/errors/mapping_test.go (full file: ~250 lines; locate TestMapToHTTPStatus_Sentinels at line 13-54 and any TestFirstKnownSentinelName or TestIsKnownError further down)
    - cmd/error-doc-gen/main.go (lines 140-219 for scan logic, lines 193-203 for default 500 handling)
    - .planning/phases/23-api-gorm-sentinel/23-RESEARCH.md (AUDIT-05 section, Pitfall 5)
  </read_first>
  <behavior>
    - Test 1 (TestMapToHTTPStatus_Sentinels extension): three new rows added; each sentinel passes through MapToHTTPStatus returning (http.StatusInternalServerError, respCodeInternalError, message string) matching existing FFmpeg/Transcription/Split rows
    - Test 2 (TestIsKnownError or TestFirstKnownSentinelName extension): each sentinel returns true from IsKnownError and returns its Go identifier name string from FirstKnownSentinelName
    - Generated docs (CI sync-check): after go generate ./internal/errors/..., docs/errors.md MUST contain the three new sentinel names in its table; git diff --exit-code -- docs/errors.md must exit 0 (idempotency); first generate introduces the rows, second generate produces empty diff
  </behavior>
  <action>
    In internal/errors/errors.go:

    1. Append a new sentinel var block BEFORE the closing parenthesis of the var block (after line 97 ErrTranscriptionUnavailable, before line 98 close-paren). Match existing phase-tagged comment style with three errors.New declarations: ErrRecordingSmartExtend, ErrRecordingSmartEarlyEnd, ErrRecordingHuaWeiStateFetchFailed. The exact error strings are: "recording smart extend failed", "recording smart early end failed", "Huawei conference state fetch failed". Include a comment line above the group: "// Smart-end recording sentinels (Phase 23 AUDIT-05): ..."

    2. Do NOT add new entries to the Code constants block at the bottom of errors.go.

    In internal/errors/mapping.go:

    3. Extend the existing 500-case switch entry at lines 90-93. Append three more errors.Is(err, ...) predicates to the existing case so all three new sentinels fall into the http.StatusInternalServerError + respCodeInternalError + "内部处理失败" return path. Do NOT add a new switch case block — extend the existing one.

    4. Append three entries to the END of the knownSentinels list (after the existing ErrForeignKeyConstraint entry at line 202). Each entry is a struct literal pair (name string, err error) with the sentinel's Go identifier name (e.g. "ErrRecordingHuaWeiStateFetchFailed") as the name field and the sentinel variable as the err field.

    In internal/errors/mapping_test.go:

    5. In TestMapToHTTPStatus_Sentinels (lines 13-54), add three new rows to the cases slice. Insert them after the existing ErrSplitFailed row (so they live in the 500-status group). Each row has 4 fields: name (string), err (the sentinel), wantStatus (http.StatusInternalServerError), wantResp (respCodeInternalError).

    6. Locate any TestFirstKnownSentinelName or TestIsKnownError function in mapping_test.go. If present, add three corresponding rows asserting each new sentinel returns its stable name string. If absent, add a new TestFirstKnownSentinelName_SmartEndRecording function with 3 subtests calling FirstKnownSentinelName on each sentinel and asserting the returned name string equals the Go identifier (e.g. "ErrRecordingHuaWeiStateFetchFailed").

    Regenerate docs:

    7. After editing the source files, run go generate ./internal/errors/... to regenerate docs/errors.md. Run it a second time and confirm git diff --exit-code -- docs/errors.md is empty (idempotency).

    8. Run go test ./internal/errors ./cmd/error-doc-gen -count=1 to confirm full package pass.

    Do NOT hand-edit docs/errors.md. Do NOT touch cmd/error-doc-gen/main.go.
  </action>
  <verify>
    <automated>go generate ./internal/errors/... && go test ./internal/errors ./cmd/error-doc-gen -count=1 -v && git diff --exit-code -- docs/errors.md</automated>
  </verify>
  <acceptance_criteria>
    - internal/errors/errors.go contains three new var declarations: ErrRecordingSmartExtend, ErrRecordingSmartEarlyEnd, ErrRecordingHuaWeiStateFetchFailed (each with errors.New call)
    - internal/errors/mapping.go switch case for 500-group contains all three new sentinels via errors.Is(...)
    - internal/errors/mapping.go knownSentinels list contains three new entries with literal names ErrRecordingSmartExtend, ErrRecordingSmartEarlyEnd, ErrRecordingHuaWeiStateFetchFailed
    - internal/errors/mapping_test.go TestMapToHTTPStatus_Sentinels has rows covering all three new sentinels
    - mapping_test.go has at least one assertion that each of the three sentinels returns true from IsKnownError (or FirstKnownSentinelName returns the Go identifier)
    - docs/errors.md is regenerated and contains all three new sentinel names in the table (verified via grep -c)
    - Running go generate ./internal/errors/... twice produces an empty git diff on docs/errors.md (idempotency)
    - All go test ./internal/errors ./cmd/error-doc-gen -count=1 tests pass
  </acceptance_criteria>
  <done>Three sentinels defined, recognized, classified as HTTP 500, covered by tests, and docs/errors.md regenerated to match.</done>
</task>

</tasks>

<verification>
After task commit:
- go generate ./internal/errors/... regenerates docs/errors.md deterministically
- go test ./internal/errors ./cmd/error-doc-gen -count=1 -v passes all sentinel + mapping + generator tests
- git diff --exit-code -- docs/errors.md exits 0 (CI sync-check passes locally)
- go vet ./internal/errors ./cmd/error-doc-gen exits 0
</verification>

<success_criteria>
[Measurable completion of Plan 23-03]
1. internal/errors/errors.go exports three new sentinels
2. All three added to knownSentinels + 500-group switch case in mapping.go
3. mapping_test.go covers all three via MapToHTTPStatus and via IsKnownError/FirstKnownSentinelName
4. docs/errors.md regenerated and contains the three new rows; second go generate produces empty diff
5. CI sync-check git diff --exit-code -- docs/errors.md exits 0 after regeneration
6. No changes to cmd/error-doc-gen/main.go
</success_criteria>

<output>
Create .planning/phases/23-api-gorm-sentinel/23-03-SUMMARY.md when done
</output>
