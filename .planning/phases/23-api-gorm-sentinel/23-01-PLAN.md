---
phase: 23-api-gorm-sentinel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/huawei/client.go
  - internal/huawei/client_test.go
autonomous: true
requirements:
  - DETECT-01
  - DETECT-04

must_haves:
  truths:
    - "HuaweiClient.GetConferenceState() returns ConferenceState struct with ConfState/JoinSum and HasConferenceFields flag"
    - "Caller can detect new-field availability: when both confState and joinSum keys exist → HasConferenceFields=true; otherwise HasConferenceFields=false and IsInConf is the fallback path"
    - "Malformed mailbox payload returns wrapped ErrRecordingHuaWeiStateFetchFailed, not a zero-value fake empty meeting"
  artifacts:
    - path: internal/huawei/client.go
      provides: "MailboxState.State new fields + ConferenceState struct + GetConferenceState method + parseMailboxState helper"
      contains: "func (c *Client) GetConferenceState"
    - path: internal/huawei/client_test.go
      provides: "Conference state fixtures"
      contains: "Test.*ConferenceState"
  key_links:
    - from: internal/huawei/client.go
      to: internal/errors
      via: "wrap of ErrRecordingHuaWeiStateFetchFailed on parse failure"
      pattern: "ErrRecordingHuaWeiStateFetchFailed"
    - from: internal/huawei/client.go
      to: parseMailboxState
      via: "stateless package-private helper"
      pattern: "parseMailboxState"
---

<objective>
Extend HuaweiClient mailbox data path to expose confState and joinSum and a stateless GetConferenceState boundary so Phase 24 watcher can OR against the new H-signal data without cross-task state inside the shared client.

Purpose: DETECT-01/04 - make WEB_GetMailboxDataAPI rich fields first-class and provide one sample per call (no 30s persistence inside client).
Output: ConferenceState struct + GetConferenceState method + presence-aware mailbox state fields + 7 fixture tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-api-gorm-sentinel/23-RESEARCH.md
@.planning/phases/23-api-gorm-sentinel/23-VALIDATION.md

Interfaces the executor must understand (from codebase read):
- MailboxState.State currently has 9 fields (Sitename, Speaker, Mic, Gk, Sip, Callstate, Calltype, Conftype, IsInConf).
- Existing GetMailboxData returns *MailboxState and silently swallows JSON parse errors. Phase 23 ADDS a new parseMailboxState helper for GetConferenceState. Do NOT modify existing GetMailboxData behavior.
- Sentinel ErrRecordingHuaWeiStateFetchFailed is defined in Plan 23-03.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MailboxState new fields + ConferenceState contract + 7 fixture tests</name>
  <files>internal/huawei/client.go, internal/huawei/client_test.go</files>
  <read_first>
    - internal/huawei/client.go (read lines 200-220 for MailboxState.State struct definition; lines 700-755 for GetMailboxData double-parse pattern and IsInConference)
    - internal/huawei/client_test.go (read entire file to see existing test fixtures, helper signatures `zapNopForTest`, `runtimeNumGoroutine`; reuse for fixtures only, do not duplicate)
    - .planning/phases/23-api-gorm-sentinel/23-RESEARCH.md (Technical Approach section: stateless sample, pointer fields or presence-aware unmarshal, conservative judgment rules)
  </read_first>
  <behavior>
    - Test 1 (TestParseMailboxState_AllFieldsPresent): JSON with confState=rollcall, joinSum=10, isInConf=1, confLeftTime=600 parses successfully; MailboxState.State.ConfState=rollcall, State.JoinSum=10, State.ConfLeftTime=600, State.IsInConf=1
    - Test 2 (TestParseMailboxState_EmptyMeeting): JSON with confState=, joinSum=0, isInConf=0 parses successfully with zero/empty values (NOT an error)
    - Test 3 (TestParseMailboxState_OldDeviceNoFields): JSON without confState/joinSum/confLeftTime, only isInConf=0 → MailboxState.State.ConfState="" and State.JoinSum=0 (no presence detection issue)
    - Test 4 (TestParseMailboxState_PartialFields): JSON missing only one of confState OR joinSum (has the other plus isInConf) → both still parse as zero/empty since field-level presence detection tracks individual fields
    - Test 5 (TestParseMailboxState_EmptyData): data string is empty → returns error wrapping ErrRecordingHuaWeiStateFetchFailed (NOT a zero-value success)
    - Test 6 (TestParseMailboxState_MalformedJSON): data string is not valid JSON or state field is missing/wrong type → returns error wrapping ErrRecordingHuaWeiStateFetchFailed
    - Test 7 (TestGetConferenceState_FallbackFlag): JSON has only isInConf=0 (no new fields) → returned ConferenceState.HasConferenceFields=false, IsInConf=0, ConfState empty, JoinSum 0 (detection must distinguish absent vs present-but-empty)
  </behavior>
  <action>
    In internal/huawei/client.go:

    1. Extend MailboxState.State struct: add `ConfState string json:\"confState\"`, `JoinSum int json:\"joinSum\"`, `ConfLeftTime int json:\"confLeftTime\"`. Keep all existing 9 fields intact (no rename). Existing fields remain primary; new fields are appended below IsInConf with the JSON tag names exactly as TE40 firmware returns them (camelCase as documented in PRD).

    2. Add a package-private helper `parseMailboxState(data string) (*MailboxState, error)` placed immediately after the MailboxState type definition:
       - If data == \"\" → return nil, fmt.Errorf(\"mailbox data empty: %w\", apperrors.ErrRecordingHuaWeiStateFetchFailed)
       - Unmarshal data into a wrapper struct { State json.RawMessage `json:\"state\"` }
       - If outer unmarshal fails OR State is empty/nil → return nil, fmt.Errorf(\"mailbox outer parse failed: %w\", apperrors.ErrRecordingHuaWeiStateFetchFailed)
       - If inner unmarshal into MailboxState.State fails → return nil, fmt.Errorf(\"mailbox state parse failed: %w\", apperrors.ErrRecordingHuaWeiStateFetchFailed)
       - On success return &mailboxState, nil
       - Implementation detail: presence tracking for the new H-signal fields is achieved via the conservative judgment rule below in GetConferenceState — NOT via custom UnmarshalJSON on the State struct (which would complicate IsInConference callers). The helper just performs raw double-decode.

    3. Define a new exported type ConferenceState placed below MailboxState:
       - `ConfState string` (renamed pascal export of State.ConfState)
       - `JoinSum int`
       - `ConfLeftTime int`
       - `IsInConf int` (copied from State for fallback; not 0/1 boolean because TE40 firmware uses 0/1 int convention)
       - `HasConferenceFields bool` (true when both confState and joinSum JSON keys were present in the source response)

    4. Add a new exported method on `*HuaweiClient` (or `*Client` — pick whichever is consistent with the rest of the file; grep shows both Client and HuaweiClient are used):
       `func (c *HuaweiClient) GetConferenceState(ctx context.Context) (*ConferenceState, error)`
       - Call existing `GetMailboxData(ctx)` to fetch the raw response (do NOT change GetMailboxData's behavior — the existing keepalive callers depend on it). If it returns an error, wrap as fmt.Errorf(\"mailbox fetch failed: %w\", apperrors.ErrRecordingHuaWeiStateFetchFailed) and return nil, err.
       - If GetMailboxData returned a *MailboxState, access the underlying raw APIResponse.Data via a new package-private function or by extending GetMailboxData with a sibling method. Simplest approach: add a sibling `getMailboxRaw(ctx context.Context) (string, error)` that returns the literal `APIResponse.Data` string from the existing Post call (duplicates 8 lines of HTTP plumbing) OR refactor GetMailboxData to keep Data accessible. Read the GetMailboxData impl closely before choosing; the chosen path must not change existing test expectations of TestHuaweiClient_StopExitsKeepAliveGoroutine.
       - Pass the raw data string through `parseMailboxState(rawData)` to get a strictly-parsed *MailboxState. On parse error, propagate the wrapped sentinel.
       - Build ConferenceState: copy ConfState, JoinSum, ConfLeftTime, IsInConf from the parsed state.
       - Compute HasConferenceFields = both `confState` AND `joinSum` keys were present in the source raw JSON. Implementation: use a presence-detection wrapper struct parsed ONLY when needed (e.g., parse an anonymous struct { State struct { ConfState json.RawMessage; JoinSum json.RawMessage `json:\"joinSum\"` } `json:\"state\"` } — both fields non-empty RawMessage = present). Place this presence helper as a separate package-private function `detectConferenceFields(rawData string) bool`.
       - Return &ConferenceState{...}, nil.

    5. Conservative judgment (Phase 24 watcher will use): if HasConferenceFields is true AND ConfState != \"\" → state is active (regardless of JoinSum). If HasConferenceFields is true AND ConfState == \"\" AND JoinSum == 0 → empty candidate (Phase 24 applies huawei_persist_s). If HasConferenceFields is false → fallback IsInConf path.

    In internal/huawei/client_test.go append 7 subtests inside one TestParseMailboxState function using table-driven pattern with subtest names mapping to the 7 behaviors above. Use json.Marshal to build input fixture strings. The 7th subtest (TestGetConferenceState_FallbackFlag) requires testing the exported method indirectly via parseMailboxState + detectConferenceFields since the exported method requires an *HuaweiClient (skip full client setup, test the helpers directly). Do NOT use live HTTP or session — fixtures only.

    Use the existing test helper zapNopForTest() for any logger creation (do not introduce new logger patterns).
  </action>
  <verify>
    <automated>go test ./internal/huawei -run 'TestParseMailboxState|TestGetConferenceState' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `internal/huawei/client.go` contains the literal text `func (c *HuaweiClient) GetConferenceState(ctx context.Context)`
    - `internal/huawei/client.go` contains a package-private `parseMailboxState(data string)` function
    - `internal/huawei/client.go` defines exported type `ConferenceState` with fields `ConfState string`, `JoinSum int`, `ConfLeftTime int`, `IsInConf int`, `HasConferenceFields bool`
    - `internal/huawei/client.go` MailboxState.State struct has been extended with json tags `json:\"confState\"`, `json:\"joinSum\"`, `json:\"confLeftTime\"` next to existing IsInConf
    - `internal/huawei/client_test.go` contains test function `TestParseMailboxState` with 7 subtests (test names must include: AllFieldsPresent, EmptyMeeting, OldDeviceNoFields, PartialFields, EmptyData, MalformedJSON)
    - `internal/huawei/client_test.go` contains test function `TestGetConferenceState_FallbackFlag`
    - Errors returned by parseMailboxState wrap `apperrors.ErrRecordingHuaWeiStateFetchFailed` (errors.Is(err, ErrRecordingHuaWeiStateFetchFailed) returns true)
    - Existing test `TestHuaweiSanitizeResponseBody` and `TestHuaweiClient_StopExitsKeepAliveGoroutine` still pass (no regression)
  </acceptance_criteria>
  <done>GetConferenceState returns sample + HasConferenceFields flag; parseMailboxState returns sentinel-wrapped error on empty Data or malformed JSON; 7 fixture tests pass; no changes to existing GetMailboxData contract.</done>
</task>

</tasks>

<verification>
Run quick package test after task commit:
- `go test ./internal/huawei -count=1 -race` → must pass (including pre-existing TestHuaweiSanitizeResponseBody and TestHuaweiClient_StopExitsKeepAliveGoroutine)

Run vet:
- `go vet ./internal/huawei` → must exit 0

Sample fixture matrix is exercised by the 7 subtests defined in Task 1 behavior block.
</verification>

<success_criteria>
[Measurable completion of Plan 23-01]
1. `internal/huawei/client.go` exports `ConferenceState` struct with `HasConferenceFields` flag and 5 fields total (ConfState/JoinSum/ConfLeftTime/IsInConf/HasConferenceFields)
2. `internal/huawei/client.go` provides `(*HuaweiClient).GetConferenceState(ctx) (*ConferenceState, error)` that internally calls a package-private `parseMailboxState(data string) (*MailboxState, error)` helper
3. parseMailboxState returns wrapped `ErrRecordingHuaWeiStateFetchFailed` (introduced in Plan 23-03) for empty Data, missing state field, or inner-state type error — never silently returns zero-value success
4. HasConferenceFields is computed by a presence-detection helper that distinguishes JSON-absent keys from JSON-present-but-empty values
5. Seven fixture tests pass (mix of new fields, fallback, partial fields, malformed, transport error)
6. No regression on existing TestHuaweiSanitizeResponseBody / TestHuaweiClient_StopExitsKeepAliveGoroutine
</success_criteria>

<output>
Create `.planning/phases/23-api-gorm-sentinel/23-01-SUMMARY.md` when done
</output>
