---
phase: 23-api-gorm-sentinel
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/smart_end.go
  - internal/config/smart_end_test.go
  - internal/config/config.go
autonomous: true
requirements:
  - CFG-01

must_haves:
  truths:
    - "SmartEndConfig struct has 14 typed fields with mapstructure/json/yaml tags"
    - "All 14 default values load correctly when YAML omits the smart_end section"
    - "Explicit smart_end.enabled=false in YAML is preserved (not overwritten by SetDefault true)"
    - "SmartEndConfig.Validate() rejects non-positive durations/counts and out-of-range silence dB"
  artifacts:
    - path: internal/config/smart_end.go
      provides: "SmartEndConfig struct + 14 fields + applySmartEndDefaults + Validate function"
      contains: "type SmartEndConfig"
    - path: internal/config/smart_end_test.go
      provides: "Defaults / explicit-false preservation / invalid threshold rejection tests"
      contains: "TestSmartEndConfig"
    - path: internal/config/config.go
      provides: "Config.SmartEnd field + Viper SetDefault for true-valued bools BEFORE Unmarshal"
      contains: "SmartEnd"
  key_links:
    - from: internal/config/config.go
      to: internal/config/smart_end.go
      via: "Config.SmartEnd SmartEndConfig field, Viper.SetDefault for 3 true-valued bools, applySmartEndDefaults handles numeric"
      pattern: "SetDefault.*smart_end"
    - from: internal/config/smart_end.go
      to: Config struct
      via: "SmartEndConfig.Validate returns errors that wrap apperrors.ErrInvalidInput"
      pattern: "ErrInvalidInput"
---

<objective>
Define SmartEndConfig struct with 14 typed fields, integrate it into the root Config and Viper defaults, and provide Validate + test coverage so Phase 25 can simply read cfg.SmartEnd.* and call cfg.SmartEnd.Validate() on boot.

Purpose: CFG-01 - provide strong typing and validation gates for the 14 smart-end configuration knobs without leaking them across packages.
Output: New file internal/config/smart_end.go, new file internal/config/smart_end_test.go, plus minimal edits to internal/config/config.go for field + Viper SetDefault + Validate call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-api-gorm-sentinel/23-RESEARCH.md
@.planning/phases/23-api-gorm-sentinel/23-VALIDATION.md

Interfaces the executor must understand (from codebase read):
- Config struct at internal/config/config.go:25-42 has 14 sub-config fields each with mapstructure X json X yaml X triple tags. Add a SmartEnd SmartEndConfig field after Security (line 41).
- setDefaults at internal/config/config.go:404+ uses if cfg.X.Field equals zero_value then assign default. For Phase 23 numeric defaults, follow the same shape inside a new applySmartEndDefaults(cfg) called from setDefaults. Do NOT scatter if !cfg.SmartEnd.X because the bool-default trap (Pitfall 3) overwrites operator explicit false.
- Bool-default workaround per RESEARCH.md A5: use Viper v.SetDefault with smart_end.enabled = true etc. The 3 bool fields with true defaults (Enabled, HuaweiEnabled, DegradeOnSilenceLoss) MUST be registered via SetDefault BEFORE v.Unmarshal at line 378. The two-stage Viper rebuild at lines 367-369 already gives a hook: add SetDefault calls immediately after v = viper.New() (line 367) and BEFORE v.Unmarshal (line 378). Numeric defaults go in applySmartEndDefaults invoked from setDefaults post-Unmarshal.
- Closest analog: HuaweiConfig struct at internal/config/config.go:187-198 uses the same triple-tag convention. Reference it for tag style.
- Validation pattern: Config has no global Validate() but per-cluster validation is common in credential tests. Use apperrors.ErrInvalidInput for any range violations (existing pattern at the model layer).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SmartEndConfig struct + 14 defaults + Validate + tests + Config integration</name>
  <files>internal/config/smart_end.go (new), internal/config/smart_end_test.go (new), internal/config/config.go</files>
  <read_first>
    - internal/config/config.go (full file, especially lines 25-42 Config struct, lines 187-198 HuaweiConfig triple-tag style, lines 327-402 Load two-stage Viper rebuild, lines 404-644 setDefaults)
    - internal/config/config_test.go (lines 153-294 for table-driven Validate pattern)
    - .planning/phases/23-api-gorm-sentinel/23-RESEARCH.md (CFG-01 section + Architectural Patterns + Common Pitfalls Pitfall 3)
    - .planning/phases/23-api-gorm-sentinel/23-VALIDATION.md (Task 23-04-01 row)
  </read_first>
  <behavior>
    - Test 1 (TestSmartEndConfig_Defaults): load a Config with no smart_end YAML section; cfg.SmartEnd.* must equal all 14 expected default values from REQUIREMENTS.md:14
    - Test 2 (TestSmartEndConfig_ExplicitFalsePreserved): set smart_end.enabled=false and smart_end.huawei_enabled=false and smart_end.degrade_on_silence_loss=false via Viper; after Unmarshal, the three bools stay false (NOT overwritten by SetDefault true). The trap from Pitfall 3 is fixed.
    - Test 3 (TestSmartEndConfig_InvalidRejection): table-driven Validate cases covering silence_db out-of-range, non-positive durations/counts, file_min_growth_bps negative; each case asserts errors.Is(returned, apperrors.ErrInvalidInput)
  </behavior>
  <action>
    Create internal/config/smart_end.go (NEW file, package config):

    1. Define SmartEndConfig struct with exactly 14 fields. All fields use mapstructure/json/yaml triple tags with identical keys to the YAML names in REQUIREMENTS.md:58 (e.g. mapstructure huawei_poll_interval_s, json huawei_poll_interval_s, yaml huawei_poll_interval_s). Field types and defaults: Enabled bool (default true), SilenceDB int (default -30, range -100..0 inclusive), SilenceDurationS int (default 30, must be > 0), FileStallS int (default 120, must be > 0), FileMinGrowthBPS int64 (default 1024, must be >= 0), HuaweiEnabled bool (default true), HuaweiPollIntervalS int (default 30, must be > 0), HuaweiPersistS int (default 30, must be > 0), HuaweiFailureThreshold int (default 3, must be > 0), CheckIntervalS int (default 5, must be > 0), ExtendStepMin int (default 30, must be > 0), MaxExtendCount int (default 4, must be > 0), StatFailureThreshold int (default 3, must be > 0), DegradeOnSilenceLoss bool (default true).

    2. Add a function applySmartEndDefaults(cfg *Config) that sets all numeric defaults IF the current value equals the zero value (numeric pattern: if cfg.SmartEnd.X == 0 then assign default). Do NOT touch the 3 bool fields here - they are handled by Viper SetDefault in config.go edit (step 5).

    3. Add a method func (c *SmartEndConfig) Validate() error that returns the first violation wrapped in apperrors.ErrInvalidInput (use fmt.Errorf with %w apperrors.ErrInvalidInput). Order: SilenceDB range, then duration/threshold positive checks (each > 0), then FileMinGrowthBPS >= 0, then MaxExtendCount > 0.

    4. Pre-define error message constants for each violation in the same file. Package-private.

    Edit internal/config/config.go:

    5. Add SmartEnd SmartEndConfig to Config struct (after Security, line 41). Triple-tag: smart_end for all three.

    6. In Load() (lines 327-401), after the second v = viper.New() at line 367 and BEFORE bindSecretEnv(v) at line 375, add Viper SetDefault calls for the 3 true-valued bool fields so explicit YAML false is preserved: v.SetDefault(smart_end.enabled, true); v.SetDefault(smart_end.huawei_enabled, true); v.SetDefault(smart_end.degrade_on_silence_loss, true).

    7. In setDefaults (line 404+), add a call to applySmartEndDefaults(cfg) so all numeric fields get defaults after Unmarshal completes.

    8. After setDefaults, call cfg.SmartEnd.Validate(); if err != nil: return nil, fmt.Errorf(... wrapping with apperrors.ErrInternal).

    Create internal/config/smart_end_test.go (NEW file, package config):

    9. Test 1 (TestSmartEndConfig_Defaults): construct a fresh viper instance; do NOT pre-populate smart_end; simulate load by reading an empty minimal YAML or calling viper.Set on the 3 bools with defaults; assert all 14 defaults. Use viper.New() + v.ReadConfig(strings.NewReader(yaml)) pattern from existing config_test.go.

    10. Test 2 (TestSmartEndConfig_ExplicitFalsePreserved): use v.SetConfigType(yaml); v.ReadConfig(strings.NewReader with smart_end setting enabled=false, huawei_enabled=false, degrade_on_silence_loss=false, and silence_duration_s=99). Then v.SetDefault for the 3 bools before ReadConfig. Unmarshal into Config; assert the 3 bools remain false and SilenceDurationS=99. The trap from Pitfall 3 is REVERSED here.

    11. Test 3 (TestSmartEndConfig_InvalidRejection): table-driven. For each invalid value, construct a SmartEndConfig literal directly, call Validate(), assert errors.Is(err, apperrors.ErrInvalidInput) returns true. Cover all the violation cases listed in behavior Test 3. Add one positive case: a fully-default SmartEndConfig with all valid values returns nil from Validate().
  </action>
  <verify>
    <automated>go test ./internal/config -run TestSmartEndConfig -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - File internal/config/smart_end.go exists and contains a type SmartEndConfig struct declaration with 14 fields
    - Each field has matching lowercase mapstructure/json/yaml triple tags (e.g. huawei_poll_interval_s)
    - SmartEndConfig has a Validate() error method
    - File internal/config/smart_end_test.go exists with 3 test functions (TestSmartEndConfig_Defaults, TestSmartEndConfig_ExplicitFalsePreserved, TestSmartEndConfig_InvalidRejection)
    - internal/config/config.go Config struct has new field SmartEnd SmartEndConfig with triple tag smart_end
    - internal/config/config.go Load() registers v.SetDefault for the 3 true-valued bools (enabled, huawei_enabled, degrade_on_silence_loss) BETWEEN the v = viper.New() rebuild and v.Unmarshal
    - internal/config/config.go setDefaults calls applySmartEndDefaults (or equivalent inline numeric defaulting for all 14 numeric fields)
    - internal/config/config.go Load() calls cfg.SmartEnd.Validate() and wraps any non-nil error with apperrors.ErrInternal
    - All 3 new tests pass; existing config tests still pass (no regression)
  </acceptance_criteria>
  <done>SmartEndConfig struct compiles, loads all 14 defaults, preserves explicit false, validates numeric and bool ranges with apperrors.ErrInvalidInput wrapping; Config struct surfaces SmartEnd and Load plumbs Validate.</done>
</task>

</tasks>

<verification>
After task commit:
- go test ./internal/config -run TestSmartEndConfig -count=1 -v - 3 tests pass
- go test ./internal/config -count=1 -race - full package pass including any existing tests
- go vet ./internal/config exits 0
- go build ./... confirms no consumer breaks on the new Config field (Config has no breaking changes since SmartEnd defaults are zero-value-safe)
</verification>

<success_criteria>
[Measurable completion of Plan 23-04]
1. internal/config/smart_end.go exists with SmartEndConfig struct (14 fields with triple tags) + applySmartEndDefaults + Validate
2. internal/config/smart_end_test.go has 3 passing test functions
3. internal/config/config.go Config struct exposes SmartEnd SmartEndConfig field
4. Viper SetDefault for 3 true-valued bools is registered BEFORE Unmarshal
5. applySmartEndDefaults sets all numeric defaults post-Unmarshal via zero-value check pattern
6. cfg.SmartEnd.Validate() is invoked during Load() and any error is wrapped with apperrors.ErrInternal
7. No regression on existing config_test.go tests
</success_criteria>

<output>
Create .planning/phases/23-api-gorm-sentinel/23-04-SUMMARY.md when done
</output>
