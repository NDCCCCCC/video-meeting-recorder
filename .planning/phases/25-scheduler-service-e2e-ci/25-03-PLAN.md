---
phase: 25
plan: 03
type: execute
wave: 1
depends_on:
  - 25-01
  - 25-02
files_modified:
  - internal/observability/smart_end_metrics.go
  - internal/observability/smart_end_metrics_test.go
  - internal/services/video_recording_task_service.go
  - internal/scheduler/video_scheduler.go
  - internal/recorder/activity_watcher.go
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
  - OBS-05
user_setup: []
must_haves:
  truths:
    - "smart_extend log line contains task=<id> count=<n> new_end=<ts> reason=<text> exactly as the OBS-01 schema specifies"
    - "smart_early_end log line contains task=<id> reason=<text> snapshot=<json> exactly as the OBS-02 schema specifies"
    - "max_extend_reached log line is emitted at WARN with task=<id> force_end=true when ExtensionCount hits the cap"
    - "activity_watcher_degraded log line is emitted at ERROR with reason=<text> by the watcher whenever degrade paths activate (silence parser / huawei poller / file stat)"
    - "Three atomic.Int64 counters (smart_extend_total / smart_early_end_total / watcher_degraded_total) are exposed via Record* functions, without importing prometheus client_golang"
  artifacts:
    - path: "internal/observability/smart_end_metrics.go"
      provides: "3 atomic.Int64 counters + Record* accessors"
      contains: "RecordSmartExtend"
    - path: "internal/observability/smart_end_metrics_test.go"
      provides: "Test asserting atomic increment + reset helper"
      contains: "TestSmartEndMetrics_Record"
    - path: "internal/services/video_recording_task_service.go"
      provides: "UpdateTaskExtension emits OBS-01 log + RecordSmartExtend(); MarkTaskEndedEarly emits OBS-02 log + RecordSmartEarlyEnd()"
      contains: "RecordSmartExtend"
    - path: "internal/scheduler/video_scheduler.go"
      provides: "max_extend_reached WARN log emission in handleEndTimeReached"
      contains: "max_extend_reached"
  key_links:
    - from: "internal/services/video_recording_task_service.go::UpdateTaskExtension"
      to: "internal/observability/smart_end_metrics.go::RecordSmartExtend"
      via: "function call after the GORM Updates succeeds"
      pattern: "observability\\.RecordSmartExtend"
    - from: "internal/services/video_recording_task_service.go::MarkTaskEndedEarly"
      to: "internal/observability/smart_end_metrics.go::RecordSmartEarlyEnd"
      via: "function call after the GORM Updates succeeds"
      pattern: "observability\\.RecordSmartEarlyEnd"
    - from: "internal/recorder/activity_watcher.go::huaweiPoller / fileTicker / silenceScanner"
      to: "internal/observability/smart_end_metrics.go::RecordWatcherDegraded"
      via: "function call inside the existing zap.Warn(\"activity_watcher_degraded\") branches"
      pattern: "observability\\.RecordWatcherDegraded"
    - from: "internal/scheduler/video_scheduler.go::handleEndTimeReached"
      to: "internal/observability/smart_end_metrics.go"
      via: "optional, no atomic increment for max_extend_reached (the WARN log is the signal; this avoids double-counting with UpdateTaskExtension callers)"
      pattern: "max_extend_reached"
---

<objective>
Land the 5-class observability surface (4 zap structured log fields + 3 atomic counters) called from the service and scheduler entry points built in plans 01 and 02, without introducing any new dependencies.

Purpose: Plan 03 wires the OBS-01..05 contract that downstream observability tooling (and the CI golden JSON assertions in plan 04) consume. Without the counters, plan 04's audit assertions cannot verify the increment behavior. Without the WARN/ERROR log fields, the locked field schema cannot be cross-checked.

Output:
- `internal/observability/smart_end_metrics.go` new file with 3 `atomic.Int64` counters and 3 `Record*` functions.
- `internal/observability/smart_end_metrics_test.go` new test file with 4 PASS lines.
- `internal/services/video_recording_task_service.go` extended to call the OBS-01/02 log + atomic increment inside the methods from plan 01.
- `internal/scheduler/video_scheduler.go` `handleEndTimeReached` extended to emit the OBS-03 WARN log.
- `internal/recorder/activity_watcher.go` extended to call `RecordWatcherDegraded()` inside the three existing `activity_watcher_degraded` warn branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-scheduler-service-e2e-ci/25-RESEARCH.md
@.planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md
@.planning/phases/25-scheduler-service-e2e-ci/25-01-PLAN.md
@.planning/phases/25-scheduler-service-e2e-ci/25-02-PLAN.md
@internal/services/video_recording_task_service.go
@internal/scheduler/video_scheduler.go
@internal/recorder/activity_watcher.go
@go.mod
</context>

<interfaces>
From internal/recorder/activity_watcher.go (re-confirmed for plan 03):
- Three `w.logger.Warn("activity_watcher_degraded", ...)` call sites: line 273 (silence_parser_failed), line 389 (huawei_client_nil), line 417 (huawei_api_unreachable). The file_stat_failed path uses `closeEnded("file_stat_failed")` which already calls `logger.Info("smart_early_end (watcher)", ...)` but does not log "activity_watcher_degraded" — DO NOT change that distinction in this plan; OBS-04 is satisfied by the three warn branches.

From internal/services/video_recording_task_service.go (delivered by plan 01):
```go
func (s *VideoRecordingTaskService) UpdateTaskExtension(ctx context.Context, taskID uint, deltaMin int, reason string, snap recorder.ActivitySnapshot) error
func (s *VideoRecordingTaskService) MarkTaskEndedEarly(ctx context.Context, taskID uint, reason string, byHuaWeiAPI bool, snap recorder.ActivitySnapshot) error
```

From internal/scheduler/video_scheduler.go (delivered by plan 02):
- The `handleEndTimeReached` helper inside the rewritten `monitorTask` already has the comparison `if currentCount >= max { ... }`. Plan 03 only needs to add the WARN log + the completeTask call wiring inside that branch.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add smart_end_metrics.go with 3 atomic counters and Record* functions</name>
  <files>internal/observability/smart_end_metrics.go, internal/observability/smart_end_metrics_test.go</files>
  <read_first>
    - go.mod (verify sync/atomic is stdlib and no new dependency is required)
    - internal/recorder/activity_watcher.go (lines 270-277, 385-394, 414-422 — the three activity_watcher_degraded warn branches; OBS-05 wiring calls RecordWatcherDegraded immediately after the existing zap.Warn)
    - internal/services/video_recording_task_service.go (the methods from plan 01; OBS-05 wiring goes after the GORM Updates but before the audit log call so the counter increments even when audit is nil)
    - .planning/phases/25-scheduler-service-e2e-ci/25-RESEARCH.md §Code Examples "observability/smart_end_metrics.go 接入点" and §Pitfall 7
  </read_first>
  <action>
    Create `internal/observability/smart_end_metrics.go` (new file). Package `observability`. Imports: `sync/atomic`. The file must contain:

    1. A package doc comment (5-8 lines) that explains: (a) this is the OBS-05 atomic counter access point; (b) it intentionally does not import `github.com/prometheus/client_golang` (per project memory `project-planning-gitignored.md` and PRD `OBS-05` "无 prom 集成则仅做日志"); (c) future Prometheus wiring should wrap the `Record*` functions rather than read the atomic vars directly.

    2. Three unexported package-level `atomic.Int64` counters:
       ```go
       var (
           smartExtendTotal     atomic.Int64
           smartEarlyEndTotal   atomic.Int64
           watcherDegradedTotal atomic.Int64
       )
       ```
       Place them below the doc comment with a single line comment naming each counter's purpose (matching the OBS-01/02/04 trigger points).

    3. Three exported `Record*` functions:
       ```go
       func RecordSmartExtend()     { smartExtendTotal.Add(1) }
       func RecordSmartEarlyEnd()   { smartEarlyEndTotal.Add(1) }
       func RecordWatcherDegraded() { watcherDegradedTotal.Add(1) }
       ```

    4. Three exported getter functions (for tests + future Prometheus handler):
       ```go
       func SmartExtendTotal() int64     { return smartExtendTotal.Load() }
       func SmartEarlyEndTotal() int64   { return smartEarlyEndTotal.Load() }
       func WatcherDegradedTotal() int64 { return watcherDegradedTotal.Load() }
       ```

    5. One exported `ResetForTest()` function (used by the unit test below to start from a known baseline; not safe to call concurrently with production traffic — doc comment must state this):
       ```go
       // ResetForTest zero-all counters. Test-only helper; not safe to call
       // alongside production traffic.
       func ResetForTest() {
           smartExtendTotal.Store(0)
           smartEarlyEndTotal.Store(0)
           watcherDegradedTotal.Store(0)
       }
       ```

    Create `internal/observability/smart_end_metrics_test.go`. Use the standard `testing` package (no testify — the metrics package is small and shouldn't drag in a test framework). The test must:
    1. Call `ResetForTest()` in a TestMain or per-test setup.
    2. `TestSmartEndMetrics_RecordExtend` — call `RecordSmartExtend()` 3 times, assert `SmartExtendTotal() == 3`.
    3. `TestSmartEndMetrics_RecordEarlyEnd` — call `RecordSmartEarlyEnd()` 5 times, assert `SmartEarlyEndTotal() == 5`.
    4. `TestSmartEndMetrics_RecordWatcherDegraded` — call `RecordWatcherDegraded()` 2 times, assert `WatcherDegradedTotal() == 2`.
    5. `TestSmartEndMetrics_ResetForTest` — call all three Record* functions, then `ResetForTest()`, assert all three getters return 0.
    6. Each test must end with `ResetForTest()` in a `t.Cleanup` so other tests in the suite start from zero.
  </action>
  <accept_criteria>
    - `go build ./internal/observability` exits 0.
    - `grep -n "RecordSmartExtend\\|RecordSmartEarlyEnd\\|RecordWatcherDegraded" internal/observability/smart_end_metrics.go` returns exactly 3 lines.
    - `grep -n "atomic.Int64" internal/observability/smart_end_metrics.go` returns at least 3 lines.
    - `grep -n "prometheus/client_golang" internal/observability/smart_end_metrics.go` returns 0 lines (explicitly absent).
    - `go test ./internal/observability -count=1 -v` exits 0 with at least 4 PASS lines.
    - `go.mod` diff is empty (no new dependencies).
  </accept_criteria>
  <verify>
    <automated>go build ./internal/observability && go test ./internal/observability -count=1 -v && go vet ./internal/observability</automated>
  </verify>
  <done>The OBS-05 atomic access point exists with 3 counters and 3 Record* functions, no prometheus client_golang import, and 4 unit tests cover increment and reset paths.</done>
</task>

<task type="auto">
  <name>Task 2: Wire OBS-01..04 log fields and OBS-05 atomic increments into service, scheduler, and watcher call sites</name>
  <files>internal/services/video_recording_task_service.go, internal/scheduler/video_scheduler.go, internal/recorder/activity_watcher.go</files>
  <read_first>
    - internal/services/video_recording_task_service.go (UpdateTaskExtension body and MarkTaskEndedEarly body delivered by plan 01 — read the log call sites, auditSvc call sites, and where the GORM Updates returns)
    - internal/scheduler/video_scheduler.go (the `if currentCount >= max` branch inside `handleEndTimeReached` from plan 02)
    - internal/recorder/activity_watcher.go (lines 270-280 silenceParser degrade branch, 385-395 huawei_client_nil branch, 414-425 huawei_api_unreachable branch)
    - internal/observability/smart_end_metrics.go (the new Record* functions from task 1)
    - .planning/phases/25-scheduler-service-e2e-ci/25-RESEARCH.md §phase_requirements OBS-01..05 rows and §Pitfall 7
  </read_first>
  <action>
    In `internal/services/video_recording_task_service.go`:
    1. Add import `github.com/NDCCCCCC/video-meeting-recorder/internal/observability` (the new package from task 1).
    2. In `UpdateTaskExtension`, after the GORM `Updates(...)` call but BEFORE the audit log block, insert:
       ```go
       observability.RecordSmartExtend()
       ```
       Then update the existing INFO log so its field names match the locked OBS-01 schema exactly. The current log is `s.logger.Info("smart_extend", zap.Uint("task", taskID), zap.Int("count", newCount), zap.Time("new_end", newEnd), zap.String("reason", reason))`. The locked schema is `INFO smart_extend task=<id> count=<n> new_end=<ts> reason=<text>`. Verify each field name is already correct: `task` (uint), `count` (int), `new_end` (time), `reason` (string). The zap field name is the JSON key after the log encoder strips/sanitizes — `zap.Uint("task", ...)` produces `task=<id>`. No changes needed to the log itself; just confirm the call site already matches.
    3. In `MarkTaskEndedEarly`, after the GORM `Updates(...)` call but BEFORE the audit log block, insert:
       ```go
       observability.RecordSmartEarlyEnd()
       ```
       Verify the existing `s.logger.Info("smart_early_end", zap.Uint("task", taskID), zap.String("reason", reason), zap.Any("snapshot", ...))` matches the locked OBS-02 schema `INFO smart_early_end task=<id> reason=<text> snapshot=<json>`. Field names already match; no log change required.
    4. Place the `observability.Record*` calls in the success path only. If `Updates` returned an error, the function returns the error before reaching the increment, which is correct (no spurious counter bumps on failure).

    In `internal/scheduler/video_scheduler.go`:
    1. Inside `handleEndTimeReached`, in the `if currentCount >= max` branch, BEFORE the call to `s.completeTask(...)`, insert the OBS-03 WARN log:
       ```go
       s.logger.Warn("max_extend_reached",
           zap.Uint("task_id", task.ID),
           zap.Bool("force_end", true),
           zap.Int("extension_count", currentCount),
           zap.Int("max_extend_count", max),
       )
       ```
       The locked OBS-03 schema is `WARN max_extend_reached task=<id> force_end=true`. The `task_id` and `extension_count` and `max_extend_count` fields are added for diagnostic richness; the schema-required fields are satisfied by the first two.
    2. No atomic counter is incremented here. Rationale: the failure-to-extend path is a single user-visible event; calling `RecordSmartExtend` would double-count (one for the cap rejection, one for the prior successful extension that brought the count to MaxExtendCount). The OBS-05 counter is incremented only on successful extensions (in the service); max-extend is signaled exclusively by the WARN log.

    In `internal/recorder/activity_watcher.go`:
    1. Add import `github.com/NDCCCCCC/video-meeting-recorder/internal/observability` if not already present.
    2. In the `silenceParser` branch at line 273, immediately after the `w.logger.Warn("activity_watcher_degraded", ...)` call, insert `observability.RecordWatcherDegraded()`. Place it INSIDE the condition so the counter increments exactly when the log fires; a single `degrade` event should produce exactly one log + one counter increment.
    3. In the `huawei_client_nil` branch at line 389, add `observability.RecordWatcherDegraded()` after the WARN log.
    4. In the `huawei_api_unreachable` branch at line 417, add `observability.RecordWatcherDegraded()` after the WARN log.
    5. The `file_stat_failed` path uses `closeEnded("file_stat_failed")` which logs at INFO (`smart_early_end (watcher)`). The locked OBS-04 schema requires the ERROR-level `activity_watcher_degraded` log. Phase 24 RESEARCH.md Pitfall 4 documents that the file_stat_failed case is intentionally distinct (it's a stream-death signal, not a degrade event). DO NOT change that behavior in this plan; the existing Phase 24 INFO log is sufficient. However, document this design choice in a comment in `fileTicker` near the `closeEnded("file_stat_failed")` call: "// file_stat_failed is an early-end signal (INFO), not a watcher-degrade event (ERROR); deliberately not counted as RecordWatcherDegraded — see 25-03 plan §OBS-04 design note."

    Verify the full module still builds and all existing tests still pass.
  </action>
  <accept_criteria>
    - `grep -n "observability.RecordSmartExtend" internal/services/video_recording_task_service.go` returns 1 line inside UpdateTaskExtension.
    - `grep -n "observability.RecordSmartEarlyEnd" internal/services/video_recording_task_service.go` returns 1 line inside MarkTaskEndedEarly.
    - `grep -n "max_extend_reached" internal/scheduler/video_scheduler.go` returns the OBS-03 WARN log (now 1 line; was already present from plan 02 as comment, now the actual log).
    - `grep -n "observability.RecordWatcherDegraded" internal/recorder/activity_watcher.go` returns 3 lines (silence_parser_failed, huawei_client_nil, huawei_api_unreachable).
    - `grep -n "smart_extend task" internal/services/video_recording_task_service.go` returns the INFO log call.
    - `grep -n "smart_early_end task" internal/services/video_recording_task_service.go` returns the INFO log call.
    - `go build ./...` exits 0.
    - `go test ./internal/services ./internal/scheduler ./internal/recorder ./internal/observability -count=1` exits 0.
    - `go vet ./...` exits 0.
  </accept_criteria>
  <verify>
    <automated>go build ./... && go test ./internal/services ./internal/scheduler ./internal/recorder ./internal/observability -count=1 && go vet ./...</automated>
  </verify>
  <done>OBS-01 through OBS-05 contract surfaces are wired at all call sites: the 4 zap log fields match the locked schemas exactly, and the 3 atomic counters are incremented at the success-path call sites; no prometheus dependency introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → observability | direct function calls; counters are process-wide and visible to all callers |
| watcher → observability | direct function calls; 3 sites; all inside the locked OBS-04 schema branches |
| scheduler → zap | direct log call; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-12 | Tampering | atomic.Int64 overflow | accept | 2^63 is far beyond realistic smart-end event frequency (years of operation at thousands of events/day) |
| T-25-13 | Information Disclosure | counter visibility to other packages | accept | counters carry no sensitive data; only event counts |
| T-25-14 | Denial of Service | ResetForTest called in production | mitigate | doc comment explicitly states "Test-only"; no production caller path can invoke it |
| T-25-15 | Tampering | OBS-04 counter double-counting | mitigate | each of the 3 emit sites is in a distinct goroutine; each increments exactly once per emission; double-check the silence branch is inside the `>= threshold` guard |
| T-25-16 | Tampering | max_extend_reached counter double-counting | accept | by design, this path does NOT increment; the WARN log is the sole signal to avoid misleading the metric |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | plan 03 introduces zero new packages; go.mod unchanged; prometheus client_golang explicitly absent (verified by grep) |
</threat_model>

<verification>
- `go build ./...` exits 0.
- `go test ./internal/observability -count=1 -v` exits 0 with at least 4 PASS lines.
- `go test ./internal/services ./internal/scheduler ./internal/recorder ./internal/observability -count=1` exits 0.
- `go vet ./...` exits 0.
- `git diff go.mod` produces no output.
- The grep gates above each produce the expected counts.
- `grep -rn "prometheus/client_golang" internal/observability/` returns 0 lines.
</verification>

<success_criteria>
- The 4 OBS-01..04 log fields are emitted at the correct call sites with the locked field names (`task`, `count`, `new_end`, `reason`, `force_end`, `reason` for activity_watcher_degraded).
- The 3 OBS-05 atomic counters are incremented exactly at the success-path call sites, and the gettable counters return the right values via unit tests.
- Zero new external dependencies; go.mod unchanged; `prometheus/client_golang` is explicitly absent.
- All existing tests across the recorder, scheduler, and services packages still pass.
- The 25-04 plan can now rely on this surface for its golden JSON assertions and race-detector full-suite run.
</success_criteria>

<output>
Create `.planning/phases/25-scheduler-service-e2e-ci/25-03-SUMMARY.md` when done.
</output>
