---
phase: 25
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/recorder/activity_watcher.go
  - internal/recorder/activity_watcher_test.go
  - internal/services/video_recording_task_service.go
  - internal/services/video_recording_task_service_test.go
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - EXTEND-01
  - EARLY-01
  - EARLY-02
user_setup: []
must_haves:
  truths:
    - "ActivitySnapshot exposes FileSizeBytes and FileGrowthBps so service-layer audit logs can include the file telemetry"
    - "Service layer offers UpdateTaskExtension(task, deltaMin, reason) as the single entry point for timer-driven extensions"
    - "Service layer offers MarkTaskEndedEarly(task, reason, byHuaWeiAPI) as the single entry point for watcher-driven early-end events"
    - "UpdateTaskExtension refuses to extend when ExtensionCount >= cfg.SmartEnd.MaxExtendCount and returns a wrapped ErrRecordingSmartExtend"
    - "MarkTaskEndedEarly writes EndedEarly=true, EndedEarlyReason, EndedByHuaWeAPI and a snapshot to the audit log via AuditLogService.RecordChange"
  artifacts:
    - path: "internal/recorder/activity_watcher.go"
      provides: "ActivitySnapshot.FileSizeBytes + FileGrowthBps + fileTicker lastFileGrowthBps cache"
      contains: "FileSizeBytes"
    - path: "internal/recorder/activity_watcher_test.go"
      provides: "Snapshot extension test asserting the two new fields populate from fileTicker"
      contains: "TestActivityWatcher_SnapshotExtension"
    - path: "internal/services/video_recording_task_service.go"
      provides: "UpdateTaskExtension + MarkTaskEndedEarly + auditSvc + cfg fields + nil-safe audit guard"
      contains: "UpdateTaskExtension"
    - path: "internal/services/video_recording_task_service_test.go"
      provides: "5-field read/write assertions + audit snapshot JSON test + max-extend error path"
      contains: "TestUpdateTaskExtension_AuditSnapshot"
  key_links:
    - from: "internal/services/video_recording_task_service.go::UpdateTaskExtension"
      to: "internal/services/audit/audit_log_service.go::RecordChange"
      via: "auditSvc.RecordChange(ctx, RecordChangeOpts{...})"
      pattern: "auditSvc\\.RecordChange"
    - from: "internal/recorder/activity_watcher.go::Snapshot"
      to: "internal/services/video_recording_task_service.go::UpdateTaskExtension"
      via: "snapshot parameter (value copy from w.mu)"
      pattern: "Snapshot\\(\\)"
---

<objective>
Extend the watcher snapshot with file telemetry, then introduce two service-layer methods that become the single audit + GORM entry points for smart-end events.

Purpose: Phase 25 plan 01 closes AUDIT-02/03/04 + EXTEND-01 + EARLY-01/02 contract surfaces without touching the scheduler or observability layers. Every later plan depends on `UpdateTaskExtension` and `MarkTaskEndedEarly` being available, so this plan is the foundation of the phase.

Output:
- `internal/recorder/activity_watcher.go` Snapshot exposes `FileSizeBytes` and `FileGrowthBps`; `fileTicker` caches `lastFileGrowthBps`.
- `internal/services/video_recording_task_service.go` gains `UpdateTaskExtension` and `MarkTaskEndedEarly` plus injected `auditSvc` and `cfg` fields; constructor signature preserved by using a variadic for the audit dependency.
- `internal/recorder/activity_watcher_test.go` adds `TestActivityWatcher_SnapshotExtension`.
- `internal/services/video_recording_task_service_test.go` adds 5-field assertions, audit snapshot JSON test, and the max-extend error path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-scheduler-service-e2e-ci/25-RESEARCH.md
@.planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md
@internal/recorder/activity_watcher.go
@internal/recorder/coordinator.go
@internal/services/video_recording_task_service.go
@internal/services/audit/audit_log_service.go
@internal/config/smart_end.go
@internal/errors/errors.go
@internal/models/video_recording_task.go
@internal/services/video_recording_task_service_test.go
</context>

<interfaces>
From internal/recorder/activity_watcher.go:
```go
type ActivitySnapshot struct {
    SilenceSince         time.Time
    LastFileGrowthAt     time.Time
    HuaWeiEmptySince     time.Time
    LastHuaWeiStateEmpty bool
    HuaWeiDegraded       bool
    SilenceDegraded      bool
    Ended                bool
    EndedReason          string
    LastSilenceStart     time.Time
    TotalSilenceDuration time.Duration
}

func (w *ActivityWatcher) Snapshot() ActivitySnapshot
func (w *ActivityWatcher) ExtendStepMin() time.Duration
func (w *ActivityWatcher) EndedCh() <-chan struct{}
func (w *ActivityWatcher) IsActive() bool
```

From internal/services/audit/audit_log_service.go:
```go
type RecordChangeOpts struct {
    Action     string
    Module     string
    Resource   string
    ResourceID *uint
    OldData    interface{}
    NewData    interface{}
}

func (s *AuditLogService) RecordChange(ctx context.Context, opts RecordChangeOpts) error
```

From internal/config/smart_end.go:
```go
type SmartEndConfig struct {
    Enabled           bool  // CFG-03
    HuaweiEnabled     bool  // CFG-04
    ExtendStepMin     int
    MaxExtendCount    int
    // ... other 10 fields unchanged
}
```

From internal/errors/errors.go:
```go
ErrRecordingSmartExtend       = errors.New("recording smart extend failed")  // EXTEND-02 sentinel
ErrRecordingSmartEarlyEnd     = errors.New("recording smart early end failed")
ErrRecordingHuaWeiStateFetchFailed = errors.New("Huawei conference state fetch failed")
```

From internal/models/video_recording_task.go (Phase 23 GORM fields, AutoMigrate applied):
```go
ExtensionCount      int    `gorm:"not null;default:0"`
LastExtensionReason string `gorm:"type:text;not null;default:''"`
EndedEarly          bool   `gorm:"not null;default:false"`
EndedEarlyReason    string `gorm:"type:text"`
EndedByHuaWeAPI     bool   `gorm:"not null;default:false;column:ended_by_huawei_api"`
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend ActivitySnapshot with FileSizeBytes + FileGrowthBps and update fileTicker</name>
  <files>internal/recorder/activity_watcher.go, internal/recorder/activity_watcher_test.go</files>
  <read_first>
    - internal/recorder/activity_watcher.go (entire file, focus on lines 65-77 struct definition, 166-181 Snapshot method, 315-367 fileTicker method, 40-61 internal state field block)
    - internal/recorder/activity_watcher_test.go (entire file for existing test patterns and fakeClock usage)
    - .planning/phases/25-scheduler-service-e2e-ci/25-RESEARCH.md §Code Examples "ActivitySnapshot 字段扩展"
  </read_first>
  <action>
    Add two new public fields to the `ActivitySnapshot` struct in `internal/recorder/activity_watcher.go` immediately after the existing `TotalSilenceDuration` field: `FileSizeBytes int64 \`json:"file_size_bytes"\`` and `FileGrowthBps int64 \`json:"file_growth_bps"\``. The struct comment at line 63 must be updated to mention the two new fields so future readers know they are populated by `fileTicker`.

    Add a single new mutex-protected field to the `ActivityWatcher` struct alongside `lastFileSize` (line 43): `lastFileGrowthBps int64`. This is the cached most recent growth value, refreshed every time the ticker crosses the `FileMinGrowthBPS` threshold.

    Update `Snapshot()` (lines 166-181) so the returned `ActivitySnapshot` value copies `w.lastFileSize` into `FileSizeBytes` and `w.lastFileGrowthBps` into `FileGrowthBps` while still under the same `w.mu.Lock()` critical section — do not break the existing "锁内复制保证读到一致的字段组合" invariant.

    Update `fileTicker` (lines 315-367) inside the existing `<-ticker.C` branch: when the `growthBps >= w.cfg.SmartEnd.FileMinGrowthBPS` branch is taken (currently updates `w.lastFileSize` and `w.lastFileGrowthAt`), also assign `w.lastFileGrowthBps = growthBps` BEFORE the unlock. When the threshold is NOT met, do NOT touch `w.lastFileGrowthBps` (matches the existing pattern that preserves `lastFileGrowthAt` when growth stalls). The `StatFailureThreshold` close-ended path must NOT zero `lastFileGrowthBps` (matches Pitfall 6 behaviour for `lastFileGrowthAt`).

    In `internal/recorder/activity_watcher_test.go` add a new top-level test `TestActivityWatcher_SnapshotExtension` placed after `TestSnapshot`. Use the existing `fakeClock` (or construct a fresh `ActivityWatcher` with a tmp file path) to:
    1. Set up a watcher whose `filePath` points to a `t.TempDir()`-based mkv file.
    2. Write 1 KiB, advance the fake clock by 1s, then read `Snapshot()` and assert `FileSizeBytes == 1024` and `FileGrowthBps == 0` (below the default 1024 B/s threshold).
    3. Write 16 KiB (well above `FileMinGrowthBPS * CheckIntervalS = 1024*1`), advance the fake clock by 1s, then read `Snapshot()` and assert `FileSizeBytes == 17408` and `FileGrowthBps >= 1024`.
    4. Do not call `Start()` — drive the ticker logic by calling `w.fileTicker(ctx)` with a `context.WithCancel` whose `cancel` is deferred, so the test stays deterministic without sleeping.
  </action>
  <accept_criteria>
    - `grep -n "FileSizeBytes" internal/recorder/activity_watcher.go` returns at least 3 lines (struct field, Snapshot return assignment, comment or test helper).
    - `grep -n "FileGrowthBps" internal/recorder/activity_watcher.go` returns at least 3 lines (struct field, Snapshot return assignment, fileTicker cache write).
    - `grep -n "lastFileGrowthBps" internal/recorder/activity_watcher.go` returns the new struct field declaration and at least one assignment site.
    - `go test ./internal/recorder -run TestActivityWatcher_SnapshotExtension -count=1 -v` exits 0 and reports at least 2 sub-assertion PASS lines.
    - `go vet ./internal/recorder` exits 0 with no warnings.
    - `go test ./internal/recorder -count=1` (full recorder suite, including all Phase 24 tests) still exits 0.
  </accept_criteria>
  <verify>
    <automated>go test ./internal/recorder -run TestActivityWatcher_SnapshotExtension -count=1 -v && go vet ./internal/recorder</automated>
  </verify>
  <done>ActivitySnapshot.FileSizeBytes and FileGrowthBps are populated from the fileTicker, the new unit test covers both above-threshold and below-threshold cases, and existing Phase 24 recorder tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Add UpdateTaskExtension + MarkTaskEndedEarly to VideoRecordingTaskService with audit and cfg injection</name>
  <files>internal/services/video_recording_task_service.go, internal/services/video_recording_task_service_test.go</files>
  <read_first>
    - internal/services/video_recording_task_service.go (entire file: lines 25-45 service struct + constructor, lines 1056-1073 UpdateTaskStatus for column update pattern, lines 311-318 GetTaskByID for ctx/db pattern, lines 1040-1055 GetDB returning raw *gorm.DB)
    - internal/services/audit/audit_log_service.go (lines 60-82 RecordChangeOpts + RecordChange, lines 100-106 ActionRead pattern; confirm Module + Action constants in internal/models/audit_log.go)
    - internal/models/audit_log.go (ActionUpdate / ModuleTask / StatusSuccess constants)
    - internal/models/video_recording_task.go (the 5 GORM fields, line 39-44)
    - internal/config/smart_end.go (SmartEndConfig.ExtendStepMin and MaxExtendCount)
    - internal/recorder/activity_watcher.go (ActivitySnapshot struct to import as parameter type)
    - .planning/phases/25-scheduler-service-e2e-ci/25-RESEARCH.md §Code Examples "service UpdateTaskExtension" and §Pitfall 4 (save oldCount local before Updates)
  </read_first>
  <action>
    In `internal/services/video_recording_task_service.go`:
    1. Add imports if missing: `internal/recorder` (alias `recorder`), `internal/services/audit` (alias `auditpkg`), `internal/config` (already present as `config`).
    2. Extend the `VideoRecordingTaskService` struct (line 25-32) with two new fields: `auditSvc *auditpkg.AuditLogService` and `cfg *config.Config`. Place them after `encryptor` with a `// Phase 25 AUDIT-04` doc comment.
    3. Update `NewVideoRecordingTaskService` (line 36-45) to accept variadic `auditSvc ...*auditpkg.AuditLogService` and `cfg ...*config.Config` after the existing `encryptor` variadic. Variadic position 3 = audit, position 4 = cfg; nil entries are tolerated. Preserve existing call site behaviour: callers passing nothing keep working unchanged. Do not break the Phase 19 D2 `encryptor` variadic — append after it.
    4. Add two public methods, placed after `UpdateRecordingPaths`:
       - `UpdateTaskExtension(ctx context.Context, taskID uint, deltaMin int, reason string, snap recorder.ActivitySnapshot) error`
       - `MarkTaskEndedEarly(ctx context.Context, taskID uint, reason string, byHuaWeiAPI bool, snap recorder.ActivitySnapshot) error`
    5. `UpdateTaskExtension` body:
       - `s.db.WithContext(ctx).First(&task, taskID)`; on error return it wrapped with `apperrors.ErrInternal`.
       - Guard: `if task.ExtensionCount >= s.cfg.SmartEnd.MaxExtendCount { return fmt.Errorf("extension limit %d reached for task %d: %w", s.cfg.SmartEnd.MaxExtendCount, taskID, apperrors.ErrRecordingSmartExtend) }`.
       - Save `extensionOld := task.ExtensionCount` and `newCount := extensionOld + 1` (Pitfall 4 — save before Updates to avoid post-Update value confusion).
       - Compute `newEnd := task.EndTime.Add(time.Duration(deltaMin) * time.Minute)`.
       - Call `s.db.WithContext(ctx).Model(&task).Updates(map[string]interface{}{"end_time": newEnd, "extension_count": newCount, "last_extension_reason": reason})`.
       - Assign `task.EndTime = newEnd; task.ExtensionCount = newCount` so the caller (scheduler) reads the new values.
       - Emit INFO log: `s.logger.Info("smart_extend", zap.Uint("task", taskID), zap.Int("count", newCount), zap.Time("new_end", newEnd), zap.String("reason", reason))`. Field name `task` matches the locked OBS-01 schema exactly.
       - If `s.auditSvc != nil`, build a `map[string]interface{}` payload with keys `end_time`, `extension_count`, `last_extension_reason`, `silence_since`, `last_file_growth`, `file_size_bytes`, `file_growth_bps`, `new_end_time`, `ended_by_huawei_api`, `ended_early_reason` (omitempty for last two — they remain unchanged on extension). `silence_since` and `last_file_growth` use `*time.Time` pointers (nil when zero) so JSON marshals them as `null` (Pitfall 4). Call `s.auditSvc.RecordChange(ctx, audit.RecordChangeOpts{Action: models.ActionUpdate, Module: models.ModuleTask, Resource: "video_recording_task", ResourceID: &taskID, OldData: oldMap, NewData: newMap})`.
       - Return nil on success.
    6. `MarkTaskEndedEarly` body:
       - `s.db.WithContext(ctx).First(&task, taskID)`; on error return it wrapped.
       - `Updates(map[string]interface{}{"ended_early": true, "ended_early_reason": reason, "ended_by_huawei_api": byHuaWeiAPI})`.
       - Update in-memory `task` for the caller.
       - Emit INFO log: `s.logger.Info("smart_early_end", zap.Uint("task", taskID), zap.String("reason", reason), zap.Any("snapshot", snapshotMap))`. Field name `task` matches locked OBS-02 schema.
       - If `s.auditSvc != nil`, call `RecordChange` with the same 10-key payload shape but `NewData` includes `ended_early: true, ended_by_huawei_api, ended_early_reason`. `OldData` captures the pre-update values for diff.
       - Return nil on success.

    In `internal/services/video_recording_task_service_test.go`:
    1. Add a new test `TestUpdateTaskExtension_Exists` that simply asserts the method exists with the documented signature using `reflect.TypeOf((*VideoRecordingTaskService)(nil)).Elem().MethodByName("UpdateTaskExtension")` and `MethodByName("MarkTaskEndedEarly")` so future removals break the build (same pattern as `TestConversionService_InterfaceCompilationCheck` in `video_scheduler_test.go`).
    2. Add `TestUpdateTaskExtension_AuditSnapshot` that builds a fresh `newTestDB(t)`, creates a `VideoRecordingTask` row with `ExtensionCount=0`, constructs a `VideoRecordingTaskService` with a fake `AuditLogService` (or nil and assert no audit error), calls `UpdateTaskExtension(ctx, taskID, 30, "active_meeting", ActivitySnapshot{FileSizeBytes: 1024, FileGrowthBps: 2048, LastFileGrowthAt: fixedTime})`, then reads back the task and asserts `ExtensionCount==1`, `LastExtensionReason=="active_meeting"`, `EndTime` advanced by 30min, and that a `audit_logs` row exists with `ResourceID` pointing at the task and `NewData` JSON containing the 6 AUDIT-02 snapshot fields. If `auditSvc` is nil in this test, the assertion is reduced to the GORM column updates only — document which variant is being tested.
    3. Add `TestUpdateTaskExtension_MaxLimit` that creates a task with `ExtensionCount=4` (already at the default cap), calls `UpdateTaskExtension`, and asserts the returned error wraps `apperrors.ErrRecordingSmartExtend` via `errors.Is`.
    4. Add `TestMarkTaskEndedEarly_HuaweiSignal` (reason="huawei_state_empty", byHuaWeiAPI=true) and `TestMarkTaskEndedEarly_BothSilenceAndStall` (reason="both_silence_and_stall", byHuaWeiAPI=false) — both assert the 3 GORM columns and the audit log row ResourceID. Both tests use a fresh `newTestDB(t)` plus the same `VideoRecordingTaskService` constructor invocation that the existing tests use.

    NOTE: `TestServiceEntrypoint_OnlyPath` is NOT defined in this plan. Sole ownership belongs to plan 04 (25-04-PLAN.md Task 1 step 1) to avoid duplicate test function declarations across plans. This plan delivers the service methods + the AUDIT-02/03 happy-path tests; plan 04 owns the antipattern grep test.
  </action>
  <accept_criteria>
    - `grep -n "UpdateTaskExtension" internal/services/video_recording_task_service.go` returns at least 4 lines (method definition, doc comment, log call, audit call sites).
    - `grep -n "MarkTaskEndedEarly" internal/services/video_recording_task_service.go` returns at least 4 lines.
    - `grep -n "auditSvc" internal/services/video_recording_task_service.go` returns at least 4 lines (struct field, constructor, 2 nil-guards).
    - `grep -n "s.cfg.SmartEnd.MaxExtendCount" internal/services/video_recording_task_service.go` returns the guard and the error wrap.
    - `go build ./...` exits 0.
    - `go test ./internal/services -run 'TestUpdateTaskExtension_|TestMarkTaskEndedEarly_' -count=1 -v` exits 0 with at least 4 PASS lines and no FAIL.
    - `go test ./internal/services -count=1` (full existing service test suite) still exits 0.
    - `go vet ./internal/services` exits 0 with no warnings.
  </accept_criteria>
  <verify>
    <automated>go build ./... && go test ./internal/services -run 'TestUpdateTaskExtension_|TestMarkTaskEndedEarly_' -count=1 -v && go vet ./internal/services</automated>
  </verify>
  <done>UpdateTaskExtension and MarkTaskEndedEarly are the only entry points for smart-end GORM writes + audit logs; the max-extend guard wraps `ErrRecordingSmartExtend`; the new tests cover the happy path, max-extend rejection, and both H/A+B reason variants without breaking any existing service test. The antipattern grep test (`TestServiceEntrypoint_OnlyPath`) is deferred to plan 04 where it has sole ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service.UpdateTaskExtension caller → DB | caller passes raw deltaMin + reason; service validates cap and writes 3 columns |
| service.MarkTaskEndedEarly caller → DB | caller passes reason + bool; service writes 3 columns and audit log |
| activity_watcher.Snapshot → service | value copy of internal state; no pointer aliasing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | UpdateTaskExtension | mitigate | deltaMin is added once and atomically with ExtensionCount; the cap guard rejects any call that would push ExtensionCount past MaxExtendCount |
| T-25-02 | Tampering | MarkTaskEndedEarly | mitigate | only writes the 3 booleans/strings; cannot modify Status, EndTime, or other unrelated columns |
| T-25-03 | Information Disclosure | audit log snapshot | mitigate | Snapshot contains task_id, file sizes, timestamps; no password/secret paths; reuse existing Sanitizer inside RecordChange |
| T-25-04 | Denial of Service | audit queue saturation | mitigate | auditSvc is optional; nil-guard prevents the service from blocking when audit infrastructure is disabled (test runs without audit) |
| T-25-05 | Elevation of Privilege | scheduler calling service.UpdateTaskExtension for manual extend | mitigate | service does not trust caller — re-reads ExtensionCount and re-checks MaxExtendCount; SCHED-04 manual-update path goes through `taskUpdateChans` only, never calls UpdateTaskExtension |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 25 introduces zero new packages; go.mod unchanged from Phase 24/26 |
</threat_model>

<verification>
- `go build ./...` exits 0 across the whole module.
- `go vet ./internal/recorder ./internal/services` exits 0 with no warnings.
- `go test ./internal/recorder -run TestActivityWatcher_SnapshotExtension -count=1 -v` exits 0.
- `go test ./internal/services -run 'TestUpdateTaskExtension_|TestMarkTaskEndedEarly_' -count=1 -v` exits 0.
- `go test ./internal/recorder ./internal/services -count=1` (full package suites) still exits 0; no regression in Phase 23/24 tests.
- `git diff --stat main..HEAD` (after execution) shows exactly the four files in `files_modified` plus their test files; no incidental churn.
</verification>

<success_criteria>
- The ActivitySnapshot FileSizeBytes / FileGrowthBps fields exist, are populated by fileTicker, and have a passing unit test that demonstrates both above- and below-threshold growth.
- VideoRecordingTaskService exposes UpdateTaskExtension and MarkTaskEndedEarly; both return wrapped sentinel errors when their preconditions fail; both call AuditLogService.RecordChange when s.auditSvc is non-nil and gracefully skip when nil.
- The 5 GORM smart-end fields are written atomically with the audit log row in a single UpdateTaskExtension/MarkTaskEndedEarly call (no two-step path that could leave the audit log out of sync).
- The max-extend guard returns an error that wraps `apperrors.ErrRecordingSmartExtend`, allowing the scheduler to route the rejection into the EXTEND-02 force-end branch in plan 02.
- Existing recorder and service tests stay green; no live code outside the four files in `files_modified` is modified.
- `TestServiceEntrypoint_OnlyPath` is NOT defined in this plan; sole ownership belongs to plan 04 (single-definition rule to prevent `go test` duplicate-function-name build failure).
</success_criteria>

<output>
Create `.planning/phases/25-scheduler-service-e2e-ci/25-01-SUMMARY.md` when done.
</output>
