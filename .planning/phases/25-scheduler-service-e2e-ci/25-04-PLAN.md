---
phase: 25
plan: 04
type: execute
wave: 1
depends_on:
  - 25-01
  - 25-02
  - 25-03
files_modified:
  - internal/services/video_recording_task_service_test.go
  - internal/scheduler/video_scheduler_test.go
  - .planning/REQUIREMENTS.md
  - .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "All 20 Phase 25 REQ-IDs are covered by at least one passing automated test"
    - "go test -race ./... exits 0 across the entire module"
    - "go vet ./... exits 0 with no warnings"
    - "go build ./... exits 0"
    - "REQUIREMENTS.md is updated to mark all 20 Phase 25 REQ-IDs as Complete"
    - "25-VALIDATION.md is updated with nyquist_compliant: true and wave_0_complete: true after all assertions pass"
  artifacts:
    - path: "internal/services/video_recording_task_service_test.go"
      provides: "Audit snapshot golden JSON test for UpdateTaskExtension + MarkTaskEndedEarly + TestServiceEntrypoint_OnlyPath (sole owner)"
      contains: "TestUpdateTaskExtension_AuditSnapshot"
    - path: "internal/scheduler/video_scheduler_test.go"
      provides: "All 7 E2E subtests passing with race detector + TestScheduler_DoesNotDirectlyUpdateTask (sole owner)"
      contains: "TestMonitorTask_"
    - path: ".planning/REQUIREMENTS.md"
      provides: "20 Phase 25 REQ-IDs marked complete with evidence pointers"
      contains: "Complete"
    - path: ".planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md"
      provides: "nyquist_compliant: true and wave_0_complete: true frontmatter flipped"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "all 4 plans (01/02/03) code"
      to: "this plan's verification"
      via: "go test -race ./... aggregates the 20 REQ-ID coverage from each plan"
      pattern: "go test -race"
    - from: "25-VALIDATION.md per-task verification map"
      to: "this plan's flipped frontmatter"
      via: "all 20 tasks must be checked [x] before flipping nyquist_compliant"
      pattern: "nyquist_compliant: true"
---

<objective>
Close the Phase 25 verification loop by extending the test suites to cover the audit-snapshot golden JSON assertions, validate the scheduler's "no direct db.Update" invariant at the test level (sole ownership of both antipattern tests), run the full module under the race detector, and flip the validation frontmatter once every requirement is covered.

Purpose: Plan 04 is the Nyquist gate. Plans 01-03 deliver the code and the per-package tests; plan 04 proves the entire phase is correct, race-free, and aligned with REQUIREMENTS.md. Until this plan runs, `nyquist_compliant: true` cannot be flipped.

Output:
- `internal/services/video_recording_task_service_test.go` extended with `TestUpdateTaskExtension_AuditSnapshot` golden JSON assertion, `TestMarkTaskEndedEarly_AuditSnapshot` golden JSON assertion, `TestAuditSnapshot_ZeroTimeOmitsSilence`, and the formal `TestServiceEntrypoint_OnlyPath` check (defined here for the first time; sole owner — no stub exists in plan 01).
- `internal/scheduler/video_scheduler_test.go` already has 7 E2E subtests from plan 02; this plan re-runs them under `-race`, adds `TestScheduler_RaceDetectorFullSweep` as a meta-test, and defines `TestScheduler_DoesNotDirectlyUpdateTask` (sole owner — no definition in plan 02).
- `.planning/REQUIREMENTS.md` Traceability table updated: all 20 Phase 25 REQ-IDs (SCHED-01..04, EXTEND-01/02, EARLY-01..04, AUDIT-02/03/04, CFG-03/04, OBS-01..05) marked Complete with commit/plan references.
- `.planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md` frontmatter flipped from `nyquist_compliant: false` / `wave_0_complete: false` to `nyquist_compliant: true` / `wave_0_complete: true`, the per-task verification map's 20 checkboxes flipped to `[x]`, the Validation Sign-Off section flipped to all checked, and the Approval line flipped from "pending" to "approved" with a body-pointer to the per-plan SUMMARYs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md
@.planning/phases/25-scheduler-service-e2e-ci/25-01-PLAN.md
@.planning/phases/25-scheduler-service-e2e-ci/25-02-PLAN.md
@.planning/phases/25-scheduler-service-e2e-ci/25-03-PLAN.md
@.planning/phases/25-scheduler-service-e2e-ci/25-01-SUMMARY.md
@.planning/phases/25-scheduler-service-e2e-ci/25-02-SUMMARY.md
@.planning/phases/25-scheduler-service-e2e-ci/25-03-SUMMARY.md
@internal/services/video_recording_task_service.go
@internal/services/video_recording_task_service_test.go
@internal/scheduler/video_scheduler.go
@internal/scheduler/video_scheduler_test.go
@internal/observability/smart_end_metrics.go
@internal/observability/smart_end_metrics_test.go
</context>

<interfaces>
From the executed plans 01-03 (re-confirmed for plan 04):
- `services.VideoRecordingTaskService.UpdateTaskExtension(ctx, taskID, deltaMin, reason, snapshot)` returns a wrapped `ErrRecordingSmartExtend` when at cap.
- `services.VideoRecordingTaskService.MarkTaskEndedEarly(ctx, taskID, reason, byHuaWeiAPI, snapshot)` returns nil on success.
- `audit.AuditLogService.RecordChange(ctx, RecordChangeOpts{...})` persists rows to `audit_logs` (table: `models.AuditLog` with columns `NewData`, `OldData`, `DiffData`, `ResourceID`, etc.).
- `observability.RecordSmartExtend / RecordSmartEarlyEnd / RecordWatcherDegraded` are pure atomic.Add(1).
- `scheduler.VideoSimpleScheduler.monitorTask` is private; tests in plan 02 already invoke it directly via `go scheduler.monitorTask(ctx, task)`.

From internal/models/audit_log.go (the audit log table structure):
- `AuditLog.OldData`, `NewData`, `DiffData` are `gorm:type:text` (stored as JSON string).
- `AuditLog.ResourceID` is `*uint`.
- `AuditLog.Module` is `ModuleTask = "task"`; `AuditLog.Action` is `ActionUpdate = "update"`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define TestServiceEntrypoint_OnlyPath (service) + TestScheduler_DoesNotDirectlyUpdateTask (scheduler) — sole owner; add audit snapshot golden JSON tests + race detector meta-test</name>
  <files>internal/services/video_recording_task_service_test.go, internal/scheduler/video_scheduler_test.go</files>
  <read_first>
    - internal/services/video_recording_task_service_test.go (entire file; verify that `TestServiceEntrypoint_OnlyPath` does NOT yet exist here — sole ownership is plan 04)
    - internal/services/video_recording_task_service.go (the UpdateTaskExtension / MarkTaskEndedEarly audit log payloads from plan 01 — read the actual `OldData` and `NewData` map key names)
    - internal/models/audit_log.go (AuditLog struct fields: NewData, OldData, ResourceID, Module, Action)
    - internal/services/audit/audit_log_service.go (LogOperation body line 168-226 to confirm OldData/NewData JSON shape: OldData and NewData are stored as JSON-encoded strings of the input map)
    - .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md §Per-Task Verification Map rows 25-02-11, 25-02-12, 25-02-13
    - .planning/phases/25-scheduler-service-e2e-ci/25-RESEARCH.md §phase_requirements AUDIT-02/03/04 rows
  </read_first>
  <action>
    In `internal/services/video_recording_task_service_test.go`:

    1. Define `TestServiceEntrypoint_OnlyPath` for the first time in plan 04 (no stub in plan 01 — single-definition rule to prevent `go test` duplicate-function-name build failure). Use `runtime.Caller(0)` to get the scheduler source path (relative to the test's working directory, the scheduler is at `../../internal/scheduler/video_scheduler.go`). Read the file with `os.ReadFile`. Assert that the content does NOT contain the substring `s.taskService.GetDB().Model(&models.VideoRecordingTask{}).Updates(`. Also assert the content does NOT contain `s.taskService.GetDB().Model(&task).Updates(` (the scheduler might use a local variable name; the antipattern is "scheduler code calls GetDB().Model(...).Updates(...) on a task"). Add a docstring that references AUDIT-04: "AUDIT-04 requires the service to be the only writer of the 5 smart-end columns; this test fails if the scheduler ever bypasses the service."

    2. Add `TestUpdateTaskExtension_AuditSnapshot` (the test was scaffolded in plan 01 with a stripped-down version; this plan fleshes it out with the full golden JSON assertion). Steps:
       - Build a fresh in-memory DB via the existing `newTestDB(t)` helper.
       - Create a `VideoRecordingTask` row with known initial state: `ExtensionCount=0`, `EndTime=now+1h`, `LastExtensionReason=""`.
       - Build a `VideoRecordingTaskService` with the existing `NewVideoRecordingTaskService(db, zap.NewNop(), nil)` constructor and then attach a real `*audit.AuditLogService` via the new auditSvc variadic argument: `NewVideoRecordingTaskService(db, zap.NewNop(), nil, audit.NewAuditLogService(db, zap.NewNop()), cfg)`. Use a `cfg *config.Config` value that sets `SmartEnd.MaxExtendCount=4` and `SmartEnd.ExtendStepMin=30`.
       - Build a `recorder.ActivitySnapshot` literal with: `FileSizeBytes=1024`, `FileGrowthBps=2048`, `LastFileGrowthAt=time.Date(2026,8,6,12,0,0,0,time.UTC)`, `SilenceSince=time.Time{}` (zero, so JSON should marshal as null when wrapped via the audit map's nullable pointer), `LastHuaWeiStateEmpty=false`.
       - Call `UpdateTaskExtension(ctx, taskID, 30, "active_meeting", snap)`. Assert returns nil.
       - Reload the task from the DB and assert: `ExtensionCount == 1`, `LastExtensionReason == "active_meeting"`, `EndTime` advanced by exactly 30 minutes, `EndedEarly == false` (unchanged).
       - Query the `audit_logs` table for the most recent row with `resource_id = taskID` and `module = 'task'`. Assert exactly one such row exists. Read `NewData` (JSON string), parse it, and assert the parsed map contains keys: `end_time`, `extension_count`, `last_extension_reason`, `silence_since`, `last_file_growth`, `file_size_bytes`, `file_growth_bps`, `new_end_time`. Assert `extension_count == 1` (json.Number or float64 — coerce). Assert `file_size_bytes == 1024`. Assert `file_growth_bps == 2048`. Assert `last_file_growth` is the same ISO 8601 instant (allow ±1s drift). Assert `silence_since` is JSON `null` (the plan 01 implementation uses a `*time.Time` pointer; verify by parsing the raw bytes and looking for the substring `"silence_since":null` or by checking the unmarshalled value is nil).
       - Verify the `OldData` map contains the pre-update values: `end_time` is the original 1h-future time, `extension_count == 0`.

    3. Add `TestMarkTaskEndedEarly_AuditSnapshot` mirroring the above but for the early-end path:
       - Same DB setup; task starts in pending state.
       - Call `MarkTaskEndedEarly(ctx, taskID, "huawei_state_empty", true, snap)`. Assert nil return.
       - Reload: `EndedEarly == true`, `EndedEarlyReason == "huawei_state_empty"`, `EndedByHuaWeAPI == true`. `ExtensionCount` UNCHANGED.
       - Assert one `audit_logs` row exists with `module='task'` and `action='update'`; the `NewData` JSON has `ended_early: true`, `ended_early_reason: "huawei_state_empty"`, `ended_by_huawei_api: true`, plus the snapshot fields.
       - Run the same path with `reason="both_silence_and_stall", byHuaWeiAPI=false`. Assert `EndedByHuaWeAPI == false` and `EndedEarlyReason == "both_silence_and_stall"`.

    4. Add a final guard test `TestAuditSnapshot_ZeroTimeOmitsSilence` that explicitly verifies the `time.Time{}` zero-value is serialized as `null` rather than `"0001-01-01T00:00:00Z"` in the audit JSON. Use the same service + a fresh task; call `UpdateTaskExtension` with a `snap` whose `SilenceSince` is `time.Time{}`; parse the audit row's `NewData` JSON; assert `parsed["silence_since"] == nil` (Go's `json.Unmarshal` puts `null` into a `*time.Time` or `interface{}` field as `nil`).

    In `internal/scheduler/video_scheduler_test.go`:

    5. Add `TestScheduler_RaceDetectorFullSweep` (top-level test, not a subtest). It runs the existing 7 E2E subtests from plan 02 in sequence under the race detector. Implementation: invoke `t.Run("TripleSelect", func(t *testing.T) { TestMonitorTask_TripleSelect(t) })` for each of the 7; the test fails if any subtest fails or panics. This is a meta-test that catches race regressions even if the individual subtests pass in isolation. The body of this test should also call `runtime.GC()` once at the end to encourage the race detector to flush pending goroutine reports.

    6. Define `TestScheduler_DoesNotDirectlyUpdateTask` for the first time in plan 04 (no definition in plan 02 — single-definition rule). The test reads the scheduler source file and asserts the antipattern substring is absent. This duplicates the service test (TestServiceEntrypoint_OnlyPath) but is a defense-in-depth check from the scheduler package perspective.
  </action>
  <accept_criteria>
    - `grep -n "TestUpdateTaskExtension_AuditSnapshot" internal/services/video_recording_task_service_test.go` returns 1 line.
    - `grep -n "TestMarkTaskEndedEarly_AuditSnapshot" internal/services/video_recording_task_service_test.go` returns 1 line.
    - `grep -n "TestAuditSnapshot_ZeroTimeOmitsSilence" internal/services/video_recording_task_service_test.go` returns 1 line.
    - `grep -n "TestServiceEntrypoint_OnlyPath" internal/services/video_recording_task_service_test.go` returns 1 line (sole owner — first definition in plan 04).
    - `grep -n "TestScheduler_RaceDetectorFullSweep" internal/scheduler/video_scheduler_test.go` returns 1 line.
    - `grep -n "TestScheduler_DoesNotDirectlyUpdateTask" internal/scheduler/video_scheduler_test.go` returns 1 line (sole owner — first definition in plan 04).
    - `go test ./internal/services -run 'TestUpdateTaskExtension_AuditSnapshot|TestMarkTaskEndedEarly_AuditSnapshot|TestAuditSnapshot_ZeroTimeOmitsSilence|TestServiceEntrypoint_OnlyPath' -count=1 -v` exits 0 with at least 4 PASS lines.
    - `go test ./internal/scheduler -run 'TestScheduler_RaceDetectorFullSweep|TestScheduler_DoesNotDirectlyUpdateTask' -count=1 -v` exits 0 with at least 2 PASS lines.
    - `go test ./... -count=1` exits 0 across the full module.
    - `go test ./... -race` exits 0 across the full module.
  </accept_criteria>
  <verify>
    <automated>go test ./internal/services -run 'TestUpdateTaskExtension_AuditSnapshot|TestMarkTaskEndedEarly_AuditSnapshot|TestAuditSnapshot_ZeroTimeOmitsSilence|TestServiceEntrypoint_OnlyPath' -count=1 -v && go test ./internal/scheduler -run 'TestScheduler_RaceDetectorFullSweep|TestScheduler_DoesNotDirectlyUpdateTask' -count=1 -v && go test ./... -race -count=1</automated>
  </verify>
  <done>Audit snapshot golden JSON tests pass for both UpdateTaskExtension and MarkTaskEndedEarly, including the zero-time-null serialization guard; the scheduler no-direct-db-Update invariant is enforced by both the service-side and scheduler-side tests (each defined for the first time in plan 04); the full module passes under the race detector.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md Traceability + flip 25-VALIDATION.md frontmatter to nyquist_compliant: true</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the existing Traceability table; only the rows for Phase 25 REQ-IDs need updating)
    - .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md (the entire file; frontmatter is lines 1-7; Per-Task Verification Map is lines 38-63; Wave 0 Requirements is lines 67-117; Validation Sign-Off is lines 121-127)
    - .planning/phases/25-scheduler-service-e2e-ci/25-01-SUMMARY.md (commit hashes from plan 01)
    - .planning/phases/25-scheduler-service-e2e-ci/25-02-SUMMARY.md (commit hashes from plan 02)
    - .planning/phases/25-scheduler-service-e2e-ci/25-03-SUMMARY.md (commit hashes from plan 03)
    - .planning/phases/25-scheduler-service-e2e-ci/25-04-SUMMARY.md (this plan's own commits, written below)
    - .planning/STATE.md (per project convention, .planning/ is gitignored, so REQUIREMENTS.md and VALIDATION.md must be committed with `git add -f`)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, update the Phase 25 rows in the Traceability table. The current state is "Pending" for all 20 REQ-IDs (SCHED-01..04, EXTEND-01/02, EARLY-01..04, AUDIT-02/03/04, CFG-03/04, OBS-01..05). For each row, set `Status` to `Complete` and add a brief `Notes` clause pointing at the plan that delivered it. Example:
    - `| SCHED-01 | Phase 25 | Complete | 25-02-PLAN.md (monitorTask three-signal select); TestMonitorTask_TripleSelect PASS under -race |`
    - `| SCHED-02 | Phase 25 | Complete | 25-02-PLAN.md (handleEndTimeReached: IsActive -> UpdateTaskExtension, else completeTask); TestMonitorTask_OnTimerActive_Extends PASS |`
    - `| SCHED-03 | Phase 25 | Complete | 25-02-PLAN.md (case <-taskEndedCh placed first in select); TestMonitorTask_TaskEnded_PreemptsTimer PASS |`
    - `| SCHED-04 | Phase 25 | Complete | 25-02-PLAN.md (manual updateChan path only reassigns endTime local, never calls UpdateTaskExtension); TestMonitorTask_ManualUpdateDoesNotResetCount PASS |`
    - `| EXTEND-01 | Phase 25 | Complete | 25-01-PLAN.md (service UpdateTaskExtension cap guard wraps ErrRecordingSmartExtend); TestUpdateTaskExtension_MaxLimit PASS |`
    - `| EXTEND-02 | Phase 25 | Complete | 25-02-PLAN.md (handleEndTimeReached max_extend_reached branch emits OBS-03 WARN and completeTask); TestMonitorTask_MaxExtendReached PASS |`
    - `| EARLY-01 | Phase 25 | Complete | 25-01+25-02 (MarkTaskEndedEarly + monitorTask <-taskEndedCh case with LastHuaWeiStateEmpty=true); TestMarkTaskEndedEarly_HuaweiSignal PASS |`
    - `| EARLY-02 | Phase 25 | Complete | 25-01+25-02 (MarkTaskEndedEarly with reason=both_silence_and_stall, byHuaWeiAPI=false); TestMarkTaskEndedEarly_BothSilenceAndStall PASS |`
    - `| EARLY-03 | Phase 25 | Complete | 25-02 (case <-taskEndedCh first in select); TestMonitorTask_TaskEnded_PreemptsTimer PASS |`
    - `| EARLY-04 | Phase 25 | Complete | 25-02 (mergeWatchers fan-in helper + WatcherChannels interface); TestMonitorTask_MultiInput_AnyEndsAll PASS |`
    - `| AUDIT-02 | Phase 25 | Complete | 25-01+25-04 (UpdateTaskExtension writes 6 snapshot fields to RecordChange); TestUpdateTaskExtension_AuditSnapshot PASS |`
    - `| AUDIT-03 | Phase 25 | Complete | 25-01+25-04 (MarkTaskEndedEarly writes 5 fields); TestMarkTaskEndedEarly_AuditSnapshot PASS |`
    - `| AUDIT-04 | Phase 25 | Complete | 25-01+25-04 (service layer is single entry point; UpdateTaskExtension + MarkTaskEndedEarly; TestServiceEntrypoint_OnlyPath defined in plan 04 sole owner) |`
    - `| CFG-03 | Phase 25 | Complete | 25-02 (monitorTask !cfg.SmartEnd.Enabled gate routes to monitorTaskEndTimeOnly); TestMonitorTask_SmartEndDisabled PASS |`
    - `| CFG-04 | Phase 25 | Complete | Phase 24 (huaweiPoller 入口直接 return on HuaweiEnabled=false; Phase 25 没有改 watcher 接线) |`
    - `| OBS-01 | Phase 25 | Complete | 25-01+25-03 (UpdateTaskExtension emits INFO smart_extend with task/count/new_end/reason; RecordSmartExtend atomic) |`
    - `| OBS-02 | Phase 25 | Complete | 25-01+25-03 (MarkTaskEndedEarly emits INFO smart_early_end with task/reason/snapshot; RecordSmartEarlyEnd atomic) |`
    - `| OBS-03 | Phase 25 | Complete | 25-02+25-03 (handleEndTimeReached emits WARN max_extend_reached with task_id/force_end) |`
    - `| OBS-04 | Phase 25 | Complete | 25-03 (RecordWatcherDegraded wired to all 3 Phase 24 activity_watcher_degraded branches) |`
    - `| OBS-05 | Phase 25 | Complete | 25-03 (smart_end_metrics.go: 3 atomic.Int64 + Record* accessors; prometheus client_golang explicitly absent) |`

    Also update the Coverage section near the bottom of REQUIREMENTS.md if it includes a phase-25 specific breakdown (the current file shows `**Phase 25**: 20 reqs`); that line stays the same (it counts the reqs, not their status).

    In `.planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md`:

    1. Flip the frontmatter (lines 1-7): `nyquist_compliant: false` -> `nyquist_compliant: true`; `wave_0_complete: false` -> `wave_0_complete: true`. Do NOT change the `status: draft` line -- Phase 25 itself is still in the "Plan complete / awaiting execution" state at this point, and the orchestrator will flip `status` after the phase verification gate. Per the project precedent (phase 22 SUMMARY.md says "Preserve status draft, nyquist_compliant false" for retro-fitted VALIDATION.md, and flip ONLY after live evidence exists), the file we are writing IS the live evidence, so `nyquist_compliant` flips now but `status: draft` is allowed to remain until the phase execution lands.

    2. Flip the 20 status checkboxes in the Per-Task Verification Map (lines 39-63). Each row's `Status` column currently has `⬜ pending`; flip to `✅ green`. The format string is `| {TASK-ID} | {PLAN} | {WAVE} | {REQ} | -- | {BEHAVIOR} | {TYPE} | {CMD} | {EXISTS?} | {STATUS} |`. Status values: `⬜ pending` -> `✅ green`. For the two rows that are "Phase 24" (`CFG-04`, `OBS-04`, also `EXTEND-03` which is Phase 24 not 25), the existing status was `✅ green` already; do not touch.

    3. Flip the Wave 0 Requirements checkboxes (lines 67-117). There are 12 checkbox items across 4 plan sections. Each is a Markdown checkbox `[ ]`; flip to `[x]`. For items that are "Plan 03 -- observability" and reference Phase 24 work (item 25-03-04 OBS-04 already-green), the checkbox was already `[x]` from the original draft; verify and leave alone.

    4. Flip the Validation Sign-Off section checkboxes (lines 121-127). Six items, all `[ ]`; flip to `[x]`.

    5. Flip the Approval line (line 127): `**Approval:** pending` -> `**Approval:** approved (Phase 25 plans 01-04 all executed; go test -race ./... exits 0; all 20 REQ-IDs marked Complete in REQUIREMENTS.md; see 25-01-SUMMARY.md through 25-04-SUMMARY.md for per-plan evidence pointers)`.

    6. After writing the file changes, stage them with `git add -f .planning/REQUIREMENTS.md .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md` (per project memory `.planning/` is gitignored; `git add -f` is required). Commit with message `docs(25-04): mark Phase 25 nyquist_compliant + REQUIREMENTS.md Complete (20/20 REQ-IDs)`. Do NOT commit the per-plan SUMMARY.md files in this commit -- they are committed by the execute-phase orchestrator after each plan.

    7. Finally, run `go test -race ./...` one more time and capture the final pass output in the SUMMARY for this plan. The output goes into a new "## Final Verification" section that records: total tests run, total tests passed, race detector findings (should be 0), build time, vet findings (should be 0).
  </action>
  <accept_criteria>
    - `grep -c "Complete" .planning/REQUIREMENTS.md` returns at least 26 (5 SEC-003a + 1 v1.x rows + 20 Phase 25) -- the exact count may vary, but the 20 Phase 25 rows are present.
    - `grep -n "nyquist_compliant" .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md` returns 1 line with value `true`.
    - `grep -n "wave_0_complete" .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md` returns 1 line with value `true`.
    - `grep -c "\[x\]" .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md` returns at least 20 (12 Wave 0 + 6 Sign-Off + 2 misc from prior plans).
    - `grep -n "Approval:" .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md` returns 1 line whose value starts with `approved`.
    - `git log --oneline -1` shows the docs(25-04) commit at HEAD.
    - `go test -race ./... -count=1` exits 0 and the final SUMMARY records the pass.
    - `go vet ./...` exits 0.
  </accept_criteria>
  <verify>
    <automated>grep -n 'nyquist_compliant\|wave_0_complete\|Approval:' .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md && go test -race ./... -count=1 && go vet ./...</automated>
  </verify>
  <done>REQUIREMENTS.md Traceability table has all 20 Phase 25 REQ-IDs marked Complete with evidence pointers; 25-VALIDATION.md is flipped to nyquist_compliant: true and wave_0_complete: true; the per-task map, wave 0 checkboxes, and sign-off checkboxes are all green; the docs(25-04) commit is the current HEAD; go test -race ./... exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> source file | tests read scheduler source via runtime.Caller + os.ReadFile; no code execution path crosses |
| audit log JSON -> test assertion | service marshals interface{} to JSON; tests assert exact key presence and zero-time omitempty behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-17 | Tampering | audit log row spoofed by test | mitigate | the test inserts a real audit row via the real AuditLogService path; no shortcut mocking of the audit table |
| T-25-18 | Information Disclosure | Test reads scheduler source | accept | source file is in the same repository; no secret material in the scheduler file |
| T-25-19 | Denial of Service | TestScheduler_RaceDetectorFullSweep exhausts CI time | accept | the 7 subtests each complete in <1s; the meta-test adds <10s total wall time on top of the 7 |
| T-25-20 | Tampering | REQUIREMENTS.md Traceability row edited to fake Complete | mitigate | 25-04 SUMMARY records the actual commit hashes; the orchestrator's `/gsd:verify-phase` re-runs the test commands and rejects if the tests don't pass |
| T-25-21 | Information Disclosure | golden JSON literal contains a real production timestamp | accept | the test fixture uses `time.Date(2026,8,6,12,0,0,0,time.UTC)` (a deterministic future date); no real user data is referenced |
| T-25-22 | Tampering | duplicate test definition across plans | mitigate | sole-ownership rule: TestServiceEntrypoint_OnlyPath and TestScheduler_DoesNotDirectlyUpdateTask are defined only in plan 04; plans 01 and 02 explicitly do not define them (single-definition rule prevents `go test` duplicate-function-name build failure) |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | plan 04 introduces zero new packages; go.mod unchanged; the test-only dependency is the existing `testify/assert` already in go.sum |
</threat_model>

<verification>
- `go build ./...` exits 0.
- `go vet ./...` exits 0.
- `go test ./internal/services -run 'TestUpdateTaskExtension_AuditSnapshot|TestMarkTaskEndedEarly_AuditSnapshot|TestAuditSnapshot_ZeroTimeOmitsSilence|TestServiceEntrypoint_OnlyPath' -count=1 -v` exits 0 with at least 4 PASS lines.
- `go test ./internal/scheduler -run 'TestScheduler_RaceDetectorFullSweep|TestScheduler_DoesNotDirectlyUpdateTask' -count=1 -v` exits 0 with at least 2 PASS lines.
- `go test ./... -race -count=1` exits 0 with the race detector reporting 0 data races.
- `grep -c "Complete" .planning/REQUIREMENTS.md` returns at least 26 (the 20 Phase 25 rows + the 5 SEC-003a rows + 1+ prior-milestone rows).
- `grep -n "nyquist_compliant" .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md` shows `true`.
- `grep -c "\[x\]" .planning/phases/25-scheduler-service-e2e-ci/25-VALIDATION.md` returns at least 20.
- `git log --oneline -1` shows the docs(25-04) commit at HEAD.
</verification>

<success_criteria>
- All 20 Phase 25 REQ-IDs are marked Complete in REQUIREMENTS.md with evidence pointers to the plan that delivered them.
- 25-VALIDATION.md is flipped to `nyquist_compliant: true` and `wave_0_complete: true`; the per-task map, wave 0 checkboxes, and sign-off checkboxes are all flipped to green; the Approval line is `approved` with a body-pointer to the per-plan SUMMARYs.
- The audit snapshot golden JSON tests prove the 6-field shape (UpdateTaskExtension) and 5-field shape (MarkTaskEndedEarly), with the zero-time-omitempty guard catching the Pitfall 4 trap.
- The scheduler no-direct-db-Update invariant is enforced by both the service-side test (TestServiceEntrypoint_OnlyPath) and scheduler-side test (TestScheduler_DoesNotDirectlyUpdateTask), with sole ownership in plan 04 (no duplicate-definition across plans).
- The full module passes `go test -race ./...` cleanly with 0 race findings.
- The phase is ready for `/gsd:execute-phase 25` and then `/gsd:verify-phase 25`.
</success_criteria>

<output>
Create `.planning/phases/25-scheduler-service-e2e-ci/25-04-SUMMARY.md` when done. Include a "## Final Verification" section that records: `go test -race ./...` final pass output, the 4 commit hashes produced by this plan, the flipped VALIDATION.md frontmatter, and the 20 REQ-ID completion evidence.
</output>
