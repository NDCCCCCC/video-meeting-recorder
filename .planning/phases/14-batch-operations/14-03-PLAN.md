---
phase: 14-batch-operations
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - frontend/src/pages/files/index.tsx
autonomous: false
requirements:
  - D-02
  - D-03
  - D-04
  - D-08
  - D-09
  - D-10
user_setup: []
must_haves:
  truths:
    - 文件列表页显示批量操作按钮（批量下载、批量转录）
    - 用户可以通过复选框选择多个文件
    - 点击批量下载按钮触发ZIP下载
    - 点击批量转录按钮弹出配置对话框
    - 配置对话框包含转录参数选择
    - 确认批量转录后显示成功/失败统计
    - 大量文件或大文件时显示警告提示
  artifacts:
    - path: "frontend/src/pages/files/index.tsx"
      provides: "批量操作UI"
      contains: "批量下载按钮"
      contains: "批量转录按钮"
      contains: "批量转录配置对话框"
  key_links:
    - from: "批量下载按钮"
      to: "frontend/src/api/video-file.ts"
      via: "batchDownloadFiles"
      pattern: "onClick.*batchDownloadFiles"
    - from: "批量转录按钮"
      to: "frontend/src/api/transcription.ts"
      via: "submitBatchTranscription"
      pattern: "onClick.*handleBatchTranscribe"
---

<objective>
在文件管理页面添加批量操作UI：批量下载按钮和批量转录按钮，支持文件多选和配置对话框。

Purpose: 提供用户友好的批量操作界面，提升用户体验和操作效率。
Output: 文件列表页的批量操作UI（按钮、对话框、提示）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-batch-operations/14-CONTEXT.md
@.planning/phases/14-batch-operations/14-PATTERNS.md
@.planning/phases/14-batch-operations/14-01-SUMMARY.md
@.planning/phases/14-batch-operations/14-02-SUMMARY.md

@frontend/src/pages/files/index.tsx
@frontend/src/api/video-file.ts
@frontend/src/api/transcription.ts
@frontend/src/types/transcription.ts

## 现有批量删除UI模式

从文件列表页的批量删除实现中学习：

```typescript
// 行选择状态
const [selectedRowKeys, setSelectedRowKeys] = useState<React.Key[]>([])
const [batchDeleting, setBatchDeleting] = useState(false)

// Table rowSelection配置
rowSelection={{
  selectedRowKeys,
  onChange: (selectedKeys) => setSelectedRowKeys(selectedKeys),
  getCheckboxProps: (record: VideoFile) => ({
    disabled: record.status === 'processing',
  }),
}}

// 批量操作按钮
{selectedRowKeys.length > 0 && (
  <Space style={{ marginBottom: 16 }}>
    <Popconfirm
      title={`确定要删除选中的 ${selectedRowKeys.length} 个文件吗？`}
      onConfirm={handleBatchDelete}
    >
      <Button danger icon={<DeleteOutlined />} loading={batchDeleting}>
        批量删除 ({selectedRowKeys.length})
      </Button>
    </Popconfirm>
  </Space>
)}

// 批量删除处理
const handleBatchDelete = useCallback(async () => {
  if (selectedRowKeys.length === 0) {
    message.warning('请先选择要删除的文件')
    return
  }

  setBatchDeleting(true)
  try {
    const response = await videoFileApi.batchDeleteFiles(selectedRowKeys as number[])
    if (response.data) {
      const { success, failed } = response.data
      if (failed > 0) {
        message.warning(`删除完成：成功 ${success} 个，失败 ${failed} 个`)
      } else {
        message.success(`成功删除 ${success} 个文件`)
      }
      setSelectedRowKeys([])
      loadFiles()
      loadStats()
    }
  } catch (error) {
    message.error(error instanceof Error ? error.message : '批量删除失败')
  } finally {
    setBatchDeleting(false)
  }
}, [selectedRowKeys, loadFiles, loadStats])
```

## 现有转录配置模式

从文件列表页的单文件转录实现中学习：

```typescript
const [triggerModalOpen, setTriggerModalOpen] = useState(false)
const [selectedSamplingRate, setSelectedSamplingRate] = useState<number>(0.5)
const [cloudTranscriptionMode, setCloudTranscriptionMode] = useState<TranscriptionMode>('local')

<Modal
  title="转录配置"
  open={triggerModalOpen}
  onOk={confirmTranscription}
  onCancel={() => setTriggerModalOpen(false)}
>
  <Form layout="vertical">
    <Form.Item label="转录模式">
      <Radio.Group
        value={cloudTranscriptionMode}
        onChange={(e) => setCloudTranscriptionMode(e.target.value)}
      >
        <Radio value="local">本地转录</Radio>
        <Radio value="cloud">云端转录</Radio>
      </Radio.Group>
    </Form.Item>
    {cloudTranscriptionMode === 'local' && (
      <Form.Item label="采样率">
        <Select
          value={selectedSamplingRate}
          onChange={setSelectedSamplingRate}
          options={samplingRateOptions}
        />
      </Form.Item>
    )}
  </Form>
</Modal>
```

## 决策实现要求

**D-02: 批量选择布局**
- 使用 table rowSelection，复选框在每行开头
- 实现方式：复用现有的 rowSelection 配置

**D-03: 进度显示方式**
- 使用 Toast 通知显示操作进度
- 实现方式：message.loading() → message.success()

**D-04: 数量限制策略**
- 无硬性限制
- 如果文件很大（>1GB）或很多（>100）时显示警告提示
- 实现方式：Modal.confirm() 显示警告

**D-08: 批量转录入口**
- 在文件列表页，选中文件后显示「批量转录」按钮
- 点击后弹出转录配置对话框
- 实现方式：条件渲染按钮，Modal 对话框

**D-09: 配置交互**
- 显示模态对话框，包含转录配置（语言、PPT 提取等）和确认按钮
- 实现方式：Form 表单，sampling_rate 和 mode 选择

**D-10: 创建后反馈**
- 显示「已创建 N 个转录任务」的 Toast 通知
- 用户可到转录任务页查看进度
- 实现方式：message.success() 显示成功数量

## UI设计要求

**批量操作按钮布局：**
- 显示在文件列表上方
- 与批量删除按钮并列
- 使用 Space 组件分隔
- 按钮显示选中数量

**批量下载按钮：**
- 图标：DownloadOutlined
- 文本：「批量下载 (N)」
- 点击直接触发下载
- 显示 loading 状态

**批量转录按钮：**
- 图标：ThunderboltOutlined
- 文本：「批量转录 (N)」
- 点击弹出配置对话框
- 显示 loading 状态

**配置对话框：**
- 标题：「批量转录配置」
- 显示选中文件数量
- 包含转录模式选择（local/cloud）
- local 模式显示采样率选择
- cloud 模式显示说明文字
- 确认按钮显示「提交转录任务」

**警告对话框：**
- 当文件数量 > 100 时显示
- 当文件总大小 > 10GB 时显示
- 提供取消和继续选项
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加批量下载UI</name>
  <files>frontend/src/pages/files/index.tsx</files>
  <read_first>
    frontend/src/pages/files/index.tsx
    frontend/src/api/video-file.ts
  </read_first>
  <behavior>
    批量下载UI应该：
    - 在选中文件后显示批量下载按钮
    - 点击按钮调用 batchDownloadFiles API
    - 显示下载进度提示
    - 下载完成后清空选择
    - 处理错误情况
  </behavior>
  <action>
    在 frontend/src/pages/files/index.tsx 中添加：

    1. 添加状态变量：
    ```typescript
    const [batchDownloading, setBatchDownloading] = useState(false)
    ```

    2. 添加批量下载处理函数：
    ```typescript
    // 批量下载文件
    const handleBatchDownload = useCallback(async () => {
      if (selectedRowKeys.length === 0) {
        message.warning('请先选择要下载的文件')
        return
      }

      // 计算文件总大小
      const selectedFiles = files.filter(f => selectedRowKeys.includes(f.id))
      const totalSize = selectedFiles.reduce((sum, f) => sum + f.file_size, 0)
      const totalSizeGB = totalSize / (1024 * 1024 * 1024)

      // 如果文件很大，显示警告
      if (totalSizeGB > 1 || selectedRowKeys.length > 100) {
        Modal.confirm({
          title: '批量下载警告',
          content: `您即将下载 ${selectedRowKeys.length} 个文件，总大小约为 ${totalSizeGB.toFixed(2)} GB。这可能需要一些时间，确定要继续吗？`,
          okText: '继续下载',
          cancelText: '取消',
          onOk: async () => {
            await performBatchDownload()
          },
        })
      } else {
        await performBatchDownload()
      }
    }, [selectedRowKeys, files])

    const performBatchDownload = useCallback(async () => {
      setBatchDownloading(true)
      try {
        videoFileApi.batchDownloadFiles(selectedRowKeys as number[])
        setSelectedRowKeys([])
      } catch (error) {
        message.error(error instanceof Error ? error.message : '批量下载失败')
      } finally {
        setBatchDownloading(false)
      }
    }, [selectedRowKeys])
    ```

    3. 在批量操作区域添加批量下载按钮：
    ```typescript
    {selectedRowKeys.length > 0 && (
      <Space style={{ marginBottom: 16 }}>
        {/* 现有的批量删除按钮 */}
        <Popconfirm
          title={`确定要删除选中的 ${selectedRowKeys.length} 个文件吗？`}
          onConfirm={handleBatchDelete}
        >
          <Button
            danger
            icon={<DeleteOutlined />}
            loading={batchDeleting}
            disabled={batchDeleting || batchDownloading}
          >
            批量删除 ({selectedRowKeys.length})
          </Button>
        </Popconfirm>

        {/* 新增：批量下载按钮 */}
        <Button
          type="primary"
          icon={<DownloadOutlined />}
          loading={batchDownloading}
          disabled={batchDeleting || batchDownloading}
          onClick={handleBatchDownload}
        >
          批量下载 ({selectedRowKeys.length})
        </Button>
      </Space>
    )}
    ```

    注意事项：
    - 复用现有的 selectedRowKeys 状态
    - 与批量删除按钮并列显示
    - 添加文件大小和数量警告
    - 下载完成后清空选择
    - 禁用状态管理（下载时禁用删除，反之亦然）
  </action>
  <verify>
    <automated>grep -n "批量下载" frontend/src/pages/files/index.tsx</automated>
    <automated>grep -n "handleBatchDownload" frontend/src/pages/files/index.tsx</automated>
  </verify>
  <done>
    - 批量下载按钮添加完成
    - handleBatchDownload 函数实现完成
    - 文件大小和数量警告实现完成
    - 与现有批量操作UI协调
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加批量转录UI（按钮和对话框）</name>
  <files>frontend/src/pages/files/index.tsx</files>
  <read_first>
    frontend/src/pages/files/index.tsx
    frontend/src/api/transcription.ts
    frontend/src/types/transcription.ts
  </read_first>
  <behavior>
    批量转录UI应该：
    - 在选中文件后显示批量转录按钮
    - 点击按钮弹出配置对话框
    - 对话框包含转录模式选择
    - local 模式显示采样率选择
    - 确认后调用 submitBatchTranscription API
    - 显示成功/失败统计
  </behavior>
  <action>
    在 frontend/src/pages/files/index.tsx 中添加：

    1. 添加状态变量：
    ```typescript
    const [batchTranscribeModalOpen, setBatchTranscribeModalOpen] = useState(false)
    const [batchTranscribing, setBatchTranscribing] = useState(false)
    const [batchSamplingRate, setBatchSamplingRate] = useState<number>(0.5)
    const [batchTranscribeMode, setBatchTranscribeMode] = useState<TranscriptionMode>('local')
    ```

    2. 添加批量转录处理函数：
    ```typescript
    // 打开批量转录对话框
    const handleBatchTranscribeClick = useCallback(() => {
      if (selectedRowKeys.length === 0) {
        message.warning('请先选择要转录的文件')
        return
      }

      // 检查是否都是视频文件
      const selectedFiles = files.filter(f => selectedRowKeys.includes(f.id))
      const nonVideoFiles = selectedFiles.filter(f => f.format !== 'mp4' && f.format !== 'mkv')

      if (nonVideoFiles.length > 0) {
        message.warning(`只能转录视频文件，已忽略 ${nonVideoFiles.length} 个非视频文件`)
      }

      setBatchSamplingRate(0.5) // 重置为默认值
      setBatchTranscribeMode('local') // 重置为默认模式
      setBatchTranscribeModalOpen(true)
    }, [selectedRowKeys, files])

    // 确认批量转录
    const confirmBatchTranscription = useCallback(async () => {
      if (selectedRowKeys.length === 0) {
        return
      }

      setBatchTranscribing(true)
      try {
        const request: BatchTranscriptionRequest = {
          video_file_ids: selectedRowKeys as number[],
          sampling_rate: batchTranscribeMode === 'local' ? batchSamplingRate : undefined,
          mode: batchTranscribeMode,
        }

        const response = await submitBatchTranscription(request)

        if (response.data) {
          const { submitted_count, failed_count, errors } = response.data

          if (failed_count > 0) {
            const errorMsg = errors.length > 0
              ? `。错误详情：${errors.slice(0, 3).join('; ')}${errors.length > 3 ? '...' : ''}`
              : ''
            message.warning(`转录任务创建完成：成功 ${submitted_count} 个，失败 ${failed_count} 个${errorMsg}`)
          } else {
            message.success(`成功创建 ${submitted_count} 个转录任务`)
          }

          setBatchTranscribeModalOpen(false)
          setSelectedRowKeys([])

          // 刷新活跃转录任务列表
          loadActiveTranscriptions()
        }
      } catch (error) {
        message.error(error instanceof Error ? error.message : '批量转录失败')
      } finally {
        setBatchTranscribing(false)
      }
    }, [selectedRowKeys, batchSamplingRate, batchTranscribeMode, loadActiveTranscriptions])
    ```

    3. 在批量操作区域添加批量转录按钮：
    ```typescript
    {selectedRowKeys.length > 0 && (
      <Space style={{ marginBottom: 16 }}>
        {/* 批量删除按钮 */}
        <Popconfirm
          title={`确定要删除选中的 ${selectedRowKeys.length} 个文件吗？`}
          onConfirm={handleBatchDelete}
        >
          <Button
            danger
            icon={<DeleteOutlined />}
            loading={batchDeleting}
            disabled={batchDeleting || batchDownloading || batchTranscribing}
          >
            批量删除 ({selectedRowKeys.length})
          </Button>
        </Popconfirm>

        {/* 批量下载按钮 */}
        <Button
          type="primary"
          icon={<DownloadOutlined />}
          loading={batchDownloading}
          disabled={batchDeleting || batchDownloading || batchTranscribing}
          onClick={handleBatchDownload}
        >
          批量下载 ({selectedRowKeys.length})
        </Button>

        {/* 批量转录按钮 */}
        <Button
          type="primary"
          icon={<ThunderboltOutlined />}
          loading={batchTranscribing}
          disabled={batchDeleting || batchDownloading || batchTranscribing}
          onClick={handleBatchTranscribeClick}
        >
          批量转录 ({selectedRowKeys.length})
        </Button>
      </Space>
    )}
    ```

    4. 添加批量转录配置对话框：
    ```typescript
    <Modal
      title="批量转录配置"
      open={batchTranscribeModalOpen}
      onOk={confirmBatchTranscription}
      onCancel={() => setBatchTranscribeModalOpen(false)}
      okText="提交转录任务"
      cancelText="取消"
      confirmLoading={batchTranscribing}
      width={600}
    >
      <Form layout="vertical">
        <Form.Item label="选中文件">
          <span>已选择 {selectedRowKeys.length} 个文件进行批量转录</span>
        </Form.Item>

        <Form.Item label="转录模式">
          <Radio.Group
            value={batchTranscribeMode}
            onChange={(e) => setBatchTranscribeMode(e.target.value)}
            disabled={batchTranscribing}
          >
            <Radio value="local">本地转录（快速，免费）</Radio>
            <Radio value="cloud">云端转录（阿里通义听悟，更准确）</Radio>
          </Radio.Group>
        </Form.Item>

        {batchTranscribeMode === 'local' && (
          <Form.Item
            label="采样率"
            tooltip="采样率决定每秒提取的帧数，数值越小精度越高但文件越大"
          >
            <Select
              value={batchSamplingRate}
              onChange={setBatchSamplingRate}
              options={samplingRateOptions}
              disabled={batchTranscribing}
            />
          </Form.Item>
        )}

        {batchTranscribeMode === 'cloud' && (
          <Form.Item label="云端转录说明">
            <Alert
              message="云端转录使用阿里通义听悟服务，支持更准确的语音识别和PPT提取，但需要消耗API配额"
              type="info"
              showIcon
            />
          </Form.Item>
        )}
      </Form>
    </Modal>
    ```

    注意事项：
    - 复用现有的 selectedRowKeys 状态
    - 与批量下载和删除按钮并列显示
    - 检查文件类型（只能转录视频文件）
    - 显示详细的成功/失败统计
    - 禁用状态管理（转录时禁用其他操作）
    - 对话框显示选中文件数量
    - local 和 cloud 模式有不同的配置项
  </action>
  <verify>
    <automated>grep -n "批量转录" frontend/src/pages/files/index.tsx</automated>
    <automated>grep -n "handleBatchTranscribe" frontend/src/pages/files/index.tsx</automated>
  </verify>
  <done>
    - 批量转录按钮添加完成
    - handleBatchTranscribeClick 函数实现完成
    - confirmBatchTranscription 函数实现完成
    - 批量转录配置对话框添加完成
    - 与现有批量操作UI协调
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    前端批量操作UI已实现：批量下载按钮、批量转录按钮和配置对话框。
  </what-built>
  <how-to-verify>
    1. 启动前端开发服务器
    2. 访问文件管理页面
    3. 选择多个文件（混合视频和PPT）
    4. 验证批量操作按钮显示：
       - 批量删除 (N)
       - 批量下载 (N)
       - 批量转录 (N)
    5. 测试批量下载：
       - 点击批量下载按钮
       - 验证下载ZIP文件
       - 验证ZIP内文件分组正确
    6. 测试批量转录：
       - 点击批量转录按钮
       - 验证配置对话框弹出
       - 选择 local 模式，设置采样率
       - 点击确认
       - 验证成功提示显示
       - 检查转录任务列表
    7. 测试边界情况：
       - 选择100+文件（应显示警告）
       - 选择非视频文件进行转录（应过滤）
       - 同时进行多个批量操作（应禁用）
  </how-to-verify>
  <resume-signal>输入 "approved" 确认批量操作UI正常，或描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→API | 批量操作请求跨过此边界 |
| User→UI | 用户输入跨过此边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | S | Batch operations | mitigate | 验证用户权限，防止未授权批量操作 |
| T-14-03-02 | I | File selection | mitigate | 验证文件类型，防止恶意文件操作 |
| T-14-03-03 | X | Config injection | mitigate | 验证转录配置参数，防止注入攻击 |

实施的安全措施：
- 前端过滤非视频文件（转录操作）
- 显示警告提示（大文件或大量文件）
- 禁用并发批量操作
- 后端二次验证文件所有权
</threat_model>

<verification>
- [ ] 批量下载按钮正常工作
- [ ] 批量转录按钮正常工作
- [ ] 配置对话框正确显示
- [ ] 文件类型过滤正确
- [ ] 警告提示正确触发
- [ ] 成功/失败反馈清晰
- [ ] 人工验证通过
</verification>

<success_criteria>
- [ ] 批量操作按钮正确显示
- [ ] 批量下载功能正常
- [ ] 批量转录功能正常
- [ ] 配置对话框交互流畅
- [ ] 用户反馈清晰明确
- [ ] 边界情况处理正确
- [ ] UI与后端API集成正确
</success_criteria>

<output>
After completion, create `.planning/phases/14-batch-operations/14-03-SUMMARY.md`
</output>
