---
phase: 14-batch-operations
plan: 02
type: execute
wave: 2
depends_on: ["14-00"]
files_modified:
  - internal/models/transcription_job_group.go
  - internal/migrations/015_add_transcription_job_groups.go
  - internal/services/transcription_service.go
  - internal/handlers/transcription_handler.go
  - internal/routes/routes.go
  - frontend/src/api/transcription.ts
  - frontend/src/types/transcription.ts
autonomous: true
requirements:
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
user_setup: []
must_haves:
  truths:
    - 用户可以选择多个视频文件进行批量转录
    - 后端创建任务组（TranscriptionJobGroup）
    - 为每个文件创建转录任务（TranscriptionTask）
    - 任务顺序执行，避免资源耗尽
    - 任务组跟踪进度（completed_count/total_count）
    - 部分失败不影响其他任务
    - 前端显示任务创建成功反馈
  artifacts:
    - path: "internal/models/transcription_job_group.go"
      provides: "任务组模型"
      contains: "TranscriptionJobGroup"
      exports: ["TranscriptionJobGroup", "JobGroupStatus"]
    - path: "internal/migrations/015_add_transcription_job_groups.go"
      provides: "数据库迁移"
      contains: "CREATE TABLE transcription_job_groups"
    - path: "internal/services/transcription_service.go"
      provides: "批量转录服务"
      contains: "SubmitBatchTranscription"
      exports: ["SubmitBatchTranscription", "GetJobGroupStatus"]
    - path: "internal/handlers/transcription_handler.go"
      provides: "批量转录Handler"
      contains: "SubmitBatchTranscription"
      exports: ["SubmitBatchTranscription", "GetBatchTranscriptionStatus"]
    - path: "frontend/src/api/transcription.ts"
      provides: "批量转录API客户端"
      contains: "submitBatchTranscription"
      exports: ["submitBatchTranscription"]
    - path: "frontend/src/types/transcription.ts"
      provides: "批量转录类型定义"
      contains: "BatchTranscriptionRequest"
  key_links:
    - from: "frontend/src/pages/files/index.tsx"
      to: "internal/handlers/transcription_handler.go"
      via: "POST /api/v1/transcriptions/batch"
      pattern: "submitBatchTranscription"
    - from: "internal/handlers/transcription_handler.go"
      to: "internal/services/transcription_service.go"
      via: "service.SubmitBatchTranscription"
      pattern: "SubmitBatchTranscription"
    - from: "internal/services/transcription_service.go"
      to: "internal/models/transcription_job_group.go"
      via: "JobGroup关联Task"
      pattern: "job_group_id"
---

<objective>
实现批量转录功能：用户选择多个视频文件后，后端创建任务组，顺序提交转录任务，跟踪整体进度。

Purpose: 提供批量转录能力，用户可以一次性提交多个视频的转录任务，避免逐个提交的繁琐操作。
Output: 后端批量转录API + 数据库迁移 + 前端批量转录按钮和配置模态框
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-batch-operations/14-CONTEXT.md
@.planning/phases/14-batch-operations/14-PATTERNS.md
@.planning/phases/14-batch-operations/14-00-SUMMARY.md

@internal/models/transcription_task.go
@internal/services/transcription_service.go
@internal/handlers/transcription_handler.go
@internal/migrations/014_create_input_configs.go
@frontend/src/api/transcription.ts
@frontend/src/types/transcription.ts
@frontend/src/pages/files/index.tsx

## 单文件转录参考模式

从 `SubmitTranscription` 实现中学习：

```go
// 验证用户所有权
file, err := h.videoFileService.GetFileByID(uint(id))
if !middleware.CanAccessAllData(c) && file.CreatedBy != userID {
    response.GinError(c, response.CodeInvalidRequest, "无权操作此视频文件")
    return
}

// 提交转录任务
err := h.transcriptionService.SubmitTranscriptionWithMode(
    uint(id),
    req.SamplingRate,
    req.Mode,
    userID,
)
```

## 数据库迁移参考模式

从 `Migration014_CreateInputConfigs` 实现中学习：

```go
func (m *Migration014_CreateInputConfigs) Up(db *gorm.DB) error {
    // 检查表是否已存在
    var count int64
    db.Raw("SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name='input_configs'").Scan(&count)
    if count == 0 {
        // 创建表
        err := db.Exec(`CREATE TABLE input_configs (...)`).Error
        // 创建索引
        db.Exec("CREATE INDEX IF NOT EXISTS idx_input_configs_config_type ON input_configs(config_type)")
    }
    return nil
}
```

## 决策实现要求

**D-08: 批量转录入口**
- 在文件列表页，选中文件后显示「批量转录」按钮
- 点击后弹出转录配置对话框
- 实现方式：条件渲染按钮，Modal 对话框

**D-09: 配置交互**
- 显示模态对话框，包含转录配置（语言、PPT 提取等）和确认按钮
- 实现方式：Form 表单， sampling_rate 和 mode 选择

**D-10: 创建后反馈**
- 显示「已创建 N 个转录任务」的 Toast 通知
- 用户可到转录任务页查看进度
- 实现方式：message.success() 显示成功数量

**D-11: 任务组织方式**
- 使用任务组模式（TranscriptionJobGroup）
- 新增 `transcription_job_groups` 表
- 每个子任务关联 `job_group_id`
- 实现方式：新建模型和迁移

**D-12: 任务创建方式**
- 顺序创建任务，每个任务完成后才开始下一个
- 避免资源耗尽
- 实现方式：在 SubmitBatchTranscription 中顺序调用 SubmitTranscriptionWithMode

**D-13: 错误处理策略**
- 如果某个文件转录失败，继续处理剩余文件
- 最后汇总成功和失败数量
- 实现方式：收集错误，返回统计结果
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建TranscriptionJobGroup模型和迁移</name>
  <files>internal/models/transcription_job_group.go, internal/migrations/015_add_transcription_job_groups.go</files>
  <read_first>
    internal/models/transcription_task.go
    internal/migrations/014_create_input_configs.go
    internal/migrations/helpers.go
  </read_first>
  <behavior>
    TranscriptionJobGroup 模型应该：
    - 包含 id, user_id, status, total_count, completed_count 字段
    - 包含 created_at, updated_at 时间戳
    - status 有四种状态：pending, processing, completed, failed
    - 与 TranscriptionTask 一对多关联
    - 包含进度计算方法
    - 包含状态流转方法
  </behavior>
  <action>
    1. 创建 internal/models/transcription_job_group.go：
    ```go
    package models

    import "time"

    // TranscriptionJobGroup 转录任务组模型
    type TranscriptionJobGroup struct {
        ID             uint       `gorm:"primaryKey" json:"id"`
        UserID         uint       `gorm:"not null;index" json:"user_id"`
        User           *User      `gorm:"foreignKey:UserID" json:"user,omitempty"`
        Status         string     `gorm:"type:varchar(20);default:'pending';index" json:"status"`
        TotalCount     int        `gorm:"default:0" json:"total_count"`
        CompletedCount int        `gorm:"default:0" json:"completed_count"`
        FailedCount    int        `gorm:"default:0" json:"failed_count"`
        Tasks          []TranscriptionTask `gorm:"foreignKey:JobGroupID" json:"tasks,omitempty"`
        CreatedAt      time.Time  `json:"created_at"`
        UpdatedAt      time.Time  `json:"updated_at"`
    }

    // 任务组状态常量
    const (
        JobGroupStatusPending    = "pending"
        JobGroupStatusProcessing = "processing"
        JobGroupStatusCompleted  = "completed"
        JobGroupStatusFailed     = "failed"
    )

    // TableName 指定表名
    func (TranscriptionJobGroup) TableName() string {
        return "transcription_job_groups"
    }

    // GetPercentage 获取完成百分比
    func (g *TranscriptionJobGroup) GetPercentage() int {
        if g.TotalCount == 0 {
            return 0
        }
        return (g.CompletedCount * 100) / g.TotalCount
    }

    // IsCompleted 检查是否全部完成
    func (g *TranscriptionJobGroup) IsCompleted() bool {
        return g.CompletedCount+g.FailedCount >= g.TotalCount
    }

    // UpdateStatus 根据任务完成情况更新状态
    func (g *TranscriptionJobGroup) UpdateStatus() {
        if g.TotalCount == 0 {
            g.Status = JobGroupStatusPending
            return
        }

        if g.IsCompleted() {
            if g.FailedCount > 0 && g.CompletedCount == 0 {
                g.Status = JobGroupStatusFailed
            } else if g.FailedCount > 0 {
                g.Status = JobGroupStatusCompleted // 部分成功也算完成
            } else {
                g.Status = JobGroupStatusCompleted
            }
        } else if g.CompletedCount > 0 || g.FailedCount > 0 {
            g.Status = JobGroupStatusProcessing
        } else {
            g.Status = JobGroupStatusPending
        }
    }
    ```

    2. 创建 internal/migrations/015_add_transcription_job_groups.go：
    ```go
    package migrations

    import (
        "fmt"
        "gorm.io/gorm"
    )

    // Migration015_AddTranscriptionJobGroups 添加转录任务组表
    type Migration015_AddTranscriptionJobGroups struct{}

    func (m *Migration015_AddTranscriptionJobGroups) Name() string {
        return "015_add_transcription_job_groups"
    }

    func (m *Migration015_AddTranscriptionJobGroups) Up(db *gorm.DB) error {
        // 创建 transcription_job_groups 表
        var count int64
        db.Raw("SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name='transcription_job_groups'").Scan(&count)
        if count == 0 {
            err := db.Exec(`
                CREATE TABLE transcription_job_groups (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    user_id INTEGER NOT NULL,
                    status VARCHAR(20) DEFAULT 'pending',
                    total_count INTEGER DEFAULT 0,
                    completed_count INTEGER DEFAULT 0,
                    failed_count INTEGER DEFAULT 0,
                    created_at DATETIME,
                    updated_at DATETIME,
                    FOREIGN KEY(user_id) REFERENCES users(id)
                )
            `).Error
            if err != nil {
                return fmt.Errorf("failed to create transcription_job_groups table: %w", err)
            }

            // 创建索引
            db.Exec("CREATE INDEX IF NOT EXISTS idx_transcription_job_groups_user_id ON transcription_job_groups(user_id)")
            db.Exec("CREATE INDEX IF NOT EXISTS idx_transcription_job_groups_status ON transcription_job_groups(status)")
        }

        // 为 transcription_tasks 表添加 job_group_id 字段
        var columnExists bool
        db.Raw("SELECT COUNT(*) > 0 FROM pragma_table_info('transcription_tasks') WHERE name='job_group_id'").Scan(&columnExists)
        if !columnExists {
            if err := db.Exec("ALTER TABLE transcription_tasks ADD COLUMN job_group_id INTEGER").Error; err != nil {
                return fmt.Errorf("failed to add job_group_id column: %w", err)
            }

            // 创建外键约束（SQLite 的外键约束需要手动创建）
            db.Exec("CREATE INDEX IF NOT EXISTS idx_transcription_tasks_job_group_id ON transcription_tasks(job_group_id)")
        }

        return nil
    }

    func (m *Migration015_AddTranscriptionJobGroups) Down(db *gorm.DB) error {
        db.Exec("DROP INDEX IF EXISTS idx_transcription_tasks_job_group_id")
        db.Exec("ALTER TABLE transcription_tasks DROP COLUMN job_group_id")
        db.Exec("DROP TABLE IF EXISTS transcription_job_groups")
        return nil
    }
    ```

    注意事项：
    - 使用 GORM 关联定义外键关系
    - 添加索引优化查询性能
    - 实现进度计算和状态更新逻辑
    - 迁移需要向后兼容（检查字段是否存在）
  </action>
  <verify>
    <automated>cd internal/models && go test -v -run TestTranscriptionJobGroup</automated>
    <automated>grep -n "CREATE TABLE transcription_job_groups" internal/migrations/015_add_transcription_job_groups.go</automated>
  </verify>
  <done>
    - TranscriptionJobGroup 模型创建完成
    - 数据库迁移文件创建完成
    - 进度计算方法实现完成
    - 状态更新逻辑实现完成
    - 测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现后端批量转录服务层</name>
  <files>internal/services/transcription_service.go</files>
  <read_first>
    internal/services/transcription_service.go
    internal/models/transcription_job_group.go
    internal/models/transcription_task.go
  </read_first>
  <behavior>
    批量转录服务应该：
    - 接受视频文件ID列表和转录配置
    - 创建 TranscriptionJobGroup 记录
    - 为每个文件创建 TranscriptionTask
    - 顺序提交任务到队列（避免并发）
    - 跟踪任务组进度
    - 处理部分失败情况
    - 返回任务组ID和统计信息
  </behavior>
  <action>
    在 internal/services/transcription_service.go 中添加：

    1. 添加批量转录请求和响应结构：
    ```go
    // BatchTranscriptionRequest 批量转录请求
    type BatchTranscriptionRequest struct {
        VideoFileIDs  []uint  `json:"video_file_ids" binding:"required,min=1,dive,min=1"`
        SamplingRate  float64 `json:"sampling_rate"`
        Mode          string  `json:"mode"`
        UserID        uint    `json:"-"`
    }

    // BatchTranscriptionResult 批量转录结果
    type BatchTranscriptionResult struct {
        JobGroupID    uint   `json:"job_group_id"`
        TotalCount    int    `json:"total_count"`
        SubmittedCount int   `json:"submitted_count"`
        FailedCount   int    `json:"failed_count"`
        Errors        []string `json:"errors"`
    }
    ```

    2. 实现 SubmitBatchTranscription 方法：
    ```go
    func (s *TranscriptionService) SubmitBatchTranscription(req *BatchTranscriptionRequest) (*BatchTranscriptionResult, error) {
        // 验证转录模式
        if req.Mode != TranscriptionModeLocal && req.Mode != TranscriptionModeCloud {
            return nil, fmt.Errorf("无效的转录模式: %s", req.Mode)
        }

        // 创建任务组
        jobGroup := &models.TranscriptionJobGroup{
            UserID:     req.UserID,
            Status:     models.JobGroupStatusPending,
            TotalCount: len(req.VideoFileIDs),
        }
        if err := s.db.Create(jobGroup).Error; err != nil {
            return nil, fmt.Errorf("创建任务组失败: %w", err)
        }

        // 结果统计
        result := &BatchTranscriptionResult{
            JobGroupID: jobGroup.ID,
            TotalCount: len(req.VideoFileIDs),
        }

        // 顺序创建任务
        for i, videoFileID := range req.VideoFileIDs {
            // 创建转录任务
            task := &models.TranscriptionTask{
                VideoFileID:  videoFileID,
                JobGroupID:   &jobGroup.ID,
                SamplingRate: req.SamplingRate,
                Mode:         req.Mode,
                Status:       models.TranscriptionStatusPending,
                CreatedBy:    req.UserID,
            }

            if err := s.db.Create(task).Error; err != nil {
                result.FailedCount++
                result.Errors = append(result.Errors, fmt.Sprintf("文件 %d 创建任务失败: %v", videoFileID, err))
                s.logger.Warn("批量转录创建任务失败",
                    zap.Uint("video_file_id", videoFileID),
                    zap.Error(err),
                )
                continue
            }

            // 提交到队列
            select {
            case s.taskQueue <- task:
                result.SubmittedCount++
                s.logger.Info("批量转录任务已提交",
                    zap.Uint("job_group_id", jobGroup.ID),
                    zap.Uint("task_id", task.ID),
                    zap.Int("index", i+1),
                    zap.Int("total", len(req.VideoFileIDs)),
                )
            default:
                result.FailedCount++
                result.Errors = append(result.Errors, fmt.Sprintf("文件 %d 队列已满", videoFileID))
                s.logger.Warn("批量转录队列已满",
                    zap.Uint("video_file_id", videoFileID),
                )
            }
        }

        // 更新任务组状态
        if result.SubmittedCount > 0 {
            jobGroup.Status = models.JobGroupStatusProcessing
            jobGroup.CompletedCount = result.SubmittedCount
            jobGroup.FailedCount = result.FailedCount
            jobGroup.UpdateStatus()
            s.db.Save(jobGroup)
        }

        return result, nil
    }
    ```

    3. 实现 GetJobGroupStatus 方法：
    ```go
    func (s *TranscriptionService) GetJobGroupStatus(jobGroupID uint, userID uint, isAdmin bool) (*models.TranscriptionJobGroup, error) {
        var jobGroup models.TranscriptionJobGroup
        err := s.db.Where("id = ?", jobGroupID).
            Preload("Tasks").
            First(&jobGroup).Error
        if err != nil {
            return nil, err
        }

        // 验证权限
        if !isAdmin && jobGroup.UserID != userID {
            return nil, fmt.Errorf("无权访问此任务组")
        }

        // 重新计算进度
        var completedCount, failedCount int
        for _, task := range jobGroup.Tasks {
            if task.Status == models.TranscriptionStatusCompleted {
                completedCount++
            } else if task.Status == models.TranscriptionStatusFailed {
                failedCount++
            }
        }
        jobGroup.CompletedCount = completedCount
        jobGroup.FailedCount = failedCount
        jobGroup.UpdateStatus()
        s.db.Save(&jobGroup)

        return &jobGroup, nil
    }
    ```

    4. 在现有的 ProcessTask 方法中添加任务组进度更新：
    ```go
    // 在任务完成后更新任务组进度
    if task.JobGroupID != nil {
        s.updateJobGroupProgress(*task.JobGroupID)
    }
    ```

    5. 添加 updateJobGroupProgress 辅助方法：
    ```go
    func (s *TranscriptionService) updateJobGroupProgress(jobGroupID uint) {
        var jobGroup models.TranscriptionJobGroup
        if err := s.db.Preload("Tasks").First(&jobGroup, jobGroupID).Error; err != nil {
            s.logger.Error("更新任务组进度失败", zap.Uint("job_group_id", jobGroupID), zap.Error(err))
            return
        }

        var completedCount, failedCount int
        for _, task := range jobGroup.Tasks {
            if task.Status == models.TranscriptionStatusCompleted {
                completedCount++
            } else if task.Status == models.TranscriptionStatusFailed {
                failedCount++
            }
        }

        jobGroup.CompletedCount = completedCount
        jobGroup.FailedCount = failedCount
        jobGroup.UpdateStatus()
        s.db.Save(&jobGroup)
    }
    ```

    注意事项：
    - 使用事务确保任务组和任务的一致性
    - 顺序提交任务避免队列满
    - 实时更新任务组进度
    - 处理部分失败情况
    - 添加详细的日志记录
  </action>
  <verify>
    <automated>cd internal/services && go test -v -run TestTranscriptionService_SubmitBatchTranscription</automated>
  </verify>
  <done>
    - SubmitBatchTranscription 方法实现完成
    - GetJobGroupStatus 方法实现完成
    - 任务组进度更新逻辑实现完成
    - 错误处理完整
    - 测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现后端批量转录Handler</name>
  <files>internal/handlers/transcription_handler.go, internal/routes/routes.go</files>
  <read_first>
    internal/handlers/transcription_handler.go
    internal/routes/routes.go
  </read_first>
  <behavior>
    批量转录 Handler 应该：
    - 解析请求中的文件ID列表和转录配置
    - 验证用户身份和文件所有权
    - 调用服务层创建任务组
    - 返回任务组ID和统计信息
    - 支持查询任务组状态
  </behavior>
  <action>
    在 internal/handlers/transcription_handler.go 中添加：

    1. 实现 SubmitBatchTranscription 方法：
    ```go
    // SubmitBatchTranscription handles POST /api/v1/transcriptions/batch
    func (h *TranscriptionHandler) SubmitBatchTranscription(c *gin.Context) {
        var req struct {
            VideoFileIDs []uint  `json:"video_file_ids" binding:"required,min=1,dive,min=1"`
            SamplingRate float64 `json:"sampling_rate"`
            Mode         string  `json:"mode"`
        }
        if err := c.ShouldBindJSON(&req); err != nil {
            response.GinError(c, response.CodeInvalidRequest, "参数错误")
            return
        }

        // 设置默认值
        if req.Mode == "" {
            req.Mode = "local"
        }
        if req.Mode == "local" && req.SamplingRate == 0 {
            req.SamplingRate = 0.5
        }

        // 验证模式
        validModes := map[string]bool{"local": true, "cloud": true}
        if !validModes[req.Mode] {
            response.GinError(c, response.CodeInvalidRequest, "无效的转录模式，必须是 local 或 cloud")
            return
        }

        // 获取用户信息
        userID := middleware.GetUserID(c)
        isAdmin := middleware.CanAccessAllData(c)

        // 验证文件所有权
        for _, videoFileID := range req.VideoFileIDs {
            file, err := h.videoFileService.GetFileByID(videoFileID)
            if err != nil {
                response.GinError(c, response.CodeNotFound, fmt.Sprintf("视频文件 %d 不存在", videoFileID))
                return
            }
            if !isAdmin && file.CreatedBy != userID {
                response.GinError(c, response.CodeForbidden, fmt.Sprintf("无权操作视频文件 %d", videoFileID))
                return
            }
        }

        // 调用服务层
        batchReq := &services.BatchTranscriptionRequest{
            VideoFileIDs: req.VideoFileIDs,
            SamplingRate: req.SamplingRate,
            Mode:         req.Mode,
            UserID:       userID,
        }
        result, err := h.transcriptionService.SubmitBatchTranscription(batchReq)
        if err != nil {
            h.logger.Error("批量转录失败",
                zap.Uint("user_id", userID),
                zap.Int("file_count", len(req.VideoFileIDs)),
                zap.Error(err),
            )
            response.GinError(c, response.CodeInternalError, "批量转录失败: "+err.Error())
            return
        }

        h.logger.Info("批量转录成功",
            zap.Uint("user_id", userID),
            zap.Uint("job_group_id", result.JobGroupID),
            zap.Int("total", result.TotalCount),
            zap.Int("submitted", result.SubmittedCount),
            zap.Int("failed", result.FailedCount),
        )

        response.GinSuccess(c, result)
    }
    ```

    2. 实现 GetBatchTranscriptionStatus 方法：
    ```go
    // GetBatchTranscriptionStatus handles GET /api/v1/transcriptions/batch/:id
    func (h *TranscriptionHandler) GetBatchTranscriptionStatus(c *gin.Context) {
        idStr := c.Param("id")
        id, err := strconv.ParseUint(idStr, 10, 32)
        if err != nil {
            response.GinError(c, response.CodeInvalidRequest, "无效的任务组ID")
            return
        }

        userID := middleware.GetUserID(c)
        isAdmin := middleware.CanAccessAllData(c)

        jobGroup, err := h.transcriptionService.GetJobGroupStatus(uint(id), userID, isAdmin)
        if err != nil {
            response.GinError(c, response.CodeNotFound, "任务组不存在")
            return
        }

        response.GinSuccess(c, jobGroup)
    }
    ```

    3. 在 internal/routes/routes.go 中注册路由：
    ```go
    // 在 transcriptionsAPI 路由组中添加
    transcriptionsAPI.POST("batch", transcriptionHandler.SubmitBatchTranscription)
    transcriptionsAPI.GET("batch/:id", transcriptionHandler.GetBatchTranscriptionStatus)
    ```

    注意事项：
    - 验证所有文件的所有权
    - 返回详细的统计信息（成功/失败数量）
    - 支持查询任务组状态
    - 添加详细的日志记录
  </action>
  <verify>
    <automated>cd internal/handlers && go test -v -run TestTranscriptionHandler_SubmitBatchTranscription</automated>
    <automated>grep -n "POST.*batch" internal/routes/routes.go</automated>
  </verify>
  <done>
    - SubmitBatchTranscription handler 实现完成
    - GetBatchTranscriptionStatus handler 实现完成
    - 路由注册完成
    - 权限验证通过
    - 测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 实现前端批量转录API客户端</name>
  <files>frontend/src/api/transcription.ts, frontend/src/types/transcription.ts</files>
  <read_first>
    frontend/src/api/transcription.ts
    frontend/src/types/transcription.ts
  </read_first>
  <behavior>
    前端批量转录 API 应该：
    - 接受文件ID列表和转录配置
    - 发送 POST 请求到 /api/v1/transcriptions/batch
    - 接收任务组ID和统计信息
    - 支持查询任务组状态
  </behavior>
  <action>
    1. 在 frontend/src/types/transcription.ts 中添加类型定义：
    ```typescript
    // 批量转录请求
    export interface BatchTranscriptionRequest {
      video_file_ids: number[]
      sampling_rate?: number  // 仅用于 local 模式
      mode?: TranscriptionMode  // local 或 cloud，默认 local
    }

    // 批量转录结果
    export interface BatchTranscriptionResult {
      job_group_id: number
      total_count: number
      submitted_count: number
      failed_count: number
      errors: string[]
    }

    // 转录任务组状态
    export interface TranscriptionJobGroup {
      id: number
      user_id: number
      status: 'pending' | 'processing' | 'completed' | 'failed'
      total_count: number
      completed_count: number
      failed_count: number
      tasks?: TranscriptionTask[]
      created_at: string
      updated_at: string
      percentage?: number  // 计算属性
    }
    ```

    2. 在 frontend/src/api/transcription.ts 中添加 API 函数：
    ```typescript
    // 批量提交转录任务
    export async function submitBatchTranscription(
      request: BatchTranscriptionRequest
    ): Promise<ApiResponse<BatchTranscriptionResult>> {
      return apiRequest<BatchTranscriptionResult>('/api/v1/transcriptions/batch', {
        method: 'POST',
        body: JSON.stringify(request),
      })
    }

    // 获取批量转录任务组状态
    export async function getBatchTranscriptionStatus(
      jobGroupId: number
    ): Promise<ApiResponse<TranscriptionJobGroup>> {
      return apiRequest<TranscriptionJobGroup>(`/api/v1/transcriptions/batch/${jobGroupId}`)
    }
    ```

    注意事项：
    - 复用现有的 apiRequest 函数
    - 类型定义与后端响应匹配
    - 支持可选参数（sampling_rate, mode）
  </action>
  <verify>
    <automated>grep -n "submitBatchTranscription" frontend/src/api/transcription.ts</automated>
    <automated>grep -n "BatchTranscriptionRequest" frontend/src/types/transcription.ts</automated>
  </verify>
  <done>
    - submitBatchTranscription 函数实现完成
    - getBatchTranscriptionStatus 函数实现完成
    - 类型定义添加完成
    - 与后端API匹配
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | 批量转录请求跨过此边界 |
| API→database | 任务组和任务创建跨过此边界 |
| service→queue | 任务提交到队列跨过此边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | S | SubmitBatchTranscription | mitigate | 验证每个视频文件的用户所有权 |
| T-14-02-02 | I | JobGroup creation | mitigate | 限制批量任务数量，防止资源耗尽 |
| T-14-02-03 | D | Task queue | accept | 转录任务数据无敏感信息 |
| T-14-02-04 | E | Sequential processing | mitigate | 限制并发任务数，使用队列缓冲 |

实施的安全措施：
- 文件所有权验证：每个文件必须属于当前用户
- 批量数量限制：前端和后端都限制批量数量
- 队列满处理：队列满时返回错误，不阻塞
- 顺序处理：避免并发导致的资源耗尽
</threat_model>

<verification>
- [ ] 模型和迁移测试通过
- [ ] 服务层测试通过
- [ ] Handler测试通过
- [ ] 前端API函数正确调用后端
- [ ] 权限验证正确
- [ ] 错误处理完整
</verification>

<success_criteria>
- [ ] 用户可以选择多个视频文件
- [ ] 后端创建任务组和任务
- [ ] 任务顺序执行
- [ ] 任务组跟踪进度
- [ ] 部分失败不影响其他任务
- [ ] 前端显示成功反馈
- [ ] 测试覆盖主要场景
</success_criteria>

<output>
After completion, create `.planning/phases/14-batch-operations/14-02-SUMMARY.md`
</output>
