---
phase: 14-batch-operations
plan: 01
type: execute
wave: 1
depends_on: ["14-00"]
files_modified:
  - internal/services/video_file_service.go
  - internal/handlers/video_file_handler.go
  - internal/routes/routes.go
  - frontend/src/api/video-file.ts
  - frontend/src/types/video-file.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
user_setup: []
must_haves:
  truths:
    - 用户可以选择多个文件进行批量下载
    - 后端将文件打包为ZIP并流式响应
    - ZIP内按文件类型分组（video/, ppt/, other/）
    - 前端触发浏览器下载ZIP文件
    - 下载进度和状态有用户反馈
  artifacts:
    - path: "internal/services/video_file_service.go"
      provides: "批量下载服务"
      contains: "BatchDownloadFiles"
      exports: ["BatchDownloadFiles", "BatchDownloadFilesRequest"]
    - path: "internal/handlers/video_file_handler.go"
      provides: "批量下载HTTP处理器"
      contains: "BatchDownloadFiles"
      exports: ["BatchDownloadFiles"]
    - path: "frontend/src/api/video-file.ts"
      provides: "批量下载API客户端"
      contains: "batchDownloadFiles"
      exports: ["batchDownloadFiles", "BatchDownloadFilesRequest"]
    - path: "frontend/src/types/video-file.ts"
      provides: "批量下载类型定义"
      contains: "BatchDownloadFilesRequest"
  key_links:
    - from: "frontend/src/pages/files/index.tsx"
      to: "internal/handlers/video_file_handler.go"
      via: "POST /api/v1/files/batch/download"
      pattern: "batchDownloadFiles"
    - from: "internal/handlers/video_file_handler.go"
      to: "internal/services/video_file_service.go"
      via: "service.BatchDownloadFiles"
      pattern: "BatchDownloadFiles"
---

<objective>
实现批量下载功能：用户选择多个文件后，后端将文件打包为ZIP并流式响应，ZIP内按文件类型分组到不同文件夹。

Purpose: 提供批量文件下载能力，提升用户操作效率，避免逐个下载的繁琐操作。
Output: 后端批量下载API + 前端批量下载按钮和交互
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-batch-operations/14-CONTEXT.md
@.planning/phases/14-batch-operations/14-PATTERNS.md
@.planning/phases/14-batch-operations/14-00-SUMMARY.md

@internal/services/video_file_service.go
@internal/handlers/video_file_handler.go
@internal/handlers/file_handler.go
@frontend/src/api/video-file.ts
@frontend/src/types/video-file.ts
@frontend/src/pages/files/index.tsx

## 批量删除参考模式

从 `BatchDeleteFiles` 实现中学习：

```go
// 请求结构
type BatchDeleteFilesRequest struct {
    IDs []uint `json:"ids" binding:"required,min=1"`
}

// 结果结构
type BatchDeleteFilesResult struct {
    Success int      `json:"success"`
    Failed  int      `json:"failed"`
    Errors  []string `json:"errors"`
}
```

## 流式下载参考模式

从 `FileHandler.Download` 实现中学习：

```go
// 设置响应头
c.Header("Content-Disposition", fmt.Sprintf("attachment; filename=\"%s\"", filename))
c.Header("Content-Type", "application/octet-stream")
c.Header("Accept-Ranges", "bytes")

// 流式响应
c.DataFromReader(200, -1, "application/octet-stream", reader, nil)
```

## 前端下载参考模式

从 `downloadVideoFile` 实现中学习：

```typescript
export function downloadVideoFile(id: number, fileName?: string): void {
  const url = `${API_BASE_URL}/api/v1/files/${id}/download`
  
  fetch(url, { headers: { Authorization: `Bearer ${token}` } })
    .then(response => response.blob())
    .then(blob => {
      const blobUrl = URL.createObjectURL(blob)
      const link = document.createElement('a')
      link.href = blobUrl
      link.download = fileName || `video_${id}.mp4`
      link.click()
      URL.revokeObjectURL(blobUrl)
    })
}
```

## 行选择参考模式

从文件列表页的批量删除实现中学习：

```typescript
const [selectedRowKeys, setSelectedRowKeys] = useState<React.Key[]>([])

// Table rowSelection配置
rowSelection={{
  selectedRowKeys,
  onChange: (selectedKeys) => setSelectedRowKeys(selectedKeys),
  getCheckboxProps: (record: VideoFile) => ({
    disabled: record.status === 'processing',
  }),
}}

// 批量操作按钮
{selectedRowKeys.length > 0 && (
  <Space style={{ marginBottom: 16 }}>
    <Button icon={<DownloadOutlined />}>
      批量下载 ({selectedRowKeys.length})
    </Button>
  </Space>
)}
```

## 决策实现要求

**D-01: 下载交互方式**
- 使用 ZIP 打包下载，用户点击一次下载整个 ZIP 包
- 实现方式：后端创建 ZIP 流，前端触发 blob 下载

**D-02: 批量选择布局**
- 使用 table rowSelection，复选框在每行开头
- 实现方式：复用现有的 rowSelection 模式

**D-03: 进度显示方式**
- 使用 Toast 通知显示打包和下载进度
- 实现方式：message.success() / message.warning()

**D-04: 数量限制策略**
- 无硬性限制
- 如果文件很大（>1GB）或很多（>100）时显示警告提示
- 实现方式：Modal.confirm 显示警告

**D-05: ZIP 打包位置**
- 在 Go 后端使用 `archive/zip` 标准库创建 ZIP 文件
- 实现方式：import "archive/zip"

**D-06: 响应方式**
- 流式响应：边打包边写入 HTTP 响应流
- 实现方式：使用 io.Pipe() 和 goroutine

**D-07: 文件组织方式**
- ZIP 内按文件类型分组到不同文件夹
- 实现方式：createFileInZip(path, folder) 函数
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现后端批量下载服务层</name>
  <files>internal/services/video_file_service.go</files>
  <read_first>
    internal/services/video_file_service.go
    internal/models/video_file.go
    internal/models/ppt_file.go
  </read_first>
  <behavior>
    后端批量下载服务应该：
    - 接受文件ID列表作为输入
    - 查询所有文件并验证所有权
    - 按文件类型分组（.mp4/.mkv → video/, .pptx → ppt/, 其他 → other/）
    - 使用 archive/zip 创建 ZIP 文件
    - 使用 io.Pipe 实现流式响应
    - 返回 io.ReadCloser 供 handler 使用
    - 处理文件不存在的情况（跳过并记录）
    - 返回 ZIP 文件名：files_batch_YYYYMMDD_HHMMSS.zip
  </behavior>
  <action>
    在 internal/services/video_file_service.go 中添加：

    1. 添加请求和响应结构：
    ```go
    // BatchDownloadFilesRequest 批量下载请求
    type BatchDownloadFilesRequest struct {
        IDs []uint `json:"ids" binding:"required,min=1,dive,min=1"`
    }

    // BatchDownloadFilesResponse 批量下载响应
    type BatchDownloadFilesResponse struct {
        Reader      io.ReadCloser
        Filename    string
        ContentType string
        FileCount   int
        TotalSize   int64
    }
    ```

    2. 实现 BatchDownloadFiles 方法：
    ```go
    func (s *VideoFileService) BatchDownloadFiles(ids []uint, userID uint, isAdmin bool) (*BatchDownloadFilesResponse, error) {
        // 查询所有文件
        var files []models.VideoFile
        if err := s.db.Where("id IN ?", ids).Find(&files).Error; err != nil {
            return nil, err
        }

        // 验证所有权
        var validFiles []models.VideoFile
        for _, file := range files {
            if isAdmin || file.CreatedBy == userID {
                if file.Exists() {
                    validFiles = append(validFiles, file)
                }
            }
        }

        if len(validFiles) == 0 {
            return nil, errors.New("没有有效的文件可下载")
        }

        // 创建流式 ZIP
        pr, pw := io.Pipe()
        go func() {
            defer pw.Close()
            zipWriter := zip.NewWriter(pw)
            defer zipWriter.Close()

            for _, file := range validFiles {
                folder := s.getFileFolder(file)
                if err := s.addFileToZip(zipWriter, &file, folder); err != nil {
                    s.logger.Warn("添加文件到ZIP失败",
                        zap.Uint("file_id", file.ID),
                        zap.Error(err),
                    )
                }
            }
        }()

        // 生成文件名
        filename := fmt.Sprintf("files_batch_%s.zip", time.Now().Format("20060102_150405"))

        return &BatchDownloadFilesResponse{
            Reader:      pr,
            Filename:    filename,
            ContentType: "application/zip",
            FileCount:   len(validFiles),
            TotalSize:   0, // 流式响应无法预先知道大小
        }, nil
    }
    ```

    3. 添加辅助方法：
    ```go
    // getFileFolder 根据文件类型返回ZIP内文件夹
    func (s *VideoFileService) getFileFolder(file models.VideoFile) string {
        ext := strings.ToLower(filepath.Ext(file.FilePath))
        switch ext {
        case ".mp4", ".mkv", ".avi", ".mov", ".wmv":
            return "video"
        case ".pptx":
            return "ppt"
        default:
            return "other"
        }
    }

    // addFileToZip 添加文件到ZIP
    func (s *VideoFileService) addFileToZip(zipWriter *zip.Writer, file *models.VideoFile, folder string) error {
        // 打开文件
        f, err := os.Open(file.FilePath)
        if err != nil {
            return err
        }
        defer f.Close()

        // 创建ZIP内文件路径
        zipPath := filepath.Join(folder, filepath.Base(file.FilePath))

        // 创建ZIP文件头
        header := &zip.FileHeader{
            Name:   zipPath,
            Method: zip.Deflate, // 使用压缩
        }
        header.SetModTime(time.Now())

        // 创建ZIP写入器
        writer, err := zipWriter.CreateHeader(header)
        if err != nil {
            return err
        }

        // 复制文件内容
        _, err = io.Copy(writer, f)
        return err
    }
    ```

    注意事项：
    - 使用 io.Pipe 实现流式响应，避免内存占用
    - 使用 goroutine 在后台写入 ZIP
    - 验证文件所有权，防止未授权访问
    - 文件不存在时跳过并记录警告
    - 使用 zip.Deflate 压缩方法减小文件大小
  </action>
  <verify>
    <automated>cd internal/services && go test -v -run TestVideoFileService_BatchDownloadFiles</automated>
  </verify>
  <done>
    - BatchDownloadFiles 方法实现完成
    - 支持流式 ZIP 响应
    - 文件按类型分组到不同文件夹
    - 所有权验证通过
    - 测试通过（14-00 创建的测试桩）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现后端批量下载Handler</name>
  <files>internal/handlers/video_file_handler.go, internal/routes/routes.go</files>
  <read_first>
    internal/handlers/video_file_handler.go
    internal/handlers/file_handler.go
    internal/routes/routes.go
  </read_first>
  <behavior>
    批量下载 Handler 应该：
    - 解析请求中的文件ID列表
    - 验证用户身份和权限
    - 调用服务层获取 ZIP 流
    - 设置正确的 HTTP 响应头
    - 流式返回 ZIP 文件
    - 处理错误情况（无文件、权限错误）
  </behavior>
  <action>
    在 internal/handlers/video_file_handler.go 中添加：

    1. 实现 BatchDownloadFiles 方法：
    ```go
    // BatchDownloadFiles 批量下载文件（打包为ZIP）
    func (h *VideoFileHandler) BatchDownloadFiles(c *gin.Context) {
        var req struct {
            IDs []uint `json:"ids" binding:"required,min=1,dive,min=1"`
        }
        if err := c.ShouldBindJSON(&req); err != nil {
            response.GinError(c, response.CodeInvalidRequest, "参数错误")
            return
        }

        // 获取用户信息
        userID := middleware.GetUserID(c)
        isAdmin := middleware.CanAccessAllData(c)

        // 调用服务层
        resp, err := h.fileService.BatchDownloadFiles(req.IDs, userID, isAdmin)
        if err != nil {
            h.logger.Error("批量下载文件失败",
                zap.Uint("user_id", userID),
                zap.Int("file_count", len(req.IDs)),
                zap.Error(err),
            )
            response.GinError(c, response.CodeInternalError, err.Error())
            return
        }
        defer resp.Reader.Close()

        // 设置响应头
        c.Header("Content-Disposition", fmt.Sprintf("attachment; filename=\"%s\"", resp.Filename))
        c.Header("Content-Type", resp.ContentType)
        c.Header("Accept-Ranges", "none") // ZIP 不支持范围请求
        c.Header("Access-Control-Allow-Origin", "*")
        c.Header("Access-Control-Expose-Headers", "Content-Disposition, Content-Type")

        // 流式响应
        c.DataFromReader(200, -1, resp.ContentType, resp.Reader, nil)

        h.logger.Info("批量下载文件成功",
            zap.Uint("user_id", userID),
            zap.Int("file_count", resp.FileCount),
            zap.String("filename", resp.Filename),
        )
    }
    ```

    2. 在 internal/routes/routes.go 中注册路由：
    ```go
    // 在 filesAPI 路由组中添加
    filesAPI.POST("batch/download", videoFileHandler.BatchDownloadFiles)
    ```

    注意事项：
    - 使用 binding:"required,min=1,dive,min=1" 验证ID列表
    - 复用现有的权限检查中间件
    - 设置 Content-Disposition 头触发浏览器下载
    - 使用 defer 确保 Reader 关闭
    - 设置 Accept-Ranges 为 none（ZIP 不支持范围请求）
  </action>
  <verify>
    <automated>cd internal/handlers && go test -v -run TestVideoFileHandler_BatchDownloadFiles</automated>
    <automated>grep -n "POST.*batch/download" internal/routes/routes.go</automated>
  </verify>
  <done>
    - BatchDownloadFiles handler 实现完成
    - 路由注册完成
    - HTTP 响应头设置正确
    - 流式响应正常工作
    - 测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现前端批量下载API客户端</name>
  <files>frontend/src/api/video-file.ts, frontend/src/types/video-file.ts</files>
  <read_first>
    frontend/src/api/video-file.ts
    frontend/src/types/video-file.ts
  </read_first>
  <behavior>
    前端批量下载 API 应该：
    - 接受文件ID列表作为参数
    - 发送 POST 请求到 /api/v1/files/batch/download
    - 接收 ZIP 文件流
    - 触发浏览器下载 ZIP 文件
    - 处理错误情况
  </behavior>
  <action>
    1. 在 frontend/src/types/video-file.ts 中添加类型定义：
    ```typescript
    // 批量下载请求
    export interface BatchDownloadFilesRequest {
      ids: number[]
    }

    // 批量下载结果（元数据）
    export interface BatchDownloadFilesResult {
      file_count: number
      filename: string
      content_type: string
    }
    ```

    2. 在 frontend/src/api/video-file.ts 中添加 API 函数：
    ```typescript
    // 批量下载文件（打包为ZIP）
    export function batchDownloadFiles(ids: number[]): void {
      const token = getToken()
      const url = `${API_BASE_URL}/api/v1/files/batch/download`

      // 显示加载提示
      const hide = message.loading('正在打包文件，请稍候...', 0)

      fetch(url, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          ...(token ? { Authorization: `Bearer ${token}` } : {}),
        },
        body: JSON.stringify({ ids }),
      })
        .then(response => {
          if (!response.ok) {
            throw new Error(`下载失败: ${response.status}`)
          }
          return response.blob()
        })
        .then(blob => {
          // 从响应头获取文件名
          const filename = `files_batch_${dayjs().format('YYYYMMDD_HHmmss')}.zip`

          // 创建下载链接
          const blobUrl = URL.createObjectURL(blob)
          const link = document.createElement('a')
          link.href = blobUrl
          link.download = filename
          link.click()
          URL.revokeObjectURL(blobUrl)

          message.success(`成功下载 ${ids.length} 个文件`)
        })
        .catch(error => {
          console.error('批量下载失败:', error)
          message.error(error instanceof Error ? error.message : '批量下载失败')
        })
        .finally(() => {
          hide()
        })
    }
    ```

    注意事项：
    - 使用 message.loading 显示打包进度
    - 使用 fetch API 处理 blob 响应
    - 使用 dayjs 生成时间戳文件名
    - 确保下载后清理 blob URL
    - 处理错误并显示用户友好的消息
  </action>
  <verify>
    <automated>grep -n "batchDownloadFiles" frontend/src/api/video-file.ts</automated>
    <automated>grep -n "BatchDownloadFilesRequest" frontend/src/types/video-file.ts</automated>
  </verify>
  <done>
    - batchDownloadFiles 函数实现完成
    - 类型定义添加完成
    - 支持 ZIP 文件下载
    - 错误处理完整
    - 用户反馈清晰
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    后端批量下载服务层和Handler已实现，前端API客户端已添加。
  </what-built>
  <how-to-verify>
    1. 启动后端服务
    2. 启动前端开发服务器
    3. 访问文件管理页面
    4. 选择多个文件（混合视频和PPT）
    5. 点击「批量下载」按钮
    6. 验证：
       - 浏览器下载了ZIP文件
       - ZIP内文件按类型分组（video/, ppt/, other/）
       - 所有选中文件都包含在ZIP中
       - 下载进度有Toast提示
    7. 测试边界情况：
       - 选择单个文件
       - 选择大量文件（>100）
       - 选择不存在的文件（应跳过）
  </how-to-verify>
  <resume-signal>输入 "approved" 确认批量下载功能正常，或描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | 批量下载请求跨过此边界 |
| API→filesystem | 批量文件访问跨过此边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | S | BatchDownloadFiles | mitigate | 验证每个文件的用户所有权，防止未授权访问 |
| T-14-01-02 | I | ZIP creation | mitigate | 验证文件路径，防止路径遍历攻击 |
| T-14-01-03 | D | ZIP download | accept | ZIP文件用于数据打包，无敏感信息 |
| T-14-01-04 | E | ZIP streaming | mitigate | 使用io.Pipe限制内存使用，防止大文件DoS |

实施的安全措施：
- userID 和 isAdmin 验证：每个文件必须属于当前用户或用户有admin权限
- 文件路径验证：使用 filepath.Base 防止路径遍历
- 流式响应：使用 io.Pipe 避免大ZIP占用内存
</threat_model>

<verification>
- [ ] 后端服务层测试通过
- [ ] 后端Handler测试通过
- [ ] 前端API函数正确调用后端
- [ ] 人工验证批量下载功能正常
- [ ] ZIP 文件结构正确
- [ ] 文件分组正确
</verification>

<success_criteria>
- [ ] 用户可以选择多个文件
- [ ] 后端打包为ZIP并流式响应
- [ ] ZIP内文件按类型分组
- [ ] 前端触发浏览器下载
- [ ] 有进度和状态反馈
- [ ] 测试覆盖主要场景
</success_criteria>

<output>
After completion, create `.planning/phases/14-batch-operations/14-01-SUMMARY.md`
</output>
