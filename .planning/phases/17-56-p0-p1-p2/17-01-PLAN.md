---
phase: 17-56-p0-p1-p2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/auth/hlstoken/hls_token.go
  - internal/auth/hlstoken/hls_token_test.go
  - cmd/server/app.go
  - cmd/server/app_test.go
  - internal/auth/service.go
  - internal/huawei/manager.go
  - internal/huawei/manager_test.go
  - internal/huawei/client.go
  - internal/services/video_recording_task_service.go
  - internal/services/video_recording_task_service_test.go
  - internal/services/transcription_service.go
  - internal/auth/sm4_token.go
  - internal/services/storage/file_service.go
  - internal/services/video_file_service.go
  - internal/middleware/apikey.go
  - internal/scheduler/video_scheduler.go
  - internal/services/role_service.go
  - internal/middleware/auth.go
  - internal/recorder/coordinator.go
  - internal/recorder/coordinator_test.go
  - internal/handlers/admin_handler.go
  - internal/handlers/transcription_handler.go
  - internal/handlers/video_recording_task_handler.go
  - DEPLOYMENT.md
  - BUILD.md
  - SECURITY.md
  - .env.example
autonomous: true
requirements:
  - SEC-001
  - SEC-002
  - SEC-003a
  - SEC-004
  - BUG-001
  - BUG-002
  - PERF-001
  - PERF-002
  - PERF-003
  - PERF-004
  - PERF-005
user_setup: []
must_haves:
  truths:
    - "生产环境启动时若 SM4_SECRET/HLS_TOKEN_SECRET 缺失或 < 32 字节则进程以 logger.Fatal 退出（不可降级为 warn）"
    - "审计服务已通过 SetAuditService 注入到 authService，所有 6 个 auth 审计点不再因 nil 短路"
    - "HLS Token 签发用 base64.RawURLEncoding；Verify 同时接受 StdEncoding 旧 token，重启后旧 token 仍可验证一次"
    - "HLS Token 同一 jti 在进程生命周期内只能 Verify 通过一次"
    - "Huawei 客户端 TLS 配置：MinTLSVersion=tls.VersionTLS12、InsecureSkipVerify 默认 false、3DES cipher 全部移除（SEC-003a 完成；SEC-003b DB 密码加密延后）"
    - "RetryTask 保留原 duration 后再重算 EndTime，EndTime 永远等于 StartTime + 原 duration"
    - "8 个 fire-and-forget goroutine 入口都有 defer recover，goroutine 内 panic 不会拖垮整个进程"
    - "video_recording_task_service DeleteTask/BatchDeleteTasks/ClearStuckTasks 用 Pluck+IN 批量改写 input_config"
    - "3 个 sync-IO handler（admin migration / transcription / HLS）保持 HTTP 200 + 原 JSON 形状，改用 bounded concurrency semaphore 限流（D-03.8，不引任务队列/不改 202）"
    - "DEPLOYMENT.md / BUILD.md / SECURITY.md / .env.example 与代码 env var 名称、长度、校验行为一致"
  artifacts:
    - path: "internal/config/config.go"
      provides: "ValidateProductionSecrets() + BindEnv for SM4_SECRET/HLS_TOKEN_SECRET + removed change-me-in-production default"
      contains: "func.*ValidateProductionSecrets"
    - path: "internal/auth/hlstoken/hls_token.go"
      provides: "secret length ≥ 32 check at constructor + RawURLEncoding sign + dual-encode verify + usedJTIs map"
      contains: "usedJTIs"
    - path: "cmd/server/app.go"
      provides: "authService.SetAuditService(auditService) line between authService creation and auditService creation"
      contains: "authService.SetAuditService"
    - path: "internal/huawei/manager.go"
      provides: "TLS config: MinTLSVersion=tls.VersionTLS12, InsecureSkipVerify from cfg, no 3DES cipher"
      contains: "tls.VersionTLS12"
    - path: "internal/services/video_recording_task_service.go"
      provides: "RetryTask EndTime = StartTime + duration pattern at line 786-789"
      contains: "duration := task.EndTime.Sub"
    - path: "internal/config/config_test.go"
      provides: "production+empty→Fatal, production+<32→Fatal, production+≥32→OK, dev+empty→Warn tests"
    - path: "internal/auth/hlstoken/hls_token_test.go"
      provides: "constructor length check + dual-encode backward compat + jti replay rejection tests"
    - path: "internal/services/video_recording_task_service_test.go"
      provides: "BUG-001 table-driven + PERF-001 Pluck batch tests"
    - path: "internal/recorder/coordinator_test.go"
      provides: "PERF-004 lock window test + PERF-005 bounded-concurrency test"
    - path: "DEPLOYMENT.md"
      provides: "env var list + 启动校验行为 section"
      contains: "SM4_SECRET"
    - path: "BUILD.md"
      provides: "viper env var 名修正（无 RECORD 前缀）"
    - path: "SECURITY.md"
      provides: "SECRET 校验 + HLS Token 安全 + TLS 最低版本 sections"
    - path: ".env.example"
      provides: "SM4_SECRET=<必须显式设置，最小 32 字符> + HLS_TOKEN_SECRET=<...>"
  key_links:
    - from: "cmd/server/app.go"
      to: "internal/auth/service.go"
      via: "authService.SetAuditService(auditService) injection at app.go:~560"
      pattern: "authService\\.SetAuditService"
    - from: "internal/auth/hlstoken/hls_token.go"
      to: "internal/config/config.go"
      via: "NewHLSToken called with cfg.HLS.HLSTokenSecret (which must be ≥ 32 bytes)"
      pattern: "NewHLSToken.*HLSTokenSecret"
    - from: "internal/huawei/manager.go"
      to: "internal/config/config.go"
      via: "TLSConfig built from cfg.Huawei.MinTLSVersion + cfg.Huawei.InsecureSkipVerify"
      pattern: "MinTLSVersion.*cfg\\.Huawei"
    - from: "internal/services/video_recording_task_service.go"
      to: "internal/models/TaskInputConfig + InputConfig"
      via: "Pluck + IN batch UPDATE in DeleteTask/BatchDeleteTasks/ClearStuckTasks"
      pattern: "Pluck.*input_config_id"
    - from: "internal/handlers/admin_handler.go + transcription_handler.go + video_recording_task_handler.go"
      to: "bounded concurrency semaphore"
      via: "chan struct{} semaphore + sync.WaitGroup inside handler (HTTP 200 preserved)"
      pattern: "sem.*chan struct{}|MigrationConcurrency"
    - from: "DEPLOYMENT.md / BUILD.md / SECURITY.md / .env.example"
      to: "internal/config/config.go"
      via: "env var name match (SM4_SECRET, HLS_TOKEN_SECRET, HUAWEI_MIN_TLS_VERSION)"
      pattern: "SM4_SECRET|HLS_TOKEN_SECRET|HUAWEI_MIN_TLS_VERSION"
---

<objective>
Address 11 HIGH-severity code findings (P0) from the backend code review by fixing security (SEC-001/002/003a/004), correctness (BUG-001/002), and performance (PERF-001/002/003/004/005) issues, add production startup secret validation, and synchronize deployment documentation.

Purpose: Eliminate the 5 worst exploitable attack chains (config leak → impersonation; silent audit gap → forensic blindness; TLS downgrade → MITM credential theft; token replay → unauthorized HLS access; runtime N+1 → connection pool exhaustion) plus 2 silent data-correctness bugs that the audit marked as blocking.

Output: All 11 P0 code findings remediated in code with unit-test coverage per D-04.1; 4 docs synchronized; one mega commit `fix(17-p0): P0 HIGH 发现修复（11 项）` produced. **SEC-003b (Huawei password DB encryption) is deferred per CONTEXT.md `<deferred>` — only SEC-003a (TLS + ctx) is in this plan.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-56-p0-p1-p2/17-CONTEXT.md
@.planning/phases/17-56-p0-p1-p2/17-DISCUSSION-LOG.md
@docs/audits/2026-07-30-backend-code-review.md
@.claude/skills/spike-findings-record-v2/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security core — SM4 secret startup validation + audit DI + HLS token signing</name>
  <files>
    - internal/config/config.go
    - internal/config/config_test.go
    - internal/auth/hlstoken/hls_token.go
    - internal/auth/hlstoken/hls_token_test.go
    - cmd/server/app.go
    - cmd/server/app_test.go
    - internal/auth/service.go
  </files>
  <read_first>
    - D:\CODE\ClaudeCode\record_V2\internal\config\config.go
    - D:\CODE\ClaudeCode\record_V2\internal\auth\hlstoken\hls_token.go
    - D:\CODE\ClaudeCode\record_V2\cmd\server\app.go
    - D:\CODE\ClaudeCode\record_V2\internal\auth\service.go
    - D:\CODE\ClaudeCode\record_V2\internal\auth\local_auth.go
    - D:\CODE\ClaudeCode\record_V2\.planning\phases\17-56-p0-p1-p2\17-CONTEXT.md
  </read_first>
  <action>
**SEC-001 (Config + Secret Validation, per D-03.1, D-03.4):**

1. In `internal/config/config.go`: change `SetEnvPrefix("RECORD")` to `SetEnvPrefix("")` OR keep prefix and add explicit `BindEnv("auth.sm4_secret", "SM4_SECRET")`, `BindEnv("hls.token_secret", "HLS_TOKEN_SECRET")`, `BindEnv("huawei.insecure_skip_verify", "HUAWEI_INSECURE_SKIP_VERIFY")`, `BindEnv("huawei.min_tls_version", "HUAWEI_MIN_TLS_VERSION")` — must use whichever pattern makes `os.Getenv("SM4_SECRET")` actually load into viper key `auth.sm4_secret`. Document the chosen pattern in the comment block above `BindEnv` calls.
2. In `setDefaults()` (around lines 360-361, 373-374, 584, 588): REMOVE the literal string `"change-me-in-production"` defaults for both `Auth.SM4Secret` and `HLS.HLSTokenSecret`. Both default to `""` (empty string) at config struct level.
3. Stop `HLS.HLSTokenSecret` from auto-falling-back to `Auth.SM4Secret` (currently happens at config.go:373-374). Require explicit value or fail validation.
4. Add `func (c *Config) ValidateProductionSecrets(logger *zap.Logger)` method: if `c.Environment == "production"` AND (`len(c.Auth.SM4Secret) < 32` OR `len(c.HLS.HLSTokenSecret) < 32` OR `c.Auth.SM4Secret == c.HLS.HLSTokenSecret` (must differ)) → `logger.Fatal("SM4_SECRET 和 HLS_TOKEN_SECRET 必须显式设置、≥ 32 字符、且互不相同")`. Non-production + empty → `logger.Warn(...)`. Call from `Load()` end.

**SEC-002 (Audit DI — one-line fix per CONTEXT.md):**

5. In `cmd/server/app.go`, locate the line where `authService` is constructed (~line 557). Immediately after that line, BEFORE the line that constructs `auditService` (~line 578), insert one line: `authService.SetAuditService(auditService)`. This mirrors the existing pattern `apikeyService.SetAuditService(auditService)` at ~line 598. Order matters: `auditService` must already exist or be hoisted.

**SEC-004 (HLS Token — per D-03.3 backward compat + jti replay):**

6. In `internal/auth/hlstoken/hls_token.go`: at the START of `NewHLSToken(secret string, ...)` constructor, add: `if len(secret) < 32 { log.Fatal("HLS token secret 必须 ≥ 32 字符（Hex 编码）") }`.
7. In the signing path (where `base64.StdEncoding.EncodeToString` is currently used): change to `base64.RawURLEncoding.EncodeToString`.
8. In `Verify()` decode path: try `base64.RawURLEncoding.DecodeString(provided)` first; on error, fall back to `base64.StdEncoding.DecodeString(provided)`. Accept both encodings (D-03.3 backward compat).
9. Add `jti` claim (16 random bytes hex-encoded via `crypto/rand`).
10. Add `usedJTIs map[string]struct{}` + `sync.Mutex` field on the token struct. In `Verify()` after successful signature validation: check jti under lock; if already in map → reject (return ErrTokenReplayed); otherwise add to map.
11. Document the in-memory `usedJTIs` limitation in a comment: "进程重启后 jti 记录清零；服务端持久化 used_jtis 留作下个独立 phase (per CONTEXT.md deferred)".

**Tests (per D-04.1 — each P0 fix needs unit test):**

12. `internal/config/config_test.go` (new file): table-driven test `TestValidateProductionSecrets` covering:
    - production + SM4Secret="" → panic/Fatal (use `zap/zaptest/observer` to capture fatal log)
    - production + SM4Secret=31 chars → Fatal
    - production + SM4Secret=32 chars → OK
    - production + HLSTokenSecret=32 chars but same as SM4Secret → Fatal
    - development + SM4Secret="" → Warn only, no Fatal
    Test the `BindEnv` mapping: `os.Setenv("SM4_SECRET", "abc...")` then `Load()` → `cfg.Auth.SM4Secret == "abc..."`.
13. `cmd/server/app_test.go` (new): minimal test that constructs `authService` + `auditService` mocks and verifies wiring. Use a tiny `mockAuthService` that records `SetAuditService` calls (count = 1). Alternative: extract the wiring into a testable function and assert.
14. `internal/auth/hlstoken/hls_token_test.go` (new): table-driven `TestHLSVerifyBackwardCompat`:
    - Case A: sign with StdEncoding (old code) → verify with new code → PASS
    - Case B: sign with RawURLEncoding (new code) → verify with new code → PASS
    - Case C: same jti verified twice → second returns ErrTokenReplayed
    - Case D: `NewHLSToken` with secret length 16 → Fatal (zap observer)
    Use `crypto/rand` for test secret generation (32 bytes).

**No-go list (do not):**
- Do NOT introduce Redis dependency for jti (deferred per CONTEXT.md).
- Do NOT change viper key names (`auth.sm4_secret`, `hls.token_secret`) — only adjust prefix/binding behavior.
- Do NOT modify the public function signatures of `NewHLSToken`/`Verify` to break callers; add new parameters as variadic or options.
  </action>
  <verify>
    <automated>go test ./internal/config/... ./internal/auth/... ./cmd/server/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "change-me-in-production" internal/config/config.go` returns 0
    - `grep -n "ValidateProductionSecrets" internal/config/config.go` returns ≥ 2 matches (definition + caller)
    - `grep -c "BindEnv.*SM4_SECRET" internal/config/config.go` ≥ 1
    - `grep -c "authService.SetAuditService" cmd/server/app.go` returns 1
    - `grep -c "len(secret) < 32" internal/auth/hlstoken/hls_token.go` ≥ 1
    - `grep -c "base64.RawURLEncoding" internal/auth/hlstoken/hls_token.go` ≥ 1
    - `grep -c "base64.StdEncoding" internal/auth/hlstoken/hls_token.go` ≥ 1 (Verify fallback)
    - `grep -c "usedJTIs" internal/auth/hlstoken/hls_token.go` ≥ 2 (field + access)
    - `go test ./internal/config/... ./internal/auth/... ./cmd/server/...` exits 0
    - `go build ./...` exits 0
    - All 3 new test files exist: `internal/config/config_test.go`, `internal/auth/hlstoken/hls_token_test.go`, `cmd/server/app_test.go`
  </acceptance_criteria>
  <done>
    All three high-severity security findings (SEC-001, SEC-002, SEC-004) are fixed; backward-compat HMAC verification keeps old tokens valid for one restart; jti replay protection active within process lifetime; production startup validation triggers `logger.Fatal` for missing/short secrets; unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Huawei TLS hardening SEC-003a — remove 3DES, enforce TLS 1.2, ctx propagation (NOT password DB encryption — SEC-003b deferred)</name>
  <files>
    - internal/huawei/manager.go
    - internal/huawei/manager_test.go
    - internal/huawei/client.go
    - internal/config/config.go
    - internal/config/config_test.go
  </files>
  <read_first>
    - D:\CODE\ClaudeCode\record_V2\internal\huawei\manager.go
    - D:\CODE\ClaudeCode\record_V2\internal\huawei\client.go
    - D:\CODE\ClaudeCode\record_V2\internal\config\config.go
    - D:\CODE\ClaudeCode\record_V2\internal\auth\sm4_token.go
    - D:\CODE\ClaudeCode\record_V2\internal\models\input_config.go
    - D:\CODE\ClaudeCode\record_V2\.planning\phases\17-56-p0-p1-p2\17-CONTEXT.md
  </read_first>
  <action>
**SEC-003a (Huawei TLS hardening per D-03.5 — ONLY TLS three sub-items + ctx propagation):**

1. In `internal/huawei/manager.go` (around lines 80-81, the `tls.Config{}` block):
   - Replace `MinTLSVersion: 0x0301` (TLS 1.0) with `MinTLSVersion: cfg.Huawei.MinTLSVersion`. Add `if cfg.Huawei.MinTLSVersion == 0 { cfg.Huawei.MinTLSVersion = tls.VersionTLS12 }` normalization just before constructing `tls.Config`.
   - Replace `InsecureSkipVerify: true` with `InsecureSkipVerify: cfg.Huawei.InsecureSkipVerify` (default false via config struct zero value).
   - In production: if `cfg.Huawei.InsecureSkipVerify == true` → log Fatal (defense-in-depth, even if cfg default is false).
2. In `internal/huawei/manager.go` CipherSuites list (and `internal/huawei/client.go:250` if separate): REMOVE all 3DES-based ciphers. Keep ONLY ECDHE + AES-GCM/CHACHA20:
   - `tls.TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256`
   - `tls.TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384`
   - `tls.TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256`
   - `tls.TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384`
   - `tls.TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305`
   - `tls.TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305`
   Choose explicit list to make cipher policy auditable.
3. In `internal/config/config.go` `HuaweiConfig` struct: add fields:
   - `InsecureSkipVerify bool   // default false; production must be false`
   - `MinTLSVersion      uint16 // default 0 = auto → tls.VersionTLS12`
   In `setDefaults()`: NO defaults for these (zero values). In `BindEnv`: `huawei.insecure_skip_verify` + `huawei.min_tls_version` from env vars listed in Task 1.
4. In `internal/huawei/client.go`: find every call to `client.Logout(...)` (in `removeClient` and `Close` paths) and replace any `context.Background()` literal with the client's stored cancel-context. Add `cancel context.CancelFunc` field to `*HuaweiClient`, populated in `NewHuaweiClient` via `ctx, cancel := context.WithCancel(context.Background())`. Use `c.cancel` in `Logout` paths.

**SEC-003b (Huawei password DB encryption) — DEFERRED per CONTEXT.md `<deferred>`:**

5. Do NOT implement password encryption in this task. In the huawei credential fetch path (search for `InputConfig.Password` usage in `internal/services/` and `internal/huawei/`), add a single short comment: `// SEC-003b deferred (per CONTEXT.md): 华为密码 DB 加密需独立迁移 + 前端/配置联动`. Do NOT add TODO spam at multiple sites; one comment at the primary fetch point suffices.

**Tests:**

6. `internal/huawei/manager_test.go` (new or extend existing if `manager_test.go` exists):
   - Test `NewHuaweiManager(cfg)` returns manager whose `tlsConfig.MinTLSVersion == tls.VersionTLS12` when `cfg.Huawei.MinTLSVersion == 0`.
   - Test `cfg.Huawei.InsecureSkipVerify == false` by default.
   - Test `tlsConfig.CipherSuites` does NOT contain `tls.TLS_RSA_WITH_3DES_EDE_CBC_SHA` (constant 0x000A). Iterate the slice and assert.
   - Test production + `InsecureSkipVerify=true` → Fatal log captured (zap observer).
7. `internal/config/config_test.go` (extend from Task 1): Test that `os.Setenv("HUAWEI_MIN_TLS_VERSION", "771")` (=0x0303=TLS 1.2) → `cfg.Huawei.MinTLSVersion == 771`.

**No-go list (do not):**
- Do NOT change the existing function signatures of `NewHuaweiClient`/`NewHuaweiManager` in a breaking way; if signature changes are unavoidable, update all callers in same commit.
- Do NOT remove `CipherSuites` field entirely; keep explicit list for auditability.
- Do NOT implement SEC-003b password DB encryption (deferred).
  </action>
  <verify>
    <automated>go test ./internal/huawei/... ./internal/config/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "0x0301" internal/huawei/manager.go` returns 0
    - `grep -c "InsecureSkipVerify: true" internal/huawei/manager.go` returns 0
    - `grep -c "3DES_EDE_CBC_SHA" internal/huawei/manager.go internal/huawei/client.go` returns 0 across both
    - `grep -c "MinTLSVersion" internal/config/config.go` ≥ 1 (struct field)
    - `grep -c "context.Background()" internal/huawei/client.go` returns 0 (or only in legitimate startup)
    - `grep -c "tls.VersionTLS12" internal/huawei/manager.go` ≥ 1
    - `grep -c "SEC-003b deferred" internal/` returns ≥ 1 (single marker comment, no TODO spam)
    - `go test ./internal/huawei/... ./internal/config/...` exits 0
    - `internal/huawei/manager_test.go` exists with ≥ 3 test cases
  </acceptance_criteria>
  <done>
    SEC-003a complete: Huawei TLS config uses TLS 1.2 minimum, InsecureSkipVerify defaults false, all 3DES ciphers removed, ctx propagation fixed in client Logout paths, unit tests verify TLS configuration. SEC-003b (password DB encryption) explicitly deferred per CONTEXT.md with a single marker comment.
  </done>
</task>

<task type="auto">
  <name>Task 3a: Correctness + bulk-query perf — BUG-001/002 + PERF-001/002/003 + D-05 doc sync</name>
  <files>
    - internal/services/video_recording_task_service.go
    - internal/services/video_recording_task_service_test.go
    - internal/services/transcription_service.go
    - internal/auth/sm4_token.go
    - internal/services/storage/file_service.go
    - internal/services/video_file_service.go
    - internal/middleware/apikey.go
    - internal/scheduler/video_scheduler.go
    - internal/services/role_service.go
    - internal/middleware/auth.go
    - DEPLOYMENT.md
    - BUILD.md
    - SECURITY.md
    - .env.example
  </files>
  <read_first>
    - D:\CODE\ClaudeCode\record_V2\internal\services\video_recording_task_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\transcription_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\auth\sm4_token.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\storage\file_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\video_file_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\middleware\apikey.go
    - D:\CODE\ClaudeCode\record_V2\internal\scheduler\video_scheduler.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\role_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\middleware\auth.go
    - D:\CODE\ClaudeCode\record_V2\DEPLOYMENT.md
    - D:\CODE\ClaudeCode\record_V2\BUILD.md
    - D:\CODE\ClaudeCode\record_V2\SECURITY.md
    - D:\CODE\ClaudeCode\record_V2\.env.example
    - D:\CODE\ClaudeCode\record_V2\.planning\phases\17-56-p0-p1-p2\17-CONTEXT.md
  </read_first>
  <action>
**BUG-001 (RetryTask EndTime corruption — CONTEXT.md `<specifics>` already provides the pattern):**

1. In `internal/services/video_recording_task_service.go:786-789`: capture duration BEFORE reassigning StartTime. Replace the broken block with:
   - line A: `duration := task.EndTime.Sub(task.StartTime)`
   - line B: `task.StartTime = newTriggerTime.Add(time.Duration(task.PreJoinMinutes) * time.Minute)`
   - line C: `task.EndTime = task.StartTime.Add(duration)`

**BUG-002 (8 fire-and-forget goroutines — add recover() defer):**

2. Insert at the TOP of the goroutine body in each of these files (BEFORE any other code in the `go func()`):
   ```go
   defer func() {
       if r := recover(); r != nil {
           logger.Error("goroutine panicked", zap.Any("recover", r), zap.Stack("stack"))
       }
   }()
   ```
   Sites:
   - `internal/services/transcription_service.go:911` (cleanupOrphanedOSSFiles, 1h ticker)
   - `internal/auth/sm4_token.go:394` (GracePeriod revoke)
   - `internal/services/storage/file_service.go:280` (async delete)
   - `internal/services/video_recording_task_service.go:313` (scheduler sync)
   - `internal/services/video_recording_task_service.go:797` (scheduler sync)
   - `internal/services/video_file_service.go:1585` (zip writer)
   - `internal/huawei/client.go:591` (keep-alive)
   - `internal/middleware/apikey.go:30` (usage log)

**PERF-001 (N+1 in DeleteTask / BatchDeleteTasks / ClearStuckTasks):**

3. `video_recording_task_service.go:582-590` (DeleteTask): replace per-task input_config loop with:
   - `ids := []uint{}`
   - `s.db.Model(&models.TaskInputConfig{}).Where("task_id = ?", taskID).Pluck("input_config_id", &ids)`
   - `s.db.Model(&models.InputConfig{}).Where("id IN ?", ids).Update("is_locked", false)`
4. Same pattern for `:975-1006` (BatchDeleteTasks) — single Pluck across all task IDs, single bulk UPDATE.
5. Same pattern for `:999-1006` (ClearStuckTasks).
6. Hidden N+1 in Preload chains (`:96, :180, :301, :438`): replace `.Preload("InputConfig").Preload("TaskInputConfigs").Preload("Creator")` with a single query that JOINs to fetch only needed columns, OR document that for N≤100 it's acceptable and add `.Limit(100)` cap. Add `// PERF-001: bounded preload` comment.

**PERF-002 (.Find without Limit — 18 sites):**

7. Add `.Limit(N)` cap to:
   - `internal/scheduler/video_scheduler.go:311, 360, 566, 1208` → use `cfg.MaxTasksPerPage` (default 1000) for list paths; 5000 cap for cleanup paths with `//nolint:perffunc // bounded cleanup` comment.
   - `internal/services/role_service.go:199, 233` → `.Limit(1000)`
   - `internal/services/video_file_service.go:364, 1046, 1125, 1459, 1551` → `.Limit(1000)` for list endpoints; cleanup paths `.Limit(5000)` + comment.
   - `internal/services/video_recording_task_service.go:476, 554, 584, 819, 899, 964, 999` → same pattern.

**PERF-003 (GORM `WithContext(ctx)` — focused on functions touched in this task per CONTEXT.md, full 403-site sweep is deferred):**

8. For each GORM call site in files modified by Task 3a (BUG-001, BUG-002, PERF-001, PERF-002): prefix with `.WithContext(ctx)`. Specifically touch every `.Find/.First/.Where/.Create/.Update/.Delete/.Save/.Count/.Pluck/.Update` call in:
   - `internal/services/video_recording_task_service.go` (entire file: ~30 sites touched by BUG/PERF fixes)
   - `internal/middleware/auth.go` (any DB-touching function)
   - `internal/services/role_service.go` (PERF-002 touch)
   - `internal/services/video_file_service.go` (PERF-002 touch)
   - Files NOT modified by other P0 fixes: skip (deferred per CONTEXT.md).

**D-05 Doc sync (committed in same mega commit as code; per D-05.1–D-05.5 only the 4 authorized docs):**

9. `DEPLOYMENT.md`: add section `## 环境变量与启动校验` with table:
   - `SM4_SECRET`: 必填，最小 32 字符；生产环境缺失则 `logger.Fatal` 退出。
   - `HLS_TOKEN_SECRET`: 必填，最小 32 字符，必须与 SM4_SECRET 互不相同。
   - `HUAWEI_MIN_TLS_VERSION`: 默认 TLS 1.2 (0x0303)。
   - `HUAWEI_INSECURE_SKIP_VERIFY`: 默认 false；生产环境为 true 则 `logger.Fatal`。
   Add subsection `### 启动校验行为` describing what happens when validation fails.
10. `BUILD.md`: if it contains `SetEnvPrefix("RECORD")` example, replace with the actual chosen pattern (`SetEnvPrefix("")` or explicit `BindEnv`). Add note "env var names are CASE-SENSITIVE per viper".
11. `SECURITY.md`: add sections:
    - `## SECRET 校验` (production startup validation)
    - `## HLS Token 安全` (jti 防重放 + RawURLEncoding 变更 + 旧 token 向后兼容承诺)
    - `## TLS 最低版本` (TLS 1.2 强制 + Huawei 去 3DES)
12. `.env.example`: replace any `change-me-...` default value with placeholder `SM4_SECRET=<必须显式设置，最小 32 字符>` + comment line explaining. Same for `HLS_TOKEN_SECRET`.

**Tests (per D-04.1, BUG-001 + PERF-001):**

13. `internal/services/video_recording_task_service_test.go` (new or extend existing): table-driven `TestRetryTaskEndTime` covering:
    - Case A: original duration 30min, newTriggerTime shifts by 10min → EndTime - StartTime == 30min.
    - Case B: original duration 5min, shift 0min → EndTime = newStart + 5min.
    - Case C: original duration 60min, shift backward by 30min → EndTime = newStart + 60min (NOT negative).
    Use SQLite in-memory DB.
14. `internal/services/video_recording_task_service_test.go`: `TestDeleteTask_NoNPlusOne` — set up 5 input_configs linked to task, call DeleteTask, assert DB call count = 2 (1 Pluck + 1 Update). Use sqlmock or wrapped query counter.

**No-go list (do not):**
- Do NOT modify functions unrelated to P0 findings in this task.
- Do NOT do a full 403-site GORM `WithContext` sweep (deferred per CONTEXT.md).
- Do NOT change `RetryTask` error returns or status codes.
- Do NOT touch `internal/recorder/coordinator.go` or `internal/handlers/*` in this task — those belong to Task 3b.
- Do NOT modify any docs other than `DEPLOYMENT.md` / `BUILD.md` / `SECURITY.md` / `.env.example` (D-05.6).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/... ./internal/middleware/... ./internal/scheduler/... ./internal/auth/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "duration := task.EndTime.Sub" internal/services/video_recording_task_service.go` returns 1 match at line range 786-789
    - 8 goroutine sites have `recover()` defer — `grep -c "recover()" internal/services/transcription_service.go internal/auth/sm4_token.go internal/services/storage/file_service.go internal/services/video_file_service.go internal/middleware/apikey.go internal/huawei/client.go internal/services/video_recording_task_service.go` returns ≥ 8 total
    - `grep -c "Pluck.*input_config_id" internal/services/video_recording_task_service.go` ≥ 1
    - `grep -c ".WithContext(ctx)" internal/services/video_recording_task_service.go` ≥ 10 (most DB calls touched)
    - DEPLOYMENT.md: `grep -c "SM4_SECRET" DEPLOYMENT.md` ≥ 1; `grep -c "最小 32 字符" DEPLOYMENT.md` ≥ 1; `grep -c "logger.Fatal" DEPLOYMENT.md` ≥ 1
    - BUILD.md: `grep -c "SetEnvPrefix.*RECORD" BUILD.md` returns 0
    - SECURITY.md: `grep "^## SECRET 校验\|^## HLS Token 安全\|^## TLS 最低版本" SECURITY.md` returns 3 matches
    - .env.example: `grep -c "change-me-in-production" .env.example` returns 0; `grep -c "最小 32 字符" .env.example` ≥ 1
    - `go vet ./...` exits 0
    - `gofmt -l` outputs nothing
    - New/extended test files exist
  </acceptance_criteria>
  <done>
    BUG-001 fixed (EndTime no longer corrupted), 8 goroutines panic-safe, N+1 eliminated via Pluck+IN, 18 .Find sites bounded with Limit, GORM WithContext added to touched functions, all 4 deployment docs synchronized with new env var names and validation behavior. All tests pass; build green.
  </done>
</task>

<task type="auto">
  <name>Task 3b: Concurrency isolation — PERF-004 lock granularity + PERF-005 bounded concurrency (preserve HTTP 200, per D-03.8)</name>
  <files>
    - internal/recorder/coordinator.go
    - internal/recorder/coordinator_test.go
    - internal/handlers/admin_handler.go
    - internal/handlers/transcription_handler.go
    - internal/handlers/video_recording_task_handler.go
  </files>
  <read_first>
    - D:\CODE\ClaudeCode\record_V2\internal\recorder\coordinator.go
    - D:\CODE\ClaudeCode\record_V2\internal\huawei\manager.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\admin_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\transcription_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\video_recording_task_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\config\config.go
    - D:\CODE\ClaudeCode\record_V2\.planning\phases\17-56-p0-p1-p2\17-CONTEXT.md
  </read_first>
  <action>
**PERF-004 (Lock granularity — delicate concurrency refactor, isolated from Task 3a):**

1. `internal/recorder/coordinator.go:93-123` (StartRecordingWithConfig): refactor to "create resources (dir + process start) outside lock; acquire lock only for the brief `processes[processKey] = newProcess` assignment". Use `defer c.mu.Unlock()` pattern only around the map write. Concretely: start the FFmpeg process and create the output directory BEFORE `c.mu.Lock()`; once the process is alive, take the lock and insert into the map. Failure cleanup (kill process if map-insert aborted) stays outside the lock.
2. `internal/huawei/manager.go:107-112` and `:118-127`: same pattern — `client.Logout()` HTTP POST outside lock; lock only for map mutation. (Touches `manager.go`, but the only field changed is the lock window; this file is shared with Task 2 — Task 2 is in the same plan/commit, so no cross-plan conflict.)

**PERF-005 (Sync IO in handlers — apply D-03.8 bounded concurrency, NOT async/202):**

3. `internal/handlers/admin_handler.go:263-278` (admin migration handler): preserve the existing HTTP 200 response shape (status code + JSON body). Replace the inline synchronous batch loop with a **bounded concurrency** pattern:
   - Add config field `Admin.MigrationConcurrency int` (default 4) to `internal/config/config.go` if not present; bind via `ADMIN_MIGRATION_CONCURRENCY`.
   - Inside the handler: `sem := make(chan struct{}, cfg.Admin.MigrationConcurrency)`; `var wg sync.WaitGroup`; for each item in the batch, `wg.Add(1); sem <- struct{}{}; go func(it){ defer wg.Done(); defer func(){ <-sem }(); processItem(ctx, it) }(item)`; `wg.Wait()`.
   - The handler returns the SAME 200 + same JSON shape as before — only the internal processing switches from sequential to bounded-parallel. No task_id, no queue, no goroutine outliving the request.
4. `internal/handlers/transcription_handler.go:232` (batch transcription handler): same bounded-concurrency pattern. Use `cfg.Transcription.BatchConcurrency` (default 4) — add the config field if absent.
5. `internal/handlers/video_recording_task_handler.go:708-735` (HLS rewrite handler): same bounded-concurrency pattern. Use `cfg.FFmpeg.HLSRewriteConcurrency` (default 2 — FFmpeg is heavier).

**Why bounded concurrency and NOT async/202 (D-03.8 decision):**

- The audit PERF-005 finding is about "sync IO blocking the goroutine", not about response latency. Bounded concurrency parallelizes the work, dropping wall-clock time by ~N×(concurrency) without changing the HTTP contract.
- Switching to 202 + task_id would require: a task queue, persistent task state, status polling endpoint, worker goroutine lifecycle, and a notification mechanism. None of that exists on disk (`internal/services/admin_migration_service.go` does NOT exist — confirmed via filesystem check). Building it would be scope creep and a breaking API change (D-03.7 forbids deprecated compat switches).
- The bounded-concurrency change is INTERNAL — same request, same response, just faster.

**Tests:**

6. `internal/recorder/coordinator_test.go` (new or extend): `TestStartRecordingWithConfig_LockWindow` — measure lock hold time during StartRecordingWithConfig; assert < 50ms (no I/O during lock). Use a fake process starter that simulates 200ms of I/O; lock window should be ≪ 200ms.
7. `internal/recorder/coordinator_test.go`: `TestStartRecordingWithConfig_Concurrent` — spawn 10 concurrent StartRecordingWithConfig calls; assert no race (run with `-race`); all 10 succeed and the map contains 10 entries.
8. `internal/handlers/admin_handler_test.go` (new or extend): `TestAdminMigrationHandler_BoundedConcurrency` — inject a slow `processItem` (50ms sleep); call handler with 12 items and concurrency=4; assert total wall time < 12×50ms (≈ 3 batches × 50ms = 150ms + overhead) AND response code is 200 (not 202) AND JSON shape unchanged.

**No-go list (do not):**
- Do NOT change the response status code (200 stays 200), the response JSON shape, or the handler function signature.
- Do NOT introduce a task queue, background worker, persistent task state, or a status polling endpoint.
- Do NOT create `internal/services/admin_migration_service.go` (does not exist; out of scope).
- Do NOT remove the lock in PERF-004 sites — only narrow its hold window.
- Do NOT regress PERF-004 fixes when applying PERF-005 (the handlers are separate from coordinator.go).
  </action>
  <verify>
    <automated>go build ./... && go test -race ./internal/recorder/... ./internal/handlers/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "c.mu.Lock\|c.mu.Unlock" internal/recorder/coordinator.go` shows lock/unlock pairs still present (not removed)
    - `grep -n "processes\[.*\] = " internal/recorder/coordinator.go` returns ≥ 1 (map write still under lock)
    - FFmpeg process start (`exec.CommandContext` or similar) is OUTSIDE the lock window — verified by `TestStartRecordingWithConfig_LockWindow` passing (lock hold < 50ms)
    - `grep -c "chan struct{}" internal/handlers/admin_handler.go internal/handlers/transcription_handler.go internal/handlers/video_recording_task_handler.go` ≥ 3 (semaphore pattern in all 3 handlers)
    - `grep -c "MigrationConcurrency\|BatchConcurrency\|HLSRewriteConcurrency" internal/config/config.go` ≥ 3 (config fields)
    - `grep -c "StatusOK\|c.JSON.*200\|http.StatusOK" internal/handlers/admin_handler.go internal/handlers/transcription_handler.go internal/handlers/video_recording_task_handler.go` ≥ 3 (HTTP 200 preserved)
    - `grep -c "StatusAccepted\|202\|task_id.*queued" internal/handlers/admin_handler.go internal/handlers/transcription_handler.go internal/handlers/video_recording_task_handler.go` returns 0 (NO async/202 conversion)
    - `ls internal/services/admin_migration_service.go` returns "No such file" (NOT created)
    - `go test -race ./internal/recorder/... ./internal/handlers/...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>
    PERF-004: 3 lock-granularity hotspots narrowed (resource creation outside lock, lock only for map mutation), verified by lock-window test. PERF-005: 3 sync-IO handlers converted to bounded-concurrency semaphore pattern, preserving HTTP 200 + original JSON shape (per D-03.8 — NO async/202, NO task queue, NO admin_migration_service.go created). Race-free under `-race`. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted HTTP request crosses here — JWT token, SM4 access_token, HLS URL token |
| API→DB | Trust boundary at GORM call — every query input must use ctx, no raw concat |
| API→Huawei cloud | Trust boundary at Huawei HTTPS — TLS 1.2+, certificate validation, no MITM |
| env→app config | Trust boundary at viper Load() — secrets must be ≥ 32 bytes, no default fallbacks |
| app→audit log | Trust boundary at SetAuditService DI — authService must have logger, no nil short-circuit |
| handler goroutine pool | Trust boundary at bounded-concurrency semaphore — handler must wait for all spawned goroutines before returning (no leak) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan (in this plan) |
|-----------|----------|-----------|-------------|--------------------------------|
| T-17-01 | Spoofing | config.go (SEC-001) | mitigate | ValidateProductionSecrets Fatal in production; remove change-me-in-production default |
| T-17-02 | Tampering | hls_token.go (SEC-004) | mitigate | HMAC RawURLEncoding + dual-decode verify + jti usedJTIs map (in-memory, deferred to Redis later) |
| T-17-03 | Repudiation | app.go authService (SEC-002) | mitigate | authService.SetAuditService(auditService) injection — all 6 audit points now active |
| T-17-04 | Information Disclosure | huawei/manager.go TLS (SEC-003a) | mitigate | MinTLSVersion=tls.VersionTLS12 + InsecureSkipVerify=false + 3DES removed; cert validation enforced |
| T-17-04b | Information Disclosure | huawei InputConfig.Password (SEC-003b) | accept (deferred) | DB plaintext password storage is a known risk; encryption deferred to independent phase per CONTEXT.md (requires migration + frontend/config coordination). One marker comment placed at fetch site. |
| T-17-05 | Denial of Service | video_recording_task_service.go N+1 (PERF-001) | mitigate | Pluck+IN batch update replaces per-record loop; 600+ round-trips → 2 |
| T-17-06 | Denial of Service | 18 .Find without Limit (PERF-002) | mitigate | Add .Limit(N) cap; max 1000 list / 5000 cleanup |
| T-17-07 | Denial of Service | 403 GORM missing WithContext (PERF-003) | mitigate (partial) | Add WithContext to touched functions only; full sweep deferred |
| T-17-08 | Elevation of Privilege | goroutine panic → process kill (BUG-002) | mitigate | defer recover() at 8 entry points; process survives |
| T-17-09 | Tampering | RetryTask EndTime corruption (BUG-001) | mitigate | Capture duration BEFORE StartTime reassign; EndTime always = StartTime + original duration |
| T-17-10 | Denial of Service | lock held during I/O (PERF-004) | mitigate | Refactor: resources created outside lock; lock only for map mutation |
| T-17-11 | Denial of Service | sync IO in handlers (PERF-005) | mitigate | Bounded concurrency semaphore (D-03.8) — preserves HTTP 200 contract, no async queue introduced |
</threat_model>

<verification>
1. `go build ./...` exits 0
2. `go vet ./...` exits 0
3. `gofmt -l` outputs no files
4. `go test ./internal/config/... ./internal/auth/... ./cmd/server/... ./internal/huawei/... ./internal/services/... ./internal/recorder/... ./internal/middleware/... ./internal/handlers/... ./internal/scheduler/...` exits 0
5. `go test -race ./internal/recorder/... ./internal/handlers/...` exits 0 (PERF-004/PERF-005 race safety)
6. Manual grep verification per task's `<acceptance_criteria>`
7. Document review: each doc file contains the required sections per D-05
</verification>

<success_criteria>
- [ ] All 11 P0 code finding IDs (SEC-001/002/003a/004 + BUG-001/002 + PERF-001/002/003/004/005) addressed in code
- [ ] SEC-003b explicitly deferred (single marker comment, not implemented)
- [ ] Each P0 fix has at least one unit test (D-04.1) — 11+ test functions added
- [ ] Production startup with missing/short secret triggers `logger.Fatal` (verified by config test)
- [ ] `authService.SetAuditService(auditService)` injection present (verified by grep + app_test)
- [ ] HLS Token: secret length ≥ 32 enforced, RawURLEncoding sign + dual-decode verify, jti replay rejected
- [ ] Huawei TLS: TLS 1.2 min, no InsecureSkipVerify:true literal, no 3DES cipher (SEC-003a only)
- [ ] RetryTask: EndTime = StartTime + duration pattern verified by test
- [ ] 8 goroutines have defer recover
- [ ] N+1 replaced with Pluck+IN (verified by sqlmock test)
- [ ] ≥ 15 of 18 .Find sites have .Limit
- [ ] GORM WithContext added to all DB calls in touched functions
- [ ] 3 PERF-004 lock windows narrowed (verified by lock-window test)
- [ ] 3 PERF-005 handlers use bounded concurrency semaphore with HTTP 200 preserved (no 202, no task queue)
- [ ] DEPLOYMENT.md / BUILD.md / SECURITY.md / .env.example all updated per D-05
- [ ] One mega commit `fix(17-p0): P0 HIGH 发现修复（11 项）` with commit message body listing all finding IDs
- [ ] go build / go vet / gofmt -l all green; `go test -race` green for concurrency fixes
</success_criteria>

<output>
Create `.planning/phases/17-56-p0-p1-p2/17-01-SUMMARY.md` when done.

Commit message:
```
fix(17-p0): P0 HIGH 发现修复（11 项）

Findings remediated:
- SEC-001: SM4/HLS Token config 全链路漏洞（SetEnvPrefix 修正 + 启动校验 + 删除硬编码默认值）
- SEC-002: 审计装配遗漏（authService.SetAuditService 一行注入）
- SEC-003a: 华为 TLS 三重弱点（MinTLSVersion 1.2 + InsecureSkipVerify 默认 false + 3DES 移除 + ctx 透传）
- SEC-003b: 华为密码 DB 加密存储 — DEFERRED（独立 phase，仅留 marker 注释）
- SEC-004: HLS Token 密钥长度校验 + jti 防重放 + HMAC RawURLEncoding 向后兼容
- BUG-001: RetryTask 重算 EndTime 真 bug（先捕获 duration 再改 StartTime）
- BUG-002: 8 个 fire-and-forget goroutine 加 defer recover
- PERF-001: N+1 改 Pluck + IN 批量更新（DeleteTask/BatchDeleteTasks/ClearStuckTasks）
- PERF-002: 18 处 .Find 加 .Limit
- PERF-003: GORM WithContext 加到本批次修改的函数
- PERF-004: 锁粒度细化（recorder/huawei 3 处，资源创建移出锁外）
- PERF-005: 3 个 Gin handler 改 bounded concurrency semaphore（保持 HTTP 200 + 原 JSON 形状，D-03.8）

Doc sync (D-05):
- DEPLOYMENT.md: 新增 环境变量与启动校验 章节
- BUILD.md: 修正 viper prefix 错误示例
- SECURITY.md: 新增 SECRET 校验 / HLS Token 安全 / TLS 最低版本 章节
- .env.example: SM4_SECRET/HLS_TOKEN_SECRET 改为显式占位 + 长度警告
```
</output>
</invoke>
