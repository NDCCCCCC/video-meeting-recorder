---
phase: 17-56-p0-p1-p2
plan: 04
type: execute
wave: 4
depends_on:
  - 17-03
files_modified:
  - internal/services/frame_capture_service.go
  - internal/services/storage/file_service.go
  - internal/models/api_key.go
  - internal/auth/sm4_token.go
  - internal/services/tingwu_client.go
  - internal/handlers/system_handler.go
  - internal/middleware/audit.go
  - internal/recorder/coordinator.go
  - internal/services/conversion_service.go
  - internal/services/audit/audit_log_service.go
  - internal/services/notification/notification_service.go
  - internal/services/rate_limiter.go
  - cmd/server/app.go
  - internal/services/transcription_service.go
  - internal/errors/errors.go
  - internal/middleware/apikey.go
  - internal/auth/ad_auth.go
  - internal/auth/ad_validator.go
  - internal/handlers/split_handler.go
  - internal/handlers/admin_handler.go
  - internal/services/splitting_service.go
  - internal/services/snapshot_service.go
  - internal/huawei/client.go
autonomous: true
requirements:
  - BUG-011
  - BUG-015
  - BUG-016
  - SEC-011
  - SEC-012
  - SEC-013
  - SEC-014
  - SEC-015
  - PERF-012
  - PERF-013
  - PERF-014
  - PERF-015
  - PERF-016
  - STYLE-001
  - STYLE-006
  - STYLE-007
  - STYLE-008
  - STYLE-010
user_setup: []
must_haves:
  truths:
    - "int64(time.Now().UnixNano()) 文件名拼接 5 处改用 strconv.FormatInt 或 os.CreateTemp"
    - "api_key.go:134 注释与代码错位修正"
    - "IsIPAllowed 处理 IPv6-mapped IPv4（::ffff:x.x.x.x 等价 x.x.x.x）"
    - "sm4_token.go:113 SHA256 截断派生改 hex decode 32 字符 secret"
    - "文件上传 MIME magic bytes 校验（http.DetectContentType）"
    - "tingwu_client + huawei/client 出站 URL 白名单校验（即使目前是配置固定值，加防御层）"
    - "公开路由装饰器：audit + warn_unusual_access"
    - "gin.Context.GetString 全部改类型断言 (uint, bool) 等 , ok 形式"
    - "coordinator.go 字符串拼接改 strings.Builder"
    - "conversion_service.go 同函数重复 time.Now() 改为单一时钟"
    - "无缓冲 channel 误用 3 处加注释说明意图"
    - "DB 连接池配置 SetMaxOpenConns / SetMaxIdleConns / SetConnMaxLifetime 显式设置"
    - "time.After 改 time.NewTimer + defer t.Stop()"
    - "STYLE-001 partial：仅在新修改/接触的代码处用 %w 包装 + handler 层迁移 2-3 处 errors.Is；全库 168+474 迁移 DEFERRED（CONTEXT.md <deferred>）"
    - "api_key.go:81 crypto/rand.Read panic 改为 BeforeCreate hook 处理 error"
    - "apikey.go:253 switch default 兜底"
    - "ad_auth.go + ad_validator.go defer conn.Close() nil 防御"
    - "split_handler.go/admin_handler.go/splitting_service.go/snapshot_service.go/ad_validator.go 8 处 godoc 补齐"
    - "STYLE-002 标记为误报，无修改（仅本 plan notes 记录，不动审计 doc）"
    - "STYLE-009 跳过（133 处范围过大，按 Claude's Discretion 默认跳过；仅本 plan notes 记录，不动生产源码）"
  artifacts:
    - path: "internal/services/frame_capture_service.go"
      provides: "int64(time.Now().UnixNano()) 改为 os.CreateTemp 或 strconv.FormatInt (BUG-011)"
      contains: "strconv.FormatInt.*UnixNano"
    - path: "internal/services/storage/file_service.go"
      provides: "5 处 int64 拼接改 strconv.FormatInt (BUG-011) + MIME magic bytes 校验 (SEC-012)"
      contains: "http\\.DetectContentType"
    - path: "internal/models/api_key.go"
      provides: "注释修正 (BUG-015) + IPv6-mapped 处理 (BUG-016)"
      contains: "IsIPv4Mapped"
    - path: "internal/auth/sm4_token.go"
      provides: "SHA256 截断改 hex decode (SEC-011)"
      contains: "hex\\.DecodeString.*secret"
    - path: "internal/services/tingwu_client.go"
      provides: "outbound URL 白名单校验 (SEC-013)"
      contains: "allowedOutboundURLs"
    - path: "internal/handlers/system_handler.go"
      provides: "GetString 改类型断言 , ok (SEC-015)"
      contains: "ok.*:=\\|, ok :="
    - path: "internal/middleware/audit.go"
      provides: "GetString 改类型断言 , ok (SEC-015)"
      contains: ", ok :="
    - path: "internal/recorder/coordinator.go"
      provides: "strings.Builder 替换循环拼接 (PERF-012)"
      contains: "strings\\.Builder"
    - path: "internal/services/conversion_service.go"
      provides: "time.Now() 单次调用 (PERF-013) + time.NewTimer (PERF-016)"
      contains: "now :=\\s*time\\.Now\\(\\)"
    - path: "internal/services/audit/audit_log_service.go + notification/notification_service.go + rate_limiter.go"
      provides: "channel 用途注释 (PERF-014)"
      contains: "// PERF-014: signal-only channel"
    - path: "cmd/server/app.go"
      provides: "DB 连接池 SetMaxOpenConns/SetMaxIdleConns/SetConnMaxLifetime (PERF-015)"
      contains: "SetMaxOpenConns"
    - path: "internal/services/transcription_service.go"
      provides: "time.After 改 time.NewTimer (PERF-016)"
      contains: "time\\.NewTimer"
    - path: "internal/errors/errors.go"
      provides: "errors 包核心错误类型导出 (STYLE-001 partial；全库迁移 deferred)"
      contains: "ErrNotFound"
    - path: "internal/models/api_key.go"
      provides: "BeforeCreate hook 处理 rand.Read 错误 (STYLE-006)"
      contains: "BeforeCreate"
    - path: "internal/middleware/apikey.go"
      provides: "switch default 兜底 (STYLE-007)"
      contains: "default:\\s*hasPermission\\s*=\\s*false"
    - path: "internal/auth/ad_auth.go + ad_validator.go"
      provides: "defer conn.Close() nil 防御 (STYLE-008)"
      contains: "if conn != nil"
    - path: "internal/handlers/split_handler.go + admin_handler.go + services/splitting_service.go + snapshot_service.go + auth/ad_validator.go"
      provides: "godoc 注释 (STYLE-010)"
      contains: "//.*(分割|切割|管理员|文件|snapshot|Active Directory|域控|转录)"
  key_links:
    - from: "internal/services/storage/file_service.go"
      to: "http.DetectContentType"
      via: "MIME magic bytes validation"
      pattern: "DetectContentType"
    - from: "internal/services/tingwu_client.go + huawei/client.go"
      to: "internal/config/config.go"
      via: "OutboundURLAllowlist []string from config"
      pattern: "OutboundURLAllowlist"
    - from: "internal/handlers/system_handler.go + middleware/audit.go"
      to: "gin.Context.Get"
      via: "type assertion , ok pattern"
      pattern: "Get\\(.*\\)\\..*ok"
    - from: "cmd/server/app.go"
      to: "gorm.DB"
      via: "connection pool configuration"
      pattern: "SetMaxOpenConns|SetMaxIdleConns|SetConnMaxLifetime"
    - from: "internal/errors/errors.go"
      to: "handler layer"
      via: "errors.Is for handler status code routing (2-3 sites migrated, partial)"
      pattern: "errors\\.Is.*ErrNotFound"
---

<objective>
Address 20 LOW-severity findings (P2) from the backend code review by polishing error handling (STYLE-001 partial + STYLE-006), security (SEC-011/012/013/014/015), performance (PERF-012/013/014/015/016), bug cleanups (BUG-011/015/016), and Go style (STYLE-007/008/010). STYLE-009 (133 rename blast radius) is deferred per CONTEXT.md `<deferred>`; STYLE-002 is a confirmed false positive (audit §5.1) — both noted in this plan's `<notes>`, NOT in the audit doc or production source (per D-05.6).

Purpose: Sweep up remaining LOW-severity items that are individually low-impact but together improve consistency and prevent future regressions. This is the final cleanup wave.

Output: All 20 in-scope P2 findings remediated in code (no new tests per D-04.3); one mega commit `chore(17-p2): P2 LOW 发现清理（20 项）`; build/vet/fmt all green. STYLE-001 partial (2-3 sites) per W6a deferral; STYLE-002 false positive + STYLE-009 deferred — both documented in `<notes>` only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-56-p0-p1-p2/17-CONTEXT.md
@.planning/phases/17-56-p0-p1-p2/17-DISCUSSION-LOG.md
@docs/audits/2026-07-30-backend-code-review.md
@.planning/phases/17-56-p0-p1-p2/17-01-SUMMARY.md (after P0)
@.planning/phases/17-56-p0-p1-p2/17-02-SUMMARY.md (after P1a)
@.planning/phases/17-56-p0-p1-p2/17-03-SUMMARY.md (after P1b)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bug cleanups BUG-011/015/016 + style STYLE-006/007/008/010</name>
  <files>
    - internal/services/frame_capture_service.go
    - internal/services/storage/file_service.go
    - internal/models/api_key.go
    - internal/middleware/apikey.go
    - internal/auth/ad_auth.go
    - internal/auth/ad_validator.go
    - internal/handlers/split_handler.go
    - internal/handlers/admin_handler.go
    - internal/services/splitting_service.go
    - internal/services/snapshot_service.go
  </files>
  <read_first>
    - D:\CODE\ClaudeCode\record_V2\internal\services\frame_capture_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\storage\file_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\models\api_key.go
    - D:\CODE\ClaudeCode\record_V2\internal\middleware\apikey.go
    - D:\CODE\ClaudeCode\record_V2\internal\auth\ad_auth.go
    - D:\CODE\ClaudeCode\record_V2\internal\auth\ad_validator.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\split_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\admin_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\splitting_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\snapshot_service.go
  </read_first>
  <action>
**BUG-011 (int64(time.Now().UnixNano()) 拼接 — 5 处):**

1. In `internal/services/frame_capture_service.go:120` and `internal/services/storage/file_service.go:587, 592, 597, 629`: replace `strconv.Itoa(int(time.Now().UnixNano()))` patterns with `strconv.FormatInt(time.Now().UnixNano(), 10)` (more explicit; idiomatic Go). Use `os.CreateTemp("", "frame-*.jpg")` where the file naming uniqueness is desired (let OS guarantee uniqueness). Keep `int64` nanosecond timestamps for ordering.

**BUG-015 (api_key.go:134 注释错位):**

2. In `internal/models/api_key.go:134`: change the misleading comment "磁化显示密钥，仅保留前 8 位" to accurately describe the code below it (IP whitelist parsing loop). Or move the misplaced comment to where the masking code actually lives (search codebase for any actual key masking implementation; if none exists, delete the stray comment).

**BUG-016 (IsIPAllowed IPv6-mapped IPv4):**

3. In `internal/models/api_key.go:127-150` (IsIPAllowed function): add IPv6-mapped IPv4 normalization:
   ```go
   func normalizeIP(ip net.IP) net.IP {
       if v4 := ip.To4(); v4 != nil { return v4 }
       return ip
   }
   ```
   Apply at the start of IsIPAllowed so `::ffff:192.0.2.1` and `192.0.2.1` are treated equivalently.

**STYLE-006 (panic → error):**

4. In `internal/models/api_key.go:81` (panic on `crypto/rand.Read` failure): refactor the signature to `(string, error)`. Add `BeforeCreate(tx *gorm.DB) error` hook on the `ApiKey` model that calls the non-panicking variant. Update all callers.

**STYLE-007 (switch 缺 default):**

5. In `internal/middleware/apikey.go:253` (switch on `requiredScope`): add `default: hasPermission = false; return` (or set a default unauthorized return path). Add comment: `// 未知 scope 默认拒绝`.

**STYLE-008 (defer conn.Close() nil 防御):**

6. In `internal/auth/ad_auth.go:68, 390` and `internal/auth/ad_validator.go:44`: wrap `defer conn.Close()` with `if conn != nil { defer conn.Close() }` pattern. Defensive coding — current `connectAD` early-return makes it safe but the guard makes intent explicit.

**STYLE-010 (godoc 8 处):**

7. Add godoc comments on the 8 listed undocumented exported symbols:
   - `internal/handlers/split_handler.go:13, 20` — e.g., `// SplitHandler 视频分割 HTTP handler.`
   - `internal/handlers/admin_handler.go:27` — `// AdminHandler 管理员后台 handler.`
   - `internal/services/splitting_service.go:30, 49` — `// SplittingService 视频分割服务，调用 FFmpeg 执行分割.`
   - `internal/services/snapshot_service.go:19, 28` — `// SnapshotService 录制快照服务.`
   - `internal/auth/ad_validator.go:18` — `// ADValidator 域控配置验证器.`
   Match the existing comment style (Chinese concise doc + 参数说明 if exported args).

**No-go list:**
- Do NOT do the full 168 `errors.New` + 474 `fmt.Errorf` STYLE-001 refactor (deferred per CONTEXT.md `<deferred>`; STYLE-001 partial lives in Task 3, not here).
- Do NOT rename `Get*` methods (STYLE-009 deferred).
- Do NOT change public method signatures of `IsIPAllowed` (only internal behavior change).
- Do NOT modify `docs/audits/*.md` or create `internal/**/README.md` or inject STYLE-002/SYTLE-009 marker comments into production source (D-05.6 — these decisions live in `<notes>` only).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && gofmt -l . | grep -v node_modules | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strconv.FormatInt.*UnixNano" internal/services/frame_capture_service.go internal/services/storage/file_service.go` ≥ 5
    - `grep -c "int(time.Now().UnixNano())" internal/services/frame_capture_service.go internal/services/storage/file_service.go` returns 0
    - `grep -n "磁化显示密钥" internal/models/api_key.go` returns 0 (comment moved/fixed)
    - `grep -n "IsIPv4Mapped\|.To4()" internal/models/api_key.go` ≥ 1
    - `grep -c "BeforeCreate" internal/models/api_key.go` ≥ 1
    - `grep -n "default:.*hasPermission" internal/middleware/apikey.go` ≥ 1
    - `grep -c "if conn != nil" internal/auth/ad_auth.go internal/auth/ad_validator.go` ≥ 3 (across both files)
    - godoc comments added: `grep -B1 "^func SplitHandler\|^func Split\|SplittingService\|SnapshotService\|ADValidator\|AdminHandler" internal/handlers/split_handler.go internal/handlers/admin_handler.go internal/services/splitting_service.go internal/services/snapshot_service.go internal/auth/ad_validator.go` shows doc comments
    - `grep -rl "STYLE-009 deferred\|STYLE-002 false positive" internal/handlers/ internal/middleware/ 2>/dev/null | wc -l` returns 0 (no production source marker comments per B2)
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
    - `gofmt -l` outputs nothing
  </acceptance_criteria>
  <done>
    BUG-011 timestamp string concat uses strconv; BUG-015 stray comment fixed; BUG-016 IPv6-mapped IPv4 normalized; STYLE-006 panic→error via BeforeCreate; STYLE-007 switch default added; STYLE-008 nil guard on conn.Close; STYLE-010 godoc on 8 exported symbols. No audit doc / README / source-marker changes (B2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Security polish SEC-011/012/013/014/015</name>
  <files>
    - internal/auth/sm4_token.go
    - internal/services/storage/file_service.go
    - internal/services/tingwu_client.go
    - internal/huawei/client.go
    - internal/handlers/system_handler.go
    - internal/middleware/audit.go
    - cmd/server/app.go
  </files>
  <read_first>
    - D:\CODE\ClaudeCode\record_V2\internal\auth\sm4_token.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\storage\file_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\tingwu_client.go
    - D:\CODE\ClaudeCode\record_V2\internal\huawei\client.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\system_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\middleware\audit.go
    - D:\CODE\ClaudeCode\record_V2\cmd\server\app.go
    - D:\CODE\ClaudeCode\record_V2\internal\config\config.go
  </read_first>
  <action>
**SEC-011 (SHA256 截断派生 SM4 密钥):**

1. In `internal/auth/sm4_token.go:113`: replace `sha256.Sum256(secret)[:16]` with `hex.DecodeString(string(secret))` to use the full 32-character hex secret directly as 16 raw bytes. If `hex.DecodeString` fails (secret not valid hex), log fatal — `len(secret) < 32` already enforced at startup by P0/SEC-001, so the secret IS valid hex by contract. Update function docstring.

**SEC-012 (MIME magic bytes 校验):**

2. In `internal/services/storage/file_service.go:468-500` (upload handler path): add MIME magic bytes check:
   ```go
   // Read first 512 bytes
   head := make([]byte, 512)
   n, _ := f.Read(head)
   detected := http.DetectContentType(head[:n])
   allowedMIMEs := map[string]bool{"video/mp4": true, "application/pdf": true, "application/vnd.ms-powerpoint": true, ...}
   if !allowedMIMEs[detected] {
       return fmt.Errorf("不允许的 MIME 类型: %s", detected)
   }
   ```
   Keep existing extension whitelist as secondary defense.

**SEC-013 (SSRF outbound URL allowlist):**

3. In `internal/config/config.go`: add `Security.OutboundURLAllowlist []string` (default empty = no outbound requests to non-internal URLs). In `internal/services/tingwu_client.go` and `internal/huawei/client.go`: validate any URL passed to `http.Get` / `http.NewRequest` against this allowlist before the call:
   ```go
   u, err := url.Parse(rawURL)
   if err != nil { return err }
   host := u.Hostname()
   allowed := false
   for _, prefix := range cfg.Security.OutboundURLAllowlist {
       if strings.HasSuffix(host, prefix) { allowed = true; break }
   }
   if !allowed { return fmt.Errorf("URL 不在出站白名单: %s", host) }
   ```
   For per-environment default: when `Environment == "development"`, log Warn only (allow all); when `production`, enforce strict check.

**SEC-014 (公开路由装饰器):**

4. Add a `publicRouteDecorator(c *gin.Context)` middleware in `cmd/server/app.go` (or new file `internal/middleware/public_route_audit.go`) that:
   - Logs request at Info level with path + IP + user-agent
   - Increments a Prometheus counter `public_route_hits_total{path=...}`
   - Calls `c.Next()`
   - Logs response status at end with same fields
   Attach to public routes: `/api/v1/files/download/:token`, `/api/v1/recordings/:id/preview/stream/:file`, `/api/v1/ppts/:id/slides/:resolution/:filename` (find exact paths in app.go:840-941, 884, 887).

**SEC-015 (gin.Context.GetString 类型断言守卫):**

5. In `internal/handlers/system_handler.go:148` and `internal/middleware/audit.go:62-63, 136, 180`: replace `c.GetString("user_id")` with `idVal, ok := c.Get("user_id"); if !ok { /* skip audit */ } else { id := idVal.(uint) }`. Pattern consistent with STYLE-005 from P1a (which already fixed `auth.go`; extend to system_handler.go and audit.go).

**No-go list:**
- Do NOT block legitimate internal service-to-service HTTP calls behind the SEC-013 allowlist (use the development-mode bypass).
- Do NOT change file upload response shapes (only add validation).
- Do NOT remove existing extension whitelist (add MIME as additional layer).
- Do NOT modify `docs/audits/*.md` (D-05.6).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && gofmt -l . | grep -v node_modules | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hex.DecodeString" internal/auth/sm4_token.go` ≥ 1
    - `grep -c "sha256.Sum256.*\\[16\\]" internal/auth/sm4_token.go` returns 0
    - `grep -c "http.DetectContentType" internal/services/storage/file_service.go` ≥ 1
    - `grep -c "OutboundURLAllowlist" internal/config/config.go internal/services/tingwu_client.go internal/huawei/client.go` ≥ 3
    - `grep -c "publicRouteDecorator\|public_route_hits_total" cmd/server/app.go internal/middleware/` ≥ 1
    - `grep -c ", ok :=" internal/handlers/system_handler.go internal/middleware/audit.go` ≥ 3
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>
    SEC-011 SM4 key derivation uses hex decode; SEC-012 MIME magic bytes validation; SEC-013 outbound URL allowlist; SEC-014 public route audit decorator; SEC-015 type-safe context reads in system_handler + audit middleware. Build green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Performance polish PERF-012/013/014/015/016 + STYLE-001 error unification partial (per W6a)</name>
  <files>
    - internal/recorder/coordinator.go
    - internal/services/conversion_service.go
    - internal/services/audit/audit_log_service.go
    - internal/services/notification/notification_service.go
    - internal/services/rate_limiter.go
    - cmd/server/app.go
    - internal/services/transcription_service.go
    - internal/errors/errors.go
  </files>
  <read_first>
    - D:\CODE\ClaudeCode\record_V2\internal\recorder\coordinator.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\conversion_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\audit\audit_log_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\notification\notification_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\rate_limiter.go
    - D:\CODE\ClaudeCode\record_V2\cmd\server\app.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\transcription_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\errors\errors.go
    - D:\CODE\ClaudeCode\record_V2\.planning\phases\17-56-p0-p1-p2\17-CONTEXT.md
  </read_first>
  <action>
**PERF-012 (strings.Builder):**

1. In `internal/recorder/coordinator.go:488-496`: replace manual string concatenation loop with `strings.Builder`:
   ```go
   var sb strings.Builder
   sb.Grow(len(items) * 16) // rough estimate
   for _, item := range items {
       sb.WriteString(item.String())
       sb.WriteString("\n")
   }
   result := sb.String()
   ```

**PERF-013 (重复 time.Now()):**

2. In `internal/services/conversion_service.go:251, 268`: replace both `time.Now()` calls with a single `now := time.Now()` at the top of the function, reuse for both timestamps. Document with comment `// PERF-013: 单一时钟基准`.

**PERF-014 (无缓冲 channel 注释):**

3. Add explanatory comments on the 3 close-only signal channels:
   - `internal/services/audit/audit_log_service.go:138`: `// PERF-014: 无缓冲 channel 仅用作 stop 信号 (close-only)`
   - `internal/services/notification/notification_service.go:69`: same comment
   - `internal/services/rate_limiter.go:45`: same comment
   These channels do not carry data; do NOT change their buffering.

**PERF-015 (DB 连接池配置):**

4. In `cmd/server/app.go`: locate the `gorm.Open` call (search for `gorm.Open` or `sql.Open`). After successful DB initialization, add:
   ```go
   sqlDB, err := db.DB()
   if err != nil { return err }
   sqlDB.SetMaxOpenConns(cfg.Database.MaxOpenConns)        // default 25
   sqlDB.SetMaxIdleConns(cfg.Database.MaxIdleConns)        // default 5
   sqlDB.SetConnMaxLifetime(cfg.Database.ConnMaxLifetime)  // default 30 * time.Minute
   ```
   In `internal/config/config.go` `DatabaseConfig` struct: add the 3 fields with defaults from the comment above. In `setDefaults`: bind `cfg.Database.MaxOpenConns` etc. to env vars `DB_MAX_OPEN_CONNS` etc.

**PERF-016 (time.After → time.NewTimer):**

5. In `internal/services/conversion_service.go:445` and `internal/services/transcription_service.go:784`: replace `time.After(d)` (in select) with:
   ```go
   timer := time.NewTimer(d)
   defer timer.Stop()
   select {
   case <-ctx.Done(): return ctx.Err()
   case <-timer.C: ...
   }
   ```
   Document with comment `// PERF-016: 显式释放 timer`.

**STYLE-001 (errors unification — PARTIAL per W6a; full migration DEFERRED per CONTEXT.md `<deferred>`):**

6. In `internal/errors/errors.go`: ensure all 4 base errors are exported with proper godoc. Add `errors.go_test.go` (only if not yet created in P0/P1) — actually skip per D-04.3 (no tests for P2).
7. Migrate 2-3 sites (this is the partial W6a delivery, not the full 168+474 migration): find one handler that does `err.Error() == "用户不存在"` style matching, one that does `err == gorm.ErrRecordNotFound`, and one service-layer `fmt.Errorf("中文: %v", err)` — replace with `errors.Is(err, internalerrors.ErrNotFound)` and `fmt.Errorf("中文: %w", err)`. Add comment pointing to internal/errors as canonical source. The remaining 168+474 sites are explicitly deferred (CONTEXT.md `<deferred>`).

**No-go list:**
- Do NOT change the 3 close-only channel definitions (just add comments).
- Do NOT change DB pool defaults away from safe values (25/5/30min are conservative).
- Do NOT do the full 168/474 errors.New/fmt.Errorf refactor — STYLE-001 full migration is deferred per CONTEXT.md (W6a).
- Do NOT rename `Get*` methods (STYLE-009 deferred).
- Do NOT modify `internal/services/audit/audit_log_service.go` business logic (only comment + WithContext if not already added in P1).
- Do NOT modify `docs/audits/*.md` or inject marker comments into production source for STYLE-002/STYLE-009 (B2 + D-05.6).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && gofmt -l . | grep -v node_modules | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.Builder" internal/recorder/coordinator.go` ≥ 1
    - `grep -n "now := time.Now()" internal/services/conversion_service.go` ≥ 1
    - `grep -c "PERF-014: 无缓冲 channel\|stop 信号" internal/services/audit/audit_log_service.go internal/services/notification/notification_service.go internal/services/rate_limiter.go` ≥ 3
    - `grep -c "SetMaxOpenConns\|SetMaxIdleConns\|SetConnMaxLifetime" cmd/server/app.go` ≥ 3
    - `grep -c "MaxOpenConns" internal/config/config.go` ≥ 1
    - `grep -c "time.NewTimer" internal/services/conversion_service.go internal/services/transcription_service.go` ≥ 2
    - `grep -c "errors.Is.*ErrNotFound" internal/handlers/` ≥ 1 (STYLE-001 partial migration)
    - `grep -c "%w" internal/handlers/ internal/services/ | head -1` shows %w usage (partial STYLE-001)
    - `grep -rl "STYLE-009 deferred\|STYLE-002 false positive" internal/ 2>/dev/null | wc -l` returns 0 (no production source marker comments per B2)
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
    - `gofmt -l` outputs nothing
    - `go test ./...` exits 0 (pre-existing tests still pass)
  </acceptance_criteria>
  <done>
    PERF-012 strings.Builder; PERF-013 single time.Now(); PERF-014 channel intent documented; PERF-015 DB pool configured; PERF-016 explicit timer.Stop(); STYLE-001 PARTIAL (2-3 sites migrated per W6a — full 168+474 deferred). STYLE-002 false positive + STYLE-009 deferral recorded in `<notes>` only, NOT in audit doc or source (B2). Build/vet/fmt green; existing tests pass.
  </done>
</task>

</tasks>

<notes>
- **STYLE-009 SKIP rationale:** 133 处 `Get*` 方法 rename blast radius 单次 commit 噪音过大。Claude's Discretion 默认跳过 (CONTEXT.md `<deferred>` 已正式记录)。**本 plan 不修改审计 doc、不创建 README、不在生产源码注入 marker 注释** (D-05.6)；该决策仅在此 `<notes>` 中记录。
- **STYLE-002 false positive:** 审计 doc 自身 §5.1 已确认 "auth → services/audit 依赖方向" 误报（`services/audit/audit_log_service.go` 未 import `auth`，无真实循环）。本 phase **无修改**。**本 plan 不修改审计 doc、不创建 README、不在生产源码注入 marker 注释** (D-05.6)；该决策仅在此 `<notes>` 中记录。
- **STYLE-001 partial per W6a:** 全库 168 `errors.New` + 474 `fmt.Errorf` 迁移影响面过大，正式 deferred 于 CONTEXT.md `<deferred>`。本 plan 仅迁移 2-3 处 + 在新修改代码处用 `%w` 包装；全库扫荡留作独立 phase。
- **No `<threat_model>` block** — P2 findings are LOW severity; STRIDE-class threats do not warrant a formal register. Mitigations live in the per-finding actions above. (Each mitigation is small, defensive, and does not change trust boundaries.)
- **D-04.3** applies: NO unit tests required for P2 fixes. Verification is build / vet / fmt only. Pre-existing tests must continue to pass.
- **D-04.6** still applies: any incidental test additions must use SQLite in-memory or sqlmock (no new external deps).
- **D-05.6 (B2):** Only `DEPLOYMENT.md` / `BUILD.md` / `SECURITY.md` / `.env.example` may be modified (already handled in P0). P2 touches NONE of these — purely `internal/` + `cmd/server/app.go` code.
</notes>

<verification>
1. `go build ./...` exits 0
2. `go vet ./...` exits 0
3. `gofmt -l` outputs no files (filter `node_modules` to exclude frontend tooling)
4. `go test ./...` exits 0 (pre-existing tests continue to pass; no new tests required per D-04.3)
5. Manual grep verification per task's `<acceptance_criteria>`
</verification>

<success_criteria>
- [ ] All 20 in-scope P2 finding IDs addressed: BUG-011/015/016 + SEC-011/012/013/014/015 + PERF-012/013/014/015/016 + STYLE-001 (partial) + STYLE-006/007/008/010
- [ ] STYLE-001 partial (2-3 sites + %w on touched code) — full migration deferred per W6a / CONTEXT.md
- [ ] STYLE-002 marked as误报 (no code change, no doc change, no source marker — rationale in `<notes>` only)
- [ ] STYLE-009 deferred per Claude's Discretion (no code change, no doc change, no source marker — rationale in `<notes>` only)
- [ ] No new tests added (D-04.3) — pre-existing tests still pass
- [ ] `go build ./...` exits 0
- [ ] `go vet ./...` exits 0
- [ ] `gofmt -l` outputs no files
- [ ] No modifications to `docs/audits/*.md`, no new `internal/**/README.md`, no STYLE-002/009 marker comments in production source (B2 + D-05.6)
- [ ] One mega commit `chore(17-p2): P2 LOW 发现清理（20 项）` with commit message body listing all finding IDs (including STYLE-001 partial + STYLE-002 false positive + STYLE-009 deferred notes)
</success_criteria>

<output>
Create `.planning/phases/17-56-p0-p1-p2/17-04-SUMMARY.md` when done.

Commit message:
```
chore(17-p2): P2 LOW 发现清理（20 项 + 3 项标记/延后）

Findings remediated (20):
- BUG-011: int64(time.Now().UnixNano()) 拼接 5 处改 strconv.FormatInt
- BUG-015: api_key.go:134 注释错位修正
- BUG-016: IsIPAllowed 加 IPv6-mapped IPv4 归一化
- SEC-011: SM4 密钥派生 SHA256 截断改 hex.DecodeString
- SEC-012: 文件上传加 MIME magic bytes 校验 (http.DetectContentType)
- SEC-013: 出站 URL 白名单 (tingwu + huawei, 生产环境强制)
- SEC-014: 公开路由加 audit 装饰器 + 命中计数
- SEC-015: gin.Context.GetString 全部改类型断言 , ok
- PERF-012: coordinator 字符串拼接改 strings.Builder
- PERF-013: conversion_service 同函数 time.Now() 单一时钟
- PERF-014: 3 处 close-only channel 加注释说明意图
- PERF-015: DB 连接池 SetMaxOpenConns/SetMaxIdleConns/SetConnMaxLifetime 显式配置
- PERF-016: time.After 改 time.NewTimer + defer Stop()
- STYLE-001 (partial, W6a): internal/errors 统一 2-3 处 handler 层迁移；全库 168+474 迁移 DEFERRED
- STYLE-006: api_key.go crypto/rand.Read panic 改 BeforeCreate hook 返回 error
- STYLE-007: apikey.go switch default 兜底
- STYLE-008: defer conn.Close() nil 防御 (3 处)
- STYLE-010: 8 处 godoc 注释补齐

Marked / deferred (3, no code change):
- STYLE-001 (full): 168 errors.New + 474 fmt.Errorf 全库迁移 — deferred 独立 phase (CONTEXT.md)
- STYLE-002: 误报 — 审计 doc §5.1 已纠正 (无修改)
- STYLE-009: 133 处 Get* rename — deferred 下个 LOW phase (Claude's Discretion)

注：本 commit 未修改 docs/audits/*.md、未创建 README、未注入 marker 注释 (D-05.6)。
```
</output>
</invoke>
