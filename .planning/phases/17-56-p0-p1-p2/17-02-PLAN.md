---
phase: 17-56-p0-p1-p2
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - internal/middleware/audit.go
  - internal/middleware/audit_test.go
  - internal/models/audit_log.go
  - internal/models/audit_log_test.go
  - internal/models/notification.go
  - internal/models/notification_test.go
  - internal/models/api_key.go
  - internal/models/api_key_test.go
  - internal/models/role.go
  - internal/models/user.go
  - internal/services/usb_device_scanner.go
  - internal/services/usb_device_scanner_test.go
  - internal/services/video_recording_task_service.go
  - internal/services/video_recording_task_service_test.go
  - internal/auth/sm4_token.go
  - internal/auth/sm4_token_test.go
  - internal/scheduler/video_scheduler.go
  - internal/scheduler/video_scheduler_test.go
  - internal/recorder/coordinator.go
  - internal/recorder/coordinator_test.go
  - internal/huawei/manager.go
  - internal/huawei/client.go
  - internal/migrations/013_add_ad_fields.go
  - internal/migrations/013_add_ad_fields_test.go
  - internal/migrations/016_alter_fingerprint_to_sha256.go
  - internal/services/storage/file_service.go
  - internal/services/storage/file_service_test.go
  - internal/handlers/file_handler.go
  - internal/handlers/file_handler_test.go
  - internal/handlers/video_file_handler.go
  - internal/handlers/video_recording_task_handler.go
  - internal/handlers/ppt_handler.go
  - internal/handlers/apikey_handler.go
  - internal/handlers/system_handler.go
  - internal/handlers/user_handler.go
  - internal/handlers/auth_handler.go
  - internal/handlers/transcription_handler.go
  - internal/handlers/split_handler.go
  - internal/handlers/admin_handler.go
  - internal/config/config.go
  - internal/config/config_test.go
  - cmd/server/app.go
  - internal/middleware/auth.go
  - internal/middleware/auth_test.go
  - internal/services/audit/audit_log_service.go
autonomous: true
requirements:
  - BUG-003
  - BUG-004
  - BUG-005
  - BUG-006
  - SEC-005
  - SEC-006
  - SEC-007
  - SEC-008
  - SEC-009
  - SEC-010
  - STYLE-004
  - STYLE-005
user_setup: []
must_haves:
  truths:
    - "json.Unmarshal 错误不再被静默吞掉（6 处全部加 logger.Warn + 返回 defaultValue）"
    - "_ = json.Unmarshal/_ = cmd.Run/_ = RevokeUserSessions 等 9 处显式忽略改为带日志"
    - "time.Sleep 在 4 处被 time.NewTimer + select{ctx.Done, timer} 替代，可被 ctx 取消"
    - "SQL 字符串拼接（migrations/013_add_ad_fields.go）改用 GORM Migrator.AddColumn 或白名单校验"
    - "MD5 文件指纹改 SHA-256，文件指纹碰撞风险消除（DB 列迁移 016）"
    - "CORS 默认拒绝跨域；AllowedOrigins 从配置读取（数组精确匹配）"
    - "CSRF 保留配置开关 csrf_enabled（默认 false，handler 层读取）— 配置绑定有测试覆盖"
    - "敏感日志脱敏：file_handler token 只打末 4 位；huawei client 响应体脱敏"
    - "Token URL 白名单从 substring 改为 prefix 精确匹配列表"
    - "中间件 GetUserID 改为返回 (uint, bool)；类型断言改为 , ok；10 个 handler 调用方全部更新"
    - "BUG-005 WithContext 覆盖 audit.go + audit_log_service.go + auth.go + video_recording_task_service.go + usb_device_scanner.go 5 个文件"
  artifacts:
    - path: "internal/middleware/audit.go"
      provides: "json.Unmarshal 错误日志 + 类型化 struct (替代 interface{}) + , ok 类型断言"
      contains: "GetOldData.*json\\.Unmarshal"
    - path: "internal/migrations/013_add_ad_fields.go"
      provides: "GORM Migrator AddColumn 替代 Exec 字符串拼接"
      contains: "Migrator\\(\\)\\.AddColumn"
    - path: "internal/migrations/016_alter_fingerprint_to_sha256.go"
      provides: "fingerprint 列从 VARCHAR(32) (MD5) 迁移到 VARCHAR(64) (SHA-256)"
      contains: "ALTER.*fingerprint"
    - path: "internal/services/storage/file_service.go"
      provides: "SHA-256 文件指纹 (替代 MD5) + MIME magic bytes 校验留给 P2 SEC-012"
      contains: "sha256\\.Sum256"
    - path: "internal/config/config.go"
      provides: "CORS.AllowedOrigins []string + CSRFEnabled bool + AllowedTokenURLPrefixes []string + BindEnv"
      contains: "AllowedOrigins"
    - path: "internal/handlers/file_handler.go"
      provides: "token 日志脱敏（末 4 位）"
      contains: "len.*-4"
    - path: "internal/huawei/client.go"
      provides: "response body 脱敏 (Debug 级别)"
      contains: "Debug.*sanitize|sanitizeResponseBody"
    - path: "internal/middleware/auth.go"
      provides: "GetUserID() (uint, bool) + 类型断言 , ok + TokenURLPrefixes 精确匹配"
      contains: "GetUserID.*\\(.*uint.*bool"
  key_links:
    - from: "internal/middleware/audit.go"
      to: "internal/services/audit/sanitizer.go"
      via: "sanitizer 注入中间件 (DI 模式见 app.go:519)"
      pattern: "NewAuditMiddleware.*sanitizer"
    - from: "internal/config/config.go CORS.AllowedOrigins"
      to: "internal/middleware/cors.go (or auth.go)"
      via: "CORS middleware reads cfg.CORS.AllowedOrigins"
      pattern: "AllowedOrigins"
    - from: "internal/middleware/auth.go TokenURLPrefixes"
      to: "internal/config/config.go"
      via: "TokenURLPrefixes []string from config"
      pattern: "AllowedTokenURLPrefixes"
    - from: "internal/migrations/016_alter_fingerprint_to_sha256.go"
      to: "internal/services/storage/file_service.go"
      via: "DB column widened to 64 chars to hold SHA-256 hex"
      pattern: "ALTER.*fingerprint"
    - from: "internal/middleware/auth.go GetUserID (uint,bool)"
      to: "10 handler callers"
      via: "all callers updated to , ok pattern"
      pattern: "userID, ok := middleware.GetUserID"
---

<objective>
Address 12 MEDIUM-severity findings (P1a — error handling + security hardening) from the backend code review by fixing error-handling bugs (BUG-003/004/005/006), security hardening (SEC-005/006/007/008/009/010), and middleware typing (STYLE-004/005). Each fix includes at least one unit test per D-04.2.

Purpose: Reduce MEDIUM-severity risk surface — these findings individually are not exploitable end-to-end, but several chain together with P0 fixes (e.g., BUG-005 GORM WithContext + PERF-003 P0 partial sweep; SEC-007 CORS + SEC-010 token URL) to form complete attack surfaces.

Output: All 12 P1a findings remediated in code with ≥ 12 test functions; one mega commit `refactor(17-p1a): P1 MEDIUM 发现修复 - 错误处理 + 安全加固（12 项）`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-56-p0-p1-p2/17-CONTEXT.md
@.planning/phases/17-56-p0-p1-p2/17-DISCUSSION-LOG.md
@docs/audits/2026-07-30-backend-code-review.md
@.claude/skills/spike-findings-record-v2/SKILL.md
@.planning/phases/17-56-p0-p1-p2/17-01-SUMMARY.md (after P0 completes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error handling + BUG-003/004/005/006 + middleware type-safety STYLE-004/005</name>
  <files>
    - internal/middleware/audit.go
    - internal/middleware/audit_test.go
    - internal/middleware/auth.go
    - internal/middleware/auth_test.go
    - internal/models/audit_log.go
    - internal/models/audit_log_test.go
    - internal/models/notification.go
    - internal/models/notification_test.go
    - internal/models/api_key.go
    - internal/models/api_key_test.go
    - internal/models/role.go
    - internal/models/user.go
    - internal/services/usb_device_scanner.go
    - internal/services/usb_device_scanner_test.go
    - internal/services/video_recording_task_service.go
    - internal/services/video_recording_task_service_test.go
    - internal/auth/sm4_token.go
    - internal/auth/sm4_token_test.go
    - internal/scheduler/video_scheduler.go
    - internal/scheduler/video_scheduler_test.go
    - internal/recorder/coordinator.go
    - internal/recorder/coordinator_test.go
    - internal/huawei/manager.go
    - internal/handlers/ppt_handler.go
    - internal/handlers/apikey_handler.go
    - internal/handlers/video_file_handler.go
    - internal/handlers/system_handler.go
    - internal/handlers/user_handler.go
    - internal/handlers/auth_handler.go
    - internal/handlers/transcription_handler.go
    - internal/handlers/split_handler.go
    - internal/handlers/admin_handler.go
    - internal/handlers/video_recording_task_handler.go
  </files>
  <read_first>
    - D:\CODE\ClaudeCode\record_V2\internal\middleware\audit.go
    - D:\CODE\ClaudeCode\record_V2\internal\middleware\auth.go
    - D:\CODE\ClaudeCode\record_V2\internal\models\audit_log.go
    - D:\CODE\ClaudeCode\record_V2\internal\models\notification.go
    - D:\CODE\ClaudeCode\record_V2\internal\models\api_key.go
    - D:\CODE\ClaudeCode\record_V2\internal\models\role.go
    - D:\CODE\ClaudeCode\record_V2\internal\models\user.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\usb_device_scanner.go
    - D:\CODE\ClaudeCode\record_V2\internal\auth\sm4_token.go
    - D:\CODE\ClaudeCode\record_V2\internal\scheduler\video_scheduler.go
    - D:\CODE\ClaudeCode\record_V2\internal\recorder\coordinator.go
    - D:\CODE\ClaudeCode\record_V2\internal\huawei\manager.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\ppt_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\apikey_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\video_file_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\system_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\user_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\auth_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\transcription_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\split_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\admin_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\video_recording_task_handler.go
    - D:\CODE\ClaudeCode\record_V2\.planning\phases\17-56-p0-p1-p2\17-CONTEXT.md
  </read_first>
  <action>
**BUG-003 (json.Unmarshal 错误被吞 — 6 处):**

1. Replace each `_ = json.Unmarshal(...)` with:
   ```go
   if err := json.Unmarshal(data, &target); err != nil {
       logger.Warn("json unmarshal failed", zap.Error(err), zap.String("field", "field_name"))
       return defaultValue
   }
   ```
   Sites:
   - `internal/middleware/audit.go:47` (parse response body)
   - `internal/models/audit_log.go:136, 145, 154` (GetOldData/GetNewData/GetDiffData — these are CRITICAL because silent failure means audit replay broken)
   - `internal/models/notification.go:85, 107`
   Inject logger into model methods OR pass via receiver struct.

**BUG-004 (_ = 显式忽略 error — 9 处):**

2. Convert each `_ = someFunc()` to:
   ```go
   if err := someFunc(); err != nil {
       logger.Warn("operation failed", zap.Error(err), ...other fields)
   }
   ```
   Sites:
   - `internal/models/api_key.go:92, 112` (IP whitelist JSON parse — CRITICAL: silent failure means all IPs allowed)
   - `internal/models/role.go:38`
   - `internal/models/user.go:104`
   - `internal/services/usb_device_scanner.go:121, 336`
   - `internal/services/video_recording_task_service.go:410` (UpdateTask time compare branch)
   - `internal/auth/sm4_token.go:356`
   - `internal/scheduler/video_scheduler.go:459`
   For IP whitelist: on error, return explicit empty allowed-IP list (fail-closed) — do NOT default to all-allowed.

**BUG-005 (GORM 全部 403 处调用缺 WithContext) — partial per CONTEXT.md `<deferred>`; in THIS task add WithContext to GORM calls in the 5 specific files touched here:**

3. Add `.WithContext(ctx)` to every GORM call (`.Find/.First/.Where/.Create/.Update/.Delete/.Save/.Count/.Pluck`) in these 5 files (verify each file has ≥1 WithContext after edit):
   - `internal/middleware/audit.go` (DB-touching functions)
   - `internal/services/audit/audit_log_service.go` (PERF-009 will touch in 17-03; add WithContext now since it's the shared audit path)
   - `internal/middleware/auth.go` (STYLE-004 touches; function bodies gain ctx)
   - `internal/services/video_recording_task_service.go` (BUG-004 touches)
   - `internal/services/usb_device_scanner.go` (BUG-004 touches)

**BUG-006 (time.Sleep 不可被 ctx 取消 — 4 处):**

4. Replace `time.Sleep(d)` with:
   ```go
   timer := time.NewTimer(d)
   defer timer.Stop()
   select {
   case <-ctx.Done():
       return ctx.Err()
   case <-timer.C:
       // continue
   }
   ```
   Sites:
   - `internal/recorder/coordinator.go:267` (reconnect delay 10s)
   - `internal/huawei/manager.go:203` (1s poll)
   - `internal/scheduler/video_scheduler.go:355` (per-minute delay)
   - `internal/auth/sm4_token.go:395` (GracePeriod)
   Inject ctx parameter into each affected function (if not present) OR add ctx-aware variant.

**STYLE-004 (middleware GetUserID zero-value semantics — full caller enumeration, breaking signature change):**

5. In `internal/middleware/auth.go:13-62`: change all 6 helpers (`GetUserID`, `GetUsername`, `GetUserRole`, etc.) to return `(uint, bool)` / `(string, bool)` etc.
6. Update ALL 10 handler callers (grep-verified) to use the new `, ok` pattern:
   - `internal/handlers/ppt_handler.go`
   - `internal/handlers/video_recording_task_handler.go`
   - `internal/handlers/apikey_handler.go`
   - `internal/handlers/video_file_handler.go`
   - `internal/handlers/system_handler.go`
   - `internal/handlers/user_handler.go`
   - `internal/handlers/auth_handler.go`
   - `internal/handlers/transcription_handler.go`
   - `internal/handlers/split_handler.go`
   - `internal/handlers/admin_handler.go`
   Caller pattern:
   ```go
   userID, ok := middleware.GetUserID(c)
   if !ok {
       c.JSON(http.StatusUnauthorized, gin.H{"error": "user not in context"})
       return
   }
   ```
   Also update `internal/middleware/audit.go` which reads user_id from context.

**STYLE-005 (类型断言未守 , ok):**

7. In `internal/middleware/auth.go:15, 24, 32, 40, 48`: replace `c.MustGet(...).(uint)` etc. with `if v, ok := c.Get(...); ok { ... } else { handle missing }`. Pattern from existing `audit.go:85-94` (CONTEXT.md reference).

**Tests (per D-04.2, at least one per fix):**

8. `internal/middleware/audit_test.go` (new or extend):
   - Test json.Unmarshal with malformed JSON → Warn log + default value returned.
   - Test middleware `GetUserID` returns false when context key absent.
   - Test type assertion `, ok` pattern: set wrong-type value → returns ok=false.
9. `internal/models/audit_log_test.go` (new): Test GetOldData with corrupt JSON → Warn + nil map.
10. `internal/models/notification_test.go` (new): Test UnmarshalJSON with malformed payload.
11. `internal/models/api_key_test.go` (new): Test IsIPAllowed with malformed JSON in IP whitelist → fail-closed (empty allowed list, not all-allowed).
12. `internal/auth/sm4_token_test.go` (new or extend from P0): Test GracePeriod ticker with ctx cancellation → exits within 100ms.
13. `internal/recorder/coordinator_test.go` (new or extend from P0): Test reconnect ctx-cancel — start reconnect, cancel ctx, assert returns within 1s (not full 10s).
14. `internal/middleware/auth_test.go` (new or extend): Test GetUserID with missing key → returns (0, false). Test SetUserID then GetUserID → returns (N, true).

**No-go list:**
- Do NOT change audit log JSON field names or DB column types.
- Do NOT break existing handler JSON request/response shapes (except inserting the unauthorized early-return where GetUserID now returns ok=false — this is a behavior fix, not a shape change).
- Do NOT do full 403-site GORM WithContext sweep (deferred).
- Do NOT change `UpdateTask` public signature.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/middleware/... ./internal/models/... ./internal/auth/... ./internal/scheduler/... ./internal/recorder/... ./internal/huawei/... ./internal/services/... ./internal/handlers/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "_ = json.Unmarshal" internal/middleware/audit.go internal/models/audit_log.go internal/models/notification.go` returns 0
    - `grep -rn "_ = json.Unmarshal\|_ = cmd.Run\|_ = RevokeUserSessions" internal/models/ internal/services/ internal/auth/sm4_token.go internal/scheduler/` shows 0 hits in the 9 listed files
    - `grep -rn "time.Sleep" internal/recorder/coordinator.go internal/huawei/manager.go internal/scheduler/video_scheduler.go internal/auth/sm4_token.go` returns 0
    - `grep -n "time.NewTimer" internal/recorder/coordinator.go internal/huawei/manager.go internal/scheduler/video_scheduler.go internal/auth/sm4_token.go` ≥ 4
    - **BUG-005 WithContext across all 5 files** (W7): `grep -c ".WithContext(ctx)" internal/middleware/audit.go` ≥ 1; `grep -c ".WithContext(ctx)" internal/services/audit/audit_log_service.go` ≥ 1; `grep -c ".WithContext(ctx)" internal/middleware/auth.go` ≥ 1; `grep -c ".WithContext(ctx)" internal/services/video_recording_task_service.go` ≥ 1; `grep -c ".WithContext(ctx)" internal/services/usb_device_scanner.go` ≥ 1
    - `grep -n "GetUserID.*(uint, bool)\|GetUserID.*(uint, bool)" internal/middleware/auth.go` returns 1
    - `grep -c ", ok :=" internal/middleware/auth.go` ≥ 5
    - **STYLE-004 full caller update** (W14): `grep -rn "middleware.GetUserID\b" internal/ | grep -v "_test.go" | grep -v "(uint, bool)" | grep -v "func GetUserID"` returns 0 lines (no caller still uses the old single-return signature); `grep -rn "middleware.GetUserID" internal/handlers/ | wc -l` ≥ 10 (all 10 handlers updated)
    - All test files from acceptance list exist
    - `go test ./internal/middleware/... ./internal/models/... ./internal/handlers/...` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>
    All 6 BUG-003/004/005/006 sites fixed with logger.Warn on error; time.Sleep replaced with ctx-aware timers; middleware GetUserID returns (uint, bool) with all 10 handler callers updated; type assertions use , ok pattern; BUG-005 WithContext covers all 5 files (audit.go, audit_log_service.go, auth.go, video_recording_task_service.go, usb_device_scanner.go); ≥ 7 test functions cover the fixes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Security hardening SEC-005/006/007/008/009/010</name>
  <files>
    - internal/migrations/013_add_ad_fields.go
    - internal/migrations/013_add_ad_fields_test.go
    - internal/migrations/016_alter_fingerprint_to_sha256.go
    - internal/services/storage/file_service.go
    - internal/services/storage/file_service_test.go
    - internal/handlers/file_handler.go
    - internal/handlers/file_handler_test.go
    - internal/handlers/video_file_handler.go
    - internal/handlers/video_recording_task_handler.go
    - internal/config/config.go
    - internal/config/config_test.go
    - cmd/server/app.go
    - internal/middleware/auth.go
    - internal/middleware/auth_test.go
    - internal/huawei/client.go
    - internal/huawei/client_test.go
  </files>
  <read_first>
    - D:\CODE\ClaudeCode\record_V2\internal\migrations\013_add_ad_fields.go
    - D:\CODE\ClaudeCode\record_V2\internal\services\storage\file_service.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\file_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\video_file_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\handlers\video_recording_task_handler.go
    - D:\CODE\ClaudeCode\record_V2\internal\config\config.go
    - D:\CODE\ClaudeCode\record_V2\internal\middleware\auth.go
    - D:\CODE\ClaudeCode\record_V2\internal\huawei\client.go
    - D:\CODE\ClaudeCode\record_V2\cmd\server\app.go
    - D:\CODE\ClaudeCode\record_V2\.planning\phases\17-56-p0-p1-p2\17-CONTEXT.md
  </read_first>
  <action>
**SEC-005 (SQL 字符串拼接 — migrations/013_add_ad_fields.go:37):**

1. Replace `db.Exec("ALTER TABLE users ADD COLUMN " + field.column + " " + field.typ)` with GORM migrator:
   ```go
   if !db.Migrator().HasColumn(&models.User{}, field.column) {
       db.Migrator().AddColumn(&models.User{}, field.column)
   }
   ```
   For column type changes that GORM can't auto-express (e.g., `varchar(255)` vs `text`), fall back to a hardcoded whitelist:
   ```go
   var allowedTypes = map[string]string{
       "ad_dn": "varchar(255)",
       "ad_domain": "varchar(128)",
       ...
   }
   typ, ok := allowedTypes[field.column]
   if !ok { return fmt.Errorf("unknown column: %s", field.column) }
   db.Migrator().AddColumn(&models.User{}, field.column) // apply with struct tag
   ```
   Keep a comment `// SEC-005: SQL 注入防护 — column/typ 全部走白名单`.

**SEC-006 (MD5 → SHA-256, per W11 include migration file in `files_modified`):**

2. In `internal/services/storage/file_service.go:510` (file fingerprint computation): replace `md5.Sum(...)` with `sha256.Sum256(...)`.
3. Create `internal/migrations/016_alter_fingerprint_to_sha256.go`: a GORM auto-migration (`db.Migrator().AlterColumn(...)` or raw `ALTER TABLE video_files MODIFY COLUMN fingerprint VARCHAR(64)`) that widens the `fingerprint` column from VARCHAR(32) (MD5 hex) to VARCHAR(64) (SHA-256 hex). Add a `// SEC-006: fingerprint 升级 SHA-256，旧 MD5 指纹失效` comment. Existing migrations go up to 015; this is 016 (D-01.3 permits `migrations/*.go` Go code).

**SEC-007 (CORS 通配符 — 4 处):**

4. In `internal/config/config.go` `Config` struct: add `CORS.AllowedOrigins []string` field (default empty slice = deny all cross-origin). In `setDefaults`: NO defaults (empty = deny). In `BindEnv`: `cors.allowed_origins` from `CORS_ALLOWED_ORIGINS` (comma-separated).
5. In `cmd/server/app.go`: find existing CORS middleware init. Replace wildcard `"*"` with `cfg.CORS.AllowedOrigins` slice (gin-contrib/cors supports `AllowOrigins: allowedOrigins`). Default behavior: if `AllowedOrigins` is empty, deny ALL cross-origin requests.
6. In `internal/handlers/file_handler.go:141`, `video_file_handler.go:160, 410`, `video_recording_task_handler.go:718`: remove any `Access-Control-Allow-Origin: *` headers in handler-level CORS; rely on the centralized middleware. If handlers need specific headers, add them via the same config.

**SEC-008 (CSRF 全局缺失 — config flag + config-binding test per W9):**

7. In `internal/config/config.go`: add `Security.CSRFEnabled bool` (default false). In `BindEnv`: bind `security.csrf_enabled` to env var `CSRF_ENABLED`. In `cmd/server/app.go`: read this flag and (if true) attach a CSRF middleware (e.g., `github.com/gin-contrib/csrf` — already in go.mod? if not, document as TODO "CSRF middleware 待引入, 暂仅保留配置项"). For this phase, just register the config flag; full CSRF implementation is reserved for future phase.

**SEC-009 (敏感信息日志 — 2 处):**

8. In `internal/handlers/file_handler.go:127`: change `logger.Info("...", zap.String("token", accessToken))` to log only last 4 chars:
   ```go
   masked := accessToken
   if len(masked) > 4 { masked = "***" + masked[len(masked)-4:] }
   logger.Info("...", zap.String("token", masked))
   ```
9. In `internal/huawei/client.go:351`: wrap Huawei response body in a sanitizer that masks known sensitive fields (`username`, `password`, `certBase64String`, `WEB_RequestCertificateAPI` response payloads) before logging at Debug level. Reference existing `sanitizer.go` (DI-injected commit 7169029) pattern — but for Huawei client, define local `huaweiSanitize(body []byte) []byte` since sanitizer.go is auth-specific.

**SEC-010 (Token URL 白名单 — middleware/auth.go:137-138):**

10. In `internal/middleware/auth.go`: replace substring matching (`strings.Contains(path, "/download/")` etc.) with prefix matching against a configured list:
    ```go
    var allowedTokenURLPrefixes = []string{"/api/v1/files/download/", "/api/v1/recordings/", "/api/v1/ppts/"}
    for _, prefix := range cfg.Security.AllowedTokenURLPrefixes {
        if strings.HasPrefix(c.Request.URL.Path, prefix) {
            // allow token in query
        }
    }
    ```
    Move the prefix list into `internal/config/config.go` `Security.AllowedTokenURLPrefixes []string` (default = empty = NO token in query). For backward compat, seed with the current substring patterns translated to prefixes.

**Tests (per D-04.2):**

11. `internal/migrations/013_add_ad_fields_test.go` (new): Test that running the migration twice doesn't error (idempotent) — first call creates column, second call is no-op.
12. `internal/services/storage/file_service_test.go` (new or extend): Test fingerprint function returns 64-char hex (not 32-char MD5). Test two different file contents produce different fingerprints (collision sanity).
13. `internal/handlers/file_handler_test.go` (new or extend): Test that token in URL is masked in log output (use zap observer). Set `accessToken = "abcdef1234567890"`, expect log entry contains `"token": "***7890"`.
14. `internal/huawei/client_test.go` (new or extend): Test that response body containing `"password":"secret123"` is masked to `"password":"***"` in Debug log.
15. `internal/middleware/auth_test.go` (extend from Task 1): Test `isVideoDownloadEndpoint` (or successor function) — request to `/api/v1/files/download/abc` matches prefix → token allowed; request to `/api/v1/users/list?token=xxx` → no prefix match → token rejected.
16. **SEC-008 config-binding test (W9)**: in `internal/config/config_test.go` (extend from P0): `TestCSRFEnabledEnvBinding` — `os.Setenv("CSRF_ENABLED", "true"); cfg.Load(); assert cfg.Security.CSRFEnabled == true`. Also test default false when env unset.

**No-go list:**
- Do NOT introduce new external dependencies (CSRF middleware can be a stub for this phase).
- Do NOT change `models.User` struct field types (only widen `fingerprint` column via migration 016).
- Do NOT modify existing CORS test fixtures in a breaking way.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/migrations/... ./internal/services/storage/... ./internal/handlers/... ./internal/middleware/... ./internal/huawei/... ./internal/config/... ./cmd/server/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Migrator().AddColumn\|Migrator().HasColumn" internal/migrations/013_add_ad_fields.go` ≥ 2
    - `grep -c "md5.Sum\|crypto/md5" internal/services/storage/file_service.go` returns 0
    - `grep -c "sha256.Sum256\|crypto/sha256" internal/services/storage/file_service.go` ≥ 1
    - `ls internal/migrations/016_alter_fingerprint_to_sha256.go` succeeds (W11: migration file created)
    - `grep -c "ALTER.*fingerprint\|ModifyColumn.*fingerprint" internal/migrations/016_alter_fingerprint_to_sha256.go` ≥ 1
    - `grep -n "AllowedOrigins" internal/config/config.go` ≥ 1 (struct field)
    - `grep -n "Access-Control-Allow-Origin.*\\*" internal/handlers/file_handler.go internal/handlers/video_file_handler.go internal/handlers/video_recording_task_handler.go` returns 0
    - `grep -n "CSRFEnabled\|csrf_enabled" internal/config/config.go cmd/server/app.go` ≥ 2
    - `grep -c "len.*-4" internal/handlers/file_handler.go` ≥ 1 (token masking)
    - `grep -c "AllowedTokenURLPrefixes\|HasPrefix" internal/middleware/auth.go` ≥ 1
    - All test files from acceptance list exist (including SEC-008 config-binding test)
    - `go test ./internal/services/storage/... ./internal/handlers/... ./internal/middleware/... ./internal/config/...` exits 0
  </acceptance_criteria>
  <done>
    SEC-005 SQL injection via column name blocked by GORM migrator + whitelist; SEC-006 SHA-256 replaces MD5 fingerprint + migration 016 widens column; SEC-007 CORS defaults deny + reads from config; SEC-008 CSRF config flag added with config-binding test; SEC-009 sensitive log masking (token + Huawei response); SEC-010 token URL whitelist uses prefix matching; ≥ 6 test functions cover the security fixes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (P1a layer)

| Boundary | Description |
|----------|-------------|
| audit→JSON | Audit log JSON columns unparseable → silent corruption risk (BUG-003) |
| GORM→DB | All GORM queries must use ctx (BUG-005) — partial fix across 5 files here |
| SQL→migration | ALTER TABLE column names from string concat (SEC-005) |
| file→MIME | MD5 fingerprint collisions (SEC-006) |
| CORS→origin | Cross-origin policy (SEC-007) |
| CSRF→config | CSRF toggle binding (SEC-008) |
| token→URL | Token URL query leakage (SEC-010) |

## STRIDE Threat Register (P1a)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Tampering | migrations/013_add_ad_fields.go (SEC-005) | mitigate | GORM Migrator.AddColumn + column whitelist |
| T-17-13 | Tampering | file fingerprint MD5 (SEC-006) | mitigate | SHA-256; DB column migration 016 |
| T-17-14 | Information Disclosure | CORS wildcard (SEC-007) | mitigate | Read AllowedOrigins from cfg; default deny |
| T-17-15 | Repudiation | json.Unmarshal error swallowed (BUG-003) | mitigate | logger.Warn + defaultValue on all 6 sites |
| T-17-16 | Tampering | IP whitelist _ = json.Unmarshal (BUG-004 site api_key.go) | mitigate | fail-closed: empty allowed list on parse error |
| T-17-17 | Denial of Service | time.Sleep blocks shutdown (BUG-006) | mitigate | time.NewTimer + ctx.Done select |
| T-17-18 | Information Disclosure | token in log (SEC-009 file_handler) | mitigate | log only last 4 chars |
| T-17-19 | Information Disclosure | token URL substring (SEC-010) | mitigate | prefix matching against config list |
| T-17-20 | Spoofing | middleware GetUserID zero-value (STYLE-004) | mitigate | return (uint, bool); all 10 handler callers updated |
| T-17-21 | Tampering | type assertion panic (STYLE-005) | mitigate | , ok pattern from audit.go:85-94 |
| T-17-22 | Spoofing | CSRF toggle missing (SEC-008) | mitigate (partial) | config flag + env binding test; middleware stub OK this phase |
</threat_model>

<verification>
1. `go build ./...` exits 0
2. `go vet ./...` exits 0
3. `gofmt -l` outputs no files
4. `go test ./internal/middleware/... ./internal/models/... ./internal/auth/... ./internal/recorder/... ./internal/huawei/... ./internal/services/... ./internal/handlers/... ./internal/migrations/... ./internal/config/... ./internal/scheduler/... ./cmd/server/...` exits 0
5. Manual grep verification per task's `<acceptance_criteria>`
</verification>

<success_criteria>
- [ ] All 12 P1a finding IDs addressed in code (BUG-003/004/005/006 + SEC-005/006/007/008/009/010 + STYLE-004/005)
- [ ] Each P1a fix has at least one unit test (D-04.2) — 12+ test functions
- [ ] json.Unmarshal no longer swallowed in 6 sites
- [ ] _ = error ignored in 9 sites replaced with logged ignore
- [ ] BUG-005 WithContext added to all 5 files: audit.go + audit_log_service.go + auth.go + video_recording_task_service.go + usb_device_scanner.go
- [ ] time.Sleep replaced with ctx-aware timer in 4 sites
- [ ] SQL migration uses GORM Migrator.AddColumn + whitelist
- [ ] SHA-256 replaces MD5 fingerprint + migration 016 created
- [ ] CORS defaults deny + reads from config
- [ ] CSRF config flag added with env-binding test (W9)
- [ ] Sensitive logs masked
- [ ] Token URL whitelist uses prefix matching
- [ ] Middleware GetUserID returns (uint, bool) AND all 10 handler callers updated (W14)
- [ ] Type assertions use , ok
- [ ] One mega commit `refactor(17-p1a): P1 MEDIUM 发现修复 - 错误处理 + 安全加固（12 项）` with commit message body listing all finding IDs
- [ ] go build / go vet / gofmt -l all green
</success_criteria>

<output>
Create `.planning/phases/17-56-p0-p1-p2/17-02-SUMMARY.md` when done.

Commit message:
```
refactor(17-p1a): P1 MEDIUM 发现修复 - 错误处理 + 安全加固（12 项）

Findings remediated:
- BUG-003: 6 处 json.Unmarshal 错误加 logger.Warn
- BUG-004: 9 处 _ = 错误忽略改为带日志（IP 白名单 fail-closed）
- BUG-005: 5 文件触及的 GORM 调用加 WithContext（audit.go / audit_log_service.go / auth.go / video_recording_task_service.go / usb_device_scanner.go）
- BUG-006: 4 处 time.Sleep 改 time.NewTimer + select ctx
- SEC-005: SQL 字符串拼接改 GORM Migrator.AddColumn + 白名单
- SEC-006: MD5 文件指纹改 SHA-256 + DB 列迁移 016
- SEC-007: CORS 默认拒绝，从 config.AllowedOrigins 读取
- SEC-008: 保留 csrf_enabled 配置开关（含 env 绑定测试）
- SEC-009: 敏感日志脱敏（file_handler token 末 4 位 + huawei client 响应体）
- SEC-010: Token URL 白名单 substring 改 prefix 匹配
- STYLE-004: 中间件 GetUserID 改返回 (uint, bool) + 10 个 handler 调用方全部更新
- STYLE-005: 类型断言 , ok 守卫（与 audit.go:85-94 对齐）
```
</output>
</invoke>
