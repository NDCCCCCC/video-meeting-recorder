---
phase: 20-handleerror-classify-convergence
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - internal/services/video_file_service.go
  - internal/services/transcription_service.go
  - internal/services/video_recording_task_service.go
  - internal/services/ppt_editor_service.go
  - internal/services/usb_device_scanner.go
  - internal/services/dashboard_service.go
  - internal/services/conversion_service.go
  - internal/services/splitting_service.go
  - internal/services/config_service.go
  - internal/services/frame_extractor.go
  - internal/services/input_config_service.go
  - internal/services/ppt_merge_service.go
  - internal/services/python_deps.go
  - internal/services/storage/file_service.go
  - internal/auth/ad_auth.go
  - internal/auth/local_auth.go
  - internal/auth/sm4_token.go
  - internal/scheduler/video_scheduler.go
  - internal/huawei/manager.go
  - internal/huawei/client.go
autonomous: true
requirements: [REQ-20b-upgrade]
user_setup: []

must_haves:
  truths:
    - "Every `zap.Error(err)` call-site in internal/services/* (excluding ppt_file_service.go which was done in 20-02) is upgraded to `zap.Error(err), response.SentinelField(err)`"
    - "Every `zap.Error(err)` call-site in internal/auth/*, internal/scheduler/*, internal/huawei/* is upgraded similarly"
    - "internal/middleware/error_mapper.go is NOT touched in this phase — its 2 zap.Error sites are explicitly DEFERRED to the next phase per D-03.7 lock ('不动 error_mapper.go，Phase 19 已带开，本阶段不扩展')"
    - "No service-layer behavior change — only structured log enrichment"
    - "All existing service tests pass with -race"
  artifacts:
    - path: "internal/services/video_file_service.go"
      provides: "zap logs enriched with sentinel_type field (22 sites)"
    - path: "internal/services/transcription_service.go"
      provides: "zap logs enriched with sentinel_type field (20 sites)"
  key_links:
    - from: "internal/services/*.go, internal/auth/*.go, internal/scheduler/*.go, internal/huawei/*.go"
      to: "pkg/response.SentinelField"
      via: "zap.Error(err), response.SentinelField(err)"
      pattern: "zap\\.Error\\(err\\), response\\.SentinelField\\(err\\)"
---

<objective>
Zero-intrusion upgrade of ~160 service-layer + ancillary `zap.Error(err)` call-sites to also emit `response.SentinelField(err)`. This completes REQ-20b by extending structured sentinel_type logging from the handler layer (done in 20-02/20-03) into the service layer where most actual error origination happens.

Purpose: Service-layer logs are where ops teams debug production incidents. Adding sentinel_type to these logs without touching any other behavior makes sentinel coverage visible in dashboards, enabling ad-hoc error monitoring (per D-03.5 + T-20-03 mitigation).

Output: ~20 atomic commits grouped by file (services/, auth/, scheduler/, huawei/). Each commit keeps `go build ./...` + `go test -race ./...` for the touched package green. ppt_file_service.go is EXCLUDED (already done in 20-02 Task 1). internal/middleware/error_mapper.go is EXCLUDED per D-03.7 lock — deferred to next phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-handleerror-classify-convergence/20-CONTEXT.md
@.planning/phases/20-handleerror-classify-convergence/20-RESEARCH.md
@.planning/phases/20-handleerror-classify-convergence/20-VALIDATION.md
@.planning/phases/20-handleerror-classify-convergence/20-01-PLAN.md

<interfaces>
<!-- Contracts from 20-01 (must be landed first). -->

From pkg/response/sentinel.go (20-01 Task 2 output):
```go
func SentinelField(err error) zap.Field  // safe to call from any package importing pkg/response
```

Service-layer zap.Error site counts (from RESEARCH §4.2 — verify at runtime via grep):
| Directory / File | Sites | Task | Notes |
|------------------|-------|------|-------|
| **Task 1a — top-4 service files by site count:** | | | |
| internal/services/video_file_service.go | 22 | 1a | Largest single file |
| internal/services/transcription_service.go | 20 | 1a | |
| internal/services/ppt_editor_service.go | 11 | 1a | |
| internal/services/video_recording_task_service.go | 9 | 1a | |
| **Task 1a subtotal** | **62** | | |
| **Task 1b — remaining service files:** | | | |
| internal/services/conversion_service.go | 7 | 1b | |
| internal/services/usb_device_scanner.go | 6 | 1b | |
| internal/services/dashboard_service.go | 5 | 1b | |
| internal/services/splitting_service.go | 5 | 1b | |
| internal/services/frame_extractor.go | 4 | 1b | |
| internal/services/config_service.go | 3 | 1b | |
| internal/services/input_config_service.go | 3 | 1b | |
| internal/services/ppt_merge_service.go | 3 | 1b | |
| internal/services/python_deps.go | 3 | 1b | |
| internal/services/storage/file_service.go | 3 | 1b | |
| **Task 1b subtotal** | **42** | | |
| **Task 2 — auth/scheduler/huawei:** | | | |
| internal/scheduler/video_scheduler.go | 28 | 2 | Largest in scheduler |
| internal/auth/ad_auth.go | 11 | 2 | Touched by 20-01 Task 3; this plan adds zap upgrade |
| internal/auth/local_auth.go | 6 | 2 | |
| internal/auth/sm4_token.go | 4 | 2 | |
| internal/huawei/manager.go | 5 | 2 | |
| internal/huawei/client.go | 2 | 2 | |
| **Task 2 subtotal** | **56** | | |
| internal/services/ppt_file_service.go | 5 | — | **EXCLUDED — done in 20-02 Task 1** |
| internal/middleware/error_mapper.go | 2 | — | **DEFERRED per D-03.7** — not touched this phase |
| **TOTAL (this plan, 1a+1b+2)** | **~160** | | (208 total phase scope − 5 ppt_file − ~43 handlers done in 20-02/20-03 − 2 middleware deferred = ~160) |

Verification grep command for finding sites at runtime:
`grep -rn 'zap\.Error(err)' internal/services/ internal/auth/ internal/scheduler/ internal/huawei/ --include='*.go' | grep -v '_test.go'`
(Note: internal/middleware/ intentionally excluded from this grep — D-03.7 lock.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Upgrade zap.Error in top-4 service files by site count (video_file / transcription / ppt_editor / video_recording_task)</name>
  <files>internal/services/video_file_service.go, internal/services/transcription_service.go, internal/services/ppt_editor_service.go, internal/services/video_recording_task_service.go</files>
  <read_first>
    - .planning/phases/20-handleerror-classify-convergence/20-RESEARCH.md (§4.2 — full per-file counts)
    - pkg/response/sentinel.go (20-01 output — confirm SentinelField signature)
    - internal/services/video_file_service.go (largest, 22 sites — read once to confirm pattern, then apply across files)
    - go.mod (confirm response package path: github.com/NDCCCCCC/video-meeting-recorder/pkg/response)
  </read_first>
  <action>
Atomic commit per service file (4 commits total — one per .go file in the files list). These are the 4 highest-site-count service files (22 / 20 / 11 / 9 sites).

For EACH service file:
(a) Add import `"github.com/NDCCCCCC/video-meeting-recorder/pkg/response"` if not already present. (Some service files may not yet import pkg/response — adding it does NOT create a circular dependency because pkg/response imports internal/errors, not internal/services. RESEARCH §3 confirms the dependency direction is safe.)
(b) Run grep to enumerate `zap.Error(err)` sites in the file at runtime (line numbers drift).
(c) For each site, transform `zap.Error(err)` → `zap.Error(err), response.SentinelField(err)`. Use Edit tool with exact-match replacement to avoid accidental touches.
(d) Run `go build ./internal/services/...` after each file to catch import errors early.

DO NOT modify any other logic — no error handling changes, no return value changes, no test changes. Pure mechanical log-field addition.

DO NOT touch `internal/services/ppt_file_service.go` — it was already upgraded in 20-02 Task 1.
DO NOT touch `internal/middleware/error_mapper.go` — D-03.7 lock, deferred to next phase.

Atomic commit message per file:
`feat(20-logger): add response.SentinelField to {filename} zap.Error call-sites`
  </action>
  <verify>
    <automated>grep -rL 'response\.SentinelField' internal/services/video_file_service.go internal/services/transcription_service.go internal/services/ppt_editor_service.go internal/services/video_recording_task_service.go  # must return empty (all files contain SentinelField)</automated>
    <automated>grep -c 'response\.SentinelField' internal/services/video_file_service.go  # must be ~22</automated>
    <automated>grep -c 'response\.SentinelField' internal/services/transcription_service.go  # must be ~20</automated>
    <automated>go build ./... && go vet ./internal/services/...</automated>
    <automated>go test -race ./internal/services/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rL 'response\.SentinelField' internal/services/video_file_service.go internal/services/transcription_service.go internal/services/ppt_editor_service.go internal/services/video_recording_task_service.go` returns empty (all 4 files have SentinelField)
    - `grep -c 'response\.SentinelField' internal/services/video_file_service.go` returns >= 20 (close to 22)
    - `grep -c 'response\.SentinelField' internal/services/transcription_service.go` returns >= 18 (close to 20)
    - `go build ./...` exits 0
    - `go test -race ./internal/services/... -count=1` exits 0 (no service test regression — this is a pure log addition)
  </acceptance_criteria>
  <done>
4 highest-site-count service files (video_file/transcription/ppt_editor/video_recording_task) emit sentinel_type in their zap.Error logs; no behavior change; all tests green.
  </done>
</task>

<task type="auto">
  <name>Task 1b: Upgrade zap.Error in remaining service files (usb_device_scanner / dashboard / conversion / splitting / config / frame_extractor / input_config / ppt_merge / python_deps / storage-file_service)</name>
  <files>internal/services/usb_device_scanner.go, internal/services/dashboard_service.go, internal/services/conversion_service.go, internal/services/splitting_service.go, internal/services/config_service.go, internal/services/frame_extractor.go, internal/services/input_config_service.go, internal/services/ppt_merge_service.go, internal/services/python_deps.go, internal/services/storage/file_service.go</files>
  <read_first>
    - .planning/phases/20-handleerror-classify-convergence/20-RESEARCH.md (§4.2 — full per-file counts)
    - pkg/response/sentinel.go (20-01 output — confirm SentinelField signature; same as Task 1a)
    - internal/services/conversion_service.go (highest site count in this batch, 7 sites — read once to confirm pattern matches Task 1a approach)
    - go.mod (confirm response package path)
  </read_first>
  <action>
Atomic commit per service file (10 commits total — one per .go file in the files list). These are the remaining 10 service files with 3-7 sites each (42 sites total). Same mechanical pattern as Task 1a.

For EACH service file:
(a) Add import `"github.com/NDCCCCCC/video-meeting-recorder/pkg/response"` if not already present.
(b) Run grep to enumerate `zap.Error(err)` sites in the file at runtime (line numbers drift).
(c) For each site, transform `zap.Error(err)` → `zap.Error(err), response.SentinelField(err)`. Use Edit tool with exact-match replacement.
(d) Run `go build ./internal/services/...` after each file to catch import errors early.

DO NOT modify any other logic — no error handling changes, no return value changes, no test changes. Pure mechanical log-field addition.

DO NOT touch `internal/services/ppt_file_service.go` — already upgraded in 20-02 Task 1.
DO NOT touch `internal/middleware/error_mapper.go` — D-03.7 lock, deferred to next phase.

Atomic commit message per file:
`feat(20-logger): add response.SentinelField to {filename} zap.Error call-sites`
  </action>
  <verify>
    <automated>grep -rL 'response\.SentinelField' internal/services/usb_device_scanner.go internal/services/dashboard_service.go internal/services/conversion_service.go internal/services/splitting_service.go internal/services/config_service.go internal/services/frame_extractor.go internal/services/input_config_service.go internal/services/ppt_merge_service.go internal/services/python_deps.go internal/services/storage/file_service.go  # must return empty</automated>
    <automated>for f in internal/services/usb_device_scanner.go internal/services/dashboard_service.go internal/services/conversion_service.go internal/services/splitting_service.go internal/services/config_service.go internal/services/frame_extractor.go internal/services/input_config_service.go internal/services/ppt_merge_service.go internal/services/python_deps.go internal/services/storage/file_service.go; do echo "--- $f ---"; grep -c 'response\.SentinelField' $f; done</automated>
    <automated>go build ./... && go vet ./internal/services/...</automated>
    <automated>go test -race ./internal/services/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rL 'response\.SentinelField' internal/services/usb_device_scanner.go internal/services/dashboard_service.go internal/services/conversion_service.go internal/services/splitting_service.go internal/services/config_service.go internal/services/frame_extractor.go internal/services/input_config_service.go internal/services/ppt_merge_service.go internal/services/python_deps.go internal/services/storage/file_service.go` returns empty (all 10 files have SentinelField)
    - `grep -rL 'response\.SentinelField' internal/services/ | grep -v '_test.go' | grep -v 'ppt_file_service.go'` returns empty (every non-excluded service file covered — combined with Task 1a)
    - `go build ./...` exits 0
    - `go test -race ./internal/services/... -count=1` exits 0 (no service test regression)
    - `grep -c 'response\.SentinelField' internal/services/ppt_file_service.go` confirms the EXCLUDED file was NOT touched again in this plan (its upgrade happened in 20-02)
    - `grep -c 'response\.SentinelField' internal/middleware/error_mapper.go` returns 0 (D-03.7 deferred — middleware untouched)
  </acceptance_criteria>
  <done>
All 10 remaining service files emit sentinel_type in their zap.Error logs; no behavior change; all tests green. Combined with Task 1a, all 14 service files (excluding ppt_file_service.go) are upgraded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade zap.Error in internal/auth/* + internal/scheduler/* + internal/huawei/*</name>
  <files>internal/auth/ad_auth.go, internal/auth/local_auth.go, internal/auth/sm4_token.go, internal/scheduler/video_scheduler.go, internal/huawei/manager.go, internal/huawei/client.go</files>
  <read_first>
    - .planning/phases/20-handleerror-classify-convergence/20-RESEARCH.md (§4.2 — auth/scheduler/huawei counts)
    - .planning/phases/20-handleerror-classify-convergence/20-CONTEXT.md (§decisions D-03.7 — "不动 error_mapper.go" locked; error_mapper.go is NOT in this task's files list)
    - internal/auth/ad_auth.go (current state — already touched by 20-01 Task 3 for ErrADUserNotRegistered migration; verify imports include pkg/response or add it)
    - internal/scheduler/video_scheduler.go (largest scheduler file, 28 sites)
    - pkg/response/sentinel.go (20-01 output)
  </read_first>
  <action>
Atomic commits grouped by directory:
- Commit 1: `internal/auth/ad_auth.go` (11 sites) + `internal/auth/local_auth.go` (6 sites) + `internal/auth/sm4_token.go` (4 sites) — one commit for the auth package.
- Commit 2: `internal/scheduler/video_scheduler.go` (28 sites) — one commit (large file alone).
- Commit 3: `internal/huawei/manager.go` (5 sites) + `internal/huawei/client.go` (2 sites) — one commit for the huawei package.

For EACH file:
(a) Add `"github.com/NDCCCCCC/video-meeting-recorder/pkg/response"` import if missing.
(b) Replace `zap.Error(err)` → `zap.Error(err), response.SentinelField(err)` at every site.
(c) Build + vet the package after each file.

DO NOT touch `internal/middleware/error_mapper.go` — D-03.7 LOCKED ("不动 error_mapper.go，Phase 19 已带开，本阶段不扩展"). Its 2 zap.Error sites are explicitly DEFERRED to the next phase. The planner does not have authority to reinterpret this locked decision (per context_fidelity rule). This plan's scope excludes middleware entirely.
  </action>
  <verify>
    <automated>for f in internal/auth/ad_auth.go internal/auth/local_auth.go internal/auth/sm4_token.go internal/scheduler/video_scheduler.go internal/huawei/manager.go internal/huawei/client.go; do echo "--- $f ---"; grep -c 'response\.SentinelField' $f; done</automated>
    <automated>grep -c 'response\.SentinelField' internal/scheduler/video_scheduler.go  # must be ~28</automated>
    <automated>test "$(grep -c 'response\.SentinelField' internal/middleware/error_mapper.go 2>/dev/null || echo 0)" = "0" && echo "D-03.7 OK: middleware untouched" || echo "D-03.7 VIOLATION: middleware modified"</automated>
    <automated>go build ./... && go vet ./internal/auth/... ./internal/scheduler/... ./internal/huawei/...</automated>
    <automated>go test -race ./internal/auth/... ./internal/scheduler/... ./internal/huawei/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rL 'response\.SentinelField' internal/auth/ internal/scheduler/ internal/huawei/ | grep -v '_test.go'` returns empty (every touched file has SentinelField)
    - `grep -c 'response\.SentinelField' internal/scheduler/video_scheduler.go` returns >= 25 (close to 28)
    - `grep -c 'response\.SentinelField' internal/middleware/error_mapper.go` returns 0 (D-03.7 deferred — middleware NOT touched this phase)
    - `go build ./...` exits 0
    - `go test -race ./internal/auth/... ./internal/scheduler/... ./internal/huawei/... -count=1` exits 0
  </acceptance_criteria>
  <done>
auth/scheduler/huawei zap.Error sites enriched with sentinel_type; D-03.7 boundary on error_mapper.go respected by deferral (middleware untouched); all tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → log sink | Structured logs may be ingested by monitoring; sentinel_type field adds a tag without leaking err.Error() |
| middleware → log sink (D-03.7 deferred) | error_mapper.go backstop logs every unrecovered error; its 2 zap.Error sites remain WITHOUT SentinelField this phase — accepted monitoring gap per D-03.7 lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-log-leak | Information Disclosure | Service-layer zap.Error still logs full err | accept | zap.Error(err) ALREADY logs the full err.Error() — this plan does NOT change that. SentinelField ADDS a structured tag (sentinel_type=ErrXxx) without removing or altering zap.Error's content. No new leak surface; pre-existing log content unchanged. If err.Error() contains PII, that's a pre-existing concern out of phase scope. |
| T-20-03-adhoc-visibility | Repudiation (monitoring blind spot) | Unknown errors emit sentinel_type="ad-hoc" | mitigate | This is the PRIMARY security uplift of the plan: ad-hoc errors now have a searchable tag in logs, enabling ops to query `sentinel_type=ad-hoc` and identify unhandled error paths. Turns a blind spot into a monitored signal. |
| T-20-middleware-boundary | Tampering / Monitoring gap | error_mapper.go D-03.7 boundary | accept | D-03.7 locks "不动 error_mapper.go (Phase 19 已带开，本阶段不扩展)". This plan respects the lock by NOT touching middleware. The 2 middleware zap.Error sites will NOT have SentinelField this phase — a small accepted monitoring gap (the backstop middleware still logs via zap.Error; only the structured sentinel_type tag is absent). Deferred to next phase per user decision. The planner has no authority to reinterpret this locked decision. |
| T-20-SC-supply-chain | Tampering | No new packages | accept | pkg/response already exists; no new external deps. |
</threat_model>

<verification>
Plan-level checks (run after all three tasks complete):
- `go build ./...` exits 0
- `go vet ./...` exits 0
- `go test -race ./... -count=1` exits 0 (whole-project regression — every phase-17-touched package + new tests)
- Phase-wide zap.Error coverage: `grep -rn 'zap\.Error(err)' internal/services/ internal/auth/ internal/scheduler/ internal/huawei/ --include='*.go' | grep -v '_test.go' | wc -l` and `grep -rn 'response\.SentinelField' internal/services/ internal/auth/ internal/scheduler/ internal/huawei/ --include='*.go' | grep -v '_test.go' | wc -l` — the second count should be >= the first (every zap.Error has a paired SentinelField) EXCEPT for pkg/response/sentinel.go itself and the rare cases where err is known nil
- D-03.7 compliance: `grep -c 'response\.SentinelField' internal/middleware/error_mapper.go` returns 0 (middleware untouched per locked decision)
</verification>

<success_criteria>
- ~160 service/auth/scheduler/huawei zap.Error sites enriched with SentinelField
- Zero behavior change across all touched files (pure log enrichment)
- D-03.7 boundary on error_mapper.go respected by explicit deferral — middleware file NOT modified this phase (its 2 sites deferred to next phase per user-locked decision)
- All existing tests pass with -race
- REQ-20b-upgrade (service portion) complete; combined with 20-02/20-03 handler portion, full REQ-20b delivered
</success_criteria>

<output>
Create `.planning/phases/20-handleerror-classify-convergence/20-04-SUMMARY.md` when done
</output>
