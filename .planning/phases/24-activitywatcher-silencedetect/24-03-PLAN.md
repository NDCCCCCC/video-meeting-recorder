---
phase: 24-activitywatcher-silencedetect
plan: 03
type: execute
wave: 3
depends_on:
  - 24-02
files_modified:
  - internal/recorder/coordinator.go
autonomous: true
requirements:
  - DETECT-02
  - WATCH-05
must_haves:
  truths:
    - "buildRecordingCommand 在 args 中插入 -af silencedetect=noise=NdB:d=Ns (来自 cfg.SmartEnd)"
    - "RecordingProcess 含 OnReconnect func() 字段"
    - "restartRecording 在新 ffmpeg 启动前同步调用 process.OnReconnect()"
  artifacts:
    - path: "internal/recorder/coordinator.go"
      provides: "RecordingProcess.OnReconnect 字段 + buildRecordingCommand 注入 -af silencedetect + restartRecording 调用 OnReconnect"
      contains: "OnReconnect func()"
  key_links:
    - from: "internal/recorder/coordinator.go:buildRecordingCommand"
      to: "cfg.SmartEnd.SilenceDB / SilenceDurationS"
      via: "fmt.Sprintf 拼 args"
      pattern: "silencedetect=noise="
    - from: "internal/recorder/coordinator.go:restartRecording"
      to: "RecordingProcess.OnReconnect"
      via: "同步函数调用 (在 cmd.Start 之前)"
      pattern: "process\\.OnReconnect\\(\\)"
---

<objective>
把 ActivityWatcher 接到 ffmpeg 录制管线:在 buildRecordingCommand 中注入 -af silencedetect 过滤器 (DETECT-02 wiring),在 RecordingProcess 上加 OnReconnect 回调字段 (WATCH-05),在 restartRecording 中同步调用 (Pitfall 4 时序正确)。

Purpose: 让 ffmpeg stderr 输出 silencedetect 行供 ActivityWatcher.silenceScanner 解析;ffmpeg 重启时 ActivityWatcher 通过回调清 silenceSince 避免断流期间假静音。

Output: 修改 `internal/recorder/coordinator.go` (3 处编辑)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/24-activitywatcher-silencedetect/24-RESEARCH.md
@.planning/phases/24-activitywatcher-silencedetect/24-VALIDATION.md

# Source of truth — 当前 coordinator.go
@internal/recorder/coordinator.go

# Config (CFG-01 Phase 23 已交付,本计划读字段)
@internal/config/smart_end.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecordingProcess.OnReconnect 字段 + restartRecording 同步调用</name>
  <files>internal/recorder/coordinator.go</files>
  <read_first>
- internal/recorder/coordinator.go (RecordingProcess struct 30-48 行;restartRecording 325-399 行,需在 process.logFile 关闭之前 process.Cmd = exec.CommandContext 之前调用 OnReconnect)
- internal/recorder/activity_watcher.go (Plan 24-02 产物,OnReconnect 方法签名;signature: func (w *ActivityWatcher) OnReconnect())
- .planning/phases/24-activitywatcher-silencedetect/24-RESEARCH.md §WATCH-05 设计 §240-263 行 (Pitfall 4 §483-487 行 — 调用点严格在 cmd.Start 之前)
  </read_first>
  <behavior>
- RecordingProcess struct 末尾追加 OnReconnect func() 字段,导出,带中文注释解释用途
- restartRecording 在 process.logFile.Close() 之后、ctx, cancel := context.WithCancel(...) 之前调用 if process.OnReconnect != nil { process.OnReconnect() }
- OnReconnect 为 nil 时不 panic (Process 创建时默认 nil)
  </behavior>
  <action>
编辑 internal/recorder/coordinator.go 两处:

1. RecordingProcess struct (coordinator.go:30-48 行,字段列表末尾追加):
   ```go
   // OnReconnect 在 attemptReconnect 启动新 ffmpeg 之前由 watcher 等外部观察者注册;
   // 重连期间 watcher 应清理 silenceSince (避免断流期间假静音),但保留 lastFileGrowthAt /
   // huaweiEmptySince 等长周期计时不被重置。WATCH-05。
   OnReconnect func()
   ```
   放在 ShouldReconnect bool 字段之后。

2. restartRecording (coordinator.go:325-399 行,在 _关闭旧的日志文件_ 块之后 _创建新的录制进程_ 之前插入):
   ```go
   // WATCH-05: 重连前同步通知 watcher 清 SilenceSince (避免断流期间假静音)。
   // 严格放在 cmd.Start 之前 (RESEARCH.md Pitfall 4)。
   if process.OnReconnect != nil {
       process.OnReconnect()
   }
   ```
   插入位置精确:line ~363 (process.logFile.Close 之后) 与 line ~365 (ctx, cancel := context.WithCancel 之前)。

3. 不修改其他行为;不导出 watcher 自身,仅暴露 OnReconnect 钩子。
  </action>
  <verify>
<automated>go build ./internal/recorder && echo BUILD_OK</automated>
</verify>
  <done>
- internal/recorder/coordinator.go RecordingProcess 含 OnReconnect func() 字段 (grep "OnReconnect func()" 返回 1 行)
- restartRecording 在 cmd.Start 之前同步调用 process.OnReconnect() (grep "process.OnReconnect()" 至少 1 行)
- go build 退出 0
  </done>
</task>

<task type="auto">
  <name>Task 2: buildRecordingCommand 注入 -af silencedetect 过滤器</name>
  <files>internal/recorder/coordinator.go</files>
  <read_first>
- internal/recorder/coordinator.go:582-700 (buildRecordingCommand 当前实现;args 拼装在 line 584 开始)
- internal/config/smart_end.go:24-30 (SilenceDB / SilenceDurationS 字段,默认值 -30 / 30)
- .planning/phases/24-activitywatcher-silencedetect/24-RESEARCH.md §DETECT-02 设计 §122-162 行 (拼装位置:在 -flags +global_header 之后、-map 之前)
- .planning/phases/24-activitywatcher-silencedetect/24-RESEARCH.md §Risks #5 (smart_end.enabled=false 时不拼 -af,Phase 25 守门)
  </read_first>
  <behavior>
- buildRecordingCommand 返回的 args slice 在指定位置包含 "-af"、"silencedetect=noise=-30dB:d=30" 两个 token
- args 中 silencedetect=noise 值与 cfg.SmartEnd.SilenceDB 一致;d 值与 cfg.SmartEnd.SilenceDurationS 一致
- 若 cfg.SmartEnd.Enabled == false → args 不包含 silencedetect (Phase 25 CFG-03 守门预期;Phase 24 仍按当前 cfg 默认 true 行为,但实现守门逻辑)
  </behavior>
  <action>
编辑 internal/recorder/coordinator.go buildRecordingCommand (coordinator.go:582-700):

1. 在 line 615 args = append(args, "-flags", "+global_header") 之后、line 617 args = append(args, "-map", "0:v") 之前插入:
   ```go
   // Phase 24 DETECT-02: 注入 silencedetect 音频过滤器 (A 信号源)。
   // 仅当 cfg.SmartEnd.Enabled 才注入 (CFG-03 守门预期,Phase 25 调度不启 watcher 时不增加 ffmpeg 开销)。
   if c.config != nil && c.config.SmartEnd.Enabled {
       afSpec := fmt.Sprintf("silencedetect=noise=%ddB:d=%d",
           c.config.SmartEnd.SilenceDB,
           c.config.SmartEnd.SilenceDurationS,
       )
       args = append(args, "-af", afSpec)
       c.logger.Info("注入 silencedetect 过滤器 (ActivityWatcher A 信号源)",
           zap.String("af_spec", afSpec),
           zap.Int("silence_db", c.config.SmartEnd.SilenceDB),
           zap.Int("silence_duration_s", c.config.SmartEnd.SilenceDurationS),
       )
   }
   ```

2. 不修改 buildRecordingCommand 的其他 args 顺序;不修改 buildInputArgs;不影响 USB/RTSP/Stream 输入路径。

3. 不修改 startFFmpegProcess (cmd.Stderr 仍路由到 logFile,ActivityWatcher.silenceScanner 通过 logFile 读取,RESEARCH.md §Pitfall 1)。
  </action>
  <verify>
<automated>go build ./internal/recorder && echo BUILD_OK</automated>
</verify>
  <done>
- buildRecordingCommand 在 cfg.SmartEnd.Enabled=true 时 args 含 "-af"、"silencedetect=noise=-30dB:d=30" (默认 cfg 下)
- buildRecordingCommand 在 cfg.SmartEnd.Enabled=false 时 args 不含 "-af"
- go build 退出 0
  </done>
</task>

<task type="auto">
  <name>Task 3: StartRecordingWithConfig 内构造 ActivityWatcher 并接线 process.OnReconnect</name>
  <files>internal/recorder/coordinator.go</files>
  <read_first>
- internal/recorder/coordinator.go:89-179 (StartRecordingWithConfig 当前实现;ffmpeg 启动在 line 119-125,process 注册在 line 156-165,monitorProcessWithKey 在 line 176)
- internal/recorder/coordinator.go:548-580 (StopRecording 当前实现 — 需在 delete process 之前优雅 stop watcher)
- internal/recorder/activity_watcher.go (Plan 24-02 产物;NewActivityWatcher 签名;OnReconnect 方法;Start/Stop 方法)
- internal/recorder/huawei_state_client.go (Plan 24-01 产物;HuaweiStateClient interface — 给 watcher 注入)
- internal/config/smart_end.go:20-69 (SmartEndConfig 14 字段,watcher 全部读取)
- internal/recorder/coordinator.go (RecordingProcess struct line 31-48 — 加 OnReconnect 字段;本次任务使用)
- .planning/phases/24-activitywatcher-silencedetect/24-RESEARCH.md §Architecture Decisions 4 (per-task owned watcher 实例,避免跨任务状态污染)
  </read_first>
  <behavior>
- StartRecordingWithConfig 在 ffmpeg 启动成功 + process 注册到 c.processes 之后,构造 *ActivityWatcher 并启动 (cfg.SmartEnd.Enabled 为 true 时;false 时不构造)
- 构造参数:cfg=&c.config.SmartEnd, huaweiCli=注入的 HuaweiClient, filePath=process.OutputPath (MKVFilePath), logFile=process.logFile, endedCh=新建 buffered chan struct{} (Phase 25 SCHED-01 会从外部 select 这个 chan)
- process.OnReconnect = watcher.OnReconnect (WATCH-05 接线,让 ffmpeg 重启时清 silenceSince)
- StopRecording 在 process.CancelFunc 调用之前调用 watcher.Stop() (避免重连竞争)
- 不修改 scheduler 本身 (Phase 25 责任) ;仅在 coordinator 侧构造 watcher + 注册 callback + 暴露 taskEndedCh 字段
- RecordingProcess 新增字段 taskEndedCh chan struct{} 供 scheduler select (Phase 25 消费;Phase 24 仅构造)
  </behavior>
  <action>
编辑 internal/recorder/coordinator.go 三处:

1. RecordingProcess struct (在 OnReconnect 字段之后追加):
   ```go
   // ActivityWatcher 指针,StartRecordingWithConfig 内构造,StopRecording 内 Close。
   // nil 时 StartRecordingWithConfig 跳过 watcher 构造 (cfg.SmartEnd.Enabled=false 路径)。
   // WATCH-01: 持有 taskEndedCh 供 Phase 25 scheduler select。
   ActivityWatcher *ActivityWatcher
   // taskEndedCh buffered chan struct{},ActivityWatcher.End() 关闭一次;scheduler
   // 在 SCHED-01 select 第三个 case。Phase 24 仅构造不消费。
   taskEndedCh chan struct{}
   ```

2. StartRecordingWithConfig (coordinator.go:89-179,在 line 165 c.mu.Unlock() 之后,line 167 c.logger.Info(...) 之前插入):
   ```go
   // Phase 24 WATCH-01: per-task owned ActivityWatcher (RESEARCH.md Architecture Decision 1)。
   // 仅在 cfg.SmartEnd.Enabled 为 true 时构造;false 时纯 EndTime 行为 (Phase 25 CFG-03 守门)。
   if c.config != nil && c.config.SmartEnd.Enabled {
       endedCh := make(chan struct{}, 1)
       rec.taskEndedCh = endedCh
       rec.ActivityWatcher = NewActivityWatcher(ActivityWatcherConfig{
           Cfg:       &c.config.SmartEnd,
           HuaweiCli: c.huaweiCli, // RecordingCoordinator 持 huaweiCli 字段(本次任务新增)
           FilePath:  mkvPath,
           LogFile:   logFile,
           EndedCh:   endedCh,
       })
       process.OnReconnect = rec.ActivityWatcher.OnReconnect // WATCH-05
       rec.ActivityWatcher.Start() // 启动 huaweiPoller / fileTicker / silenceScanner goroutines
       c.logger.Info("ActivityWatcher 已启动",
           zap.Uint("task_id", task.ID),
           zap.String("process_key", processKey),
       )
   }
   ```

3. StopRecording (coordinator.go:548-580,在 process.CancelFunc() 之前):
   ```go
   // Phase 24 WATCH-01: 先停 watcher (优雅关闭 goroutines),再 cancel ffmpeg。
   // 顺序避免 watcher 在 ffmpeg 已死时尝试读 logFile 触发 EOF 误报为 stat 死亡。
   if process.ActivityWatcher != nil {
       process.ActivityWatcher.Stop()
   }
   ```

4. SimpleRecordingCoordinator struct (coordinator.go:22-28) 加 huaweiCli 字段:
   ```go
   // huaweiCli 注入给 ActivityWatcher 用于 H 信号轮询。可选 (HuaweiStateClient interface)。
   // 由 app.go 在 NewSimpleRecordingCoordinator 时注入现有 huawei.Client 实例。
   huaweiCli HuaweiStateClient
   ```
   并在 NewSimpleRecordingCoordinator (line 79-86) 加构造参数(改动需后续调用点配合,见 Acceptance Criteria)。

5. 不修改 scheduler;不修改 buildRecordingCommand(已在 Task 2 完成);不修改 startFFmpegProcess。
  </action>
  <verify>
<automated>go build ./internal/recorder && echo BUILD_OK</automated>
</verify>
  <done>
- internal/recorder/coordinator.go RecordingProcess 含 `ActivityWatcher *ActivityWatcher` 字段 (grep 命中 1 行)
- internal/recorder/coordinator.go RecordingProcess 含 `taskEndedCh chan struct{}` 字段 (grep 命中 1 行)
- StartRecordingWithConfig 在 ffmpeg 启动后调用 NewActivityWatcher + rec.ActivityWatcher.Start() + process.OnReconnect = watcher.OnReconnect
- StopRecording 在 CancelFunc() 之前调用 rec.ActivityWatcher.Stop()
- SimpleRecordingCoordinator 加 huaweiCli HuaweiStateClient 字段 + NewSimpleRecordingCoordinator 接收参数
- go build 退出 0(允许有 unused-field warning 由后续 Phase 25 接线消除;本阶段不强制 0 warning)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cfg.SmartEnd.Enabled / SilenceDB / SilenceDurationS → ffmpeg args | 配置文件可控 (运维改 config.yaml) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Tampering | -af silencedetect 字符串拼接 | mitigate | fmt.Sprintf 数字字段受 cfg.Validate 限定范围 (SilenceDB ∈ [-100,0]);不拼接用户输入 |
| T-24-08 | EoP | OnReconnect 字段被外部注入恶意代码 | mitigate | 字段由 coordinator 包内赋值;无 setter;调用点单一 (restartRecording);nil check 兜底 |
| T-24-09 | DoS | OnReconnect 在错误时点调用 | mitigate | 调用点严格在 cmd.Start 之前 (Pitfall 4);OnReconnect panic 会重启 ffmpeg,留 stderr 排查 |
</threat_model>

<verification>
- [ ] `go build ./internal/recorder` 退出 0
- [ ] `grep -c 'OnReconnect func()' internal/recorder/coordinator.go` >= 1
- [ ] `grep -c 'process.OnReconnect()' internal/recorder/coordinator.go` >= 1
- [ ] `grep -c 'silencedetect=noise=' internal/recorder/coordinator.go` >= 1
</verification>

<success_criteria>
- DETECT-02 wiring 落地:ffmpeg 命令行包含 silencedetect 过滤器
- WATCH-05 重连回调接好:ffmpeg 重启前同步通知 watcher
- 不影响其他 ffmpeg 命令行参数 (USB/RTSP/Stream/Mixed 路径不变)
</success_criteria>

<output>
Create `.planning/phases/24-activitywatcher-silencedetect/24-03-SUMMARY.md` when done
</output>