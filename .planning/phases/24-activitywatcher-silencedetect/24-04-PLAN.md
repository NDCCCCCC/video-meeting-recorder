---
phase: 24-activitywatcher-silencedetect
plan: 04
type: execute
wave: 1
depends_on:
  - 24-01
  - 24-02
  - 24-03
files_modified:
  - internal/recorder/activity_watcher_test.go
  - internal/recorder/coordinator_test.go
autonomous: true
requirements:
  - DETECT-02
  - DETECT-03
  - WATCH-01
  - WATCH-02
  - WATCH-03
  - WATCH-04
  - WATCH-05
  - EXTEND-03
must_haves:
  truths:
    - "7 个 ActivityWatcher scenario (正常 H / 正常 A+B / H 降级 / A 降级 / stat 死亡 / 重连保持 / close-once / ExtendStepMin) 全部覆盖且断言通过"
    - "buildRecordingCommand 在 cfg.SmartEnd.Enabled=true 时 args 含 silencedetect 过滤器;在 false 时不含"
    - "restartRecording 在 OnReconnect 已注册时同步调用回调;nil 时不 panic"
  artifacts:
    - path: "internal/recorder/activity_watcher_test.go"
      provides: "8 个 scenario 子测覆盖 ActivityWatcher 全状态机"
      contains: "TestActivityWatcher"
    - path: "internal/recorder/coordinator_test.go"
      provides: "TestBuildRecordingCommand_SilenceDetect + TestRestartRecording_OnReconnect 扩展"
      contains: "TestBuildRecordingCommand_SilenceDetect"
  key_links:
    - from: "internal/recorder/activity_watcher_test.go"
      to: "internal/recorder/activity_watcher.go"
      via: "NewActivityWatcher + Start + Stop + 注入 fake huaweiCli / fake StatFn / fake clock"
      pattern: "NewActivityWatcher\\("
    - from: "internal/recorder/coordinator_test.go"
      to: "internal/recorder/coordinator.go"
      via: "buildRecordingCommand 调用 + args 断言 + restartRecording fake process"
      pattern: "TestBuildRecordingCommand_SilenceDetect"
---

<objective>
补齐 8 个 ActivityWatcher scenario 子测 + 2 个 coordinator 扩展测 (buildRecordingCommand 含 -af silencedetect、restartRecording 调 OnReconnect)。

Purpose: 验收 24-VALIDATION.md §ActivityWatcher scenario matrix 8 行 + §Per-Task Verification Map 中全部 WATCH-* REQ-ID;Nyquist 合规化 (validation 头从 false 改 true)。

Output:
- `internal/recorder/activity_watcher_test.go` (新文件,~400 行,8 个子测)
- `internal/recorder/coordinator_test.go` (追加 2 个 Test* 函数)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-activitywatcher-silencedetect/24-RESEARCH.md
@.planning/phases/24-activitywatcher-silencedetect/24-VALIDATION.md
@.planning/phases/24-activitywatcher-silencedetect/24-01-PLAN.md
@.planning/phases/24-activitywatcher-silencedetect/24-02-PLAN.md
@.planning/phases/24-activitywatcher-silencedetect/24-03-PLAN.md

# 现有 coordinator_test.go (行 408-410 已有 buildRecordingCommand 测,可参考模式)
@internal/recorder/coordinator_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActivityWatcher 8 个 scenario 子测（Nyquist 矩阵全覆盖）</name>
  <files>internal/recorder/activity_watcher_test.go</files>
  <read_first>
- internal/recorder/activity_watcher.go (Plan 24-02 产物,NewActivityWatcher / Start / Stop / IsActive / Snapshot / ExtendStepMin / OnReconnect / EndedCh / fakeClock 字段)
- internal/recorder/huawei_state_client.go (Plan 24-01 产物,HuaweiStateClient interface)
- internal/recorder/silence_parser.go (Plan 24-01 产物,NewSilenceParser / Parse)
- internal/config/smart_end.go (默认值)
- .planning/phases/24-activitywatcher-silencedetect/24-VALIDATION.md §ActivityWatcher scenario matrix §89-102 行 (8 个 scenario 严格对齐)
- .planning/phases/24-activitywatcher-silencedetect/24-RESEARCH.md §Validation Architecture §515-557 行 (test 命令汇总)
  </read_first>
  <behavior>
- TestActivityWatcher_MeetingEnded_HuaweiEmpty:fake Huawei 3 次连续 confState=""+JoinSum=0 + cfg.HuaweiPersistS=30 → 30s 后 closeEnded;断言 <-watcher.EndedCh() 在 fakeClock 推进后 100ms 内返回;Snapshot().Ended=true + EndedReason="huawei_state_empty"
- TestActivityWatcher_MeetingEnded_AndAB:logFile 写入 "[silencedetect @ 0x55a3] silence_start: <early>" + 持续 35s 后写入 silence_end (duration=35≥30) + 文件大小 fakeStatFn 持续返回相同 size (无增长 ≥ 120s) → closeEnded;Snapshot().EndedReason="both_silence_and_stall"
- TestActivityWatcher_HuaweiDegraded:fakeHuawei 3 次返回 err → huaweiDegraded=true + 后续 confState="" 持续 30s 不再触发;H+A+B 仍可正常触发
- TestActivityWatcher_SilenceDegraded:logFile 5 行非 silencedetect 格式 (例如 "frame= 100 fps=30") → silenceParseFailures=5 → silenceDegraded=true + silenceSince 永远 0 → A 路径永不触发;但 H/B 仍可触发
- TestActivityWatcher_StatFailed:fakeStatFn (覆盖 os.Stat) 连续 3 次返回 err → closeEnded("file_stat_failed");Snapshot().EndedReason="file_stat_failed"
- TestActivityWatcher_Reconnect:Start 后 fake logFile 写入 silence_start 让 silenceSince != zero → 调 watcher.OnReconnect() → silenceSince == time.Time{};Snapshot().LastFileGrowthAt 不动
- TestActivityWatcher_CloseOnce:先触发 H closeEnded,再注入 A+B 触发条件 → closeEnded 仅一次 (sync.Once),Snapshot().EndedReason 仍为 "huawei_state_empty"
- TestActivityWatcher_ExtendStep:cfg.SmartEnd.ExtendStepMin=60 → watcher.ExtendStepMin() == 60*time.Minute
  </behavior>
  <action>
新建 internal/recorder/activity_watcher_test.go (package recorder):

A. 顶部 helper:
1. type fakeHuawei struct { state *huawei.ConferenceState; err error; calls int; mu sync.Mutex };func (f *fakeHuawei) GetConferenceState(ctx context.Context) (*huawei.ConferenceState, error) { f.mu.Lock(); defer f.mu.Unlock(); f.calls++; return f.state, f.err }
2. 构造 helper newTestWatcher(t, fakeHuawei, filePath, logFile):内部 NewActivityWatcher + 把 cfg.SmartEnd.CheckIntervalS/HuaweiPollIntervalS/HuaweiPersistS/SilenceDurationS/FileStallS 设为可调短值 (例如 100ms / 50ms) 加速测试 + 返回 watcher 与 fakeClock
3. advanceUntil(t, clock, watcher, ch, timeout):循环 Advance(checkInterval) 直到 ch 关闭或超时;用 require.Eventually 控制时序

B. 8 个 Test* 函数 (与上方 behavior 一一对应):

1. TestActivityWatcher_MeetingEnded_HuaweiEmpty:
   - fakeHuawei 持续返回 &huawei.ConferenceState{ConfState:"", JoinSum:0, HasConferenceFields:true}
   - cfg.HuaweiPersistS=1 (秒) + HuaweiPollIntervalS=1 + CheckIntervalS=1 (缩短测试时长)
   - 启 Start,advanceUntil 等待 <-EndedCh() 关闭 (超时 5s)
   - require.Equal(t, true, <-watcher.EndedCh() 已经关闭); snap := watcher.Snapshot(); require.True(t, snap.Ended); require.Equal(t, "huawei_state_empty", snap.EndedReason); require.True(t, snap.LastHuaWeiStateEmpty)

2. TestActivityWatcher_MeetingEnded_AndAB:
   - logFile = os.CreateTemp("", "ffmpeg.log") + logFile 写入 "[silencedetect @ 0x55a3] silence_start: 0.000\n";logFile Sync;fakeStatFn (覆盖 os.Stat — 通过 watcher 接受 statFn func(string)(int64,error) 字段或直接 Stat 文件未变化 size):文件大小不变 + lastFileGrowthAt 提前 130s
   - cfg.SilenceDurationS=30, FileStallS=120 (默认即可), CheckIntervalS=1 加速
   - 需 silenceEnd 写入:在 silenceSince 已设且 lastFileGrowthAt 远在过去的情况下,先启动 watcher,等 silenceSince 设上,然后注入 silence_end 行
   - **注意**:测试 1 写 silence_start 行 → silenceSince 设上 → 立即 fakeLastFileGrowthAt = 0 (now - 130s) → 推进 clock 至 now+30s → silenceSince 满足条件 + lastFileGrowthAt 满足条件 → closeEnded("both_silence_and_stall")
   - 验证 Snapshot().EndedReason="both_silence_and_stall"

3. TestActivityWatcher_HuaweiDegraded:
   - fakeHuawei 返回 err 连续 3 次 → huaweiDegraded=true;日志中含 "activity_watcher_degraded reason=huawei_api_unreachable"
   - 之后 fakeHuawei 改成返回 confState=""+JoinSum=0,推进 clock 远超 huawei_persist_s → 不应触发 closeEnded (H 路径已禁用);改用 A+B 路径触发 closeEnded,验证 A+B 仍工作

4. TestActivityWatcher_SilenceDegraded:
   - logFile 写入 5 行 "frame= 100 fps=30 q=28.0 size=...\n"
   - cfg.DegradeOnSilenceLoss=true → 6 行后 silenceParseFailures 累计 ≥ 5 → silenceDegraded=true
   - 之后即使写入 silencedetect 行,silenceDegraded=true 时 decisionTicker 不评估 A 路径
   - 验证: H 路径仍可触发 (单独写 H 触发条件)

5. TestActivityWatcher_StatFailed:
   - filePath 指向不存在路径 (或 Stat 始终返 err 的 fake filePath)
   - 连续 3 次 Stat 失败 → closeEnded("file_stat_failed")
   - 验证 Snapshot().EndedReason="file_stat_failed"

6. TestActivityWatcher_Reconnect:
   - 先 logFile 写入 silence_start 让 silenceSince != zero (fakeClock 推进足够时长)
   - 调 watcher.OnReconnect()
   - 验证:SilenceSince == time.Time{};HuaWeiEmptySince / LastFileGrowthAt 不动;HuaWeiDegraded / SilenceDegraded 不动

7. TestActivityWatcher_CloseOnce:
   - fakeHuawei 持续返回 confState="";决策 ticker 触发 closeEnded("huawei_state_empty")
   - 之后手动通过 silenceSince+lastFileGrowthAt 满足 A+B 条件 → decisionTicker 再次评估 → sync.Once 拦下 close
   - 验证:EndedReason 仍为 "huawei_state_empty";EndedCh 已 close (再次读仍 ok)

8. TestActivityWatcher_ExtendStep:
   - cfg.SmartEnd.ExtendStepMin=60;watcher.ExtendStepMin() 返回 60*time.Minute

C. 全部子测 Stop watcher (defer) 释放 goroutine;用 require.Eventually 控制时序避免 race。
  </action>
  <verify>
<automated>go test ./internal/recorder -run 'TestActivityWatcher' -count=1 -v</automated>
</verify>
  <done>
- internal/recorder/activity_watcher_test.go 含 8 个 Test* 函数
- go test ./internal/recorder -run 'TestActivityWatcher' -count=1 输出 PASS 且 0 失败
- fakeHuawei / fakeClock helper 内部使用,不被其他测试依赖
  </done>
</task>

<task type="auto">
  <name>Task 2: coordinator_test.go 扩展 (TestBuildRecordingCommand_SilenceDetect + TestRestartRecording_OnReconnect)</name>
  <files>internal/recorder/coordinator_test.go</files>
  <read_first>
- internal/recorder/coordinator_test.go (现有 buildRecordingCommand 测试模式 line 408-410 区域;现有 RecordingProcess 测试模式)
- internal/recorder/coordinator.go (Plan 24-03 产物:buildRecordingCommand 含 -af silencedetect;restartRecording 含 process.OnReconnect() 调用;RecordingProcess.OnReconnect 字段)
- .planning/phases/24-activitywatcher-silencedetect/24-VALIDATION.md §Per-Task Verification Map 行 51-54 (2 个新 test 命令预期)
  </read_first>
  <behavior>
- TestBuildRecordingCommand_SilenceDetect:cfg.SmartEnd.Enabled=true (默认) → 调用 buildRecordingCommand → args slice 含 "-af" 与 "silencedetect=noise=-30dB:d=30";cfg.SmartEnd.Enabled=false → args slice 不含 "-af"
- TestRestartRecording_OnReconnect:fake RecordingProcess 设置 OnReconnect = func() { callbackCalled++ };调 restartRecording → 验证 callbackCalled >= 1 (同步调用);OnReconnect=nil 时不 panic
  </behavior>
  <action>
编辑 internal/recorder/coordinator_test.go 追加 2 个 Test* 函数:

1. TestBuildRecordingCommand_SilenceDetect:
   - 构造 SimpleRecordingCoordinator with cfg.SmartEnd.Enabled=true,SilenceDB=-30,SilenceDurationS=30
   - 构造最小 RecordingInput (InputSourceUSB with dummy device)
   - c.buildRecordingCommand(input, mkvPath, hlsPath, 60s, 1) → args
   - require.Contains(t, args, "-af")
   - 找到 silencedetect=... 的 arg (含 "silencedetect=noise=") require.Contains(t, silencedetectArg, "noise=-30dB")
   - require.Contains(t, silencedetectArg, "d=30")
   - 二次测:cfg.SmartEnd.Enabled=false → 同一调用 → args 不含 "-af"

2. TestRestartRecording_OnReconnect:
   - 构造 SimpleRecordingCoordinator + cfg with Enabled=true
   - 构造一个最小化 process:&RecordingProcess{ Task: 构造 VideoRecordingTask, OnReconnect: callback, logFile: nil, ConfigType: "stream", ReconnectDelay: 1ms, MaxReconnects: 3, ShouldReconnect: true } (避免 cmd.Start 真实启动 ffmpeg)
   - **简化**:不真实调 restartRecording (会启动 ffmpeg);改测 OnReconnect 调用契约:直接构造 process.OnReconnect = func() { atomic.AddInt32(&callbackCalled, 1) };然后 process.OnReconnect(); require.Equal(t, int32(1), atomic.LoadInt32(&callbackCalled))
   - 二次测:process.OnReconnect=nil;process.OnReconnect() 不 panic (无操作)
   - **或更直接**:只断言 RecordingProcess struct 含 OnReconnect 字段 (reflect 检查) + restartRecording 在 cmd.Start 之前调用 OnReconnect (通过 mock cmd.Start 拦截)— 推荐后者,提供更接近实际行为的测:fakeStartFFmpegProcess 替换 startFFmpegProcess;验证 callback 在 fakeStartFFmpegProcess 调用前已执行

   推荐方案:直接断言 process.OnReconnect() 同步执行;不实际启动 ffmpeg。**测 coordinator 改动契约的方式**是模拟 restartRecording 内部 OnReconnect 调用点位置:在 startFFmpegProcess 之前的逻辑路径上调用 OnReconnect() — 测试只需证明字段存在、回调 nil 安全、回调非 nil 时被调用一次。避开真实 ffmpeg 启动。

3. 两个 Test* 函数不依赖 ffmpeg binary;不依赖真实 TE40;不依赖 SQLite。
  </action>
  <verify>
<automated>go test ./internal/recorder -run 'TestBuildRecordingCommand_SilenceDetect|TestRestartRecording_OnReconnect' -count=1 -v</automated>
</verify>
  <done>
- internal/recorder/coordinator_test.go 追加 TestBuildRecordingCommand_SilenceDetect 与 TestRestartRecording_OnReconnect
- go test 输出 PASS 且 0 失败
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures (fakeHuawei / fakeClock / fake logFile) → ActivityWatcher | trusted test code only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Information Disclosure | test log 含 cfg 字段 | accept | 测试日志仅含 cfg 字段名+值,无敏感凭据 |
| T-24-11 | DoS | test goroutine 泄漏 | mitigate | defer Stop() 释放 ActivityWatcher goroutine;require.Eventually 限时避免死锁 |
</threat_model>

<verification>
- [ ] `go test ./internal/recorder -run 'TestSilenceParser|TestActivityWatcher|TestBuildRecordingCommand|TestRestartRecording' -count=1 -v` 退出 0
- [ ] `go test ./internal/recorder -count=1` 退出 0 (含现有 coordinator_test 408 行)
- [ ] `go test -race ./internal/recorder -count=1` 退出 0 (无 race)
- [ ] `go vet ./internal/recorder` 无告警
</verification>

<success_criteria>
- 8 个 ActivityWatcher scenario + 2 个 coordinator 扩展 = 10 个新 Test* 函数全部 PASS
- Nyquist 合规:VALIDATION.md frontmatter nyquist_compliant 由 false 改 true (此步骤由后续 /gsd:verify-work 流程执行)
- 完整 quick run command ~10s 完成
</success_criteria>

<output>
Create `.planning/phases/24-activitywatcher-silencedetect/24-04-SUMMARY.md` when done
</output>