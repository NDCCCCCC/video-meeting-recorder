---
phase: 13
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [
  "internal/models/input_config_test.go",
  "internal/services/input_config_service_test.go",
  "internal/handlers/input_config_handler_test.go",
  "internal/scheduler/input_config_scheduler_test.go"
]
autonomous: true
requirements: ["D-01", "D-02", "D-04", "D-05", "D-06", "D-12"]
must_haves:
  truths:
    - "Test stubs exist for all InputConfig validation logic"
    - "Test stubs exist for config type mutual exclusion (huawei_auto/usb/stream)"
    - "Test stubs exist for required field validation (at least one recording source)"
    - "Test stubs exist for connection testing dispatch (USB/Stream/Huawei)"
    - "Test stubs exist for scheduler adaptation to new config types"
    - "Test stubs exist for API route handlers"
  artifacts:
    - path: "internal/models/input_config_test.go"
      provides: "InputConfig model validation test stubs"
      contains: "func TestInputConfig_Validate"
    - path: "internal/services/input_config_service_test.go"
      provides: "InputConfigService business logic test stubs"
      contains: "func TestInputConfigService_ValidateConfig"
    - path: "internal/handlers/input_config_handler_test.go"
      provides: "InputConfigHandler API test stubs"
      contains: "func TestInputConfigHandler_CreateConfig"
    - path: "internal/scheduler/input_config_scheduler_test.go"
      provides: "Scheduler integration test stubs"
      contains: "func TestVideoScheduler_InputConfig"
  key_links:
    - from: "internal/models/input_config_test.go"
      to: "internal/models/input_config.go"
      via: "test file imports model package"
      pattern: "import.*github.com/cpic/record_v2/internal/models"
    - from: "internal/services/input_config_service_test.go"
      to: "internal/services/input_config_service.go"
      via: "test file imports services package"
      pattern: "import.*github.com/cpic/record_v2/internal/services"
---

<objective>
Create test infrastructure stubs for Phase 13 - InputConfig refactoring, ensuring Nyquist compliance for all subsequent implementation tasks.

Purpose: Provide test file scaffolding so every implementation task has an existing test file to extend, preventing "MISSING — Wave 0 must create {test_file} first" delays during execution.

Output: Four test files with empty test stubs for all InputConfig functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-down/workflows/execute-plan.md
@$HOME/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-usb/13-CONTEXT.md
@.planning/phases/13-usb/13-RESEARCH.md
@.planning/phases/13-usb/13-PATTERNS.md
@internal/models/huawei_config.go
@internal/services/huawei_config_service.go
@internal/handlers/huawei_config_handler.go
@internal/scheduler/video_scheduler.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InputConfig model test stubs</name>
  <files>internal/models/input_config_test.go</files>
  <read_first>
    - internal/models/huawei_config.go (to understand existing validation patterns)
    - .planning/phases/13-usb/13-CONTEXT.md (D-01, D-02, D-04 decision details)
  </read_first>
  <action>
    Create test file `internal/models/input_config_test.go` with empty test stubs for:
    1. `TestInputConfig_Validate_HuaweiAuto_WithHuaweiEnabled` - validates Huawei fields when enabled
    2. `TestInputConfig_Validate_HuaweiAuto_WithoutHuawei` - requires USB or Stream when Huawei disabled
    3. `TestInputConfig_Validate_USB_RequiresCameraDevice` - validates USB camera requirement
    4. `TestInputConfig_Validate_Stream_RequiresURL` - validates stream URL requirement
    5. `TestInputConfig_Validate_InvalidConfigType` - validates config_type enum
    6. `TestInputConfig_Lock_Unlock` - tests locking mechanism
    7. `TestInputConfig_IsCameraBound_IsAudioBound` - tests device binding status checks

    Use pattern from `internal/models/huawei_config.go` validation (lines 99-123) and locking (lines 74-92).
    Each test should have `t.Run()` for setup but no implementation yet (use `t.Skip("not implemented")`).
  </action>
  <verify>
    <automated>go test ./internal/models -run TestInputConfig -v 2>&1 | grep -E "(SKIP|not implemented)" | wc -l</automated>
  </verify>
  <done>
    - File exists at `internal/models/input_config_test.go`
    - Contains 7 test functions with `t.Skip("not implemented")`
    - File compiles with `go test ./internal/models -c`
  </done>
</task>

<task type="auto">
  <name>Task 2: Create InputConfigService test stubs</name>
  <files>internal/services/input_config_service_test.go</files>
  <read_first>
    - internal/services/huawei_config_service.go (to understand service patterns, lines 17-31 for structure, 154-206 for CreateConfig)
    - .planning/phases/13-usb/13-CONTEXT.md (D-04, D-05 decision details)
  </read_first>
  <action>
    Create test file `internal/services/input_config_service_test.go` with empty test stubs for:
    1. `TestInputConfigService_ValidateConfig_HuaweiAuto` - validates Huawei config type
    2. `TestInputConfigService_ValidateConfig_USB` - validates USB config type
    3. `TestInputConfigService_ValidateConfig_Stream` - validates stream config type
    4. `TestInputConfigService_ValidateConfig_NoRecordingSource` - validates at least one source required
    5. `TestInputConfigService_TestConnection_USB` - tests USB device connection
    6. `TestInputConfigService_TestConnection_Stream` - tests stream URL connection using FFprobe
    7. `TestInputConfigService_TestConnection_Huawei` - tests Huawei terminal connection
    8. `TestInputConfigService_CreateConfig` - tests config creation with validation
    9. `TestInputConfigService_UpdateConfig` - tests config update
    10. `TestInputConfigService_ListConfigs` - tests config listing with filters

    Use pattern from `internal/services/huawei_config_service.go` (lines 415-537 for stream testing).
    Mock `*gorm.DB` and `*zap.Logger` - no real database calls.
  </action>
  <verify>
    <automated>go test ./internal/services -run TestInputConfigService -v 2>&1 | grep -E "(SKIP|not implemented)" | wc -l</automated>
  </verify>
  <done>
    - File exists at `internal/services/input_config_service_test.go`
    - Contains 10 test functions with `t.Skip("not implemented")`
    - File compiles with `go test ./internal/services -c`
  </done>
</task>

<task type="auto">
  <name>Task 3: Create InputConfigHandler test stubs</name>
  <files>internal/handlers/input_config_handler_test.go</files>
  <read_first>
    - internal/handlers/huawei_config_handler.go (to understand handler patterns, lines 33-67 for ListConfigs, 93-118 for CreateConfig)
    - .planning/phases/13-usb/13-CONTEXT.md (D-12 decision details)
  </read_first>
  <action>
    Create test file `internal/handlers/input_config_handler_test.go` with empty test stubs for:
    1. `TestInputConfigHandler_ListConfigs` - tests GET /api/input-configs with pagination and filters
    2. `TestInputConfigHandler_GetConfig` - tests GET /api/input-configs/:id
    3. `TestInputConfigHandler_CreateConfig` - tests POST /api/input-configs with validation
    4. `TestInputConfigHandler_UpdateConfig` - tests PUT /api/input-configs/:id
    5. `TestInputConfigHandler_DeleteConfig` - tests DELETE /api/input-configs/:id
    6. `TestInputConfigHandler_TestConnection` - tests POST /api/input-configs/:id/test
    7. `TestInputConfigHandler_ScanUSBDevices` - tests GET /api/input-configs/usb-devices

    Use pattern from `internal/handlers/huawei_config_handler.go` (lines 33-67 for list handler structure).
    Mock `*services.InputConfigService`, `*services.USBDeviceScanner`, `*zap.Logger`.
    Use `gin-test` or `httptest` for HTTP request/response testing.
  </action>
  <verify>
    <automated>go test ./internal/handlers -run TestInputConfigHandler -v 2>&1 | grep -E "(SKIP|not implemented)" | wc -l</automated>
  </verify>
  <done>
    - File exists at `internal/handlers/input_config_handler_test.go`
    - Contains 7 test functions with `t.Skip("not implemented")`
    - File compiles with `go test ./internal/handlers -c`
  </done>
</task>

<task type="auto">
  <name>Task 4: Create scheduler integration test stubs</name>
  <files>internal/scheduler/input_config_scheduler_test.go</files>
  <read_first>
    - internal/scheduler/video_scheduler.go (to understand scheduler patterns)
    - .planning/phases/13-usb/13-CONTEXT.md (D-06 decision details)
  </read_first>
  <action>
    Create test file `internal/scheduler/input_config_scheduler_test.go` with empty test stubs for:
    1. `TestVideoScheduler_StartRecordingWithConfig_HuaweiAuto` - tests Huawei terminal control before recording
    2. `TestVideoScheduler_StartRecordingWithConfig_USB` - tests direct USB recording start
    3. `TestVideoScheduler_StartRecordingWithConfig_Stream` - tests direct stream recording start
    4. `TestVideoScheduler_ValidateTask_InputConfig` - tests task validation with InputConfig instead of HuaweiConfig

    Use pattern from RESEARCH.md (lines 327-348) for config type dispatch logic.
    Mock `*models.InputConfig`, `*services.InputConfigService`, `*gorm.DB`.
    No actual recording operations - just validate the dispatch logic.
  </action>
  <verify>
    <automated>go test ./internal/scheduler -run TestVideoScheduler_InputConfig -v 2>&1 | grep -E "(SKIP|not implemented)" | wc -l</automated>
  </verify>
  <done>
    - File exists at `internal/scheduler/input_config_scheduler_test.go`
    - Contains 4 test functions with `t.Skip("not implemented")`
    - File compiles with `go test ./internal/scheduler -c`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input crosses API boundary (config_type, stream_url, device paths) |
| API→Service | Validated input to business logic |
| Service→DB | Parameterized queries via GORM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Spoofing | config_type validation | mitigate | Server-side validation of enum values (huawei_auto/usb/stream), reject unknown types |
| T-13-02 | Tampering | stream_url injection | mitigate | GORM parameterized queries, URL validation in service layer |
| T-13-03 | Information Disclosure | password fields in API response | mitigate | Use `json:"-"` tag on Password fields (per D-01, existing pattern) |
| T-13-04 | Tampering | USB device path injection | mitigate | Validate device paths against scanned device list, whitelist allowed backends |
| T-13-05 | Denial of Service | FFprobe timeout | mitigate | Context with 15-second timeout (existing pattern in service line 287) |
| T-13-06 | Elevation | Admin-only access | accept | Existing RBAC system enforces admin-only access to input configs (ASVS V4) |
</threat_model>

<verification>
- All test files compile without errors: `go test ./internal/models ./internal/services ./internal/handlers ./internal/scheduler -c`
- All tests show "SKIP" status (not implemented yet)
- Test count matches expected: 7 + 10 + 7 + 4 = 28 test stubs
- No test files are MISSING for subsequent implementation plans
</verification>

<success_criteria>
- 4 test files created with 28 total test stubs
- All stubs use `t.Skip("not implemented")` pattern
- `go test` compiles successfully for all packages
- Nyquist compliance achieved: no subsequent plan will block due to missing test files
</success_criteria>

<output>
After completion, create `.planning/phases/13-usb/13-00-SUMMARY.md`
</output>
