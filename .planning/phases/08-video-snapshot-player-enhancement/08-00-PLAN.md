---
phase: 08-video-snapshot-player-enhancement
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/services/snapshot_service_test.go
  - frontend/src/hooks/__tests__/useKeyboardShortcuts.test.ts
  - frontend/src/hooks/__tests__/useVideoFrameNavigation.test.ts
  - frontend/src/components/__tests__/VideoPlayerModal.test.tsx
autonomous: true
requirements:
  - SNAPSHOT-01
  - SNAPSHOT-02
  - PLAYER-01
  - PLAYER-02
  - PLAYER-03
  - EDGE-01
  - EDGE-02
user_setup: []
must_haves:
  truths:
    - "All test stubs compile with t.Skip() and integrate cleanly with existing test infrastructure"
    - "Test stubs establish executable specifications for Phase 8 features"
    - "No regressions in existing test suite - all tests pass"
  artifacts:
    - path: internal/services/snapshot_service_test.go
      provides: "Test stubs for snapshot service enhancements"
      contains: "TestGenerateSnapshot_Concurrent, TestGenerateSnapshot_Naming, TestGenerateSnapshot_TimeRange, TestGenerateSnapshot_Interrupted"
    - path: frontend/src/hooks/__tests__/useKeyboardShortcuts.test.ts
      provides: "Test stubs for keyboard shortcuts hook"
      contains: "describes keyboard shortcut behavior test suite"
    - path: frontend/src/hooks/__tests__/useVideoFrameNavigation.test.ts
      provides: "Test stubs for frame navigation hook"
      contains: "describes frame navigation behavior test suite"
    - path: frontend/src/components/__tests__/VideoPlayerModal.test.tsx
      provides: "Test stubs for video player enhancements"
      contains: "describes frame navigation and keyboard integration tests"
  key_links:
    - from: "snapshot_service_test.go"
      to: "models.VideoFile"
      via: "GORM model and SnapshotOffset field"
      pattern: "snapshot_offset.*duration.*incremental"
---

# Phase 08: Plan 00 - Wave 0 Test Stubs

<objective>
Create test stub specifications for Phase 8 video snapshot and player enhancements following Nyquist validation contract.

Purpose: Establish executable specifications before implementation to ensure test coverage for all requirements.
Output: Test stub files with t.Skip() for concurrent snapshot handling, naming conventions, frame navigation, keyboard shortcuts, and edge cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-video-snapshot-player-enhancement/08-RESEARCH.md
@.planning/phases/08-video-snapshot-player-enhancement/08-PATTERNS.md
@.planning/phases/08-video-snapshot-player-enhancement/08-VALIDATION.md
@internal/services/snapshot_service.go
@internal/models/video_file.go
@frontend/src/components/VideoPlayerModal.tsx
@internal/services/split_handler_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Go test stubs for SnapshotService enhancements</name>
  <files>internal/services/snapshot_service_test.go</files>
  <read_first>
    - internal/services/snapshot_service.go (lines 17-77 for service struct, constructor, and incremental logic)
    - internal/models/video_file.go (for VideoFile model with SnapshotOffset field)
    - internal/services/split_handler_test.go (lines 1-30 for test pattern reference)
  </read_first>
  <action>
Create or extend `internal/services/snapshot_service_test.go` with test stubs:

1. Add test for concurrent snapshot protection (EDGE-01):
```go
func TestGenerateSnapshot_Concurrent(t *testing.T) {
    t.Skip("TODO: Implement concurrent snapshot mutex protection")
    // Setup: Create test database, service, and recording task
    // Action: Launch 5 concurrent goroutines calling GenerateSnapshot for same task
    // Assert: All snapshots complete sequentially with sequential offsets (no duplicates)
}
```

2. Add test for enhanced naming convention (SNAPSHOT-02):
```go
func TestGenerateSnapshot_Naming(t *testing.T) {
    t.Skip("TODO: Implement enhanced snapshot naming with task context")
    // Setup: Create task with name containing special chars "测试录制/2024-04-20"
    // Action: Generate snapshot
    // Assert: Filename sanitized to "测试录制_2024-04-20_snapshot_001_20260420_150405.mp4"
    // Assert: No path traversal characters (.., /, \) in filename
}
```

3. Add test for time range validation (EDGE-01, EDGE-02):
```go
func TestGenerateSnapshot_TimeRange(t *testing.T) {
    t.Skip("TODO: Implement time range validation")
    // Setup: Create recording task with 60s duration
    // Action 1: Generate snapshot with offset > recording duration
    // Assert 1: Returns error "快照偏移量超过录制时长"
    // Action 2: Generate snapshot with negative duration
    // Assert 2: Returns error "录制时长无效"
}
```

4. Add test for recording interruption handling (EDGE-02):
```go
func TestGenerateSnapshot_Interrupted(t *testing.T) {
    t.Skip("TODO: Implement graceful handling of recording interruption")
    // Setup: Create recording task, start snapshot generation
    // Action: Simulate recording stop during snapshot (set status to stopped)
    // Assert: Snapshot fails gracefully with error "录制已停止"
}
```

5. Add test for incremental offset calculation (SNAPSHOT-01):
```go
func TestGenerateSnapshot_IncrementalOffset(t *testing.T) {
    t.Skip("TODO: Verify incremental snapshot offset calculation")
    // Setup: Create task with 2 existing snapshots (offset 0-10, offset 10-20)
    // Action: Generate third snapshot
    // Assert: Third snapshot starts at offset 20
    // Assert: SnapshotOffset field set correctly in database
}
```
  </action>
  <verify>
    <automated>go test ./internal/services/... -run TestGenerateSnapshot -v | grep "SKIP:"</automated>
  </verify>
  <done>
    - Test stub file created with 5 test functions
    - All tests compile with t.Skip()
    - No compilation errors in test file
    - Test names follow Test{Service}_{Method} pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: Create frontend test stubs for keyboard shortcuts and frame navigation</name>
  <files>
    frontend/src/hooks/__tests__/useKeyboardShortcuts.test.ts
    frontend/src/hooks/__tests__/useVideoFrameNavigation.test.ts
    frontend/src/components/__tests__/VideoPlayerModal.test.tsx
  </files>
  <read_first>
    - frontend/src/components/VideoPlayerModal.tsx (lines 1-200 for component structure and state)
    - frontend/src/pages/files/__tests__/TranscriptionDropdown.test.ts (for test pattern reference)
  </read_first>
  <action>
Create three test stub files for frontend enhancements:

1. Create `frontend/src/hooks/__tests__/useKeyboardShortcuts.test.ts`:
```typescript
import { renderHook, act } from '@testing-library/react'

describe('useKeyboardShortcuts', () => {
  it('should initialize without errors', () => {
    // TODO: Test hook initializes correctly
    expect(true).toBe(true)
  })

  it('should handle Space key for play/pause', () => {
    // TODO: Test Space key triggers play/pause callback
  })

  it('should handle Arrow keys for seeking', () => {
    // TODO: Test ArrowLeft/ArrowRight triggers seek callback
  })

  it('should ignore keyboard events when disabled', () => {
    // TODO: Test keyboard shortcuts ignored when enabled=false
  })

  it('should ignore events from input elements', () => {
    // TODO: Test shortcuts don't trigger when typing in input
  })
})
```

2. Create `frontend/src/hooks/__tests__/useVideoFrameNavigation.test.ts`:
```typescript
import { renderHook } from '@testing-library/react'

describe('useVideoFrameNavigation', () => {
  it('should initialize with frame navigation functions', () => {
    // TODO: Test hook returns nextFrame, prevFrame, supportsFrameCallback
  })

  it('should advance video by one frame (1/30 second)', () => {
    // TODO: Test nextFrame increments currentTime by ~0.033s
  })

  it('should rewind video by one frame', () => {
    // TODO: Test prevFrame decrements currentTime by ~0.033s
  })

  it('should detect requestVideoFrameCallback support', () => {
    // TODO: Test supportsFrameCallback returns true in supported browsers
  })
})
```

3. Extend or create `frontend/src/components/__tests__/VideoPlayerModal.test.tsx`:
```typescript
import { render, screen } from '@testing-library/react'
import { VideoPlayerModal } from '../VideoPlayerModal'

describe('VideoPlayerModal frame navigation', () => {
  it('should render frame navigation buttons when supported', () => {
    // TODO: Test frame navigation buttons render in supported browsers
  })

  it('should not render frame navigation when unsupported', () => {
    // TODO: Test frame navigation hidden in unsupported browsers
  })

  it('should integrate with useKeyboardShortcuts hook', () => {
    // TODO: Test keyboard shortcuts attached when modal opens
    // TODO: Test keyboard shortcuts removed when modal closes
  })
})
```
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="useKeyboardShortcuts|useVideoFrameNavigation|VideoPlayerModal" --passWithNoTests 2>&1 | grep -E "(PASS|FAIL|SKIP)"</automated>
  </verify>
  <done>
    - Three test stub files created
    - All test suites compile without errors
    - Test names describe intended behavior clearly
    - Follows existing frontend test patterns
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted client requests snapshot generation |
| API→FFmpeg | FFmpeg command execution with user-influenced parameters |
| client→browser | Keyboard events from untrusted user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering | SnapshotService concurrent requests | mitigate | Mutex per task serializes snapshot requests (test: TestGenerateSnapshot_Concurrent) |
| T-8-02 | Tampering | Snapshot filename generation | mitigate | Sanitize task name, validate no path traversal chars (test: TestGenerateSnapshot_Naming) |
| T-8-03 | Tampering | FFmpeg command injection | mitigate | Use exec.Command with arg array, never shell strings (existing pattern) |
| T-8-04 | Denial of Service | Recording interruption during snapshot | mitigate | Validate task status before snapshot, handle partial files (test: TestGenerateSnapshot_Interrupted) |
| T-8-05 | Spoofing | Keyboard shortcut XSS | accept | React escapes JSX by default, no dangerouslySetInnerHTML in VideoPlayerModal |
</threat_model>

<verification>
- All test stubs compile without errors
- Test stubs cover all Phase 8 requirements (SNAPSHOT-01, SNAPSHOT-02, PLAYER-01, PLAYER-02, PLAYER-03, EDGE-01, EDGE-02)
- No regressions in existing test suite
- Test infrastructure ready for implementation plans
</verification>

<success_criteria>
- [ ] 5 Go test stubs created in snapshot_service_test.go
- [ ] 3 frontend test stub files created
- [ ] All test stubs compile with t.Skip()
- [ ] Existing test suite passes without regressions
- [ ] Test stubs establish clear specifications for implementation
</success_criteria>

<output>
After completion, create `.planning/phases/08-video-snapshot-player-enhancement/08-00-SUMMARY.md`
</output>
