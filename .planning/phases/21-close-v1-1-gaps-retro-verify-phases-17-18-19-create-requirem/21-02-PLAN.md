---
phase: 21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-credential-static-encryption-sec-003b/.gitkeep
  - .planning/phases/18-credential-static-encryption-sec-003b/18-SUMMARY.md
  - .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md
autonomous: true
requirements: [P21-R2]
tags: [retro-verify, phase-18, verification, sec-003b, docs, dir-reconstruction]

must_haves:
  truths:
    - ".planning/phases/18-credential-static-encryption-sec-003b/ 目录存在 (重建)"
    - "18-SUMMARY.md 副本入目录 (root 18-SUMMARY.md 原版保持不动 per D-02.6)"
    - "18-VERIFICATION.md 含 frontmatter status: passed + score 11/11 must-haves"
    - "18-VERIFICATION.md 引用 9 个 commit hash (W1a..W4d, body 为准 per Pitfall 2)"
    - "18-VERIFICATION.md 显式标注 5d536ec 为预存证据 (非 phase 21 交付物 per Pitfall 6)"
    - "Evidence Limitations 段标注 SUMMARY frontmatter (bd84fe2) vs body (0c018f2) HEAD 不一致 (body 为准)"
  artifacts:
    - path: .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md
      provides: Phase 18 retro-verify VERIFICATION report
      contains: "status: passed"
      min_lines: 100
    - path: .planning/phases/18-credential-static-encryption-sec-003b/18-SUMMARY.md
      provides: Phase 18 SUMMARY 副本 (内容同 root 18-SUMMARY.md)
      contains: "SM4-GCM"
  key_links:
    - from: 18-VERIFICATION.md D18-5 (encrypt-on-write/decrypt-on-read)
      to: live code internal/services/input_config_service.go EncryptGCM 调用
      via: grep `EncryptGCM` in input_config_service.go
      pattern: "EncryptGCM"
    - from: 18-VERIFICATION.md D18-7 (production decrypt invariant)
      to: commit 5d536ec (TestHuaweiDBAdapter_ProductionDecrypts)
      via: git show 5d536ec --stat
      pattern: "5d536ec"
---

<objective>
为 phase 18 (凭据静态加密 + 密钥轮换 SEC-003b) 重建 phase 目录 + 写 VERIFICATION.md，关闭 v1.1-MILESTONE-AUDIT.md 标记的 phase-18 missing-directory blocker。

Purpose: phase 18 目录被早期清理/从未建 (.planning/phases/18-* 不存在), 但交付物全部落地 main. 重建最小目录 + 复制 root SUMMARY + 写 VERIFICATION.md, 让 phase 目录自洽, 同时不破坏 git 历史中的 root 原版.
Output: `.planning/phases/18-credential-static-encryption-sec-003b/` 目录 (含 .gitkeep + 18-SUMMARY.md 副本 + 18-VERIFICATION.md)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem/21-CONTEXT.md
@.planning/phases/21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem/21-RESEARCH.md
@18-SUMMARY.md
@.planning/phases/20-handleerror-classify-convergence/20-VERIFICATION.md

<interfaces>
<!-- 20-VERIFICATION.md 是 gold reference (10/10 passed), 镜像其章节结构 (与 21-01 同) -->

Phase 18 must-haves (派生自 ROADMAP Progress table + 18-SUMMARY + RESEARCH §3, 编号 D18-1..D18-11):
| # | Must-Have | Live Code Evidence | Git Evidence |
|---|-----------|--------------------|--------------|
| D18-1 | SM4-GCM envelope 格式 `SM4:<version>:<base64(nonce_12B|ciphertext|tag_16B)>` | internal/utils/sm4_password.go:19 注释 + EncryptGCM/DecodeCredentialEnvelope/ParseCredentialEnvelope 导出 | e6315ce (W1a) |
| D18-2 | PKCS#7 padding (gmsm v1.4.1 GCMDecrypt 块对齐要求) | internal/utils/sm4_password.go PKCS7 实现注释 | e6315ce |
| D18-3 | 密钥族隔离 (CREDENTIAL_SM4_* vs SM4_SECRET vs HLS_TOKEN_SECRET) | internal/config/config.go AuthConfig.CredentialSM4* 字段 + ValidateCredentialSM4Config | 1dbb3b0 (W1b) |
| D18-4 | CredentialEncryptor service (encrypt/decrypt/rotate/invariant scan) | internal/services/credential_encryptor.go 存在 + 导出方法 | edaa4ae (W1c) |
| D18-5 | encrypt-on-write / decrypt-on-read 接入业务层 | input_config_service.go 注入 encryptor + config_service.go 删除 base64-stub 死代码 | 558f723 (W2) |
| D18-6 | fail-closed 启动期 10 步扫描 | cmd/server/app.go Initialize() 内 MigratePlaintextToGCM + 双 InvariantScan + RotateIfNeeded | bd84fe2 (W3) |
| D18-7 | 生产解密不变式测试 (huaweiDBAdapter 必须解密 SM4 信封) | cmd/server/app_test.go:53 TestHuaweiDBAdapter_ProductionDecrypts | 5d536ec (post-audit, 2026-08-02) |
| D18-8 | operator 轮换手册 + 物理残留章节 | DEPLOYMENT.md 两章节 | 0c018f2 (W4d) |
| D18-9 | 重复轮换测试 (v1→v2→v3) | internal/services/credential_encryptor_test.go::TestRepeatedRotation_V1ToV2ToV3 | 3822497 (W4b) |
| D18-10 | per-site/version 计数日志 (after_migrate/after_rotate/after_invariant 三阶段) | credential_encryptor.go::CountByVersion/LogVersionCounts + app.go::Initialize 三处调用 | 8796ca3 (W4a) |
| D18-11 | SEC-003b marker comment 更新 (deferred→done) | internal/huawei/manager.go:132-134 注释 "SEC-003b/Phase 21: ... 此层视为明文边界" | 5d536ec |

Phase 18 commit hash (按 wave, body 为准 per Pitfall 2 — frontmatter 标 bd84fe2 是 W3 终点, body 实际延伸到 0c018f2 是 W4d):
| Wave | Commit | 范围 |
|------|--------|------|
| W1a | e6315ce | SM4-GCM envelope + PKCS7 padding + tamper 检测 |
| W1b | 1dbb3b0 | AuthConfig.CredentialSM4* + BindEnv + ValidateCredentialSM4Config |
| W1c | edaa4ae | CredentialEncryptor service + 列宽扩 + 集成测试 |
| W2  | 558f723 | encrypt-on-write + decrypt-on-read + admin 端点修复 + 删除 base64 stubs 死代码 |
| W3  | bd84fe2 | fail-closed 启动 + 列宽扩展 + 集成测试 + 文档 |
| W4a | 8796ca3 | per-site/version 计数日志 |
| W4b | 3822497 | 重复轮换测试 (v1→v2→v3) |
| W4c | a182cd6 | (W4 子步, 详见 18-SUMMARY) |
| W4d | 0c018f2 | DEPLOYMENT.md operator 手册 + 物理残留章节 |
| 后续 | 5d536ec | SEC-003b marker comment + TestHuaweiDBAdapter_ProductionDecrypts (post-audit, 2026-08-02) |

关键时间线 (per Pitfall 6):
- 5d536ec 是 2026-08-02 commit, 早于 phase 21 启动 (2026-08-03)
- commit message 提 "SEC-003b/Phase 21: done" 但实际是 phase 21 启动前的预存证据
- phase 21 不改业务代码, 此 commit 作为 phase 18 retro-verify 的强证据使用, 不是 phase 21 的交付物

Phase 18 关键技术细节 (18-SUMMARY §"与计划差异", 在 VERIFICATION 里要诚实反映):
1. gmsm v1.4.1 nonce mutation: GCM 内部修改 nonce slice backing array → 实现层做 defensive copy
2. PKCS#7 padding 必需: gmsm v1.4.1 GCMDecrypt 仅支持块对齐密文
3. No `migrations/017_*` file: 列宽扩展由 cmd/server/app.go::widenPasswordColumns() 在 Initialize() 内直接执行 (dormant registry 不触发)
4. 每轮旋转 5 envelopes (非 4): Live3.stream_password 首次 Migrate 时就是 v1, 也被覆盖
5. GORM .Scan(&rows) 不支持 → db.Raw(...).Rows() + manual scan workaround (在 CountByVersion 中)

Phase 18 Evidence Limitations (retro-verify 必加):
- 原 PLAN.md / CONTEXT.md / DISCUSSION-LOG 永久丢失 (从未 git 入库)
- 18-SUMMARY frontmatter `Final HEAD: bd84fe2` (W3 终点) 与 body 实际终点 `0c018f2` (W4d) 不一致 — body 为准
- 真实生产数据 post-audit 未做 (18-SUMMARY 列为 out-of-scope)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重建 phase 18 目录 + 复制 SUMMARY + 写 18-VERIFICATION.md (status: passed, 11/11 must-haves)</name>
  <files>.planning/phases/18-credential-static-encryption-sec-003b/.gitkeep, .planning/phases/18-credential-static-encryption-sec-003b/18-SUMMARY.md, .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md</files>

  <read_first>
  必读:
  - `18-SUMMARY.md` (root, 21.7 KB — phase 18 唯一完整历史记录, 将被复制进新目录; 注意 frontmatter `Final HEAD: bd84fe2` 与 body §Wave 4 `0c018f2` 不一致, body 为准 per Pitfall 2)
  - `.planning/STATE.md` §Phase 18 (line 52-78) + §"Phase 18 Base HEAD" 记录 (line 64-72)
  - `.planning/ROADMAP.md` Phase 18 行 (Progress table + `**Goal:**`)
  - `.planning/phases/21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem/21-RESEARCH.md` §1 (格式契约) + §3 (Phase 18 证据点 + commit hash 表) + §Pitfall 2/6
  - `.planning/phases/20-handleerror-classify-convergence/20-VERIFICATION.md` (GOLD REFERENCE)
  验证用 (按需 grep):
  - `internal/utils/sm4_password.go` (grep EncryptGCM/DecodeCredentialEnvelope/ParseCredentialEnvelope)
  - `internal/services/credential_encryptor.go` (grep CountByVersion/LogVersionCounts/MigratePlaintextToGCM/InvariantScan/RotateIfNeeded)
  - `internal/services/input_config_service.go` (grep CredentialEncryptor 注入 + EncryptGCM 调用 — L3 wiring 证据)
  - `internal/services/config_service.go` (grep 删除 base64-stub 死代码痕迹)
  - `internal/config/config.go` (grep CredentialSM4* 字段 + ValidateCredentialSM4Config)
  - `cmd/server/app.go` (grep MigratePlaintextToGCM/InvariantScan/RotateIfNeeded/widenPasswordColumns/CountByVersion)
  - `cmd/server/app_test.go` (grep TestHuaweiDBAdapter_ProductionDecrypts)
  - `internal/huawei/manager.go:132-134` (SEC-003b marker comment)
  - `DEPLOYMENT.md` (grep 凭据密钥配置与轮换 + 物理残留章节)
  预校验 commit hash (避免引用错误 hash):
  - `git log --oneline e6315ce 1dbb3b0 edaa4ae 558f723 bd84fe2 8796ca3 3822497 a182cd6 0c018f2 5d536ec`
  </read_first>

  <action>
  **A. 重建目录结构**:
  - 创建目录 `.planning/phases/18-credential-static-encryption-sec-003b/` (slug per CONTEXT D-02.4 discretion, 匹配 .planning/phases/NN-<slug>/ 既有约定)
  - 创建空文件 `.gitkeep` 占位
  - 复制 `18-SUMMARY.md` (root) → `.planning/phases/18-credential-static-encryption-sec-003b/18-SUMMARY.md` (保持文件名不变; root 原版保持不动 per CONTEXT D-02.6)

  **B. 写 `.planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md`** (镜像 20-VERIFICATION.md 结构 + 加 retro 字段):

  **B.1 frontmatter**:
  - `phase: 18-credential-static-encryption-sec-003b`
  - `verified: 2026-08-03T00:00:00Z`
  - `status: passed`
  - `score: 11/11 must-haves verified`
  - `method: retro-active goal-backward (SUMMARY + git history + live code)` (retro-verify 必加)
  - `overrides_applied: 0`
  - `gaps: []`
  - `deferred:` 列 18-SUMMARY §"不在本 phase 范围" 项 (KMS/Vault 集成 / staging 真实数据 post-audit / FileShare.Password 加密 / StreamURL URL-embedded 凭据拒绝 / dormant migration registry 清理 / 前端 transport GCM 迁移)
  - `human_verification: []`
  - `evidence_limitations:` 列 3 条 (原 PLAN/CONTEXT/DISCUSSION-LOG 永久丢失 / SUMMARY frontmatter vs body HEAD 不一致 body 为准 / 真实生产数据 post-audit 未做)

  **B.2 文档头**:
  - H1: `# Phase 18: 凭据静态加密 + 密钥轮换 (SEC-003b) - Verification Report`
  - `**Phase Goal:**` 拷贝 ROADMAP.md Phase 18 Goal (line 53 区域: "凭据静态加密 + 密钥轮换 (SEC-003b)")
  - 段落说明: "本 VERIFICATION 于 phase 21 retro-verify 时重建. phase 18 目录被早期清理, 本目录由 phase 21 重建 (复制 root 18-SUMMARY.md + 新写 VERIFICATION.md), root 18-SUMMARY.md 原版保持不动 (per CONTEXT D-02.6)."

  **B.3 `## Goal Achievement` 章节**:

  **`### Observable Truths`** 表格 11 行 (D18-1..D18-11), 列 `| # | Truth | Status | Evidence |`:
  每行 Evidence 列含 L1+L2+L3 三层证据 (per Pitfall 5, 不能只给"文件存在"):
  - D18-1 SM4-GCM envelope → VERIFIED → L1 sm4_password.go 存在; L2 EncryptGCM/DecodeCredentialEnvelope/ParseCredentialEnvelope 导出; L3 input_config_service.go 调用 EncryptGCM (grep `EncryptGCM` 命中); commit e6315ce
  - D18-2 PKCS#7 padding → VERIFIED → sm4_password.go 实现注释; commit e6315ce
  - D18-3 密钥族隔离 → VERIFIED → config.go CredentialSM4* 字段独立 + ValidateCredentialSM4Config 与 SM4_SECRET/HLS_TOKEN_SECRET 分离; commit 1dbb3b0
  - D18-4 CredentialEncryptor service → VERIFIED → L1 services/credential_encryptor.go 存在; L2 Encrypt/Decrypt/Rotate/Migrate/InvariantScan 方法导出; L3 cmd/server/app.go::Initialize 装配 + input_config_service.go 注入; commit edaa4ae
  - D18-5 encrypt-on-write/decrypt-on-read → VERIFIED → input_config_service.go EncryptGCM 调用 (grep); config_service.go base64-stub 死代码已删 (grep 无残留); commit 558f723
  - D18-6 fail-closed 10 步启动扫描 → VERIFIED → app.go::Initialize 内 MigratePlaintextToGCM + 双 InvariantScan + RotateIfNeeded 三段调用 (grep 三处命中); commit bd84fe2
  - D18-7 生产解密不变式测试 → VERIFIED → cmd/server/app_test.go:53 TestHuaweiDBAdapter_ProductionDecrypts (61 行); commit 5d536ec — **显式标注: 此 commit 为 2026-08-02 (phase 21 启动前), 是预存证据 NOT phase 21 交付物 (per Pitfall 6)**
  - D18-8 operator 轮换手册 → VERIFIED → DEPLOYMENT.md 两章节 (凭据密钥配置与轮换 / 凭据存储的物理残留); commit 0c018f2 (W4d)
  - D18-9 重复轮换测试 (v1→v2→v3) → VERIFIED → credential_encryptor_test.go::TestRepeatedRotation_V1ToV2ToV3; commit 3822497 (W4b)
  - D18-10 per-site/version 计数日志 → VERIFIED → credential_encryptor.go::CountByVersion/LogVersionCounts + app.go::Initialize 三处调用 (after_migrate/after_rotate/after_invariant); commit 8796ca3 (W4a)
  - D18-11 SEC-003b marker comment → VERIFIED → internal/huawei/manager.go:132-134 注释从 "SEC-003b deferred" 改为 "SEC-003b/Phase 21: done"; commit 5d536ec — **同 D18-7 标注: 预存证据 NOT phase 21 交付物**

  **`### Required Artifacts`** 表格 (列 `| Artifact | Expected | Status | Details |`): 列 8-10 个核心文件 (sm4_password.go / credential_encryptor.go / credential_encryptor_test.go / app_test.go / config.go / app.go / input_config_service.go / DEPLOYMENT.md / huawei/manager.go), 每个 L1+L2+L3 证据

  **`### Key Link Verification`** 表格 (列 `| From | To | Via | Status | Details |`):
  - CredentialEncryptor.EncryptGCM ← input_config_service.go 写入路径 → VERIFIED
  - CredentialEncryptor.DecryptGCM ← huaweiDBAdapter 读取路径 → VERIFIED (manager.go 注释 "此层视为明文边界")
  - ValidateCredentialSM4Config ← config.Load() production 分支 → VERIFIED
  - MigratePlaintextToGCM + InvariantScan ← app.go::Initialize → VERIFIED
  - TestHuaweiDBAdapter_ProductionDecrypts ← go test 运行 → VERIFIED

  **`### Behavioral Spot-Checks`** 表格 (命令 < 30s):
  - `go build ./...` → 0 errors → VERIFIED
  - `go test -race ./internal/services/... ./cmd/server/...` → PASS (含 TestHuaweiDBAdapter_ProductionDecrypts + TestRepeatedRotation_V1ToV2ToV3) → VERIFIED
  - `grep -c 'EncryptGCM' internal/services/input_config_service.go` → ≥1 (L3 wiring) → VERIFIED
  - `git show 5d536ec --stat` → 列出 cmd/server/app_test.go + internal/huawei/manager.go → VERIFIED (证明预存证据存在)
  - `git show 0c018f2 --stat` → 列出 DEPLOYMENT.md → VERIFIED (证明 W4d 真实存在, body 为准 per Pitfall 2)

  **B.4 `## Requirements Coverage`** 章节: 列 REQ-18-001..005 (将在 phase 21-04 REQUIREMENTS.md 里登记), 验证证据列引用本 VERIFICATION 的 D18-N 条款

  **B.5 `## Anti-Patterns Found`**: INFO 级 (例: 18-SUMMARY §"与计划差异" 列的 5 个 deviations — gmsm nonce mutation / PKCS#7 必需 / no migrations file / 5 envelopes 非 4 / GORM .Scan workaround)

  **B.6 `## Human Verification Required`**: "None required for retro-verify — 所有 must-haves 程序化验证. 真实生产数据 post-audit 是 18-SUMMARY 列的 out-of-scope 项, 不属 retro-verify 范围."

  **B.7 `## Gaps Summary`**: "No functional gaps found. 11 个 deliverables 全部有 commit + live code 三层证据. SUMMARY frontmatter vs body HEAD 不一致已标注 (body 为准)."

  **B.8 `## Evidence Limitations`** (retro-verify 必加):
  - "原 PLAN.md / CONTEXT.md / DISCUSSION-LOG 永久丢失 (从未 git 入库) — 本验证基于 SUMMARY + 10 commits + 实时代码三源交叉, 非基于原始计划文档"
  - "18-SUMMARY frontmatter `Final HEAD: bd84fe2` (W3 终点) 与 body §Wave 4 实际终点 `0c018f2` (W4d) 不一致 — body 为准 (per 21-RESEARCH §3 + §Pitfall 2)"
  - "真实生产数据 post-audit 未做 (18-SUMMARY §'不在本 phase 范围' 列出)"
  - "commit 5d536ec (2026-08-02) 是 phase 21 启动前的预存证据, 不是 phase 21 的交付物 (per 21-RESEARCH §Pitfall 6)"

  **B.9 `## Deferred (confirmed not done, by design)`** (retro-verify 必加): 列 18-SUMMARY §"不在本 phase 范围" 全部项 (KMS/Vault / staging post-audit / FileShare.Password / StreamURL URL 凭据拒绝 / dormant registry / 前端 transport GCM)

  **B.10 文件尾**: `_Verified: 2026-08-03T00:00:00Z_` + `_Verifier: Claude (gsd-verifier) — retro-active_`

  **C. 提交** (per CONTEXT D-06.1 + Pitfall 4 — `.planning/` 在 .gitignore line 74):
  - Stage with `git add -f .planning/phases/18-credential-static-encryption-sec-003b/.gitkeep .planning/phases/18-credential-static-encryption-sec-003b/18-SUMMARY.md .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md` (必须 `-f`)
  - Commit subject: `docs(21): retro-verify phase 18 — reconstruct dir + VERIFICATION.md`
  - Commit body (段落形式): "Per v1.1-MILESTONE-AUDIT gaps_found: phase 18 目录缺失. 重建最小目录 + 复制 root 18-SUMMARY.md (root 原版不动 per D-02.6) + 写 18-VERIFICATION.md. status: passed (11/11 must-haves, 含 5d536ec 预存证据)."
  - Trailer: `Co-Authored-By: Claude <noreply@anthropic.com>`

  **禁止**:
  - 移动 root 18-SUMMARY.md (只能复制, root 原版保持原位 per CONTEXT D-02.6)
  - 修改 docs/audits/*.md (immutable)
  - 修改业务代码 (phase 21 不改业务代码, 仅 auth:57 一处例外, 由 21-05 plan 处理)
  - 在 VERIFICATION 里把 5d536ec 称为 phase 21 交付物 (per Pitfall 6 — 它是预存证据)
  - 在 VERIFICATION 里只列 frontmatter 的 5 commits (W1a-W3) 而漏掉 W4a..W4d (per Pitfall 2 — body 为准, 9 commits 全列)
  - L3 wiring 证据遗漏 (per Pitfall 5 — 每个 must-have 至少一条消费者调用证据)
  </action>

  <verify>
  <automated>
  # 1. 目录与文件结构
  test -d .planning/phases/18-credential-static-encryption-sec-003b && \
    test -f .planning/phases/18-credential-static-encryption-sec-003b/.gitkeep && \
    test -f .planning/phases/18-credential-static-encryption-sec-003b/18-SUMMARY.md && \
    test -f .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md

  # 2. root 18-SUMMARY.md 保持原位 (未移动)
  test -f 18-SUMMARY.md

  # 3. frontmatter 关键字段
  grep -c '^status: passed' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md | grep -q '^1$' && \
    grep -q 'score: 11/11' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md && \
    grep -q 'method: retro-active' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md

  # 4. 三大 retro-verify 必加章节
  grep -qE '^## Evidence Limitations' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md && \
    grep -qE '^## Deferred \(confirmed not done, by design\)' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md

  # 5. 9 个 wave commit hash 全部引用 (W1a..W4d, body 为准 per Pitfall 2)
  for h in e6315ce 1dbb3b0 edaa4ae 558f723 bd84fe2 8796ca3 3822497 a182cd6 0c018f2; do \
    grep -q "$h" .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md || { echo "MISSING commit $h"; exit 1; }; \
  done

  # 6. 5d536ec 显式标注为预存证据 (per Pitfall 6)
  grep -q '5d536ec' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md && \
    grep -qE '(预存证据|pre-existing|not a phase 21 deliverable|不是 phase 21 交付物)' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md

  # 7. SUMMARY frontmatter vs body HEAD 不一致已标注 (per Pitfall 2)
  grep -q 'bd84fe2' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md && \
    grep -q '0c018f2' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md

  # 8. 11 must-haves 都有 VERIFIED 标记
  grep -c 'VERIFIED' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md | awk '{if ($1 < 11) {print "TOO FEW VERIFIED: "$1; exit 1}}'

  # 9. 行数门槛
  awk 'END {if (NR < 100) {print "TOO SHORT: "NR" lines"; exit 1}}' .planning/phases/18-credential-static-encryption-sec-003b/18-VERIFICATION.md

  # 10. 提交成功 + 含 3 个文件 (.gitkeep + SUMMARY + VERIFICATION)
  git log --oneline -1 | grep -q 'docs(21): retro-verify phase 18' && \
    git show --stat HEAD --format= | grep -c '18-credential-static-encryption-sec-003b' | awk '{if ($1 < 3) {print "TOO FEW FILES: "$1; exit 1}}'

  # 11. 业务代码未被误触 (仅 .planning/18-* 入 stage)
  git show --stat HEAD --format= | grep -E 'internal/|cmd/|docs/audits/' && exit 1 || true
  </automated>
  </verify>

  <acceptance_criteria>
  - `.planning/phases/18-credential-static-encryption-sec-003b/` 目录存在
  - 目录内含 `.gitkeep` + `18-SUMMARY.md` (root 副本) + `18-VERIFICATION.md`
  - root `18-SUMMARY.md` 保持原位 (未移动, 未删除)
  - 18-VERIFICATION.md frontmatter `status: passed` + `score: 11/11 must-haves verified` + `method: retro-active goal-backward`
  - 11 条 Observable Truths (D18-1..D18-11) 每条 Status = VERIFIED 且 Evidence 列含三层证据 (L1 文件存在 + L2 真实导出符号 + L3 消费者调用, per Pitfall 5)
  - 9 个 wave commit hash (W1a..W4d) 全部引用: e6315ce / 1dbb3b0 / edaa4ae / 558f723 / bd84fe2 / 8796ca3 / 3822497 / a182cd6 / 0c018f2 (per Pitfall 2, body 为准)
  - `5d536ec` 显式标注为 "phase 21 启动前的预存证据, 不是 phase 21 交付物" (per Pitfall 6)
  - Evidence Limitations 段标注 SUMMARY frontmatter (bd84fe2) vs body (0c018f2) HEAD 不一致, body 为准 (per Pitfall 2)
  - Behavioral Spot-Checks 含至少 4 条 < 30s 可运行命令 (含 `git show 5d536ec --stat` + `git show 0c018f2 --stat` + `go test` + grep)
  - 文件 ≥ 100 行
  - git commit subject 为 `docs(21): retro-verify phase 18 — reconstruct dir + VERIFICATION.md`
  - 提交含 3 个文件 (.gitkeep + 18-SUMMARY.md + 18-VERIFICATION.md), 不含业务代码 / docs/audits/*
  </acceptance_criteria>

  <done>
  phase 18 目录重建 + 18-VERIFICATION.md 落地 main; status: passed (11/11); 9 个 wave commits 全列 (body 为准); 5d536ec 显式标预存证据; SUMMARY frontmatter vs body 不一致已诚实标注
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (N/A — pure docs) | 本 plan 仅在 `.planning/` 下重建目录 + 复制 SUMMARY + 新写 VERIFICATION.md, 不改运行时代码. 无输入跨越信任边界. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-SUMMARY | Tampering | 误把 root 18-SUMMARY.md 移动而非复制 | mitigate | task action 显式说 "复制 (非移动)"; `<verify>` 断言 `test -f 18-SUMMARY.md` (root 原版仍在) |
| T-21-02-HEAD | Information Disclosure (false) | 信 frontmatter `bd84fe2` 漏掉 W4 交付物 | mitigate | per Pitfall 2, body 为准; `<verify>` 断言 9 个 commit hash 全部引用; Evidence Limitations 段标注不一致 |
| T-21-02-5D536EC | Information Disclosure (false) | 误把 5d536ec 当 phase 21 交付物写入 VERIFICATION | mitigate | per Pitfall 6, 显式标 "预存证据"; `<verify>` 断言 VERIFICATION 含 "预存证据" 或 "pre-existing" 字样 |
| T-21-02-WIRING | Repudiation (audit) | L3 wiring 证据遗漏, VERIFICATION 只列文件存在 | mitigate | per Pitfall 5, 11 must-haves 每个 L1+L2+L3; `<verify>` 断言 `grep -c VERIFIED ≥ 11` |
| T-21-02-INTEGRITY | Tampering | docs commit 误触业务代码 | mitigate | task `files_modified` 显式限定 .planning/18-*; `<verify>` 反向断言 `git show --stat HEAD` 不含 internal/ cmd/ docs/audits/ |
</threat_model>

<verification>
- `.planning/phases/18-credential-static-encryption-sec-003b/` 目录 + 3 个文件齐全
- root `18-SUMMARY.md` 保持原位 (未移动)
- 18-VERIFICATION.md frontmatter `status: passed` + `score: 11/11` + `method: retro-active`
- 9 个 wave commit hash 全部引用 (body 为准 per Pitfall 2)
- `5d536ec` 显式标注为预存证据 (per Pitfall 6)
- Evidence Limitations 段标注 SUMMARY frontmatter vs body 不一致
- 11 must-haves 每个 L1+L2+L3 证据齐全 (per Pitfall 5)
- git commit 落地 main, 仅含 .planning/18-* 文件
</verification>

<success_criteria>
- v1.1-MILESTONE-AUDIT.md 的 "phase-18 missing-directory" gap 可标记关闭
- 18-VERIFICATION.md 通过 gsd-verifier goal-backward 校验 (status: passed, 11/11, 无 functional gap)
- phase 18 目录自洽 (无需依赖 root SUMMARY 即可理解全部交付)
- 跨 phase 契约 (phase 17 SEC-003b deferred → phase 18 兑现 → phase 19/20 消费) 显式记录
</success_criteria>

<output>
创建 `.planning/phases/21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem/21-02-SUMMARY.md` 记录: 提交 hash, 重建目录路径, status: passed, 11/11 must-haves, 9 wave commits + 5d536ec (预存), SUMMARY frontmatter vs body 不一致处理
</output>
