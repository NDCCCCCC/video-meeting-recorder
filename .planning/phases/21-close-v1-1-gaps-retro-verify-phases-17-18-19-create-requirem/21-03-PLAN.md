---
phase: 21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-ctx-cascade-sec-004-style-001-error/.gitkeep
  - .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-SUMMARY.md
  - .planning/phases/19-ctx-cascade-sec-004-style-001-error/phase-19-D5-D21-summary.md
  - .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md
autonomous: true
requirements: [P21-R3]
tags: [retro-verify, phase-19, verification, ctx-cascade, sec-004, style-001, docs, dir-reconstruction]

must_haves:
  truths:
    - ".planning/phases/19-ctx-cascade-sec-004-style-001-error/ 目录存在 (重建)"
    - "19-SUMMARY.md 副本 + phase-19-D5-D21-summary.md 副本入目录 (root 原版保持不动 per D-02.6)"
    - "19-VERIFICATION.md 含 frontmatter status: passed + score 10/10 must-haves"
    - "19-VERIFICATION.md 引用 21 个 refactor(19/dN) commit hash + 11 个 wave commit hash"
    - "Evidence Limitations 段标注 SUMMARY frontmatter vs body HEAD 不一致 + 原 PLAN/CONTEXT 丢失"
  artifacts:
    - path: .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md
      provides: Phase 19 retro-verify VERIFICATION report
      contains: "status: passed"
      min_lines: 120
    - path: .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-SUMMARY.md
      provides: Phase 19 SUMMARY 副本
      contains: "ctx"
    - path: .planning/phases/19-ctx-cascade-sec-004-style-001-error/phase-19-D5-D21-summary.md
      provides: D5-D21 增量总结副本
      contains: "d5"
  key_links:
    - from: 19-VERIFICATION.md D19-1 (ctx 全量级联)
      to: live code internal/services/video_recording_task_service.go (42 WithContext sites)
      via: grep `.WithContext(ctx)` count
      pattern: "\\.WithContext\\(ctx\\)"
    - from: 19-VERIFICATION.md D19-6 (hls_jti_records 表)
      to: live code internal/models/hls_jti_record.go + cmd/server/app.go AutoMigrate
      via: grep `HLSJtiRecord`
      pattern: "HLSJtiRecord"
---

<objective>
为 phase 19 (ctx 全量级联 + SEC-004 replay + STYLE-001 error) 重建 phase 目录 + 写 VERIFICATION.md，关闭 v1.1-MILESTONE-AUDIT.md 标记的 phase-19 missing-directory blocker。

Purpose: phase 19 目录被早期清理/从未建, 但 11 wave commits + 21 dN commits 全部落地 main. 重建最小目录 + 复制 root SUMMARY + 复制 D5-D21 增量总结 + 写 VERIFICATION.md.
Output: `.planning/phases/19-ctx-cascade-sec-004-style-001-error/` 目录 (含 .gitkeep + 19-SUMMARY.md 副本 + phase-19-D5-D21-summary.md 副本 + 19-VERIFICATION.md)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem/21-CONTEXT.md
@.planning/phases/21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem/21-RESEARCH.md
@19-SUMMARY.md
@docs/audits/phase-19-D5-D21-summary.md
@.planning/phases/20-handleerror-classify-convergence/20-VERIFICATION.md

<interfaces>
<!-- 20-VERIFICATION.md 是 gold reference (10/10 passed), 镜像其章节结构 (与 21-01/21-02 同) -->

Phase 19 must-haves (派生自 ROADMAP Progress table + 19-SUMMARY + D5-D21 summary + RESEARCH §4, 编号 D19-1..D19-10):
| # | Must-Have | Live Code Evidence | Git Evidence |
|---|-----------|--------------------|--------------|
| D19-1 | ctx 全量级联 ~190 service 方法 | `grep -c '.WithContext(ctx)' internal/services/video_recording_task_service.go` = 42 [VERIFIED live] | W3-W5 commits (213710c..a6c21b6 + 34b07f7..b08255d) |
| D19-2 | SEC-004 jti 防重放 (多分片 HLS 修复, 不加 DB 表 + TTL sweeper) | internal/auth/hlstoken/hls_token.go::TestVerify_MultiSegmentSameToken | 6fbdad4 (W1) |
| D19-3 | STYLE-001 三组件 (mapping.go + HandleError + error_mapper.go) | internal/errors/mapping.go + pkg/response/response.go::HandleError + internal/middleware/error_mapper.go | cacc294 (W2) |
| D19-4 | 24 sentinels (D5-D21 新增) | internal/errors/mapping.go:157-203 knownSentinels slice 共 42 条 (19 pre-existing + 23 new, 去重 42 行 — 20-VERIFICATION 标 42 sentinels) | d5-d21 commits |
| D19-5 | ~356 散点 sentinel 化 (13 service + 9 handler + middleware + utility) | grep `apperrors.Err|response.HandleError` across services/handlers | d5-d21 commits |
| D19-6 | HMAC jti 升级为 hls_jti_records 表 (跨实例/重启持久化) | internal/models/hls_jti_record.go::HLSJtiRecord + cmd/server/app.go:340 `&models.HLSJtiRecord{}` AutoMigrate 注册 + hls_token.go:55,92,95 注释 | 1f0ec35 (D3) |
| D19-7 | taskServiceAdapter 与 VideoRecordingTaskService 合并 | cmd/server/app.go 无独立 adapter struct; NewVideoRecordingTaskService 接受 *CredentialEncryptor 可变参数 | 3b2d41f (D2) |
| D19-8 | gorm wrap → BusinessError + HandleError (handler string-match → sentinel-driven) | notification_service.go + ppt_file_service.go::RenamePPTFile + video_file_service.go::RenameVideoFile + ppt_handler.go::RenamePPTFile + video_file_handler.go::RenameVideoFile | 3d171de (W6) |
| D19-9 | 取消传播测试 (pre-cancelled ctx → GORM context.Canceled) | internal/services/ctx_cancellation_test.go (4 测试) + cmd/server/taskservice_adapter_ctx_test.go::TestTaskServiceAdapter_CancellationPropagation | W3 + W4 |
| D19-10 | foreignKey sentinel + dual-%w wrap (Go 1.20+ 双错误链) | internal/errors/errors.go::ErrForeignKeyConstraint + video_file_service.go::createWithDuplicateCheck | 20ee289 (D1) |

Phase 19 wave commit 序列 (19-SUMMARY line 397-408 + 511-519 + STATE.md §Phase 19):
| Wave | Commit | 范围 |
|------|--------|------|
| W0 | ad7d0a8 | ctx 残留清理 (PERF-003/BUG-005) — dashboard_service 11 处 / audit_log_service lifecycle ctx / sm4_token 5 处 |
| W1 | 6fbdad4 | SEC-004 jti replay 模型重写 (多分片 HLS 修复) |
| W2 | cacc294 | STYLE-001 error-mapping 基础设施三组件 |
| W3 | 213710c..a6c21b6 (8 commits) | ctx 级联 13 leaf/mid 服务 |
| W4 | 9a00cbe + 2281927 | TaskServiceInterface 原子三元组 (interface + adapter + mock + scheduler 10 调用点) |
| W5 | 34b07f7 (VideoRecordingTaskService 22 方法 ctx-first) + e2b0b6b/7828fc3/7a5a1cc/1ae6be0/b08255d (VideoFileService + caller) | ctx 全量级联 |
| W6 | 3d171de | STYLE-001 error 迁移 (gorm wrap → BusinessError + HandleError) |
| 收尾 D1 | 20ee289 | FOREIGN KEY strings.Contains → sentinel |
| 收尾 D2 | 3b2d41f | taskServiceAdapter 与 VideoRecordingTaskService 合并 |
| 收尾 D3 | 1f0ec35 | HLS jti 升级为 hls_jti_records 表 |
| 收尾 D4 | f4291f5 | errors 包增量迁移 |
| 最终 docs | 6edb772 | Wave 6 summary + 范围对账 |

Phase 19 D5-D21 增量 (21 个 commits, docs/audits/phase-19-D5-D21-summary.md):
| Commit | dN | 范围 | Sentinel 增量 | 散点迁移 |
|--------|----|----|---------------|---------|
| 7a0a7af | d5 | user_service + handler | +5 | 14 |
| da8aaf9 | d6 | ad_auth + local_auth Login | +4 | 33 |
| 964fb8f | d7 | sm4_token + middleware | +4 | 11 |
| c8ed97f | d8 | ip_validator | 0 | 9 |
| 2a807a6 | d9 | role_service + handler | +4 | 9 |
| d277f37 | d10 | apikey_service + handler | +5 | 11 |
| 7b0d817 | d11 | hls_token | 0 | 6 |
| 00df988 | d12 | auth/service.go | 0 | 4 |
| e1dd2dd | d13 | ppt_file_service + handler | +1 | 1 |
| 1fa66d8 | d14 | tingwu_client | +1 | 23 |
| 98e14ca | d15 | storage/file_service | 0 | 22 |
| 8eec84a | d16 | scheduler + recorder | 0 | 33 |
| cc49867 | d17 | huawei SDK 适配 | 0 | 25 |
| 71e5be0 | d18 | utils/sm4_password | 0 | 28 |
| ddf047c | d19 | migrations + models + input_config | 0 | 47 |
| ffdc0c6 | d20 | transcription/oss/notification/local_driver/config | 0 | 53 |
| f358602 | d21 | video_recording_task_service | 0 | 24 |

合计: 17 D5-D21 commits + 4 D1-D4 收尾 = 21 refactor(19/dN) commits, 新增 24 sentinels, 迁移 ~356 散点, 行变化 +461 / -270.

Phase 19 Evidence Limitations (retro-verify 必加):
- 原 PLAN.md / CONTEXT.md / DISCUSSION-LOG 永久丢失 (从未 git 入库) — 与 phase 18 同
- 19-SUMMARY frontmatter `Final HEAD: cacc294` (W2 终点) 与 body 实际最终 `6edb772` (docs 终点) 不一致 — body 为准
- STATE.md §Phase 19 line 204 标 `2281927` 为 "进入基线" (W4 docs 前), 与 SUMMARY base HEAD `89d4cc9` (W0 起点) 不冲突 (一个是 phase 19 整体起点, 一个是 W4 docs 之前)

Phase 19 → 18/20 契约 (integration, 20-VERIFICATION §Key Link 已部分验证):
- 上游 (Phase 18): D2 taskServiceAdapter 合并保留 Phase 18 SM4-GCM 解密逻辑 (VideoRecordingTaskService 接受 *CredentialEncryptor 可选参数)
- 下游 (Phase 20): HandleError + 24 sentinels + BusinessError 三件套是 phase 20 直接前置; 20-VERIFICATION 已验证 phase 19 → 20 contract intact
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重建 phase 19 目录 + 复制 SUMMARY + D5-D21 summary + 写 19-VERIFICATION.md (status: passed, 10/10 must-haves)</name>
  <files>.planning/phases/19-ctx-cascade-sec-004-style-001-error/.gitkeep, .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-SUMMARY.md, .planning/phases/19-ctx-cascade-sec-004-style-001-error/phase-19-D5-D21-summary.md, .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md</files>

  <read_first>
  必读:
  - `19-SUMMARY.md` (root, 33 KB — phase 19 完整历史, 含 wave commit 序列 + D1-D4 收尾 + W6 范围对账; frontmatter `Final HEAD: cacc294` 与 body `6edb772` 不一致, body 为准)
  - `docs/audits/phase-19-D5-D21-summary.md` (8.5 KB — D5-D21 逐项 commit/sentinel/scatter 表)
  - `.planning/STATE.md` §Phase 19 (line 145-206) + §Phase 19 Final Status (line 513-533)
  - `.planning/ROADMAP.md` Phase 19 行 (Progress table + Goal)
  - `.planning/phases/21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem/21-RESEARCH.md` §1 (格式契约) + §4 (Phase 19 证据点 + 21 dN commits 表)
  - `.planning/phases/20-handleerror-classify-convergence/20-VERIFICATION.md` (GOLD REFERENCE + §Key Link 已验证 phase 19→20 contract)
  验证用 (按需 grep):
  - `internal/errors/mapping.go` (grep knownSentinels slice 长度 + MapToHTTPStatus)
  - `internal/errors/errors.go` (grep //go:generate + ErrForeignKeyConstraint)
  - `internal/middleware/error_mapper.go` (grep 注册)
  - `pkg/response/response.go` (grep HandleError 实现)
  - `internal/services/video_recording_task_service.go` (grep `.WithContext(ctx)` count = 42, L3 wiring 证据)
  - `internal/services/ctx_cancellation_test.go` (grep 4 个测试)
  - `internal/auth/hlstoken/hls_token.go` (grep TestVerify_MultiSegmentSameToken + 注释 line 55,92,95)
  - `internal/models/hls_jti_record.go` (grep HLSJtiRecord + TableName 返回 "hls_jti_records")
  - `cmd/server/app.go:340` 区域 (grep `&models.HLSJtiRecord{}` AutoMigrate 注册)
  - `internal/services/video_file_service.go` (grep createWithDuplicateCheck + ErrForeignKeyConstraint + dual-%w)
  预校验 commit hash (避免引用错误):
  - `git log --oneline ad7d0a8 6fbdad4 cacc294 9a00cbe 2281927 34b07f7 3d171de 20ee289 3b2d41f 1f0ec35 f4291f5 6edb772`
  - `git log --oneline 7a0a7af da8aaf9 964fb8f c8ed97f 2a807a6 d277f37 7b0d817 00df988 e1dd2dd 1fa66d8 98e14ca 8eec84a cc49867 71e5be0 ddf047c ffdc0c6 f358602`
  </read_first>

  <action>
  **A. 重建目录结构**:
  - 创建目录 `.planning/phases/19-ctx-cascade-sec-004-style-001-error/` (slug per CONTEXT D-02.5 discretion)
  - 创建空文件 `.gitkeep` 占位
  - 复制 `19-SUMMARY.md` (root) → `.planning/phases/19-ctx-cascade-sec-004-style-001-error/19-SUMMARY.md` (root 原版保持不动 per CONTEXT D-02.6)
  - 复制 `docs/audits/phase-19-D5-D21-summary.md` → `.planning/phases/19-ctx-cascade-sec-004-style-001-error/phase-19-D5-D21-summary.md` (保持文件名; docs/audits/ 原版保持不动, docs/audits/* 是 immutable source of truth)

  **B. 写 `.planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md`** (镜像 20-VERIFICATION.md 结构):

  **B.1 frontmatter**:
  - `phase: 19-ctx-cascade-sec-004-style-001-error`
  - `verified: 2026-08-03T00:00:00Z`
  - `status: passed`
  - `score: 10/10 must-haves verified`
  - `method: retro-active goal-backward (SUMMARY + D5-D21 summary + git history + live code)`
  - `overrides_applied: 0`
  - `gaps: []`
  - `deferred:` 列 19-SUMMARY §DEFERRED 项 (video_file_service:891 strings.Contains "FOREIGN KEY" 仅诊断日志 / taskServiceAdapter 与 VideoFileService 合并 含 phase 18 SM4-GCM 解密逻辑独立 phase / internal/errors 包 0 service import 部分迁移 / HMAC jti DB 表 5min TTL 单实例窗口风险)
  - `human_verification: []`
  - `evidence_limitations:` 列 3 条 (原 PLAN/CONTEXT/DISCUSSION-LOG 永久丢失 / SUMMARY frontmatter vs body HEAD 不一致 body 为准 / STATE.md 进入基线 vs SUMMARY base HEAD 不冲突)

  **B.2 文档头**:
  - H1: `# Phase 19: ctx 全量级联 + SEC-004 replay 修复 + STYLE-001 error 迁移 - Verification Report`
  - `**Phase Goal:**` 拷贝 ROADMAP.md Phase 19 Goal 原文 (含 "ctx 全量级联 + SEC-004 replay + STYLE-001 error")
  - `**Status:** passed (10/10 must-haves verified; retro-active)`
  - `**Verifier:** Claude (gsd-verifier) — retro-active, goal-backward methodology`
  - 段落说明: "本 VERIFICATION 于 phase 21 retro-verify 时重建. phase 19 目录被早期清理, 本目录由 phase 21 重建 (复制 root 19-SUMMARY.md + docs/audits/phase-19-D5-D21-summary.md + 新写 VERIFICATION.md). root 原版保持不动 (per CONTEXT D-02.6). 证据基于 11 wave commits + 21 D1-D21 commits + 实时代码多源交叉."

  **B.3 `## Goal Achievement` 章节**:

  **`### Observable Truths`** 表格 10 行 (D19-1..D19-10), 列 `| # | Truth | Status | Evidence |`:
  每行三层证据 (L1+L2+L3, per Pitfall 5):
  - D19-1 ctx 全量级联 ~190 service 方法 → VERIFIED → L1 video_recording_task_service.go 存在; L2 grep `.WithContext(ctx)` count = 42 (已 VERIFIED live in RESEARCH §4); L3 scheduler/handlers 调用 VideoRecordingTaskService 时传 ctx (grep scheduler caller); 11 wave commits (W3-W5: 213710c..b08255d)
  - D19-2 SEC-004 jti 防重放 (多分片 HLS, 不加 DB 表 + TTL sweeper) → VERIFIED → hls_token.go::TestVerify_MultiSegmentSameToken; commit 6fbdad4 (W1)
  - D19-3 STYLE-001 三组件 → VERIFIED → L1 三文件存在 (mapping.go + response.go::HandleError + error_mapper.go); L2 MapToHTTPStatus/HandleError 导出; L3 middleware 注册 error_mapper (grep `error_mapper` in cmd/server/app.go); commit cacc294 (W2)
  - D19-4 24 sentinels 新增 → VERIFIED → mapping.go:157-203 knownSentinels slice 42 行 (19 pre-existing + 23 new, 去重 42; 20-VERIFICATION 标 42 sentinels); d5-d21 commits
  - D19-5 ~356 散点 sentinel 化 → VERIFIED → grep `apperrors.Err|response.HandleError` across services/handlers 命中数百处; d5-d21 commits
  - D19-6 HMAC jti 升级为 hls_jti_records 表 → VERIFIED → L1 models/hls_jti_record.go 存在; L2 HLSJtiRecord struct + TableName 返回 "hls_jti_records"; L3 cmd/server/app.go:340 AutoMigrate 注册 + hls_token.go:55,92,95 注释引用; commit 1f0ec35 (D3)
  - D19-7 taskServiceAdapter 与 VideoRecordingTaskService 合并 → VERIFIED → cmd/server/app.go 无独立 adapter struct (grep `taskServiceAdapter` 减少); NewVideoRecordingTaskService 接受 *CredentialEncryptor 可变参数; commit 3b2d41f (D2)
  - D19-8 gorm wrap → BusinessError + HandleError → VERIFIED → notification_service.go + ppt_file_service.go::RenamePPTFile + video_file_service.go::RenameVideoFile + ppt_handler.go::RenamePPTFile + video_file_handler.go::RenameVideoFile (grep each); commit 3d171de (W6)
  - D19-9 取消传播测试 → VERIFIED → internal/services/ctx_cancellation_test.go (4 测试) + cmd/server/taskservice_adapter_ctx_test.go::TestTaskServiceAdapter_CancellationPropagation; W3+W4
  - D19-10 foreignKey sentinel + dual-%w wrap → VERIFIED → internal/errors/errors.go::ErrForeignKeyConstraint + video_file_service.go::createWithDuplicateCheck (grep dual-%w `errors.Is.*ErrForeignKeyConstraint`); commit 20ee289 (D1)

  **`### Required Artifacts`** 表格 (列 `| Artifact | Expected | Status | Details |`): 列 10-12 个核心文件, 每个三层证据 (mapping.go / errors.go / response.go / error_mapper.go / hls_token.go / hls_jti_record.go / ctx_cancellation_test.go / video_recording_task_service.go / video_file_service.go / ppt_file_service.go / notification_service.go / app.go)

  **`### Key Link Verification`** 表格:
  - hls_jti_records 表 ← HLS jti 持久化路径 → VERIFIED (跨实例/重启持久化, phase 17 in-memory map 升级)
  - HandleError + sentinels + BusinessError 三件套 → Phase 20 直接前置 → VERIFIED (20-VERIFICATION §Key Link 已验证 contract intact)
  - VideoRecordingTaskService ← scheduler/handlers 调用 → VERIFIED (ctx 透传)
  - credential_encryptor (phase 18) → VideoRecordingTaskService 可选参数 → VERIFIED (跨 phase 18→19 contract, D2 合并保留 SM4-GCM 解密)
  - mapping.go → pkg/response.HandleError → middleware/error_mapper 全局注册 → VERIFIED

  **`### Behavioral Spot-Checks`** 表格 (命令 < 30s):
  - `go build ./...` → 0 errors → VERIFIED
  - `go test -race ./internal/services/... ./internal/auth/hlstoken/... ./internal/middleware/... ./internal/errors/...` → PASS (含 ctx_cancellation_test + TestVerify_MultiSegmentSameToken) → VERIFIED
  - `grep -c '.WithContext(ctx)' internal/services/video_recording_task_service.go` → 42 (per RESEARCH §4 VERIFIED) → VERIFIED
  - `grep -c 'HLSJtiRecord' cmd/server/app.go internal/models/hls_jti_record.go internal/auth/hlstoken/hls_token.go` → ≥3 (L3 wiring) → VERIFIED
  - `git show 6fbdad4 --stat` → 列出 hls_token.go → VERIFIED (W1 SEC-004 修复)
  - `git show 1f0ec35 --stat` → 列出 hls_jti_record.go + app.go → VERIFIED (D3 表升级)
  - `grep -c 'knownSentinels' internal/errors/mapping.go` → ≥1 (sentinel slice 单源) → VERIFIED

  **B.4 `## Requirements Coverage`** 章节: 列 REQ-19-ctx / REQ-19-SEC-004 / REQ-19-STYLE-001 / REQ-19-jti-db (将在 phase 21-04 REQUIREMENTS.md 登记), 验证证据列引用本 VERIFICATION D19-N 条款 + commit

  **B.5 `## Anti-Patterns Found`**: INFO 级 (例: video_file_service.go:891 strings.Contains "FOREIGN KEY" deferred — 仅诊断日志, 非 user error path)

  **B.6 `## Human Verification Required`**: "None required — 所有 must-haves 程序化验证. ctx 取消传播有契约测试覆盖; SEC-004 jti 防重放有 multi-segment 测试覆盖."

  **B.7 `## Gaps Summary`**: "No functional gaps found. 11 wave commits + 21 D1-D21 commits 全部落地 main (32+ commits 总计), 新增 24 sentinels, 迁移 ~356 散点, 行变化 +461/-270. `go test -race ./...` 全绿."

  **B.8 `## Evidence Limitations`** (retro-verify 必加):
  - "原 PLAN.md / CONTEXT.md / DISCUSSION-LOG 永久丢失 (从未 git 入库) — 与 phase 18 同; 本验证基于 SUMMARY + D5-D21 summary + 32+ commits + 实时代码多源交叉"
  - "19-SUMMARY frontmatter `Final HEAD: cacc294` (W2 终点) 与 body 实际最终 `6edb772` (docs 终点) 不一致 — body 为准"
  - "STATE.md §Phase 19 line 204 标 `2281927` 为 '进入基线' (W4 docs commit 前) 与 SUMMARY base HEAD `89d4cc9` (W0 起点) 不冲突 — 一个是 phase 19 整体起点, 一个是 W4 docs 之前"

  **B.9 `## Deferred (confirmed not done, by design)`** (retro-verify 必加): 列 19-SUMMARY §DEFERRED 表 4 行 + 说明每项去向 (独立 phase 处理 / future work)

  **B.10 文件尾**: `_Verified: 2026-08-03T00:00:00Z_` + `_Verifier: Claude (gsd-verifier) — retro-active_`

  **C. 提交** (per CONTEXT D-06.1 + Pitfall 4):
  - Stage with `git add -f .planning/phases/19-ctx-cascade-sec-004-style-001-error/.gitkeep .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-SUMMARY.md .planning/phases/19-ctx-cascade-sec-004-style-001-error/phase-19-D5-D21-summary.md .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md`
  - Commit subject: `docs(21): retro-verify phase 19 — reconstruct dir + VERIFICATION.md`
  - Commit body (段落): "Per v1.1-MILESTONE-AUDIT gaps_found: phase 19 目录缺失. 重建最小目录 + 复制 root 19-SUMMARY.md + docs/audits/phase-19-D5-D21-summary.md (原版均不动 per D-02.6) + 写 19-VERIFICATION.md. status: passed (10/10 must-haves, 32+ commits + 24 sentinels + ~356 散点)."
  - Trailer: `Co-Authored-By: Claude <noreply@anthropic.com>`

  **禁止**:
  - 移动 root 19-SUMMARY.md 或 docs/audits/phase-19-D5-D21-summary.md (只能复制)
  - 修改 docs/audits/*.md (immutable)
  - 修改业务代码 (phase 21 不改业务代码, auth:57 由 21-05 处理)
  - 在 VERIFICATION 里只列 frontmatter cacc294 为终点 (per Pitfall 2 — body 6edb772 为准)
  - L3 wiring 证据遗漏 (per Pitfall 5 — 每个 must-have 至少一条消费者调用证据)
  - 漏掉 21 个 dN commits 中的任何一个 (全部需引用, 至少在表格里)
  </action>

  <verify>
  <automated>
  # 1. 目录与文件结构 (4 个文件)
  test -d .planning/phases/19-ctx-cascade-sec-004-style-001-error && \
    test -f .planning/phases/19-ctx-cascade-sec-004-style-001-error/.gitkeep && \
    test -f .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-SUMMARY.md && \
    test -f .planning/phases/19-ctx-cascade-sec-004-style-001-error/phase-19-D5-D21-summary.md && \
    test -f .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md

  # 2. root 原版保持原位 (未移动)
  test -f 19-SUMMARY.md && test -f docs/audits/phase-19-D5-D21-summary.md

  # 3. frontmatter 关键字段
  grep -c '^status: passed' .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md | grep -q '^1$' && \
    grep -q 'score: 10/10' .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md && \
    grep -q 'method: retro-active' .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md

  # 4. 三大 retro-verify 必加章节
  grep -qE '^## Evidence Limitations' .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md && \
    grep -qE '^## Deferred \(confirmed not done, by design\)' .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md

  # 5. 11 个 wave commit hash 引用 (W0-W6 + D1-D4 收尾 + docs)
  for h in ad7d0a8 6fbdad4 cacc294 9a00cbe 2281927 34b07f7 3d171de 20ee289 3b2d41f 1f0ec35 f4291f5 6edb772; do \
    grep -q "$h" .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md || { echo "MISSING wave commit $h"; exit 1; }; \
  done

  # 6. D5-D21 commits 引用 (至少 10 个代表, 不要求 17 个全列但表必须存在)
  cnt=0; for h in 7a0a7af da8aaf9 964fb8f c8ed97f 2a807a6 d277f37 7b0d817 00df988 e1dd2dd 1fa66d8 98e14ca 8eec84a cc49867 71e5be0 ddf047c ffdc0c6 f358602; do \
    grep -q "$h" .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md && cnt=$((cnt+1)); \
  done; echo "D5-D21 commits referenced: $cnt"; awk -v c=$cnt 'BEGIN {if (c<10) {print "TOO FEW D5-D21: "c; exit 1}}'

  # 7. SUMMARY frontmatter vs body HEAD 不一致标注 (per Pitfall 2)
  grep -q 'cacc294' .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md && \
    grep -q '6edb772' .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md

  # 8. 10 must-haves 都有 VERIFIED 标记
  grep -c 'VERIFIED' .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md | awk '{if ($1 < 10) {print "TOO FEW VERIFIED: "$1; exit 1}}'

  # 9. 行数门槛
  awk 'END {if (NR < 120) {print "TOO SHORT: "NR" lines"; exit 1}}' .planning/phases/19-ctx-cascade-sec-004-style-001-error/19-VERIFICATION.md

  # 10. 提交成功 + 含 4 个文件
  git log --oneline -1 | grep -q 'docs(21): retro-verify phase 19' && \
    git show --stat HEAD --format= | grep -c '19-ctx-cascade-sec-004-style-001-error' | awk '{if ($1 < 4) {print "TOO FEW FILES: "$1; exit 1}}'

  # 11. 业务代码未被误触
  git show --stat HEAD --format= | grep -E 'internal/|cmd/|docs/audits/' && exit 1 || true
  </automated>
  </verify>

  <acceptance_criteria>
  - `.planning/phases/19-ctx-cascade-sec-004-style-001-error/` 目录存在
  - 目录内含 4 个文件: `.gitkeep` + `19-SUMMARY.md` (root 副本) + `phase-19-D5-D21-summary.md` (docs/audits 副本) + `19-VERIFICATION.md`
  - root `19-SUMMARY.md` 与 `docs/audits/phase-19-D5-D21-summary.md` 保持原位
  - 19-VERIFICATION.md frontmatter `status: passed` + `score: 10/10 must-haves verified` + `method: retro-active goal-backward`
  - 10 条 Observable Truths (D19-1..D19-10) 每条 Status = VERIFIED 且三层证据齐全 (L1+L2+L3 per Pitfall 5)
  - 11 个 wave commit hash + 至少 10 个 D5-D21 commit hash 引用 (per body 为准 Pitfall 2)
  - Evidence Limitations 段标注 SUMMARY frontmatter (cacc294) vs body (6edb772) 不一致, body 为准
  - Behavioral Spot-Checks 含至少 4 条 < 30s 可运行命令 (含 `grep -c '.WithContext(ctx)' ... = 42` + `grep -c 'HLSJtiRecord'` + go test + git show)
  - 文件 ≥ 120 行
  - git commit subject 为 `docs(21): retro-verify phase 19 — reconstruct dir + VERIFICATION.md`
  - 提交含 4 个文件, 不含业务代码 / docs/audits/*
  </acceptance_criteria>

  <done>
  phase 19 目录重建 + 19-VERIFICATION.md 落地 main; status: passed (10/10); 11 wave + 至少 10 dN commits 引用; SUMMARY frontmatter vs body 不一致已诚实标注; L3 wiring 证据齐全
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (N/A — pure docs) | 本 plan 仅在 `.planning/` 下重建目录 + 复制 SUMMARY + 新写 VERIFICATION.md, 不改运行时代码. 无输入跨越信任边界. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-SUMMARY | Tampering | 误把 root 19-SUMMARY.md / docs/audits/phase-19-D5-D21-summary.md 移动而非复制 | mitigate | task action 显式 "复制 (非移动)"; `<verify>` 断言 root 原版仍在 |
| T-21-03-AUDIT-IMMUTABLE | Tampering | 误修改 docs/audits/phase-19-D5-D21-summary.md 原版 | mitigate | docs/audits/* 标 immutable; task `<verify>` 反向断言 `git show --stat HEAD` 不含 docs/audits/ |
| T-21-03-HEAD | Information Disclosure (false) | 信 frontmatter cacc294 漏掉 W3-W6 交付物 | mitigate | per Pitfall 2, body 6edb772 为准; `<verify>` 断言两 hash 都引用 + Evidence Limitations 标注 |
| T-21-03-WIRING | Repudiation (audit) | L3 wiring 证据遗漏 | mitigate | per Pitfall 5, 10 must-haves 每个 L1+L2+L3; `<verify>` 断言 `grep -c VERIFIED ≥ 10` + grep wiring target |
| T-21-03-COMMIT-COUNT | Information Disclosure (false) | D5-D21 21 commits 引用不齐 | mitigate | `<verify>` 断言至少 10 个 dN commits 引用 (覆盖代表样本) |
| T-21-03-INTEGRITY | Tampering | docs commit 误触业务代码 | mitigate | task files_modified 限定; `<verify>` 反向断言 |
</threat_model>

<verification>
- `.planning/phases/19-ctx-cascade-sec-004-style-001-error/` 目录 + 4 文件齐全
- root `19-SUMMARY.md` 与 `docs/audits/phase-19-D5-D21-summary.md` 保持原位
- 19-VERIFICATION.md frontmatter `status: passed` + `score: 10/10` + `method: retro-active`
- 11 wave commit hash + ≥10 D5-D21 commit hash 全部引用
- Evidence Limitations 段标注 SUMMARY frontmatter vs body 不一致
- 10 must-haves 每个 L1+L2+L3 证据齐全 (per Pitfall 5)
- git commit 落地 main, 仅含 .planning/19-* 文件
</verification>

<success_criteria>
- v1.1-MILESTONE-AUDIT.md 的 "phase-19 missing-directory" gap 可标记关闭
- 19-VERIFICATION.md 通过 gsd-verifier goal-backward 校验 (status: passed, 10/10, 无 functional gap)
- phase 19 目录自洽 (无需依赖 root SUMMARY 或 docs/audits 即可理解全部交付)
- 跨 phase 契约 (phase 18 SM4-GCM 解密逻辑 → phase 19 D2 合并保留; phase 19 → phase 20 三件套前置) 显式记录
</success_criteria>

<output>
创建 `.planning/phases/21-close-v1-1-gaps-retro-verify-phases-17-18-19-create-requirem/21-03-SUMMARY.md` 记录: 提交 hash, 重建目录路径, status: passed, 10/10 must-haves, 11 wave + dN commits 引用清单, SUMMARY frontmatter vs body 处理
</output>
