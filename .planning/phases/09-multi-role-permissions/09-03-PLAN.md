---
phase: 09
plan: 03
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - internal/services/video_file_service.go
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-07
  - D-09
  - D-10
  - D-11
  - D-12
user_setup: []

must_haves:
  truths:
    - "Users with shared_viewer role see all data (no created_by filter)"
    - "Users without shared_viewer role see only their own data"
    - "shared_viewer role does NOT grant operation permissions"
    - "Permission checks use OR logic across all user roles"
    - "Data visibility checked before permission checks"
  artifacts:
    - path: "internal/services/video_file_service.go"
      provides: "ListFiles() with shared_viewer visibility logic"
      min_lines: 30
      exports: ["VideoFileService.ListFiles"]
  key_links:
    - from: "VideoFileService.ListFiles"
      to: "VideoFile.CreatedBy"
      via: "WHERE created_by filter condition"
      pattern: "created_by.*\?"
    - from: "User.HasRole(shared_viewer)"
      to: "created_by filter"
      via: "Skip filter if true, apply if false"
      pattern: "HasRole.*shared_viewer"
---

# 09-03: Data Visibility Control - Shared Viewer Implementation

## Objective

Implement data visibility control so users with the shared_viewer role can see all users' data, while operation permissions remain unchanged.

**Purpose:** Allow cross-user data visibility for shared viewers without granting operation permissions (D-01, D-02, D-03)

**Output:** Updated service layer methods that check shared_viewer role and conditionally apply created_by filters

## Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

@.planning/phases/09-multi-role-permissions/09-CONTEXT.md
@.planning/phases/09-multi-role-permissions/09-RESEARCH.md
@internal/services/video_file_service.go
@internal/models/user.go
@internal/models/video_file.go
@internal/models/video_recording_task.go

## Tasks

<task type="auto">
  <name>Task 1: Update VideoFileService.ListFiles for shared_viewer visibility</name>
  <files>internal/services/video_file_service.go</files>
  <read_first>
    - internal/services/video_file_service.go (existing ListFiles method with created_by filter)
    - internal/models/user.go (to verify HasRole() method exists from 09-02)
    - internal/models/video_file.go (to confirm CreatedBy field exists per D-09)
  </read_first>
  <action>
Update ListFiles() method to support shared_viewer visibility per D-11 (RESEARCH.md lines 266-307):

1. FIND the ListFiles method that accepts *models.User parameter

2. LOCATE the created_by filter line (typically: `query = query.Where("created_by = ?", user.ID)`)

3. REPLACE with conditional filter:
   ```go
   // DATA VISIBILITY: Shared viewers see all data (D-11, D-12)
   if !user.HasRole(models.RoleSharedViewer) {
       // Non-shared-viewers only see their own data (D-10)
       query = query.Where("created_by = ?", user.ID)
   }
   // shared_viewers skip created_by filter to see all data (D-02)
   ```

4. ENSURE the filter is applied AFTER other filters (status, source_type) but BEFORE pagination

5. ADD comment explaining visibility vs permission separation (D-12):
   ```go
   // Visibility check (data scope) happens before permission checks (operation authorization)
   // shared_viewer affects data scope, not operation permissions
   ```

6. DO NOT modify permission middleware - shared_viewer has NO permissions per D-01/D-03

7. If ListFiles has overloads (different signatures), update all that filter by created_by
  </action>
  <verify>
    <automated>grep -n "HasRole.*RoleSharedViewer" internal/services/video_file_service.go && grep -B2 -A2 "created_by.*\?" internal/services/video_file_service.go | grep -q "if"</automated>
  </verify>
  <done>
    - ListFiles() checks user.HasRole(models.RoleSharedViewer)
    - created_by filter skipped for shared_viewers
    - created_by filter applied for non-shared_viewers
    - Comment explains visibility vs permission separation
    - Method compiles: go build ./internal/services/
  </done>
</task>

<task type="auto">
  <name>Task 2: Search and update other services with created_by filters</name>
  <files>internal/services/*.go</files>
  <read_first>
    - internal/services/video_file_service.go (to confirm pattern established in Task 1)
    - internal/models/video_recording_task.go (to confirm CreatedBy field exists)
  </read_first>
  <action>
Find and update all service methods that filter by created_by:

1. SEARCH for all created_by filters:
   ```bash
   grep -rn "created_by.*\?" internal/services/
   ```

2. For each service file found:
   - VideoRecordingTaskService (if exists)
   - PPTFileService (if has created_by filter)
   - Any other service with data ownership filtering

3. APPLY the same pattern as Task 1:
   ```go
   // DATA VISIBILITY: Shared viewers see all data
   if !user.HasRole(models.RoleSharedViewer) {
       query = query.Where("created_by = ?", user.ID)
   }
   ```

4. RECORD in task output which services were updated:
   - Service name
   - Method name
   - Line number of change

5. If no other services have created_by filters, note this in completion
  </action>
  <verify>
    <automated>grep -rn "HasRole.*RoleSharedViewer" internal/services/ | wc -l | grep -q "[1-9]"</automated>
  </verify>
  <done>
    - All services with created_by filters updated
    - Pattern consistent across all services
    - List of updated services documented
    - All service files compile: go build ./internal/services/
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs for visibility control</name>
  <files>internal/services/video_file_service_test.go</files>
  <read_first>
    - internal/services/video_file_service.go (updated ListFiles method)
    - .planning/phases/08-video-snapshot-player-enhancement/08-00-SUMMARY.md (Wave 0 test stub pattern)
  </read_first>
  <action>
Create or extend test file with Wave 0 stubs for visibility:

1. FIND existing video_file_service_test.go or create new file

2. ADD test stubs for shared_viewer visibility:
   - TestListFiles_WithSharedViewerRole_ReturnsAllFiles (D-02)
   - TestListFiles_WithoutSharedViewerRole_ReturnsOwnFilesOnly (D-10, D-11)
   - TestListFiles_SharedViewerHasNoOperationPermissions (D-01, D-03)
   - TestListFiles_VisibilityCheckedBeforePermissions (D-12)

3. For each test, add Setup/Action/Assert comments:
   - Setup: Create users with/without shared_viewer role, create files by different users
   - Action: Call ListFiles() with different user contexts
   - Assert: Verify correct files returned based on visibility rules

4. USE t.Skip() for Wave 0 stubs

5. TEST scenarios to cover:
   - shared_viewer sees files created by other users
   - regular user sees only own files
   - shared_viewer still needs operation permissions for actions (delete, edit)
   - multiple shared_viewers see same all-user data
  </action>
  <verify>
    <automated>grep -c "TestListFiles.*Shared" internal/services/video_file_service_test.go | grep -q "4"</automated>
  </verify>
  <done>
    - Test stubs created for all visibility scenarios
    - Tests use t.Skip() for Wave 0
    - Test names clearly indicate scenario
    - Setup/Action/Assert comments present
    - Tests compile without errors
  </done>
</task>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Layer → Database | Visibility filters applied to SQL queries |
| User Context → Service | HasRole() check determines query scope |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Information Disclosure | shared_viewer sees all data | accept | INTENDED behavior per D-02; role only assignable by admin (D-13) |
| T-09-11 | Elevation of Privilege | shared_viewer grants operation perms | mitigate | Explicitly verify shared_viewer has NO permissions in role table; visibility != permissions (D-01) |
| T-09-12 | Tampering | HasRole() bypassed | mitigate | Use models.RoleSharedViewer constant; role name validated in DB |
| T-09-13 | Denial of Service | Empty result sets | accept | Normal query behavior; no special handling needed |

## Verification

1. VideoFileService updated: `grep -n "HasRole.*RoleSharedViewer" internal/services/video_file_service.go`
2. All services checked: `grep -l "HasRole.*RoleSharedViewer" internal/services/*.go`
3. Tests created: `test -f internal/services/video_file_service_test.go && grep -c "TestListFiles" internal/services/video_file_service_test.go | grep -q "[1-9]"`
4. Services compile: `go build ./internal/services/`
5. No permission grants to shared_viewer: `! grep "shared_viewer.*permission" internal/models/role.go` (should not exist)

## Success Criteria

- [ ] VideoFileService.ListFiles() checks shared_viewer role
- [ ] created_by filter skipped for shared_viewers
- [ ] All other services with created_by filters updated
- [ ] shared_viewer does NOT grant operation permissions
- [ ] Test stubs created for visibility scenarios
- [ ] All service files compile without errors

## Output

After completion, create `.planning/phases/09-multi-role-permissions/09-03-SUMMARY.md`

**Next:** Execute plan 09-04 to update UserService and create shared_viewer role
