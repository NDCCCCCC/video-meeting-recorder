# 工作交接 - 自动登出问题根本解决方案

**更新时间:** 2026-04-30 13:30
**状态:** 实施完成，待测试

---

## 📋 任务概述

**问题:** 文件管理页面自动登出，停留在该页面时频繁出现 401 错误

**根本原因:** 后端 token 轮换机制与前端并发请求模式的架构不匹配
- 后端在 refresh token 验证通过后**立即撤销**该 token
- 前端多个并发 401 请求导致多个刷新请求同时到达后端
- 后端检测到"token reuse"，撤销所有用户会话

**解决方案:** 后端增加 5 秒宽限期机制 + 前端批量 PPT 检查优化

---

## ✅ 已完成工作

### 1. 后端宽限期机制

**修改的文件:**
- `internal/models/session.go` ✅
- `internal/auth/sm4_token.go` ✅
- `migrations/20260430_add_last_used_at_to_sessions.up.sql` ✅

### 2. 批量 PPT 检查 API

**修改的文件:**
- `internal/handlers/ppt_handler.go` ✅ (BatchGetPptsByVideos 函数已存在)
- `cmd/server/app.go` ✅ (已注册路由 POST /api/v1/ppts/batch-check)
- `frontend/src/api/ppt.ts` ✅ (已添加 batchCheckPpts 函数)

### 3. 前端批量 API 使用

**修改的文件:**
- `frontend/src/pages/files/index.tsx` ✅
  - 删除了单独的 checkHasPpt 函数
  - 改用 batchCheckPpts 批量 API
  - 减少了文件管理页面的 HTTP 请求数

### 4. 前端构建

**状态:** ✅ 编译成功
- 前端已重新构建 (dist/ 目录已更新)

### 5. 数据库迁移

**状态:** ✅ 已通过 GORM AutoMigrate
- Session 模型已包含 LastUsedAt 字段
- 服务器启动时会自动添加该字段

---

## 🧪 待测试验证

### 测试步骤

1. **启动服务器**
   ```bash
   cd D:/CODE/ClaudeCode/record_V2
   go run cmd/server/main.go
   ```

2. **验证数据库迁移**
   - 检查 sessions 表是否有 last_used_at 字段

3. **验证批量 API**
   ```bash
   curl -X POST http://localhost:8080/api/v1/ppts/batch-check \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer <token>" \
     -d '{"video_ids": [1,2,3]}'
   ```

4. **测试自动登出问题**
   - 登录系统
   - 停留在文件管理页面
   - 观察是否还会频繁出现 401 错误
   - 检查后端日志，确认宽限期机制生效

---

## 📝 技术细节

### 宽限期机制 (Grace Period)

```go
const GracePeriod = 5 * time.Second

// 在宽限期内重复使用同一个 refresh token：
// 1. 返回缓存的 token 对（幂等性）
// 2. 不视为重放攻击
// 3. 宽限期过后才真正撤销 token
```

### 批量 API 优化

**之前:** 每个视频 1 个请求
- 20 个视频 = 20 个并发请求

**之后:** 所有视频 1 个请求
- 20 个视频 = 1 个批量请求

---

## 🚀 下一步

1. 启动服务器并验证功能
2. 测试自动登出问题是否解决
3. 提交代码到 Git
4. 部署到测试环境

---

## 📁 修改的文件清单

| 文件 | 修改内容 |
|------|----------|
| cmd/server/app.go | 添加批量 API 路由注册 |
| internal/auth/sm4_token.go | 宽限期机制实现 |
| internal/models/session.go | 添加 LastUsedAt 字段 |
| frontend/src/api/ppt.ts | 添加 batchCheckPpts 函数 |
| frontend/src/pages/files/index.tsx | 改用批量 API |

---

**恢复指令:** "继续测试自动登出问题的根本解决方案"
